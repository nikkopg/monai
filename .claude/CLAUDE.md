# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# Fable mode × GSD
When executing any /gsd-* or /gsd:* command, or working with files
under .planning/, apply the fable-mode skill (including its
references/gsd.md phase mapping). This applies to subagents too.
