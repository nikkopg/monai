---
phase: 13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend/writes.py
autonomous: true
requirements: [XFER-01]
must_haves:
  truths:
    - "A liquid→liquid transfer writes two paired Transaction rows sharing transfer_pair_id in one caller commit (D-03)"
    - "Each leg writes its own AuditLog row (two per transfer), inherited one-per-entity from apply_add_transaction — the composed function adds none (D-02)"
    - "Both legs are is_transfer=true and each leg carries its own amount+currency (D-09)"
    - "Editing or deleting a paired leg raises ValueError unless called with allow_paired=True (D-04)"
    - "writes.py still never commits the session (D-01)"
  artifacts:
    - "backend/writes.py: new apply_add_transfer function"
    - "backend/writes.py: allow_paired guard on apply_edit_transaction and apply_delete_transaction"
  key_links:
    - "Both legs' transfer_pair_id set to leg A's own id after both flush (shared-group-id)"
    - "apply_add_transfer composes apply_add_transaction twice; the caller owns the single commit"
    - "Existing edit/delete callers unaffected (allow_paired defaults False)"
---

<objective>
Add `apply_add_transfer` to `backend/writes.py` (liquid→liquid transfer: two paired
`Transaction` rows, shared `transfer_pair_id`, both `is_transfer=true`, dual per-leg
amounts) and add the leg-protection guard to `apply_edit_transaction` /
`apply_delete_transaction`. Closes XFER-01 and SC #1.

Purpose: The first mutation-layer function. Establishes the shared-group-id pairing
convention (D-03) that migration 011 already mirrors, and the application-layer
leg-protection raise (D-04) that keeps single-leg edits from orphaning a pair —
enforced in Python, not a DB trigger, per the repo's no-triggers / LLM-never-emits-SQL
precedent. Composes existing primitives and never commits (D-01), so atomicity is free
under the caller's single commit.
Output: `backend/writes.py` gains one function and a guard on two existing functions;
plan 01's transfer + guard tests go GREEN.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-CONTEXT.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-RESEARCH.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add allow_paired leg-protection guard to apply_edit_transaction and apply_delete_transaction (D-04)</name>
  <files>backend/writes.py</files>
  <read_first>
    - backend/writes.py — `apply_edit_transaction` (L79-95) and `apply_delete_transaction`
      (L98-104) in full; the module docstring (L1-14, the never-commit + one-audit-row-per-entity
      contract); the flush idiom.
    - backend/main.py — grep `_execute_proposal_payload` and the REST endpoints for existing
      call-sites of `apply_edit_transaction`/`apply_delete_transaction` to confirm adding a
      defaulted `allow_paired=False` parameter breaks none of them.
    - .planning/phases/13-.../13-PATTERNS.md §"leg-protection guard" (exact insertion point and
      raise message shape) and .planning/phases/13-.../13-RESEARCH.md §"Code Examples" guard block.
  </read_first>
  <action>
    Add a keyword parameter `allow_paired: bool = False` to both `apply_edit_transaction` and
    `apply_delete_transaction`. Immediately after the existing `db.get(Transaction, tx_id)`
    null-check, insert a guard: if the target row's `transfer_pair_id` is not None and
    `allow_paired` is False, raise `ValueError` with a message identifying the transaction and its
    pair and directing the caller to use the pair-aware function. The default `False` preserves
    every current call-site (no other file changes). Leave the rest of each function body
    unchanged. Do not add a code comment that repeats the session-commit call literal — this file
    is grep-gated to contain zero session-commit calls.
  </action>
  <verify>
    <automated>pytest backend/tests/test_write_tools.py::test_paired_leg_edit_blocked backend/tests/test_write_tools.py::test_paired_leg_delete_blocked -x</automated>
  </verify>
  <done>Editing or deleting a row with a non-NULL transfer_pair_id raises ValueError without allow_paired, and proceeds with allow_paired=True; all pre-existing edit/delete callers still compile and their tests pass.</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_write_tools.py::test_paired_leg_edit_blocked backend/tests/test_write_tools.py::test_paired_leg_delete_blocked -x` exits 0.
    - `grep -n "allow_paired" backend/writes.py` shows the parameter on both functions and the guard.
    - Existing transaction-edit/delete tests still pass: `pytest backend/tests/test_transaction_crud.py backend/tests/test_write_tools.py -k "edit or delete" -x` exits 0.
    - `grep -vE '^\s*#' backend/writes.py | grep -c 'db\.commit'` returns 0 (never-commit contract intact, comments excluded).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add apply_add_transfer (paired liquid→liquid transfer, D-03/D-09)</name>
  <files>backend/writes.py</files>
  <read_first>
    - backend/writes.py — `apply_add_transaction` (L54-76): its `after`-dict fields (account,
      currency, amount, category, is_transfer default False at L70, notes), the flush-before-audit
      idiom (L72-76), and `_get_or_create_account` usage (L58). This is the primitive
      apply_add_transfer composes twice.
    - .planning/phases/13-.../13-RESEARCH.md §"transfer_pair_id Shape — Recommended Convention"
      (shared-group-id seeded from leg A's id, and why over mutual self-reference).
    - .planning/phases/13-.../13-PATTERNS.md §"new apply_* functions" (composition-then-mutate,
      flush ordering, is_transfer explicit-True, Decimal(str(x)) money idiom, account-by-name).
    - backend/tests/test_write_tools.py::test_apply_add_transfer_pairs_both_legs (the GREEN target
      written in plan 01) — read it to match the exact `after`-dict contract it calls with.
  </read_first>
  <action>
    Add `apply_add_transfer(db, ...)` composing `apply_add_transaction` twice: leg A = the sent
    side (source account name, its currency, a negative amount, `is_transfer=True`), leg B = the
    received side (destination account name, its currency, a positive amount, `is_transfer=True`).
    Each leg carries its OWN amount and currency (D-09 dual amounts — no new columns). Resolve
    accounts by name via the existing `_get_or_create_account` path that `apply_add_transaction`
    already uses; never hard-code an account id (Finding 1). Every money field constructed with
    `Decimal(str(x))` (LOAD-BEARING). After BOTH legs flush (each `apply_add_transaction` already
    flushes to populate `.id`), set `leg_a.transfer_pair_id = leg_a.id` and
    `leg_b.transfer_pair_id = leg_a.id` (shared-group-id = leg A's own primary key, per D-03 /
    RESEARCH recommendation — matches migration 011's `min(id)` scheme). Each leg's own
    `AuditLog` row is written by `apply_add_transaction` (D-02); the composed function adds none
    extra. Do NOT commit — return both legs (e.g. a dict) and let the caller (Phase 14) own the
    single commit (D-01). Match the exact `after`-dict field names the plan-01 test calls with.
  </action>
  <verify>
    <automated>pytest backend/tests/test_write_tools.py::test_apply_add_transfer_pairs_both_legs -x</automated>
  </verify>
  <done>apply_add_transfer writes exactly two Transaction rows, both is_transfer=true, both transfer_pair_id = leg A's id, each with its own amount+currency, two audit rows, zero internal commits; the plan-01 test is GREEN.</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_write_tools.py::test_apply_add_transfer_pairs_both_legs -x` exits 0.
    - `grep -n "def apply_add_transfer" backend/writes.py` matches.
    - Both legs' `transfer_pair_id` equal leg A's `id` after the call (asserted by the test).
    - `grep -vE '^\s*#' backend/writes.py | grep -c 'db\.commit'` returns 0.
    - No literal integer account id in the new function (accounts resolved by name).
    - `pytest backend/tests/test_write_tools.py -x` still passes for all previously-green tests (no regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase 14 confirm/endpoint) → writes.py | Untrusted amounts/account names/currencies cross into the mutation layer; the layer must not commit and must tag transfer rows correctly. |
| writes.py → Postgres | Two-row write whose atomicity depends entirely on the caller's single commit boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering (data integrity) | apply_add_transfer partial write | high | mitigate | Compose primitives, never commit (D-01); both legs persist or neither, under the caller's single commit. `grep -c 'db\.commit'` (comments excluded) stays 0. |
| T-13-02 | Tampering (orphaned leg) | apply_edit/delete_transaction | high | mitigate | `allow_paired` guard raises ValueError on any single-leg edit/delete of a paired row (D-04); loud raise, not silent corruption. |
| T-13-07 | Repudiation (double-count leak) | transfer legs | high | mitigate | Both legs explicitly set `is_transfer=True` so they fall out of spending/income/net totals (Pitfall 5); plan-01 test asserts it. |
| T-13-05 | Tampering | leg amount fields | high | mitigate | `Decimal(str(x))` before every `Decimal()` (writes.py:62 idiom). |
| T-13-10 | Tampering (id drift) | account resolution | high | mitigate | Accounts resolved by name via `_get_or_create_account`; no hard-coded ids (Finding 1). |
| T-13-06 | Tampering | any raw SQL | medium | mitigate | Bound `:param` `text()` only; the guard/transfer use ORM + existing primitives. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `pytest backend/tests/test_write_tools.py -x` GREEN for the transfer + guard tests.
- `grep -vE '^\s*#' backend/writes.py | grep -c 'db\.commit'` returns 0 (never-commit contract preserved).
- `pytest backend/tests/test_transaction_crud.py -x` GREEN (no existing-caller regression from the new parameter).
- No literal account id introduced in writes.py.
</verification>

<success_criteria>
SC #1 met: a liquid→liquid transfer writes two paired transaction rows (via transfer_pair_id) atomically under one caller commit; editing or deleting one leg is blocked outside pair-aware functions.
</success_criteria>

<output>
Create `.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-03-SUMMARY.md` when done.
</output>
