---
phase: 13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_write_tools.py
  - backend/tests/test_cashflow_summary.py
autonomous: true
requirements: [ACCT-02, XFER-01, XFER-02, XFER-03, XFER-04]
must_haves:
  truths:
    - "Running pytest collects the new writes.py tests without import/collection errors"
    - "Each new test fails RED before implementation (function absent), not errors at collection"
    - "Every locked write behavior (D-03, D-04, D-05, D-06, D-07, D-08, D-09) has an executable assertion"
  artifacts:
    - "backend/tests/test_write_tools.py (extended with 6 new tests)"
    - "backend/tests/test_cashflow_summary.py (extended with 1 adjustment-exclusion test)"
  key_links:
    - "Function-local imports (from backend.writes import ...) inside each test body so collection succeeds while the target is still missing"
    - "Adjustment-exclusion test asserts is_transfer=true rows fall out of the existing is_transfer=false cashflow filter"
---

<objective>
Create the RED-first validation scaffold for every new `backend/writes.py` mutation
function this phase adds (liquid→liquid transfer, liquid→investment transfer, funded
buy/sell, balance adjustment) and the leg-protection guard — plus the D-08
adjustment-exclusion integration test. This is the Wave 0 test-first plan: it encodes
the six phase success criteria (SC1–SC5) as executable assertions BEFORE any function
exists, mirroring Phase 12's `12-01` scaffold precedent.

Purpose: Give the three sequential writes.py implementation plans (03, 04, 05) a fixed,
pre-written test target so each implements to GREEN with no interpretation. Locks the
`after`-dict contract, the shared-group-id pairing convention (D-03/D-09), the
leg-protection raise (D-04), the source_account_id link (D-05/D-06), and the
derived-balance delta including transfers (D-07, Finding 2).
Output: Extended `test_write_tools.py` and `test_cashflow_summary.py`, all new tests RED.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-CONTEXT.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-RESEARCH.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for the five new writes.py apply_* functions</name>
  <files>backend/tests/test_write_tools.py</files>
  <read_first>
    - backend/tests/test_write_tools.py — the file being modified. Read the fixture block (`db_available`, `db_session`, L1-96) and the analog test `test_apply_add_portfolio_event_audits_and_recomputes` (L587-630) plus the seed helpers `_make_account`, `_make_holding`, `_make_platform`. Copy their shape exactly (function-local imports, seed → call apply_* → `db_session.commit()` as the TEST's caller-commit → assert on live DB → explicit cleanup).
    - backend/writes.py — read `apply_add_transaction` (L54-76) and `apply_add_portfolio_event` (L247-311) to fix the exact `after`-dict field names these primitives already accept (account, currency, amount, category, is_transfer, notes / ticker, event_type, quantity, price, platform_id, currency). The new composed functions extend this contract.
    - .planning/phases/13-.../13-RESEARCH.md §"Validation Architecture" — the Req→Test map (exact test names + expected behavior per requirement).
    - .planning/phases/13-.../13-PATTERNS.md §"backend/tests/test_write_tools.py (extend)" — fixture/per-test idiom and the "tests own the commit" note.
  </read_first>
  <action>
    Add five RED tests to test_write_tools.py, each importing its target function-locally
    (so collection succeeds while the function is still absent). Names and asserted
    behavior are FIXED here — plans 03/04 implement to satisfy them exactly:

    1. `test_apply_add_transfer_pairs_both_legs` (XFER-01, D-03, D-09): seed two named liquid
       accounts; call `apply_add_transfer` with a sent leg (account A, negative amount+currency)
       and a received leg (account B, positive amount+currency); commit; assert exactly two
       Transaction rows written, BOTH with `is_transfer=true`, BOTH sharing the same non-NULL
       `transfer_pair_id`, and that shared value equals leg A's own `id` (shared-group-id
       convention); assert two `audit_log` rows added (one per leg, D-02); assert the two legs
       may carry different `currency` values (dual amounts on the rows themselves, D-09).
    2. `test_apply_add_investment_transfer` (XFER-02, D-05): seed one liquid source account and
       one platform; call `apply_add_investment_transfer`; commit; assert one Transaction row on
       the liquid account with `is_transfer=true`, one PortfolioEvent deposit whose
       `source_account_id` equals the liquid account's id, and that NO new `accounts` row was
       created for the investment side.
    3. `test_apply_add_funded_buy_one_commit_boundary` (XFER-03, D-06): seed a liquid source and a
       platform; call `apply_add_funded_buy`; commit; assert one cash-leg Transaction
       (`is_transfer=true`) debiting the source, one PortfolioEvent `buy`, and a recomputed
       Holding row for (ticker, platform_id); assert `PortfolioEvent.source_account_id` equals the
       source account id. Assert atomicity by construction: the function returns both entities and
       nothing is persisted until the test's own commit (see grep gate in Task-1 acceptance).
    4. `test_funded_buy_dual_currency_legs` (XFER-04, D-09/D-10): call `apply_add_funded_buy` with a
       cash-leg currency (e.g. IDR) different from the PortfolioEvent currency (e.g. USD); assert
       both currencies are stored independently on their respective rows and no schema column
       beyond existing `amount`/`currency` is touched.
    5. `test_apply_add_balance_adjustment_delta` (ACCT-02, D-07, Finding 2): seed a liquid account,
       insert BOTH a non-transfer transaction and a transfer transaction on it; call
       `apply_add_balance_adjustment(db, account, target)`; commit; assert exactly one new
       "Adjustment"-tagged Transaction whose amount equals `target − SUM(all amounts on the
       account, transfer rows included)`, and that `SUM(all amounts)` after the write equals
       `target`. The seeded transfer row is what makes this test catch the Finding-2 bug (a
       filtered SUM would produce the wrong delta).

    Use the existing `_make_account`/`_make_platform` seed helpers; build `after` dicts inline.
    Clean up every seeded row (accounts, transactions, events, holdings, audit rows) at test end,
    matching `_cleanup_ticker`. Do NOT implement any writes.py function in this task — tests only.
  </action>
  <verify>
    <automated>pytest backend/tests/test_write_tools.py --collect-only -q 2>&1 | grep -Eq "test_apply_add_transfer_pairs_both_legs|test_apply_add_balance_adjustment_delta"</automated>
  </verify>
  <done>All five tests collect without import/collection error and FAIL RED when run (target functions absent), never error at collection time.</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_write_tools.py --collect-only -q` exits 0 and lists all five new test ids.
    - Running `pytest backend/tests/test_write_tools.py -k "transfer or funded or balance_adjustment or investment_transfer" -x` fails RED (assertion/attribute/import-at-call), not a collection error.
    - Each new test imports its target with a function-local `from backend.writes import ...` (grep: no new module-level import of an unimplemented apply_* symbol).
    - `test_apply_add_balance_adjustment_delta` seeds at least one `is_transfer=true` row on the account so a filtered-SUM implementation would fail the test.
    - `test_apply_add_transfer_pairs_both_legs` asserts both legs' `transfer_pair_id` equal leg A's `id` (shared-group-id, per D-03).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RED tests for the leg-protection guard (D-04) and adjustment cashflow exclusion (D-08)</name>
  <files>backend/tests/test_write_tools.py, backend/tests/test_cashflow_summary.py</files>
  <read_first>
    - backend/tests/test_write_tools.py — the existing edit/delete transaction tests (search for `apply_edit_transaction`/`apply_delete_transaction` usage) to copy their seed/assert shape.
    - backend/writes.py — `apply_edit_transaction` (L79-95) and `apply_delete_transaction` (L98-104) current signatures, so the guard test targets the real parameter list (`allow_paired` will be added by plan 03).
    - backend/tests/test_cashflow_summary.py — the file being modified; read how it computes spending_total/income_total/net_total (which cashflow read tool it calls) so the exclusion assertion targets the real totals.
    - .planning/phases/13-.../13-RESEARCH.md §"Validation Architecture" rows for XFER-01(D-04) and ACCT-02(D-08); §"Common Pitfalls" Pitfall 5.
  </read_first>
  <action>
    Add three RED tests:

    1. `test_paired_leg_edit_blocked` (XFER-01, D-04): seed a transaction with a non-NULL
       `transfer_pair_id`; call `apply_edit_transaction(db, tx_id, after, before)` WITHOUT the
       override; assert it raises `ValueError` mentioning the pair; then assert the same call with
       `allow_paired=True` does NOT raise (edit proceeds). This fixes the guard's contract before
       plan 03 adds the `allow_paired` parameter.
    2. `test_paired_leg_delete_blocked` (XFER-01, D-04): same shape for `apply_delete_transaction` —
       raises without override, proceeds with `allow_paired=True`.
    3. In test_cashflow_summary.py, `test_adjustment_excluded_from_cashflow` (ACCT-02, D-08):
       capture spending_total/income_total/net_total; write a balance-adjustment
       (`apply_add_balance_adjustment`) that produces a non-zero delta on a liquid account; commit;
       assert the three cashflow totals are UNCHANGED (adjustment excluded), while a fresh
       unfiltered `SUM(amount)` on that account DID change by the delta (adjustment affects the
       derived balance). This encodes both hard constraints of D-08 in one test.

    Function-local imports throughout. Clean up seeded rows. No implementation in this task.
  </action>
  <verify>
    <automated>pytest backend/tests/test_write_tools.py backend/tests/test_cashflow_summary.py --collect-only -q 2>&1 | grep -Eq "test_paired_leg_edit_blocked|test_adjustment_excluded_from_cashflow"</automated>
  </verify>
  <done>All three tests collect cleanly and fail RED; the exclusion test asserts both D-08 constraints (excluded from cashflow totals AND included in derived balance).</done>
  <acceptance_criteria>
    - `test_paired_leg_edit_blocked` asserts a `ValueError` is raised without `allow_paired` and NOT raised with `allow_paired=True`.
    - `test_paired_leg_delete_blocked` asserts the same for delete.
    - `test_adjustment_excluded_from_cashflow` asserts spending/income/net totals are unchanged AND unfiltered `SUM(amount)` on the account changed by the adjustment delta.
    - All three collect without error and fail RED pre-implementation.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Postgres | Tests write/read the shared live DB (no mock, no rollback fixture); a test that mutates and fails to clean up pollutes the shared account/transaction space (the exact Phase 12 leftover-test-account problem). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Tampering (test data leak) | new tests writing to shared live DB | medium | mitigate | Every new test explicitly cleans up all seeded rows (accounts, transactions, events, holdings, audit rows) in a `finally`/end-of-test block, matching `_cleanup_ticker`; use uniquely-named seed accounts (e.g. a `zz13test-` prefix) so a missed cleanup is identifiable — do not reuse real account names. |
| T-13-06 | Tampering | any raw SQL in tests | low | mitigate | Test SQL uses bound `:param` `text()` (existing idiom), never string-formatted account ids/names. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); pytest already pinned. |
</threat_model>

<verification>
- `pytest backend/tests/test_write_tools.py backend/tests/test_cashflow_summary.py --collect-only -q` exits 0 (all 8 new tests collectable).
- Running the new tests fails RED (features absent) — this is the expected Wave 0 state, NOT a plan failure.
- No new module-level import references an unimplemented `apply_*` symbol (function-local imports only).
</verification>

<success_criteria>
Eight executable tests exist and are RED, one per locked write behavior (D-03, D-04 edit, D-04 delete, D-05, D-06, D-07, D-09, D-08), giving plans 03/04/05 a fixed GREEN target.
</success_criteria>

<output>
Create `.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-01-SUMMARY.md` when done.
</output>
