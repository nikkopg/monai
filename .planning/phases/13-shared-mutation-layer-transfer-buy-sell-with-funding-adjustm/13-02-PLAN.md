---
phase: 13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/011_retro_pair_transfers.py
  - backend/tests/test_transfer_retro_pairing.py
autonomous: true
requirements: [XFER-05]
must_haves:
  truths:
    - "Historical is_transfer=true rows that match exactly one opposite-amount, same-date, distinct-account counterpart are paired via transfer_pair_id"
    - "Rows with zero or multiple candidate matches are left transfer_pair_id NULL and printed/logged, never guessed (D-11)"
    - "Re-running upgrade() is a no-op on already-paired rows (idempotent)"
  artifacts:
    - "alembic/versions/011_retro_pair_transfers.py (down_revision chained off 010)"
    - "backend/tests/test_transfer_retro_pairing.py (standalone importlib unit test)"
  key_links:
    - "COUNT(*)-per-row guard precedes any pairing UPDATE (no blind LIMIT 1)"
    - "UPDATE ... WHERE transfer_pair_id IS NULL guard makes the pass idempotent"
    - "No hard-coded account ids; matches on a.account_id <> b.account_id only (Finding 1)"
---

<objective>
Add the one-time Alembic migration `011_retro_pair_transfers.py` that backfills
`transactions.transfer_pair_id` for historical imported transfers (XFER-05, D-11), and
a standalone unit test for its pure matching logic. This is a fully independent track —
it touches no code the writes.py plans (03/04/05) touch, so it runs in parallel from
Wave 1.

Purpose: Close SC #6 — historical imported transfer rows are retro-paired by a
migration pass (matched by date + equal-absolute-opposite-sign amount + distinct
accounts), unmatched rows flagged and left as-is, never guessed. Non-destructive and
idempotent, per the project migration mandate.
Output: Migration `011` + `test_transfer_retro_pairing.py`, both GREEN, migration
applied to the live DB.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-CONTEXT.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-RESEARCH.md
@.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED unit test for the retro-pairing matching logic (standalone import)</name>
  <files>backend/tests/test_transfer_retro_pairing.py</files>
  <read_first>
    - backend/tests/test_category_migration.py — the closest analog: read its exact pattern for
      importing a migration module standalone via `importlib` (bypassing the alembic runner) to
      unit-test pure logic. Copy that import shape rather than inventing one.
    - backend/tests/test_write_tools.py — the `db_available`/`db_session` fixtures (L1-96) and how
      seeded rows are cleaned up; reuse them (no new conftest per RESEARCH Wave 0 Gaps).
    - .planning/phases/13-.../13-RESEARCH.md §"Code Examples" (candidate-match SQL), §"Runtime State
      Inventory" (652 matched / 326 pairs / 16 unmatched live snapshot), §"Common Pitfalls" #4
      (flag-not-guess, no blind LIMIT 1).
    - .planning/phases/13-.../13-PATTERNS.md §"alembic/versions/011_retro_pair_transfers.py".
  </read_first>
  <action>
    Create test_transfer_retro_pairing.py. Seed a KNOWN, self-contained set of transfer rows on
    uniquely-named test accounts (do NOT hard-code or assert the live 326/16 figures — Phase 12's
    lesson: id-agnostic, self-seeded, so the test survives DB drift). Cover three cases the
    migration must handle:
      - exactly-one match: two rows, same date, `amount = -amount`, distinct accounts, both
        is_transfer=true → after pairing both share `transfer_pair_id = min(id)`;
      - zero match: one lone is_transfer=true row with no opposite → stays `transfer_pair_id` NULL;
      - multiple candidates: three rows where two are valid opposite matches for a third → the
        ambiguous row is left NULL (flagged), never arbitrarily paired.
    Import the migration module's matching/pairing helper standalone via importlib (matching
    test_category_migration.py) and drive it against a real `db_session`; assert the three
    outcomes. Add a separate idempotency assertion: running the pairing routine twice produces no
    additional changes on the second run. Clean up all seeded rows at test end.

    This is RED first — the migration module does not exist yet (Task 2 creates it). Import it
    function-locally so collection succeeds while it is missing.
  </action>
  <verify>
    <automated>pytest backend/tests/test_transfer_retro_pairing.py --collect-only -q 2>&1 | grep -q test_</automated>
  </verify>
  <done>Test collects cleanly (function-local import), fails RED until Task 2 lands the migration, and covers exactly-one / zero / multiple-candidate cases plus idempotency, using self-seeded rows (no frozen live counts).</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_transfer_retro_pairing.py --collect-only -q` exits 0.
    - The test seeds its own rows and asserts pairing outcomes structurally; it does NOT assert the literal live numbers 326 or 16.
    - Covers the multiple-candidate branch (left NULL/flagged) even though live data currently has zero such cases.
    - Includes an idempotency assertion (second pairing run changes nothing).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement migration 011 and apply it to the live DB</name>
  <files>alembic/versions/011_retro_pair_transfers.py</files>
  <read_first>
    - alembic/versions/010_typed_accounts.py — read the revision header shape and confirm its
      `revision` id (research states `f1a2b3c4d5e6`); the new migration's `down_revision` MUST equal
      010's actual `revision`. Also copy 010's idempotent guarded-step idiom (`op.get_bind()`,
      inspector, `UPDATE ... WHERE ... IS NULL`). Note 010's hard-coded `ACCOUNT_TYPE` id map is the
      anti-pattern this migration must NOT repeat.
    - alembic/versions/009_category_hierarchy.py (L1-60, L124-136) — the `assert_parity`
      loud-reporting idiom; retro-pairing REPORTS (prints) flagged rows rather than raising, because
      "0 or multiple matches" is an expected non-fatal outcome per D-11.
    - backend/entrypoint.sh — confirms `alembic upgrade head` runs on container start (this is how
      the migration reaches the live DB).
    - .planning/phases/13-.../13-RESEARCH.md §"Architecture Patterns" retro-pairing block,
      §"Code Examples" candidate-match SQL, Open Question 1 (no new column — a flagged row is simply
      `is_transfer=true AND transfer_pair_id IS NULL`).
    - .planning/phases/13-.../13-PATTERNS.md §"alembic/versions/011_retro_pair_transfers.py".
  </read_first>
  <action>
    Write `upgrade()`: select candidate pairs with the verified query — join transactions a↔b on
    `a.date::date = b.date::date AND a.amount = -b.amount AND a.account_id <> b.account_id AND
    a.id < b.id` where both `is_transfer = true`. For each row, COUNT its candidate counterparts
    FIRST; only when the count is exactly 1 do you `UPDATE transactions SET transfer_pair_id =
    :group_id WHERE id IN (:leg_a, :leg_b) AND transfer_pair_id IS NULL`, where `:group_id` is the
    lower of the two ids (shared-group-id convention, consistent with the writes.py transfer
    function in plan 03). Rows with zero or multiple candidates: leave `transfer_pair_id` NULL and
    print the flagged ids to stdout (mirroring 009's per-group summary) — do NOT raise, do NOT
    guess, do NOT add a new column. All SQL uses bound `:param` placeholders. Set
    `down_revision` to 010's actual revision id. Write a minimal `downgrade()` that nulls only the
    `transfer_pair_id` values this pass could have set (or documents it as a non-reversible
    backfill, matching 009/010's downgrade posture — follow whichever those two use).

    Never hard-code any account id; the match is purely relational (`a.account_id <> b.account_id`).
    Keep the `WHERE transfer_pair_id IS NULL` guard on every UPDATE so re-running `upgrade()` is a
    no-op on already-paired rows (idempotent, D-11). After writing, apply it: run
    `alembic upgrade head` against the live DB and record the printed flagged-row count in the
    SUMMARY (do not assert a frozen number in code).
  </action>
  <verify>
    <automated>pytest backend/tests/test_transfer_retro_pairing.py -x</automated>
  </verify>
  <done>Migration 011 pairs exactly-one-match rows via shared-group-id, leaves zero/multiple-candidate rows NULL and prints them, is idempotent on re-run, hard-codes no account id, and `alembic upgrade head` applies cleanly to the live DB (upgrade → downgrade → upgrade round-trip leaves paired rows consistent).</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_transfer_retro_pairing.py -x` exits 0 (all cases + idempotency GREEN).
    - `grep -n "down_revision" alembic/versions/011_retro_pair_transfers.py` shows it equals 010's actual `revision` id.
    - Every UPDATE in the migration includes a `transfer_pair_id IS NULL` guard (idempotent) — verify by reading the file.
    - The candidate query is preceded by a per-row COUNT guard; there is no `LIMIT 1` on the candidate-selection without a `COUNT(*) = 1` check (Pitfall 4).
    - No literal integer account id appears in the migration (Finding 1 / Pitfall 2).
    - Second `alembic upgrade` run (or second pairing-routine call) reports zero additional pairings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live financial data | `upgrade()` mutates historical `transactions` rows on the shared live Postgres; a wrong or non-idempotent pass corrupts pairing state that later read paths (Phase 17 REC-05) trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Tampering / DoS (data integrity) | upgrade() backfill | high | mitigate | Non-destructive (only sets a previously-NULL column); `WHERE transfer_pair_id IS NULL` guard makes re-run a no-op; runs inside Alembic's single-transaction model so any error rolls the whole pass back (009 precedent). |
| T-13-09 | Tampering (incorrect pairing) | ambiguous-match handling | high | mitigate | COUNT(*)-per-row guard before any UPDATE; exactly-one → pair, zero/multiple → leave NULL + print. No blind `LIMIT 1` (Pitfall 4). Multiple-candidate branch is unit-tested even though live data has none today. |
| T-13-10 | Tampering (id drift) | account resolution in migration | high | mitigate | Match is purely relational (`a.account_id <> b.account_id`); zero hard-coded ids, unlike 010's now-stale `ACCOUNT_TYPE` map (Finding 1). |
| T-13-06 | Tampering | candidate-match / UPDATE SQL | medium | mitigate | Bound `:param` placeholders only; no string-formatted SQL. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; migration uses the same `sqlalchemy`/`alembic` imports as 001–010. |
</threat_model>

<verification>
- `pytest backend/tests/test_transfer_retro_pairing.py -x` GREEN (exactly-one / zero / multiple-candidate + idempotency).
- `alembic upgrade head` applies `011` cleanly against the live DB; the flagged-row count is printed and recorded in the SUMMARY (not asserted as a frozen literal).
- `alembic downgrade -1 && alembic upgrade head` round-trips without error.
- Reading `011` confirms: down_revision = 010's revision, IS NULL guard on every UPDATE, COUNT guard before pairing, no literal account id.
</verification>

<success_criteria>
SC #6 met: historical imported transfer rows are retro-paired by an idempotent, non-destructive migration matched on date+amount+distinct-accounts; unmatched (0 or >1 candidate) rows are flagged and left as-is, never guessed.
</success_criteria>

<output>
Create `.planning/phases/13-shared-mutation-layer-transfer-buy-sell-with-funding-adjustm/13-02-SUMMARY.md` when done (record the live flagged-row count observed during `alembic upgrade`).
</output>
