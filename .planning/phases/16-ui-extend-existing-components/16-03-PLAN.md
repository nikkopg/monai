---
phase: 16-ui-extend-existing-components
plan: 03
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - ui/app/cashflow/AccountManager.tsx
  - ui/app/investments/PlatformManager.tsx
autonomous: true
requirements: [ACCT-01, PLAT-02]

must_haves:
  truths:
    - "Creating a liquid account posts type:liquid explicitly on POST /api/accounts (D-07)"
    - "AccountManager edit stays name-only — account type is NOT user-editable this phase (D-07 / Pitfall 4)"
    - "Editing a platform updates BOTH name and kind via PUT /api/platforms/{id} (D-08)"
    - "The 422→affected_count reassign delete flow in both managers is untouched"
  artifacts:
    - ui/app/cashflow/AccountManager.tsx
    - ui/app/investments/PlatformManager.tsx
  key_links:
    - "AccountManager.saveAdd POST /api/accounts body gains type:liquid"
    - "PlatformManager edit row gains a kind input; saveEdit PUT body gains kind"
---

<objective>
Close the two small CRUD-parity gaps that make the account and platform managers cover
the v1.2 typed model without a rebuild: AccountManager sends type:liquid on create
(D-07 / ACCT-01), and PlatformManager's inline edit row gains a kind input so edit
reaches true add/edit parity (D-08 / PLAT-02).

Purpose: ACCT-01 + PLAT-02. Both are minimal, surgical additions to already-correct,
already-tested components — the reassign-on-delete flow and all other structure stay
byte-identical.
Output: two extended manager components that turn platform-crud.spec.ts and the
cashflow-crud account-create assertion GREEN.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-extend-existing-components/16-CONTEXT.md
@.planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md
@.planning/phases/16-ui-extend-existing-components/16-PATTERNS.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "AccountManager
saveAdd saveEdit POST accounts"` and `graphify query "PlatformManager edit row kind
saveEdit"` to orient BEFORE reading raw source; open only the ranges you edit.

This plan depends on Plan 01: ui/e2e/platform-crud.spec.ts (new) and the extended
cashflow-crud.spec.ts account-create assertion already exist (RED). Make them GREEN
without editing the specs.

Backend contracts (already shipped — backend/schemas.py, do not change):
- AccountCreate accepts `type` (server-defaults liquid); POST /api/accounts (main.py:229).
- PlatformUpdate already accepts `kind`; PUT /api/platforms/{id} (main.py:352).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccountManager sends type:liquid on create (D-07 / ACCT-01)</name>
  <files>ui/app/cashflow/AccountManager.tsx</files>
  <read_first>
    - ui/app/cashflow/AccountManager.tsx (current saveAdd and saveEdit — the diff target)
    - ui/e2e/cashflow-crud.spec.ts (the account-create type:liquid assertion this task must satisfy, plus the untouched reassign-delete test)
    - .planning/phases/16-ui-extend-existing-components/16-PATTERNS.md (AccountManager D-07 one-line diff)
    - .planning/phases/16-ui-extend-existing-components/16-RESEARCH.md (Common Pitfall 4 — do NOT make account type editable)
    - .planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md (Component Contract 5 — no visible change)
  </read_first>
  <behavior>
    - Creating an account posts a body that includes type equal to "liquid".
    - Editing an account still posts only the name (no type field) — edit is name-only.
    - The delete-with-reassign (422→affected_count) flow is unchanged.
  </behavior>
  <action>
    In saveAdd, add `type: "liquid"` to the JSON body of the POST /api/accounts request
    (the body currently carries only name). This is the ONLY change to this file. Do NOT
    touch saveEdit — it stays name-only (account type is not user-switchable post-create
    this phase; RESEARCH Pitfall 4 and the deferred-ideas list). Do NOT add a type picker
    or any type UI. Leave the table, the inline edit row, the Add-account form, the
    ConfirmDialog reassign flow, extractDetail, and all copy byte-identical (UI-SPEC 5 —
    no visible change).
  </action>
  <verify>
    <automated>cd ui && npx playwright test e2e/cashflow-crud.spec.ts -g "account"</automated>
  </verify>
  <acceptance_criteria>
    - The cashflow-crud account-create scenario passes: POST /api/accounts body includes type "liquid".
    - `grep -c "liquid" ui/app/cashflow/AccountManager.tsx` ≥ 1.
    - `grep -c "type:" ui/app/cashflow/AccountManager.tsx` shows the type key only in the
      create body (saveEdit body still carries name only — no type added there).
    - The existing account reassign-then-delete scenario still passes (flow untouched).
    - `cd ui && npx tsc --noEmit -p tsconfig.json` passes.
  </acceptance_criteria>
  <done>Account create sends type:liquid; edit remains name-only; reassign-delete unchanged; the account spec scenarios pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlatformManager edit row edits kind too (D-08 / PLAT-02)</name>
  <files>ui/app/investments/PlatformManager.tsx</files>
  <read_first>
    - ui/app/investments/PlatformManager.tsx (current editName state, the "Edit" click handler that seeds it, saveEdit body, edit-row JSX, and the Add-form's existing kind input to mirror)
    - ui/e2e/platform-crud.spec.ts (the add / edit-with-kind / delete-reassign scenarios this task must satisfy)
    - .planning/phases/16-ui-extend-existing-components/16-PATTERNS.md (PlatformManager D-08 diff: editKind state, seed, body, input)
    - .planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md (Component Contract 6 — kind input mirrors the Add-form input)
  </read_first>
  <behavior>
    - Entering the inline edit row seeds the kind input from the platform's current kind.
    - Editing a platform posts a PUT body with BOTH name and the edited kind.
    - Adding a platform still posts name + kind (unchanged add path).
    - The delete-with-reassign (422→affected_count) flow is unchanged.
  </behavior>
  <action>
    Add an `editKind` state alongside the existing editName. In the "Edit" click handler
    that currently seeds editName from the platform, also seed editKind from that
    platform's kind (empty string when kind is null). In the inline edit-row JSX, add a
    second input beside the name input, styled and placeholdered identically to the
    Add-form's existing kind input (same input token with the ~160px width, same
    "e.g. brokerage, crypto app" placeholder), bound to editKind. In saveEdit, change the
    PUT /api/platforms/{id} body to include kind alongside name (send null when editKind is
    empty, since PlatformUpdate accepts kind). No other change — table structure, the
    DeleteFlowState / extractDetail / ConfirmDialog reassign flow, the Add form, and the
    empty-state copy stay exactly as today (UI-SPEC 6).
  </action>
  <verify>
    <automated>cd ui && npx playwright test e2e/platform-crud.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - platform-crud.spec.ts is GREEN: add posts name+kind; inline edit posts a PUT body with BOTH name and the edited kind; delete-reassign flow works.
    - `grep -c "editKind" ui/app/investments/PlatformManager.tsx` ≥ 3 (state, seed, input binding).
    - The saveEdit PUT body includes a kind field (`grep -q "kind" ui/app/investments/PlatformManager.tsx` at the edit body).
    - `cd ui && npx tsc --noEmit -p tsconfig.json` passes.
  </acceptance_criteria>
  <done>Platform inline edit updates both name and kind, reaching add/edit parity; add path and reassign-delete unchanged; platform-crud spec GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client → Next.js /api proxy → FastAPI | Account name+type and platform name+kind cross here. The /api/* proxy injects MONAI_API_KEY server-side; the client never holds the key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Tampering | account create type field (AccountManager) | low | accept | This manager is liquid-by-definition and sends type:liquid; the backend server-defaults liquid anyway. account.type is a discriminator, not a security boundary, in a single-user self-hosted app. edit stays name-only, so type is not re-writable via this UI. |
| T-16-07 | Tampering / XSS | platform kind free-text input (PlatformManager) | low | accept | kind is free text with no server enum; React escapes it on render (no dangerouslySetInnerHTML), so no injection surface. Single-user app; no mitigation beyond the existing React escaping. |
| T-16-SC | Tampering | npm installs | low | accept | Zero packages installed this plan. No supply-chain surface. |
</threat_model>

<verification>
- `cd ui && npx playwright test e2e/platform-crud.spec.ts e2e/cashflow-crud.spec.ts` GREEN.
- `cd ui && npx tsc --noEmit -p tsconfig.json` passes.
- Wave merge: `cd ui && npm run e2e` full Playwright suite green.
</verification>

<success_criteria>
- Account create sends type:liquid; account edit remains name-only (ACCT-01, D-07).
- Platform inline edit updates both name and kind, reaching CRUD parity (PLAT-02, D-08).
- Both managers' reassign-on-delete flows remain untouched and green.
</success_criteria>

<output>
Create `.planning/phases/16-ui-extend-existing-components/16-03-SUMMARY.md` when done.
</output>
