---
phase: 16-ui-extend-existing-components
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - ui/e2e/record-modal.spec.ts
  - ui/e2e/platform-crud.spec.ts
  - ui/e2e/cashflow-crud.spec.ts
autonomous: true
requirements: [ACCT-01, PLAT-02, REC-04]

must_haves:
  truths:
    - "ui/e2e/record-modal.spec.ts exists, compiles, and its REC-04 scenarios are discovered by Playwright"
    - "ui/e2e/platform-crud.spec.ts exists, compiles, and mirrors the account reassign-delete route-mock idiom (PLAT-02)"
    - "cashflow-crud.spec.ts asserts an account create sends type:liquid (ACCT-01)"
    - "Every scenario asserts a request BODY or endpoint, not just a UI toggle, so it is a real feedback signal for Wave 1"
  artifacts:
    - ui/e2e/record-modal.spec.ts
    - ui/e2e/platform-crud.spec.ts
    - ui/e2e/cashflow-crud.spec.ts
  key_links:
    - "record-modal scenarios route-mock POST/PUT /api/transactions and POST /api/transactions/transfer and assert body shape"
    - "platform-crud scenarios route-mock POST/PUT/DELETE /api/platforms and assert kind round-trips through edit"
    - "cashflow-crud account-create scenario route-mocks POST /api/accounts and asserts type:liquid"
---

<objective>
Create the Wave 0 e2e test scaffolds that pin the REC-04 / PLAT-02 / ACCT-01
behavioral contract BEFORE the modal and managers are extended. These specs are
expected to run RED against today's code and turn GREEN as Wave 1 (Plans 02, 03)
lands — they are the feedback signal Nyquist sampling runs after every Wave 1 task.

Purpose: no Wave 1 task can claim "done" without an automated check; writing the
contract first also freezes the copy/selectors/endpoints the implementation must match.
Output: `ui/e2e/record-modal.spec.ts` (new), `ui/e2e/platform-crud.spec.ts` (new),
and an extended `ui/e2e/cashflow-crud.spec.ts` account-create assertion.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md
@.planning/phases/16-ui-extend-existing-components/16-RESEARCH.md
@.planning/phases/16-ui-extend-existing-components/16-VALIDATION.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Before reading any raw source
(the existing specs, the modal, the managers) run `graphify query "<question>"`
to orient, then open only the specific ranges you need.

Test harness facts (from ui/playwright.config.ts, already verified):
- Runner: Playwright @playwright/test 1.61.1; testDir ./e2e; baseURL http://127.0.0.1:3001.
- webServer auto-runs `npm run dev` on 3001 with reuseExistingServer:true — do NOT
  start a server manually and do NOT run `playwright install` (Chromium is preinstalled).
- Backend is NEVER hit live: every spec route-mocks `/api/*` with `page.route(...)`.
  Copy the fixture + route-mock idiom verbatim from cashflow-crud.spec.ts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: record-modal.spec.ts — Expense/Income (/api/transactions) segment, sign, currency, add-another, edit reverse-map (REC-04)</name>
  <files>ui/e2e/record-modal.spec.ts</files>
  <read_first>
    - ui/e2e/cashflow-crud.spec.ts (route-mock + fixture idiom, and the existing modal-open navigation — reuse it, do not invent selectors)
    - .planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md (Copywriting Contract — exact segment labels, button copy, field labels; Component Contracts 1–4)
    - .planning/phases/16-ui-extend-existing-components/16-RESEARCH.md (Validation Architecture → Wave 0 Gaps list; Patterns 1–3, 7)
    - ui/playwright.config.ts (webServer + baseURL)
  </read_first>
  <action>
    Create ui/e2e/record-modal.spec.ts. Reuse cashflow-crud.spec.ts's route-mock
    helpers/fixtures (summary + accounts + categories) and its modal-open navigation
    idiom. Cover the Expense/Income (`POST/PUT /api/transactions`) path with these
    scenarios, each asserting a request BODY intercepted via page.route:
    (1) Default segment — on modal open in create mode the record-type segmented
        control renders three buttons labelled Expense / Income / Transfer in that
        order, and Expense is the active segment.
    (2) Expense posts a negative amount — pick Expense, type an unsigned magnitude
        (e.g. 25000) into the Amount field, pick an account, submit the primary
        "Add transaction" button; intercept POST /api/transactions and assert the body
        amount is the negated magnitude (e.g. -25000) and body.currency equals "IDR".
    (3) Income posts a positive amount — pick Income, same magnitude, submit; assert
        POST /api/transactions body amount is the positive magnitude.
    (4) Currency default+wired — assert the Currency field renders beside Amount with
        default value "IDR" and that value appears in the POST body (fold into 2/3).
    (5) Save & add another — create-mode only; submit via the secondary
        "Save & add another" button; assert the POST fires, the modal STAYS open (the
        modal heading is still visible afterward), the Amount / Category / Notes fields
        are reset to empty, and the segment + account + date remain unchanged.
    (6) Edit reverse-map (non-transfer) — open the modal in edit mode seeded with a
        stored NEGATIVE-amount, non-transfer row; assert Expense is the active segment
        and the Amount field shows the absolute magnitude; save and assert the request
        goes to PUT /api/transactions/{id}.
    Use the exact copy from UI-SPEC (labels "Amount", "Currency", "Account"; buttons
    "Add transaction", "Save & add another", "Save changes"). Tests target the
    not-yet-built UI, so they are RED now — that is intended.
  </action>
  <verify>
    <automated>cd ui && npx playwright test e2e/record-modal.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/record-modal.spec.ts --list` exits 0 (file compiles, all
      scenarios above are discovered as named tests).
    - `grep -c "api/transactions" ui/e2e/record-modal.spec.ts` ≥ 1 and the spec asserts a
      request body amount sign (positive for Income, negated for Expense).
    - `grep -q "Save & add another" ui/e2e/record-modal.spec.ts` (add-another scenario present).
    - Running the spec against current code fails on the missing segmented control /
      unsigned-amount behavior (documented RED baseline; turns GREEN in Plan 02).
  </acceptance_criteria>
  <done>record-modal.spec.ts compiles and discovers the six Expense/Income-path scenarios, each asserting a request body or endpoint; RED against current code.</done>
</task>

<task type="auto">
  <name>Task 2: record-modal.spec.ts — Transfer branch (/api/transactions/transfer) + edit-leg lock landmine (REC-04)</name>
  <files>ui/e2e/record-modal.spec.ts</files>
  <read_first>
    - ui/e2e/record-modal.spec.ts (the file from Task 1 — extend it)
    - .planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md (Component Contracts 1, 2 Transfer layout, and 7 Interaction states — edit-mode transfer lock)
    - .planning/phases/16-ui-extend-existing-components/16-RESEARCH.md (Common Pitfalls 1 and 2 — the two failure modes these scenarios must catch)
    - .planning/phases/16-ui-extend-existing-components/16-PATTERNS.md (submit-branching whitelist + From/To selects)
  </read_first>
  <action>
    Extend ui/e2e/record-modal.spec.ts with the Transfer path and the transfer-leg
    edit landmine (RESEARCH Pitfalls 1 & 2):
    (1) Transfer branch — pick Transfer; assert the Category field is no longer visible
        and two selects labelled "From account" and "To account" appear (both populated
        from the accounts fixture). Choose distinct From/To, type an unsigned amount,
        submit the primary "Add transfer" button; intercept POST /api/transactions/transfer
        and assert the body contains exactly from_account, to_account (account NAMES),
        amount (positive/unsigned), currency, date, notes — and assert it carries NO
        category field and NO is_transfer field. Assert the request did NOT go to
        POST /api/transactions.
    (2) Same-account guard — pick Transfer, set From and To to the same account, submit;
        assert an inline error reading "From and To accounts must be different." appears
        and NO request to /api/transactions/transfer is issued.
    (3) Edit-leg lock — open the modal in edit mode seeded with a row where
        is_transfer is true; assert the segmented control is disabled (attempting to
        click Income/Expense does not change the active segment), the single legacy
        "Account" select is shown (NOT From/To), the muted caption
        "This is one leg of a transfer — full pair editing isn't available yet." is
        visible, and "Save & add another" is NOT rendered. Save and assert the request
        goes to PUT /api/transactions/{id} (never POST /api/transactions/transfer).
    Build the expected transfer body assertion as an explicit field whitelist so a future
    regression that spreads general form state (leaking category) fails this test.
  </action>
  <verify>
    <automated>cd ui && npx playwright test e2e/record-modal.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/transactions/transfer" ui/e2e/record-modal.spec.ts` ≥ 1.
    - Transfer scenario asserts the POST body has from_account + to_account + amount +
      currency + date + notes and asserts absence of a category field.
    - Edit-leg-lock scenario asserts the edit path issues PUT /api/transactions and
      asserts the pair endpoint /api/transactions/transfer is NOT called during an edit.
    - `npx playwright test e2e/record-modal.spec.ts --list` exits 0 with the three new
      scenarios discovered; RED against current code.
  </acceptance_criteria>
  <done>Transfer branch + same-account guard + edit-leg-lock scenarios exist, asserting the pair-endpoint body whitelist and that edits never route to the create-only pair endpoint; RED against current code.</done>
</task>

<task type="auto">
  <name>Task 3: platform-crud.spec.ts (new, PLAT-02) + cashflow-crud account type:liquid assertion (ACCT-01)</name>
  <files>ui/e2e/platform-crud.spec.ts, ui/e2e/cashflow-crud.spec.ts</files>
  <read_first>
    - ui/e2e/cashflow-crud.spec.ts (the existing "account reassign-then-delete" test — structural template to mirror, and the file to extend for the ACCT-01 assertion)
    - .planning/phases/16-ui-extend-existing-components/16-UI-SPEC.md (Component Contracts 5 account, 6 platform; Copywriting reassign/empty-state copy)
    - .planning/phases/16-ui-extend-existing-components/16-PATTERNS.md (AccountManager D-07 diff, PlatformManager D-08 diff)
    - .planning/phases/16-ui-extend-existing-components/16-RESEARCH.md (Wave 0 Gaps → platform spec + account type coverage)
  </read_first>
  <action>
    Create ui/e2e/platform-crud.spec.ts by mirroring cashflow-crud.spec.ts's
    account reassign-delete test 1:1, swapping account/accounts for platform/platforms
    and route-mocking /api/platforms. Cover PLAT-02 CRUD parity:
    (1) Add a platform — submit the add form with a name and a kind; intercept
        POST /api/platforms and assert the body carries both name and kind.
    (2) Edit updates name AND kind — enter the inline edit row for an existing platform,
        change both the name and the kind input, save; intercept PUT /api/platforms/{id}
        and assert the body carries BOTH name and the edited kind (this is the D-08 gap
        being closed — the current UI would only send name).
    (3) Delete-with-reassign — trigger delete, mock a 422 with an affected_count body,
        assert the reassign ConfirmDialog appears, choose a destination platform,
        confirm, and assert the follow-up DELETE fires (structural copy of the account
        reassign test; this flow is unchanged code, asserted only for parity).
    Then extend ui/e2e/cashflow-crud.spec.ts: add (or augment) a scenario that creates an
    account via AccountManager and intercepts POST /api/accounts, asserting the body
    includes type equal to "liquid" (D-07 / ACCT-01). Do not alter the existing
    reassign-delete test's assertions. Tests target not-yet-built behavior (kind-on-edit,
    type-on-create) — RED now, GREEN after Plan 03.
  </action>
  <verify>
    <automated>cd ui && npx playwright test e2e/platform-crud.spec.ts e2e/cashflow-crud.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/platform-crud.spec.ts --list` exits 0 with add / edit-with-kind
      / delete-reassign scenarios discovered.
    - platform-crud edit scenario asserts the PUT /api/platforms body includes a kind field.
    - cashflow-crud.spec.ts contains an assertion that POST /api/accounts body includes
      type "liquid" (`grep -c "liquid" ui/e2e/cashflow-crud.spec.ts` ≥ 1).
    - The existing cashflow-crud reassign-delete test still lists and is unmodified in intent.
  </acceptance_criteria>
  <done>platform-crud.spec.ts (add / edit-incl-kind / delete-reassign) compiles and lists; cashflow-crud.spec.ts asserts type:liquid on account create; both RED against current code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Route-mocked; no live backend, no live DB. Tests introduce no runtime surface — they are assertions only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | e2e specs | low | accept | Specs run route-mocked with no live backend/DB; they cannot mutate real data. No mitigation needed beyond the existing route-mock convention. |
| T-16-SC | Tampering | npm installs | low | accept | This plan installs zero packages (Playwright already present, no `playwright install`). No supply-chain surface introduced. |
</threat_model>

<verification>
- `cd ui && npx playwright test e2e/record-modal.spec.ts e2e/platform-crud.spec.ts e2e/cashflow-crud.spec.ts --list` exits 0 (all three specs compile and discover their scenarios).
- Each new scenario asserts a request body or endpoint (not merely a DOM state), so Wave 1 has a real RED→GREEN signal.
- Specs are expected RED when run (not `--list`) against current code; that is the intended Wave 0 baseline.
</verification>

<success_criteria>
- record-modal.spec.ts covers REC-04: segment default, sign derivation, currency default, transfer pair-endpoint body whitelist, same-account guard, save-&-add-another, edit reverse-map, edit-leg lock.
- platform-crud.spec.ts covers PLAT-02: add, edit-incl-kind, delete-with-reassign.
- cashflow-crud.spec.ts asserts ACCT-01: account create sends type:liquid.
- All specs compile/discover; RED against current code; no new dependency added.
</success_criteria>

<output>
Create `.planning/phases/16-ui-extend-existing-components/16-01-SUMMARY.md` when done.
</output>
