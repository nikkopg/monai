---
phase: 07-investment-subsystem-v2-multi-platform-multi-currency-cash-g
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/portfolio.py
  - backend/writes.py
  - backend/schemas.py
  - backend/tests/test_portfolio.py
autonomous: true
requirements: [INV-01, INV-06, INV-07]
must_haves:
  truths:
    - "A USD-denominated buy converts to IDR at the trade-date rate; current value uses today's rate; unrealized P&L therefore includes FX gain/loss (FX-03)"
    - "A cash holding (asset_type='cash') values as quantity × fx_rate(currency,'IDR',today) with NO price_cache read and no cost-basis P&L except FX movement (CG-01)"
    - "A gold holding (asset_type='gold') gets full average-cost ledger P&L, grams × per-gram price, identical to any crypto/stock position (CG-02)"
    - "A buy/sell event whose currency mismatches its parent holding's currency raises ValueError → 422 (one currency per position)"
    - "portfolio_summary groups holdings by asset_type in addition to the existing platform grouping (VZ-01 data contract)"
    - "A failed FX lookup propagates None/unavailable — never a rate=1.0 fabricated number"
  artifacts:
    - "backend/portfolio.py — FX-aware recompute_holding_from_events + portfolio_summary cash special-case + asset_type grouping"
    - "backend/writes.py — event-currency-vs-holding-currency validation on add_portfolio_event; cash/gold pass-through verified"
    - "backend/schemas.py — portfolio_summary response carries by-asset-type grouping"
    - "backend/tests/test_portfolio.py — FX-aware P&L, cash, gold, currency-mismatch, None-propagation cases"
  key_links:
    - "recompute_holding_from_events converts ev.price*ev.quantity to IDR via fx.get_rate(ev.currency,'IDR',ev.date) BEFORE the two total_cost mutation sites (FX-03/FX-04)"
    - "portfolio_summary special-cases asset_type=='cash' BEFORE the _latest_price read (CG-01)"
    - "cash manual price is set via the D-03 holding-override path; gold manual per-gram price via price_cache source='manual' (CG-03)"
---

<objective>
Make valuation currency-aware and add cash + gold as first-class positions. Inject an
FX-conversion step into `recompute_holding_from_events` (convert each event's native
cost to IDR at its trade-date rate before accumulating — FX-03/FX-04, preserving the
D-02 average-cost invariant), special-case `asset_type='cash'` in `portfolio_summary`
(value = amount × today's FX rate, skipping price_cache entirely — CG-01), let gold ride
the normal ledger unchanged (CG-02, manual per-gram price via price_cache source='manual'
— CG-03), enforce one-currency-per-position at event write time, and add asset-type
grouping to the summary (the VZ-01 pie's data contract). This REVISES Phase 5 D-07.

Purpose: FX-03, CG-01, CG-02, CG-03, INV-01 (currency-aware add/edit), INV-06 (per-holding
P&L now including FX). Depends on Plan 01's `fx.get_rate()`.

Output: FX-aware `portfolio.py`, event-currency validation in `writes.py`, asset-type
grouping in the summary schema, extended `test_portfolio.py`.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-CONTEXT.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-RESEARCH.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-PATTERNS.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FX-aware average-cost + cash special-case + asset-type grouping (portfolio.py)</name>
  <files>backend/portfolio.py, backend/schemas.py, backend/tests/test_portfolio.py</files>
  <read_first>
    - backend/portfolio.py — module docstring (lines 1-18, "All money math is Decimal"); recompute_holding_from_events buy/sell/dividend accumulator (lines 40-97) and its two total_cost mutation sites; unrealized_pnl None-propagation contract (lines 102-113); portfolio_summary's unconditional _latest_price read (~116-129); snapshot_all_holdings per-row try/except (236-283)
    - backend/fx.py — get_rate(base, quote, as_of, db) signature from Plan 01 (returns Decimal|None, never raises)
    - backend/schemas.py — PortfolioSummary and the *Out/*Response naming family for the new asset-type grouping field
    - .planning/phases/07-.../07-RESEARCH.md — Pitfall 1 (cash special-case), Pitfall 4 (mixed-currency — orchestrator LOCKED to one-currency-per-position, so convert-at-trade-date is the path), Anti-Patterns (never rate=1.0), Open Question 2 (cash skips price_cache — LOCKED)
  </read_first>
  <behavior>
    - recompute_holding_from_events for a USD holding with one buy of 200 USD-cost qty=1 on a date whose fx rate is 15800 → total_cost accumulates 3160000 IDR (200×15800, Decimal exact); avg_cost = 3160000 IDR
    - unrealized P&L for that USD holding uses today's rate for current value, so P&L includes FX drift, not just the native price move (FX-03)
    - D-02 sell invariant preserved: a sell leaves avg_cost unchanged (only qty and total_cost drop by avg_cost×sold_qty) — assert avg_cost identical before/after a partial sell
    - cash holding: portfolio_summary computes value = quantity × fx.get_rate(currency,'IDR',today) and performs NO _latest_price/price_cache read for that row (assert via a cash fixture with no price_cache row still producing a value); unrealized gain reflects only FX movement, cost basis semantics per CG-01
    - gold holding (asset_type='gold', quantity=grams, price=per-gram): full average-cost P&L identical to a crypto fixture — reuses existing accumulator unchanged
    - fx.get_rate returning None (vendor outage / gap) makes the affected value/P&L surface None, never rate=1.0 (assert None, not a fabricated number)
    - portfolio_summary returns an asset-type-grouped breakdown alongside the existing platform grouping (each group: asset_type, total current IDR market value) — the VZ-01 pie contract
  </behavior>
  <action>
    In recompute_holding_from_events, inject an FX conversion at the two total_cost mutation sites (buy: total_cost += ev.price*ev.quantity; sell: total_cost -= avg_cost*ev.quantity): convert the native amount to IDR via fx.get_rate(ev.currency or holding.currency, "IDR", ev.date, db) BEFORE accumulating, per RESEARCH approach (a) / FX-03. Do NOT restructure the accumulator — preserve the exact D-02 avg_cost invariant (sell leaves avg_cost unchanged); only multiply through the rate at those two sites. If get_rate returns None, propagate None up (do not accumulate a rate=1.0 value). In portfolio_summary, add an explicit branch BEFORE the _latest_price read: if h.asset_type == "cash": value = h.quantity * fx.get_rate(h.currency, "IDR", today, db) and skip the price_cache lookup entirely (CG-01, Open Question 2 LOCKED) — do NOT write a synthetic price_cache row. Gold needs no branch — it flows through the existing ledger/price_cache path (CG-02; its per-gram price is a normal price_cache source='manual' row, CG-03, no code change). Add an asset-type grouping to portfolio_summary's return (group current IDR market value by asset_type) and expose it via a new field on the PortfolioSummary response schema in backend/schemas.py following the *Out/*Response convention — this is the VZ-01 pie's data contract. Extend backend/tests/test_portfolio.py with the FX-aware, cash, gold, D-02-invariant, None-propagation, and asset-type-grouping cases, mocking fx.get_rate for deterministic rates.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_portfolio.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_portfolio.py -x` passes
    - FX conversion asserted exact: 200 USD at trade-date rate 15800 → 3160000 IDR as Decimal (no float drift)
    - Cash test asserts value produced WITHOUT any price_cache row present (special-case proven)
    - D-02 invariant test asserts avg_cost unchanged across a partial sell
    - None-propagation test asserts value/P&L is None (not a number) when fx.get_rate returns None
    - `grep -n 'asset_type' backend/schemas.py` shows the new summary grouping field
  </acceptance_criteria>
  <done>recompute_holding_from_events is FX-aware at both total_cost sites with the D-02 invariant intact; cash special-cased before price_cache; gold rides the ledger unchanged; asset-type grouping in the summary; None never becomes rate=1.0; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event-currency validation + cash/gold write pass-through (writes.py)</name>
  <files>backend/writes.py, backend/tests/test_portfolio.py</files>
  <read_first>
    - backend/writes.py — apply_add_holding (lines 222-244, already passes platform_id/currency/asset_type correctly); apply_add_portfolio_event and the D-03 override write path (lines 264-285); audit_log usage
    - backend/models.py — Holding.currency, PortfolioEvent.currency (added in Plan 01) for the validation comparison
    - .planning/phases/07-.../07-RESEARCH.md — Pitfall 4 (one currency per position — orchestrator LOCKED: validate event.currency against holding.currency, raise ValueError on mismatch)
  </read_first>
  <behavior>
    - apply_add_portfolio_event for a buy whose currency == parent holding's currency succeeds and stamps event.currency
    - apply_add_portfolio_event for a buy whose currency != parent holding's currency raises ValueError (→ 422 at the API boundary) — no cross-currency averaging (correctness-by-construction)
    - apply_add_holding with asset_type='cash' persists quantity/currency and does NOT require avg_cost-based P&L wiring (cash rides the D-03 override path per CG-01, pass-through unchanged)
    - apply_add_holding with asset_type='gold' persists like any ledger holding (grams as quantity) — pass-through unchanged (CG-02)
  </behavior>
  <action>
    In backend/writes.py apply_add_portfolio_event: before persisting, resolve the parent holding for (ticker, platform_id) and validate the event's currency against holding.currency; on mismatch raise ValueError("event currency {x} does not match holding currency {y}") — the API layer already maps ValueError→422 (no cross-currency averaging, one currency per position, orchestrator LOCKED). Default the event's currency to the holding's currency when the event omits it. Verify (no code change expected, add a test asserting it) that apply_add_holding already threads currency + asset_type unchanged so cash and gold holdings persist correctly through the existing helper — cash via the D-03 override path (CG-01), gold as a normal ledger holding (CG-02). Preserve the existing audit_log write on every mutation (do not drop the audit trail). Add the validation + pass-through cases to backend/tests/test_portfolio.py (or test_write_tools.py if the executor finds the write-helper tests live there — check both).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_portfolio.py tests/test_write_tools.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Currency-mismatch test asserts ValueError raised by apply_add_portfolio_event
    - Matching-currency test succeeds and event.currency is persisted
    - Cash and gold holdings persist via apply_add_holding with asset_type + currency intact (pass-through proven)
    - audit_log row still written on the mutation (audit trail preserved)
  </acceptance_criteria>
  <done>Event currency is validated against the parent holding (one currency per position, ValueError→422); cash/gold flow through apply_add_holding unchanged; audit trail preserved; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (write) | Buy/sell events and holding balances cross here; require_api_key + Pydantic validation already enforced upstream |
| valuation → fx.get_rate | FX rate feeds money math; a wrong/missing rate corrupts displayed P&L |
| app → PostgreSQL | Parameterized SQL only; audit_log on every mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-INT | Repudiation | FX-converted cost basis / P&L integrity | high | mitigate | Convert native→IDR as Decimal(str(x)) at trade-date rate; propagate None on failed lookup (never rate=1.0) so a confident-wrong P&L is impossible; D-02 avg_cost invariant preserved (no silent restructuring of money math) |
| T-07-02-CUR | Tampering | mixed-currency buys on one position | medium | mitigate | Validate event.currency against holding.currency at write time; raise ValueError→422 on mismatch — one currency per position, no nonsensical blended avg_cost (Pitfall 4) |
| T-07-02-AUD | Repudiation | audit trail on cash/gold writes | medium | mitigate | Cash/gold ride existing apply_add_holding/apply_add_portfolio_event helpers which already write audit_log; validation added before persist does not bypass the audit write |
| T-07-02-CASH | Information Disclosure | cash valuation reading stale price_cache | low | accept | Cash special-cased to skip price_cache (CG-01); no stale-price surface for cash |
| T-07-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit); nothing to install |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_portfolio.py tests/test_write_tools.py -x` green
- `cd backend && python -m pytest` full suite green (regression gate)
- FX conversion, cash special-case, gold ledger, currency-mismatch, None-propagation all asserted
</verification>

<success_criteria>
- Cost basis converts at trade-date rate, current value at today's rate; unrealized P&L includes FX movement (FX-03)
- Cash values as amount × FX with no price_cache read (CG-01); gold rides the ledger (CG-02/CG-03)
- One currency per position enforced at write time (ValueError→422)
- Asset-type grouping present in the summary (VZ-01 data contract)
- No fabricated rate=1.0 anywhere — None propagates
</success_criteria>

<output>
Create `.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-02-SUMMARY.md` when done
</output>
