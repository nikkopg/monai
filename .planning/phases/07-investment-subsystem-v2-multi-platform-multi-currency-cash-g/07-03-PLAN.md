---
phase: 07-investment-subsystem-v2-multi-platform-multi-currency-cash-g
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - ui/app/investments/AllocationPieChart.tsx
  - ui/app/investments/page.tsx
autonomous: true
requirements: [INV-06]
must_haves:
  truths:
    - "The allocation pie renders current IDR market value per group, toggling between asset-type and platform groupings (both already on the summary payload)"
    - "The pie reads the /investments/summary asset-type + platform groupings shipped in Plan 02 — no new backend endpoint"
    - "The chart wrapper has an explicit resolvable height so Recharts ResponsiveContainer is not blank"
  artifacts:
    - "ui/app/investments/AllocationPieChart.tsx — Recharts PieChart clone of CategoryDonut.tsx"
    - "ui/app/investments/page.tsx — renders AllocationPieChart from the existing summary fetch"
  key_links:
    - "AllocationPieChart consumes the asset-type grouping added to PortfolioSummary in Plan 02 (VZ-01 basis = current IDR market value)"
    - "asset-type ↔ platform toggle switches the grouping key both from the same summary payload"
    - "explicit height wrapper (width 100% / height 280) — load-bearing per 04-RESEARCH Recharts pitfall"
---

<objective>
Ship the VZ-01 allocation pie: a Recharts `PieChart` (structural clone of the Phase-4
`CategoryDonut.tsx`) whose value basis is current IDR market value, with a toggle between
asset-type and platform groupings — both already present on the `/investments/summary`
payload after Plan 02. This is the data-faithful component only; per --skip-ui, exact
placement, toggle chrome, and visual polish are deferred to a later `/gsd-ui-phase 7`
pass (noted here so execute-phase treats visual layout as a follow-up, not a gap).

Purpose: VZ-01 (allocation at a glance), contributing to INV-06 (portfolio value view).
Depends on Plan 02's asset-type grouping in the summary schema.

Output: `AllocationPieChart.tsx` + a wire into `page.tsx`.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-CONTEXT.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-PATTERNS.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AllocationPieChart component (clone CategoryDonut) with asset-type↔platform toggle</name>
  <files>ui/app/investments/AllocationPieChart.tsx</files>
  <read_first>
    - ui/app/cashflow/charts/CategoryDonut.tsx — the exact PieChart/Cell/Tooltip shape to clone (colors, label formatting, dark-theme tooltip)
    - ui/app/investments/page.tsx — the existing summary shape / TypeScript types the component will consume (asset-type + platform groupings from Plan 02)
    - .planning/phases/07-.../07-PATTERNS.md — Recharts fixed-height wrapper pattern (width 100% / height 280, load-bearing) and the CategoryDonut→pie mapping notes
  </read_first>
  <action>
    Create ui/app/investments/AllocationPieChart.tsx as a near-verbatim clone of ui/app/cashflow/charts/CategoryDonut.tsx (PieChart + Pie + Cell + Tooltip + ResponsiveContainer). Swap CategoryDonut's category/total keys for a grouping prop: the component accepts the two groupings from the /investments/summary payload (by asset_type and by platform, both shipped in Plan 02) and a `groupBy: 'asset_type' | 'platform'` prop selecting which array to render; value basis = current IDR market value per group (VZ-01). Keep the explicit-height wrapper (div width 100% height 280 around ResponsiveContainer) — this is load-bearing (04-RESEARCH pitfall: ResponsiveContainer renders blank inside a flex/grid parent with no resolvable height). Format IDR values with the same currency formatting the investments page already uses. Do NOT invent placement/toggle chrome beyond a minimal prop-driven switch — visual layout is deferred to /gsd-ui-phase 7 (note in a top-of-file comment).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && npx tsc --noEmit` reports no errors
    - AllocationPieChart accepts a groupBy prop switching asset_type vs platform from one summary payload
    - `grep -n 'height: 280' ui/app/investments/AllocationPieChart.tsx` (or equivalent explicit height) present — the load-bearing wrapper
    - Value basis is current IDR market value (reads the summary's market-value field, not cost basis)
  </acceptance_criteria>
  <done>AllocationPieChart renders current-IDR-value allocation with an asset-type↔platform toggle, cloned from CategoryDonut, explicit-height wrapper intact, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AllocationPieChart into the investments page</name>
  <files>ui/app/investments/page.tsx</files>
  <read_first>
    - ui/app/investments/page.tsx — the existing summary fetch + render structure; the asset-type/platform grouping now on the payload (Plan 02)
    - ui/app/investments/AllocationPieChart.tsx — the component's props (from Task 1)
    - ui/app/api/[...proxy]/route.ts — the fetch/key-injection pattern (no new fetch needed — reuse the existing summary fetch)
  </read_first>
  <action>
    In ui/app/investments/page.tsx, render <AllocationPieChart> fed by the EXISTING /investments/summary fetch result (do NOT add a new fetch or endpoint — the asset-type + platform groupings are already on that payload after Plan 02). Add a minimal groupBy toggle (a simple state-driven asset_type↔platform switch) wired to the component prop. Keep placement minimal/functional — precise layout and toggle styling are deferred to /gsd-ui-phase 7. Do not disturb the existing holdings table / summary rendering.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && npx tsc --noEmit` clean
    - `grep -n 'AllocationPieChart' ui/app/investments/page.tsx` shows the component imported and rendered
    - No new fetch/endpoint added — the component reads the existing summary payload
    - Existing holdings table / summary blocks unchanged (still present)
  </acceptance_criteria>
  <done>The investments page renders the allocation pie from the existing summary fetch with an asset-type↔platform toggle; tsc clean; existing UI intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js proxy → API | Read-only fetch of /investments/summary (open read, existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-XSS | Tampering | Recharts label/tooltip rendering | low | mitigate | Values are numeric IDR from the trusted summary payload rendered via Recharts/React (auto-escaped); no dangerouslySetInnerHTML — clone CategoryDonut's safe rendering |
| T-07-03-INT | Information Disclosure | pie value basis correctness | medium | mitigate | Value basis reads the summary's current-IDR-market-value field (produced by Plan 02's audited valuation) — no client-side money math that could diverge from the server number |
| T-07-03-SC | Tampering | npm installs | low | accept | Recharts ^3.9.2 already installed (RESEARCH Environment Availability); no new package |
</threat_model>

<verification>
- `cd ui && npx tsc --noEmit` clean
- AllocationPieChart imported + rendered in page.tsx from the existing summary fetch
- Explicit-height wrapper present (Recharts blank-render pitfall avoided)
- Manual visual check deferred to /gsd-ui-phase 7 (per 07-VALIDATION Manual-Only table)
</verification>

<success_criteria>
- Allocation pie shows current IDR market value with an asset-type↔platform toggle (VZ-01)
- No new backend endpoint — reads the Plan 02 summary grouping
- tsc clean; existing investments UI undisturbed
</success_criteria>

<output>
Create `.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-03-SUMMARY.md` when done
</output>
