---
phase: 07-investment-subsystem-v2-multi-platform-multi-currency-cash-g
plan: 05
type: execute
wave: 2
depends_on: ["07-04"]  # file-serialization only: both touch backend/main.py; no logical dependency (CH-01 is independent of the FX model)
files_modified:
  - backend/tools.py
  - backend/main.py
  - backend/tests/test_tools.py
  - backend/tests/test_proposals.py
autonomous: true
requirements: [CHAT-03, INV-07]
must_haves:
  truths:
    - "find_platforms and find_accounts read tools return rows with ids so the agent can resolve a name→id before a propose_* call"
    - "propose_add_holding accepts platform_id and includes it in the proposal's after dict"
    - "_execute_proposal_payload's add_holding/edit_holding branches DELEGATE to writes.py helpers instead of duplicating inline Holding construction"
    - "A chat-initiated add_holding confirm succeeds and satisfies the platform_id NOT NULL constraint (the CH-01 regression is closed)"
    - "The propose→confirm + audit-log + require_api_key invariants are preserved through the delegation"
  artifacts:
    - "backend/tools.py — find_platforms, find_accounts (registered in TOOLS); propose_add_holding accepting platform_id"
    - "backend/main.py — _execute_proposal_payload add_holding/edit_holding branches calling apply_add_holding/apply_edit_holding"
    - "backend/tests/test_tools.py — find_platforms/find_accounts read-tool tests"
    - "backend/tests/test_proposals.py — confirm-path integration test proving the regression fix (both call sites)"
  key_links:
    - "find_platforms/find_accounts mirror find_transactions (ILIKE filter, clamped limit, ids-first) — CH-01"
    - "the two-site fix: propose_add_holding param + _execute_proposal_payload delegation (Pitfall 2)"
    - "delegation deletes ~35 lines and auto-inherits platform_id/coingecko_id handling from writes.py"
---

<objective>
Close the CH-01 chat write-path regression (multi-platform ripple from scope item 1) with
its full two-site fix, and add the two missing read tools. Site 1: `propose_add_holding`
in `backend/tools.py` gains a `platform_id` parameter and includes it in the proposal.
Site 2 (the actual root cause): `_execute_proposal_payload`'s `add_holding`/`edit_holding`
branches in `backend/main.py` currently DUPLICATE inline `Holding(...)` construction and
never read `platform_id` — replace them with calls to `apply_add_holding` /
`apply_edit_holding` from `writes.py` (deletes ~35 lines, auto-inherits
platform_id/coingecko_id, fixes the NOT NULL 500). Add `find_platforms` + `find_accounts`
read tools (mirroring `find_transactions`) so the agent resolves platform/account name→id
before proposing — `find_accounts` also closes the STATE.md "Pending Todos" account-id gap
that would otherwise block chat "delete my BCA account".

Purpose: CH-01, CHAT-03 (chat correlation/agentic path stays functional), INV-07 (portfolio
events via chat). Independent of the FX model — runs in parallel.

Output: two read tools + the two-site CH-01 fix + regression tests.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-CONTEXT.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-RESEARCH.md
@.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: find_platforms + find_accounts read tools; propose_add_holding accepts platform_id</name>
  <files>backend/tools.py, backend/tests/test_tools.py</files>
  <read_first>
    - backend/tools.py — find_transactions (lines 412-437, the read-tool shape: optional ILIKE filter, limit clamped max(1,min(int(limit),50)), ids-first return); _account_to_dict (519-520, reuse for find_accounts); the TOOLS registry dict; propose_add_holding (802-828, currently missing platform_id entirely)
    - backend/models.py — Platform and Account models for the query + _platform_to_dict fields
    - .planning/phases/07-.../07-PATTERNS.md — the find_platforms/find_accounts assignment (mirror find_transactions, register in TOOLS, reuse _account_to_dict, add _platform_to_dict); .planning/phases/07-.../07-CONTEXT.md CH-01
  </read_first>
  <behavior>
    - find_platforms() returns platforms with their ids; find_platforms(name='bit') filters via ILIKE '%bit%'; limit is clamped to max(1, min(int(limit), 50))
    - find_accounts() returns accounts with their ids (closing the STATE.md account-id gap); find_accounts(name='bca') filters via ILIKE; limit clamped identically
    - both are registered in the TOOLS dict alongside find_transactions (assert 'find_platforms' and 'find_accounts' are keys)
    - propose_add_holding(ticker, quantity, avg_cost, platform_id=..., ...) includes platform_id in the returned proposal's after dict (assert after['platform_id'] == the passed value)
  </behavior>
  <action>
    Add find_platforms(name: str | None = None, limit: int = 10) -> dict and find_accounts(name: str | None = None, limit: int = 10) -> dict to backend/tools.py, both mirroring find_transactions exactly: optional ILIKE filter on the name column, limit clamped via max(1, min(int(limit), 50)), returning ids so the agent can resolve name→id before a propose_* call. Reuse the existing _account_to_dict for find_accounts row serialization; write a new _platform_to_dict following the same one-line dict-literal convention. Register both in the TOOLS registry dict ("find_platforms": find_platforms, "find_accounts": find_accounts) alongside find_transactions. Add a platform_id: int parameter to propose_add_holding and include it in the after dict (CH-01 fix site 1 of 2, Pitfall 2). Add find_platforms/find_accounts read-tool tests + the propose_add_holding-includes-platform_id test to backend/tests/test_tools.py.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_tools.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_tools.py -x` passes
    - find_platforms / find_accounts return rows containing ids; ILIKE filter + clamped limit asserted
    - `grep -n 'find_platforms\|find_accounts' backend/tools.py` shows both registered in TOOLS
    - propose_add_holding test asserts after['platform_id'] equals the passed platform_id
  </acceptance_criteria>
  <done>find_platforms + find_accounts read tools exist and are registered in TOOLS; propose_add_holding accepts and forwards platform_id; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _execute_proposal_payload delegates to writes.py (CH-01 fix site 2)</name>
  <files>backend/main.py, backend/tests/test_proposals.py</files>
  <read_first>
    - backend/main.py — _execute_proposal_payload add_holding/edit_holding inline branches (lines 757-789, the duplication to delete — neither reads after.get("platform_id")); create_holding/update_holding (362-406) as the delegate-to-writes.py target pattern
    - backend/writes.py — apply_add_holding (222-244, already threads platform_id/coingecko_id correctly) and apply_edit_holding — the helpers to delegate to; audit_log write inside them
    - .planning/phases/07-.../07-RESEARCH.md — Pitfall 2 (two-site fix, the confirm-time write is the real bug), Anti-Patterns (delegate, don't duplicate), Security Domain (DoS via IntegrityError on legitimate confirm)
  </read_first>
  <behavior>
    - a chat-initiated add_holding proposal carrying platform_id, when confirmed via _execute_proposal_payload, writes a Holding WITH platform_id set (no null-value-in-column-platform_id IntegrityError) — this test FAILS against current code and PASSES after the fix (proves the regression is closed)
    - the add_holding branch now calls apply_add_holding(db, after); the edit_holding branch calls apply_edit_holding(db, row["id"], after, before) — inline Holding(...) construction is gone
    - the audit_log row is still written on confirm (delegation preserves the audit trail, since writes.py helpers write it)
    - the confirm endpoint still enforces require_api_key and the propose→confirm token flow (delegation does not bypass the existing gate)
  </behavior>
  <action>
    Replace the entire inline add_holding and edit_holding branches in _execute_proposal_payload (backend/main.py ~757-789) with calls to apply_add_holding(db, after) and apply_edit_holding(db, row["id"], after, before) from writes.py — this is CH-01 fix site 2 of 2 (Pitfall 2), the actual root cause. The delegation deletes ~35 lines and automatically inherits platform_id + coingecko_id handling (writes.py's helpers already thread them, verified). Do NOT change the surrounding propose→confirm token validation, the require_api_key dependency on the confirm route, or the audit-log behavior (writes.py already writes audit_log, so the trail is preserved — do not double-write it). Add a confirm-path integration test to backend/tests/test_proposals.py that builds an add_holding proposal WITH platform_id and asserts the confirm succeeds with platform_id persisted (this test must fail against the pre-fix inline branch and pass after delegation — proving the regression closure), plus an assertion that the audit_log row exists after confirm.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_proposals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_proposals.py -x` passes
    - Confirm-path test asserts platform_id is persisted on the created Holding (no IntegrityError) — the CH-01 regression is closed
    - `grep -n 'apply_add_holding\|apply_edit_holding' backend/main.py` shows the delegation in _execute_proposal_payload
    - Inline Holding(...) construction removed from the add_holding branch (grep for the old inline `Holding(` inside _execute_proposal_payload returns nothing for that branch)
    - Audit-log assertion: a holding audit row exists after confirm (trail preserved)
  </acceptance_criteria>
  <done>_execute_proposal_payload delegates to writes.py; platform_id flows through on chat confirm (regression closed); propose→confirm + require_api_key + audit-log invariants preserved; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent/chat → propose→confirm write path | The LLM produces a proposal; the user confirms; the confirm endpoint mutates data behind require_api_key |
| client → POST /proposals/confirm | proposal token + payload cross here (untrusted) |
| API → PostgreSQL | Holding/AuditLog writes via writes.py helpers; parameterized SQL only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-DoS | Denial of Service | confirm-time IntegrityError on legitimate chat add_holding | high | mitigate | Delegate to apply_add_holding so platform_id is threaded — closes the NOT NULL 500 that currently breaks a legitimate user action (Pitfall 2 / Security Domain) |
| T-07-05-AUD | Repudiation | audit trail on chat-initiated writes | high | mitigate | Delegation to writes.py preserves the audit_log write (helpers write it); test asserts the audit row exists post-confirm — no unaudited mutation path |
| T-07-05-AUTH | Elevation of Privilege | confirm route bypass via delegation refactor | high | mitigate | The require_api_key dependency and propose→confirm token validation are left untouched; only the inner Holding-construction is delegated — the gate stays in front |
| T-07-05-INJ | Tampering | find_platforms/find_accounts name filter | low | mitigate | ILIKE via parameterized bound param (mirror find_transactions) — no raw string interpolation into SQL |
| T-07-05-SC | Tampering | npm/pip installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit); nothing to install |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_tools.py tests/test_proposals.py -x` green
- `cd backend && python -m pytest` full suite green (regression gate — the confirm-path test proves CH-01 closed)
- Delegation present in _execute_proposal_payload; inline Holding construction removed from the add_holding branch
- require_api_key + audit_log invariants asserted preserved
</verification>

<success_criteria>
- find_platforms + find_accounts resolve name→id (CH-01; find_accounts closes the STATE.md account-id gap)
- The two-site CH-01 fix lands: propose_add_holding forwards platform_id AND _execute_proposal_payload delegates to writes.py
- Chat add_holding confirm succeeds with platform_id set (regression closed), audit + auth invariants intact
</success_criteria>

<output>
Create `.planning/phases/07-investment-subsystem-v2-multi-platform-multi-currency-cash-g/07-05-SUMMARY.md` when done
</output>
