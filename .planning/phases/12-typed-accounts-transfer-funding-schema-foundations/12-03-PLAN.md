---
phase: 12-typed-accounts-transfer-funding-schema-foundations
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - backend/tools.py
autonomous: true
requirements: [ACCT-03]

must_haves:
  truths:
    - "Every cashflow TOTAL/listing reads FROM cashflow_transactions: spending_total, income_total, net_total, spending_by_category (both the _ROLLUP_FROM and the grand-total-for-percentage), transaction_count, largest_transactions, average_daily_spending (the total, not the date span), monthly_trend, find_transactions"
    - "tools.spending_total(period='all_time') excludes the ~45.9M investment phantom (Criterion 2, application half)"
    - "account_balances, the average_daily_spending MIN/MAX date span, and the account/category delete-guard COUNTs still read FROM transactions (intentionally untouched)"
  artifacts:
    - backend/tools.py
  key_links:
    - "the switch is FROM-clause-only; parameterized text() WHERE clauses and explicit column lists are unchanged (no SELECT * against the view — Pitfall 5)"
    - "raw_spending − tools.spending_total == investment_expense proves the application layer inherits the structural exclusion"
---

<objective>
Switch every cashflow total and transaction listing in backend/tools.py from `FROM transactions` to `FROM cashflow_transactions` so the application layer inherits the structural investment-exclusion the view enforces. This turns the tools-level exclusion test green and completes Criterion 2 — the double-count bug becomes impossible in any cashflow query, present or future.

Purpose: The view owns the invariant; tools.py inherits it by reading the view. This is the "structurally impossible to forget" mechanism (D-07), not convention.
Output: updated backend/tools.py.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch every cashflow-total FROM-clause in tools.py to the view</name>
  <files>backend/tools.py</files>
  <read_first>
    - backend/tools.py — read each site before editing: L127 (spending_total), L144 (income_total), L161 (net_total), L236–237 (_ROLLUP_FROM used by spending_by_category), L302 (spending_by_category grand-total for percentages — NOT listed in PATTERNS.md, found during planning; it MUST switch or by-category percentages divide by the un-excluded base), L383 (transaction_count), L403 (largest_transactions), L426 (average_daily_spending total) and L434 (its MIN/MAX date span — LEAVE), L454 (monthly_trend), L547 (find_transactions). Also confirm the LEAVE sites: L101 (currency probe), L472+ (account_balances), L880/L937/L989 (delete/reassign COUNT guards).
    - .planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-PATTERNS.md L119–134 (the switch/leave table) and 12-RESEARCH.md L199–211, L406–409 (why largest_transactions and find_transactions switch too; why account_balances stays).
  </read_first>
  <action>
    In backend/tools.py, change ONLY the table name in the FROM clause (from transactions to cashflow_transactions) at exactly these ten FROM-clause edits across nine functions (spending_by_category has two: _ROLLUP_FROM L237 and the grand-total denominator L302); leave WHERE clauses, bound params, and explicit column lists untouched:
    L127 spending_total; L144 income_total; L161 net_total; L237 _ROLLUP_FROM (feeds spending_by_category); L302 the spending_by_category grand-total (SUM(-amount) used for the percentage denominator); L383 transaction_count; L403 largest_transactions; L426 average_daily_spending total; L454 monthly_trend; L547 find_transactions.
    LEAVE these on FROM transactions (intentional): L101 currency probe; L434 the average_daily_spending MIN(date)/MAX(date) span (a date range, not a total — switching it would shrink the denominator days); L472+ account_balances (already LEFT JOINs accounts; per-account list, not a total — add a one-line code comment here that the liquid/investment net-worth split is Phase 15, CONTEXT discretion item D); L880/L937/L989 delete/reassign COUNT guards (row-existence checks, not cashflow totals).
    Do NOT introduce `SELECT * FROM cashflow_transactions` — the view's t.* is frozen at creation; keep the existing explicit column selects (Pitfall 5). After editing, confirm no cashflow-total site still reads `FROM transactions`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_cashflow_view.py::test_tools_spending_excludes_investment backend/tests/test_tools.py -x && python -m pytest backend/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - test_cashflow_view.py::test_tools_spending_excludes_investment is GREEN: tools.spending_total(period='all_time')['total'] equals the view-computed spending total (excludes the ~45.9M investment expense).
    - Full suite `python -m pytest backend/tests/` is green — no cashflow tool left reading the base table (the per-wave full suite is the net that catches a missed switch).
    - All ten listed FROM-clause sites (nine functions; spending_by_category has two) read FROM cashflow_transactions; L101, L434, account_balances (L472+), and L880/L937/L989 still read FROM transactions.
    - account_balances carries a code comment pointing the net-worth liquid/investment split to Phase 15.
  </acceptance_criteria>
  <done>All cashflow totals and listings inherit the investment exclusion from the view; the application-layer double-count is gone; the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools.py read SQL → Postgres view | LLM never emits SQL; all reads are parameterized text() with the FROM identifier as a code constant. Only the table name changed; no new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Tampering/Repudiation | a cashflow total left on FROM transactions still double-counts the investment account | high | mitigate | Explicit nine-site switch map + full-suite gate that fails if any total omits the view; the L302 grand-total (missed by PATTERNS.md) is called out explicitly. |
| T-12-04 | Tampering | a LEAVE-site (account_balances / date span / delete guards) switched by mistake, dropping NULL-account rows or shrinking a denominator | medium | mitigate | Acceptance criteria pin the exact LEAVE sites; test suite covers account_balances and average_daily_spending behavior. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Full suite `python -m pytest backend/tests/` green, including test_cashflow_view.py::test_tools_spending_excludes_investment (application-layer Criterion 2) and test_double_count_delta.
- PHASE GATE (manual, human): the running container serves stale code until rebuilt — run `docker compose up -d --build`, then check `spending_total`/`net_total` via the API and confirm the ~45.9M "Investments" phantom is gone (memory: "Deploy requires rebuild"). This is the end-to-end proof before `/gsd-verify-work`; the automated tests already prove it at the DB/tools layer.
</verification>

<success_criteria>
Criterion 2 fully met: accounts.type is DB-enforced AND every cashflow total excludes investment-typed accounts structurally. The double-count is impossible to reintroduce in any cashflow query. Full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-03-SUMMARY.md` when done.
</output>
