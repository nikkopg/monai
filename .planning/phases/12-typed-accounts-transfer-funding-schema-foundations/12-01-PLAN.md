---
phase: 12-typed-accounts-transfer-funding-schema-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_typed_accounts.py
  - backend/tests/test_cashflow_view.py
autonomous: true
requirements: [ACCT-03]

must_haves:
  truths:
    - "test_typed_accounts.py encodes Criterion 1 (D-02 classification, zero NULL), the CHECK+default constraint, and Criterion 3 pairing-column introspection — all RED before migration 010"
    - "test_cashflow_view.py encodes Criterion 2: view excludes investment rows, keeps NULL-account_id rows, and the raw−view == investment-expense double-count delta — plus a tools-level spending_total exclusion (RED until Plan 03)"
    - "both files collect cleanly (pytest --co exits 0) even though migration 010 does not exist yet"
  artifacts:
    - backend/tests/test_typed_accounts.py
    - backend/tests/test_cashflow_view.py
  key_links:
    - "the ACCOUNT_TYPE pure-function test lazy-loads alembic/versions/010_typed_accounts.py INSIDE the test body (not at module import) so collection succeeds before the migration file exists"
    - "view-level tests and tools-level tests carry distinct node ids so Plan 02 and Plan 03 can each target their own green subset"
---

<objective>
Create the Nyquist Wave 0 validation scaffold: the two pytest files that encode all three Phase 12 success criteria plus constraint enforcement, BEFORE any migration or code edit exists. These tests are RED now and turn GREEN across Plans 02 (migration) and 03 (tools switch).

Purpose: Every later task gets a concrete automated verify target. Correctness-by-construction on live financial data demands the proof exists before the migration touches the DB.
Output: backend/tests/test_typed_accounts.py, backend/tests/test_cashflow_view.py.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_typed_accounts.py — classification, CHECK+default, pairing-column introspection</name>
  <files>backend/tests/test_typed_accounts.py</files>
  <read_first>
    - backend/tests/test_category_migration.py — the importlib migration-load idiom (L13–55) AND the `_ensure_real_alembic_package()` shim (L25–55) that stops the repo's own `alembic/` dir from shadowing the pip package. Copy the shim VERBATIM; it is a required workaround.
    - backend/tests/conftest.py — confirms tests run against the LIVE dev DB with NO fresh-migrate fixture. NOTE: conftest exposes only client/api_key/purge — there is NO engine or session fixture. Import the `backend.db.engine` singleton directly (as test_tools.py does); do not hunt for or invent a fixture.
    - backend/tests/test_tools.py — the live-DB introspection/query idiom (`from backend.db import engine`, then `sa.inspect(engine)` / `engine.connect()` inline) to mirror.
    - backend/models.py L46–52 (Account.type) — current shape you are asserting the NEW state of.
  </read_first>
  <action>
    Create backend/tests/test_typed_accounts.py with four tests:
    (1) test_account_type_map — lazy-load the migration module by path INSIDE the test body (not at module scope): MIGRATION_PATH = repo-root/alembic/versions/010_typed_accounts.py, load via the importlib idiom from test_category_migration.py including the `_ensure_real_alembic_package()` shim; assert `migration.ACCOUNT_TYPE == {1: "liquid", 2: "liquid", 3: "investment", 559: "liquid"}` (D-02). Loading inside the body means collection succeeds and this test simply FAILS (FileNotFoundError) until Plan 02 writes the file — that is the intended RED.
    (2) test_account_classification — Criterion 1: query the live DB via the `backend.db.engine` singleton; assert every accounts row has type in {liquid, investment}, zero rows with type IS NULL, and specifically id 1→liquid, id 2→liquid, id 559→liquid, id 3→investment.
    (3) test_type_check_and_default — insert an Account with type='bogus' and assert it raises (CHECK ck_accounts_type violation, IntegrityError); in a separate transaction insert an Account omitting type and assert it reads back 'liquid' (server_default). Roll back both so the live DB is unchanged.
    (4) test_pairing_columns — Criterion 3: `insp = sa.inspect(engine)`; assert transactions has column transfer_pair_id nullable True and an index covering it; assert portfolio_events has column source_account_id nullable True and an index covering it.
    Import the `backend.db.engine` singleton (as test_tools.py does); do NOT reference a conftest engine/session fixture (none exists) and do NOT add a new DB connection. No new imports beyond sqlalchemy + `backend.db.engine`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_typed_accounts.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests/test_typed_accounts.py --co -q` exits 0 (all four tests collect; NO import-time error despite 010 not existing yet).
    - The four node ids exist: test_account_type_map, test_account_classification, test_type_check_and_default, test_pairing_columns.
    - Running the file (without --co) is RED (expected pre-migration), never an ERROR on collection.
  </acceptance_criteria>
  <done>test_typed_accounts.py exists, collects cleanly, and encodes Criteria 1 & 3 + constraint enforcement as four named, currently-RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_cashflow_view.py — view invariants + double-count delta + tools-level exclusion</name>
  <files>backend/tests/test_cashflow_view.py</files>
  <read_first>
    - backend/tests/test_tools.py — the live-DB SQL invariant idiom (shared engine, `text()` queries) to mirror.
    - backend/tests/conftest.py — no engine/session or fresh-migrate fixture exists (only client/api_key/purge); import the `backend.db.engine` singleton directly, as test_tools.py does.
    - .planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md L296–306 — the exact double-count proof (raw − view == investment_expense) and NULL-account_id parity check.
    - backend/tools.py L117–129 (spending_total) — the FROM/WHERE shape the tools-level test calls.
  </read_first>
  <action>
    Create backend/tests/test_cashflow_view.py with four named tests, all against the live dev DB via the `backend.db.engine` singleton:
    (1) test_view_excludes_investment — assert `SELECT COUNT(*) FROM cashflow_transactions ct JOIN accounts a ON a.id=ct.account_id WHERE a.type='investment'` returns 0.
    (2) test_view_keeps_null_account — assert `COUNT(*) WHERE account_id IS NULL` is IDENTICAL between transactions and cashflow_transactions (the NOT-EXISTS-keeps-NULL guarantee; a bare NOT IN / inner join would fail this).
    (3) test_double_count_delta — compute raw = SUM(-amount) FROM transactions WHERE amount<0 AND is_transfer=false; view = same FROM cashflow_transactions; inv = same FROM transactions JOIN accounts WHERE a.type='investment'; assert raw - view == inv (the ~45.9M "Investments" phantom, derived live — do NOT hard-code the figure).
    (4) test_tools_spending_excludes_investment — `from backend import tools`; assert `tools.spending_total(period="all_time")["total"]` equals the view-computed spending total (i.e. excludes the investment expense). This is RED until Plan 03 switches tools.py to the view; keep it a distinct node id so Plan 02 does not need it green.
    All queries reference cashflow_transactions, which does not exist until Plan 02 — tests 1–3 fail at query time (relation does not exist) = RED, not a collection error. No new fixtures.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_cashflow_view.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests/test_cashflow_view.py --co -q` exits 0 (four tests collect).
    - Node ids exist: test_view_excludes_investment, test_view_keeps_null_account, test_double_count_delta, test_tools_spending_excludes_investment.
    - The double-count-delta assertion derives the investment magnitude live (no literal 45_900_000 in the assertion).
  </acceptance_criteria>
  <done>test_cashflow_view.py exists, collects cleanly, and encodes Criterion 2 (view-level a/b/c) plus the Plan-03 tools-level exclusion target as four named, currently-RED tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live financial DB | The only untrusted crossing this phase; these tests are the instrument that proves the crossing is safe. No user input reaches SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | migration backfill mis-classifies an account | high | mitigate | test_account_classification asserts the exact D-02 map + zero NULL — this file is the automated detector for T-12-01 (mitigated in Plan 02). |
| T-12-02 | Tampering/Repudiation | exclusion view silently drops NULL-account_id rows | high | mitigate | test_view_keeps_null_account asserts NULL-account_id count parity view vs base — the detector for T-12-02 (mitigated in Plan 02). |
| T-12-03 | Denial/Tampering | non-idempotent/partial migration on live data | medium | mitigate | tests run post-`alembic upgrade head`; a partial migration fails the introspection tests loudly. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_typed_accounts.py backend/tests/test_cashflow_view.py --co -q` exits 0 (all eight tests collect; no import-time errors).
- Running the two files un-collected is RED with clear failures (missing migration / missing view / un-switched tools), never collection ERRORs — proves the scaffold is honest and ready to gate Plans 02–03.
</verification>

<success_criteria>
Both test files exist, collect cleanly, and encode all three ROADMAP success criteria plus CHECK/default enforcement as named tests. No production code changed.
</success_criteria>

<output>
Create `.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-01-SUMMARY.md` when done.
</output>
