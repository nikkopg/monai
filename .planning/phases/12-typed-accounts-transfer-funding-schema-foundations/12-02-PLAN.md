---
phase: 12-typed-accounts-transfer-funding-schema-foundations
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - alembic/versions/010_typed_accounts.py
  - backend/models.py
autonomous: true
requirements: [ACCT-03]

must_haves:
  truths:
    - "After migration: accounts id 1,2,559 → liquid, id 3 → investment; zero NULL types (Criterion 1) — from the locked audit map D-02; Stockbit(559) stays liquid because it is broker cash not stock positions D-03; id 3 'Investments' is the double-count typed investment D-04"
    - "accounts.type is DB-enforced — CHECK(type IN ('liquid','investment')) is a binary closed set with no richer subtypes D-01; plus NOT NULL + server_default 'liquid' so new/imported accounts auto-get liquid D-05 (Criterion 2, schema half)"
    - "cashflow_transactions view exists, excludes investment-account rows via NOT EXISTS keyed on type='investment' (over-include-visibly, never vanish D-06), and KEEPS NULL-account_id rows; the view is the structural exclusion mechanism D-07"
    - "transactions.transfer_pair_id (Integer, nullable, indexed, no FK) and portfolio_events.source_account_id (Integer, nullable, indexed, FK→accounts.id) exist (Criterion 3)"
  artifacts:
    - alembic/versions/010_typed_accounts.py
    - backend/models.py
    - cashflow_transactions (DB view)
    - ck_accounts_type (CHECK constraint)
  key_links:
    - "010 down_revision = e5f6a7b8c9d0 (009's revision id) — chains onto the category-hierarchy migration"
    - "view predicate is NOT EXISTS (SELECT 1 FROM accounts a WHERE a.id=t.account_id AND a.type='investment') — never inner join, never bare NOT IN, never type != 'liquid'"
    - "backend/models.py Account.type mirrors the DB: nullable=False, server_default='liquid' — so the ORM refreshes the value and the importer's _get_or_create_account keeps working unchanged"
---

<objective>
Write Alembic migration 010_typed_accounts.py and update the ORM to match: backfill the 4 live accounts from the D-02 audit map, abort-loudly if the live account set drifted, constrain accounts.type (CHECK + NOT NULL + server_default 'liquid'), add the two additive pairing columns (nullable, indexed), and create the cashflow_transactions exclusion view. This makes Criteria 1 and 3 green and creates the view that Criterion 2 depends on.

Purpose: Move the liquid/investment discriminator and the cashflow-exclusion invariant from convention into the schema, on live financial data, non-destructively and idempotently — mirroring migration 009's proven idiom.
Output: alembic/versions/010_typed_accounts.py, updated backend/models.py.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-PATTERNS.md
@.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 010_typed_accounts.py (backfill → assert → constrain → additive columns → view)</name>
  <files>alembic/versions/010_typed_accounts.py</files>
  <read_first>
    - alembic/versions/009_category_hierarchy.py — the near-exact analog. Copy the revision-chain header shape (L44–54), the module-const audit map (GROUP_META L63–77), the `sa.inspect()`-guarded add_column/index/FK idiom (L243–258), the abort-loudly RuntimeError pattern (L260–267), and the guarded downgrade (L329–347). This migration is 009 on a smaller dataset plus a view.
    - alembic/env.py L72–73 — single `begin_transaction()` online model: any raise = clean full rollback. Rely on it; do not add manual try/except.
    - backend/models.py L46–52 (Account.type currently String(64) nullable=True), Transaction (account_id, is_transfer), PortfolioEvent (platform_id FK idiom at ~L255) — the tables getting DDL.
    - .planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md L124–162 (Recommended Migration Structure) and L271–306 (backfill + abort assert + double-count proof code).
  </read_first>
  <action>
    Create alembic/versions/010_typed_accounts.py. Header: revision = a new hash string; down_revision = "e5f6a7b8c9d0" (009's revision id, VERIFIED); branch_labels = None; depends_on = None. Module const ACCOUNT_TYPE = {1: "liquid", 2: "liquid", 3: "investment", 559: "liquid"} (D-02; 559=liquid because Stockbit is broker cash, not stock positions — D-03, do NOT re-infer as investment; id 3 "Investments" is the real double-count, typed investment — D-04).
    upgrade() steps IN THIS ORDER (order is load-bearing — SET NOT NULL fails while rows are NULL):
    1. inspector = sa.inspect(op.get_bind()); conn = op.get_bind().
    2. Backfill, idempotent + non-clobbering: for id,t in ACCOUNT_TYPE.items(): conn.execute(sa.text("UPDATE accounts SET type = :t WHERE id = :id AND type IS NULL"), {"t": t, "id": id}). Bound params only.
    3. Abort-loudly asserts (the account analog of 009's unmapped-string abort): live_ids = {r[0] for r in conn.execute(sa.text("SELECT id FROM accounts"))}; if live_ids != set(ACCOUNT_TYPE): raise RuntimeError naming the unexpected/missing ids and instructing to classify them in ACCOUNT_TYPE (D-02) before re-running. Then null_types = conn.execute(sa.text("SELECT COUNT(*) FROM accounts WHERE type IS NULL")).scalar(); if null_types: raise RuntimeError.
    4. CHECK constraint (D-01: binary closed set liquid|investment, no richer subtypes), guarded on inspector.get_check_constraints('accounts'): op.create_check_constraint("ck_accounts_type", "accounts", "type IN ('liquid','investment')").
    5. Tighten: op.alter_column("accounts", "type", existing_type=sa.String(64), nullable=False, server_default="liquid").
    6. Additive columns, each guarded on get_columns/get_indexes/get_foreign_keys: add transactions.transfer_pair_id (sa.Integer, nullable=True) + index ix_transactions_transfer_pair_id; NO FK on transfer_pair_id (RESEARCH Open Q2 — pairing semantics decided in Phase 13). Add portfolio_events.source_account_id (sa.Integer, nullable=True) with FK fk_portfolio_events_source_account → accounts.id + index ix_portfolio_events_source_account_id.
    7. Create the view, guarded on 'cashflow_transactions' not in inspector.get_view_names(): op.execute("CREATE VIEW cashflow_transactions AS SELECT t.* FROM transactions t WHERE NOT EXISTS (SELECT 1 FROM accounts a WHERE a.id = t.account_id AND a.type = 'investment')"). Create the view AFTER step 6 so t.* is a superset including transfer_pair_id (RESEARCH Pitfall 5). The predicate MUST be NOT EXISTS keyed on a.type = 'investment' — this keeps NULL-account_id rows IN and makes a mis-typed account over-include visibly (D-06), never vanish.
    downgrade() strict reverse, each drop guarded: DROP VIEW IF EXISTS cashflow_transactions → drop index+column source_account_id (and its FK) → drop index+column transfer_pair_id → op.alter_column accounts.type back to nullable=True, server_default=None → op.drop_constraint("ck_accounts_type", "accounts", type_="check"). Leave backfilled type VALUES in place (009 downgrade philosophy).
  </action>
  <verify>
    <automated>alembic upgrade head && python -m pytest backend/tests/test_typed_accounts.py::test_account_type_map backend/tests/test_typed_accounts.py::test_account_classification backend/tests/test_typed_accounts.py::test_type_check_and_default backend/tests/test_typed_accounts.py::test_pairing_columns backend/tests/test_cashflow_view.py::test_view_excludes_investment backend/tests/test_cashflow_view.py::test_view_keeps_null_account backend/tests/test_cashflow_view.py::test_double_count_delta -x</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` exits 0 on the live dev DB; re-running it is a no-op (idempotency guards fire, no error).
    - `\d accounts` shows type NOT NULL, DEFAULT 'liquid', and CHECK ck_accounts_type (type IN ('liquid','investment')).
    - Post-migration: accounts id 1,2,559 → 'liquid', id 3 → 'investment'; `SELECT COUNT(*) FROM accounts WHERE type IS NULL` == 0.
    - `\d transactions` shows transfer_pair_id (nullable, index ix_transactions_transfer_pair_id, no FK); `\d portfolio_events` shows source_account_id (nullable, FK fk_portfolio_events_source_account→accounts.id, index ix_portfolio_events_source_account_id).
    - The view test subset (test_view_excludes_investment, test_view_keeps_null_account, test_double_count_delta) exits 0: NULL-account_id rows retained, investment rows excluded, raw−view == investment_expense.
    - If the live account set is not exactly {1,2,3,559}, the migration RAISES a RuntimeError naming the drift and does NOT constrain — that is the guard working (T-12-01).
  </acceptance_criteria>
  <done>Migration 010 applies cleanly and idempotently; accounts.type is DB-enforced and correctly backfilled; both pairing columns and the NOT EXISTS view exist. Criteria 1 & 3 and the view half of Criterion 2 are green.</done>
</task>

<task type="auto">
  <name>Task 2: Update backend/models.py to mirror the constrained schema</name>
  <files>backend/models.py</files>
  <read_first>
    - backend/models.py L46–52 (Account.type), L124–154 (Transaction, incl. category_id nullable-indexed idiom at ~L148), L240–264 (PortfolioEvent, platform_id FK idiom ~L255, currency server_default idiom ~L262) — the three mappings you edit and the in-file analogs to copy.
    - .planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-RESEARCH.md Pitfall 4 (why the ORM must match: expire_on_commit=False means a stale type=None survives if models.py stays nullable).
  </read_first>
  <action>
    Three edits in backend/models.py, matching the DB after migration 010:
    1. Account.type: change from `Mapped[str | None] = mapped_column(String(64), nullable=True)` to `Mapped[str] = mapped_column(String(64), nullable=False, server_default="liquid")` (mirror PortfolioEvent.currency's server_default idiom).
    2. Transaction: add `transfer_pair_id: Mapped[int | None] = mapped_column(Integer, nullable=True, index=True)` — plain Integer, NO ForeignKey (matches migration Task 1; Phase 13 decides self-ref vs group semantics). Mirror the category_id nullable-indexed idiom but without the FK.
    3. PortfolioEvent: add `source_account_id: Mapped[int | None] = mapped_column(ForeignKey("accounts.id"), nullable=True, index=True)` — mirror the platform_id FK idiom but nullable.
    Do NOT touch importer.py: _get_or_create_account constructs Account(name, currency) without type and the server_default fills 'liquid' on INSERT (D-05, verified). No other consumer changes.
  </action>
  <verify>
    <automated>python -c "import backend.models as m; assert m.Account.type.nullable is False; assert 'transfer_pair_id' in m.Transaction.__table__.columns; assert 'source_account_id' in m.PortfolioEvent.__table__.columns; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend.models.Account.type.nullable is False` and its column carries server_default 'liquid'.
    - `backend.models.Transaction.__table__.columns` contains transfer_pair_id (Integer, nullable, no FK); `PortfolioEvent.__table__.columns` contains source_account_id (nullable FK→accounts.id).
    - test_typed_accounts.py::test_type_check_and_default (omit-type → 'liquid' read-back) passes in-process, confirming the ORM and DB agree.
  </acceptance_criteria>
  <done>models.py reflects the migrated schema; the ORM no longer reports stale type=None after insert, and both pairing columns are mapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL/backfill → live financial Postgres | The only crossing. Values are hard-coded D-02 constants + bound `text()` params; the only string-built identifiers are code constants (table/column/constraint names). No external input reaches SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | backfill mis-classifies a live account → wrong cashflow totals | high | mitigate | Hard-coded D-02 ACCOUNT_TYPE map (no auto-inference) + abort-loudly assert: live ids must equal exactly {1,2,3,559} and zero NULL types remain post-backfill, else RuntimeError names the drift and the migration rolls back (env.py single transaction). |
| T-12-02 | Tampering/Repudiation | exclusion view silently drops NULL-account_id rows → phantom data loss from totals | high | mitigate | View uses NOT EXISTS keyed on a.type='investment' (never inner join / bare NOT IN / type!='liquid'); test_view_keeps_null_account asserts NULL-account_id parity view vs base. |
| T-12-03 | Denial/Tampering | non-idempotent / partial migration on live data | medium | mitigate | Every DDL step guarded by sa.inspect() existence checks (re-runnable); env.py wraps run_migrations() in one begin_transaction() so any raise = clean full rollback; downgrade() is a guarded strict reverse. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `alembic upgrade head` exits 0; a second `alembic upgrade head` is a clean no-op (idempotency).
- The Plan-01 subset for Criteria 1 & 3 and the view-level Criterion 2 tests are green.
- Manual spot-check: `SELECT id, name, type FROM accounts ORDER BY id` shows 1,2,559=liquid and 3=investment; `\d accounts` shows the CHECK + NOT NULL + default.
- NOTE: the tools-level test (test_tools_spending_excludes_investment) stays RED after this plan — it is Plan 03's target, not a regression.
</verification>

<success_criteria>
Criterion 1 (4-account audit, none NULL) and Criterion 3 (pairing columns exist, nullable, indexed) are met and automated-green. The cashflow_transactions view exists with correct NULL-account_id retention (Criterion 2 schema half). accounts.type is DB-enforced. The ORM matches the DB.
</success_criteria>

<output>
Create `.planning/phases/12-typed-accounts-transfer-funding-schema-foundations/12-02-SUMMARY.md` when done.
</output>
