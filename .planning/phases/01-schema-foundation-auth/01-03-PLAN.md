---
phase: 01-schema-foundation-auth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schemas.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_decimal.py
autonomous: true
requirements: [FND-03]
user_setup: []

must_haves:
  truths:
    - "POST /transactions with amount 123.45 stores and returns it as a JSON number (123.45), not a string"
    - "GET /transactions returns each amount as a JSON number, not a string"
    - "A single shared MoneyDecimal type is reused for all money fields (validates Decimal, serializes as number)"
  artifacts:
    - path: "backend/schemas.py"
      provides: "MoneyDecimal annotated type + TransactionCreate/Out amount converted from float"
      contains: "MoneyDecimal"
    - path: "backend/tests/conftest.py"
      provides: "Shared pytest fixtures (TestClient, API key env) reused by test_decimal and test_auth"
      contains: "TestClient"
    - path: "backend/tests/test_decimal.py"
      provides: "Decimal round-trip + JSON-number serialization tests"
      contains: "isinstance"
  key_links:
    - from: "backend/schemas.py TransactionCreate.amount"
      to: "MoneyDecimal"
      via: "field type annotation"
      pattern: "amount:\\s*MoneyDecimal"
    - from: "MoneyDecimal"
      to: "JSON number output"
      via: "PlainSerializer(float, when_used='json')"
      pattern: "PlainSerializer"
---

<objective>
Close the float-in-transit leak at the Pydantic/JSON boundary so money amounts validate as Decimal and serialize as JSON numbers, and establish the shared test fixtures the rest of the phase relies on.

Purpose: Closes the serialization side of FND-03 (storage side is Plan 01). Establishes conftest.py used by both the Decimal and Auth test suites.
Output: A reusable MoneyDecimal type, converted Transaction schemas, a shared conftest, and Decimal serialization tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-foundation-auth/01-CONTEXT.md
@.planning/phases/01-schema-foundation-auth/01-RESEARCH.md
@.planning/phases/01-schema-foundation-auth/01-PATTERNS.md

<interfaces>
<!-- D-13: retrofit reaches existing TransactionCreate.amount (line 10) and TransactionOut.amount (line 22). -->
<!-- D-14: Decimals serialize as JSON numbers, not strings. -->
<!-- D-15: ONE shared annotated Decimal money type reused across schemas. -->
<!-- Pydantic v2 default serializes Decimal as STRING (Pitfall 4) -> needs PlainSerializer(float, when_used='json'). -->
<!-- This plan OWNS backend/tests/conftest.py; Plan 02 reuses it. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MoneyDecimal type and convert Transaction schemas; add shared conftest</name>
  <files>backend/schemas.py, backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_decimal.py</files>
  <read_first>
    - .planning/phases/01-schema-foundation-auth/01-RESEARCH.md (Pattern 4 MoneyDecimal; Pitfall 4 Decimal-as-string; Validation Architecture Wave 0 gaps)
    - .planning/phases/01-schema-foundation-auth/01-PATTERNS.md (backend/schemas.py section: imports, MoneyDecimal alias, lines 10/22 to change; backend/tests/test_decimal.py section)
    - .planning/phases/01-schema-foundation-auth/01-VALIDATION.md (Wave 0 requirements: conftest fixtures, httpx for TestClient)
    - backend/schemas.py (current TransactionCreate.amount and TransactionOut.amount float fields)
    - backend/tests/test_router.py (test module structure to mirror)
  </read_first>
  <behavior>
    - A model with a MoneyDecimal field given Decimal("123456.78") produces a JSON number (float) when model_dump_json() is called — isinstance(parsed["amount"], float) is True, value == 123456.78
    - TransactionCreate accepts a Decimal amount and preserves it as Decimal in Python (tx.amount == Decimal("-25000.00"))
    - TransactionOut serializes amount as a JSON number, not a string
  </behavior>
  <action>
    In backend/schemas.py add imports `from decimal import Decimal`, `from typing import Annotated`, `from pydantic import PlainSerializer`. Define the shared alias near the top before the first class: `MoneyDecimal = Annotated[Decimal, PlainSerializer(lambda x: float(x), return_type=float, when_used="json")]` (D-14/D-15; this serializes as a JSON number per Pitfall 4). Change TransactionCreate.amount (line ~10) from `float` to `MoneyDecimal` keeping its existing Field(..., description=...). Change TransactionOut.amount (line ~22) from `float` to `MoneyDecimal`. Do not change any other schema fields in this phase (the 5 new tables have no schemas until Phase 2/5).

    Create backend/tests/__init__.py (empty, makes tests a package). Create backend/tests/conftest.py with shared pytest fixtures: a `client` fixture returning `TestClient(backend.main.app)` and a fixture/helper that sets MONAI_API_KEY for tests that need it (so Plan 02's test_auth.py can reuse it). Note in a comment that backend.auth._CONFIGURED_KEY is read at import time, so the api-key fixture must set the env BEFORE importing app or patch backend.auth._CONFIGURED_KEY directly.

    Create backend/tests/test_decimal.py: (a) a test defining an inline BaseModel with a MoneyDecimal field, dumping via model_dump_json, json.loads, asserting isinstance(parsed["amount"], float) and equality; (b) a test that TransactionCreate(amount=Decimal("-25000.00"), ...) round-trips as Decimal; (c) a test that TransactionOut serializes amount as a JSON number not a string. Mirror test_router.py's plain-function pytest style.
  </action>
  <verify>
    <automated>cd backend && grep -q "MoneyDecimal" schemas.py && grep -q "PlainSerializer" schemas.py && python -c "s=open('schemas.py').read(); assert s.count('amount: MoneyDecimal')>=2 or s.count('amount:MoneyDecimal')>=2, s" && python -m pytest tests/test_decimal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/schemas.py defines `MoneyDecimal = Annotated[Decimal, PlainSerializer(...)]`
    - TransactionCreate.amount and TransactionOut.amount both use MoneyDecimal (no remaining `amount: float` in those classes)
    - backend/tests/conftest.py exists with a TestClient `client` fixture and an api-key helper, with the import-time-key note
    - backend/tests/__init__.py exists
    - `cd backend && python -m pytest tests/test_decimal.py -q` exits 0, including the isinstance(..., float) JSON-number assertion
  </acceptance_criteria>
  <done>MoneyDecimal defined and applied to both Transaction schemas; conftest + decimal tests green; amounts serialize as JSON numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> frontend/charts | Money values cross the JSON boundary; type/precision must be correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Float rounding of money amounts in transit | mitigate | Validate/store as Decimal (Numeric in DB per Plan 01); MoneyDecimal converts to float only at JSON serialization, within float64 precision for IDR(2dp) amounts (Assumption A3). |
| T-01-09 | Information Disclosure | Decimal serialized as string breaks frontend number parsing | mitigate | PlainSerializer(float, when_used='json') ensures JSON numbers (D-14); test asserts isinstance float. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_decimal.py -q` exits 0
- schemas.py: both Transaction amount fields are MoneyDecimal; MoneyDecimal uses PlainSerializer
- JSON output of a MoneyDecimal field is a number, not a string (isinstance float assertion)
</verification>

<success_criteria>
- FND-03 (serialization side): any transaction amount flowing through the API is stored/returned as Decimal with no float rounding visible as a string in responses
- Roadmap criterion #3 satisfied for the transaction money path; MoneyDecimal is the single reusable money type for Phase 2/5 schemas
</success_criteria>

<output>
Create `.planning/phases/01-schema-foundation-auth/01-03-SUMMARY.md` when done
</output>
