---
phase: 01-schema-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/models.py
  - backend/db.py
  - backend/main.py
  - backend/Dockerfile
  - backend/entrypoint.sh
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/001_baseline.py
  - alembic/versions/002_new_tables.py
  - README.md
autonomous: false
requirements: [FND-01, FND-03]
user_setup:
  - service: postgres
    why: "alembic upgrade head must run against the live monai_pgdata volume; a pg_dump backup is required first (D-03)"
    dashboard_config:
      - task: "Take a pg_dump backup of the live DB before first migration"
        location: "docker exec monai-db pg_dump -U monai monai > backup_pre_alembic.sql"

must_haves:
  truths:
    - "alembic upgrade head applies the 5 new tables to the existing volume without destroying the 5,609 transactions or 3 accounts"
    - "A fresh database created from scratch via alembic upgrade head has accounts, transactions, all 5 new tables, and the date_helpers view"
    - "Base.metadata.create_all() no longer exists anywhere in db.py"
    - "Transaction.amount Python annotation is Mapped[Decimal], matching its Numeric(18,2) column"
  artifacts:
    - path: "alembic/env.py"
      provides: "Alembic environment wired to DATABASE_URL and Base.metadata"
      contains: "from backend.models import Base"
    - path: "alembic/versions/001_baseline.py"
      provides: "Baseline migration mirroring current accounts+transactions schema (stamped, not run, on live DB)"
      contains: "down_revision = None"
    - path: "alembic/versions/002_new_tables.py"
      provides: "Migration creating audit_log, proposals, holdings, portfolio_events, price_cache + date_helpers view"
      contains: "create_table"
    - path: "backend/models.py"
      provides: "5 new ORM models + Transaction.amount Decimal annotation fix"
      contains: "class Holding"
    - path: "backend/entrypoint.sh"
      provides: "Container entrypoint that runs alembic upgrade head before uvicorn"
      contains: "alembic upgrade head"
  key_links:
    - from: "alembic/env.py"
      to: "backend.models.Base.metadata"
      via: "target_metadata assignment"
      pattern: "target_metadata\\s*=\\s*Base.metadata"
    - from: "alembic/versions/002_new_tables.py"
      to: "date_helpers view"
      via: "op.execute(CREATE OR REPLACE VIEW)"
      pattern: "CREATE OR REPLACE VIEW date_helpers"
    - from: "backend/entrypoint.sh"
      to: "alembic upgrade head"
      via: "Docker CMD invokes entrypoint before uvicorn"
      pattern: "alembic upgrade head"
---

<objective>
Introduce Alembic onto the live, populated PostgreSQL volume non-destructively and create the five forward-looking tables, making the schema fully Alembic-managed.

Purpose: Closes FND-01 (the hard blocker for every downstream phase) and the storage side of FND-03 (Decimal). No write endpoint or new table can be trusted until schema evolution is safe.
Output: An `alembic/` layout, two migrations (baseline + new tables), the `date_helpers` view moved into a migration, five new ORM models, a removed `create_all()`, a Docker entrypoint that runs migrations, and a documented backup runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-foundation-auth/01-CONTEXT.md
@.planning/phases/01-schema-foundation-auth/01-RESEARCH.md
@.planning/phases/01-schema-foundation-auth/01-PATTERNS.md

<interfaces>
<!-- Current ORM models the baseline migration MUST mirror exactly. Extracted from backend/models.py. -->
<!-- accounts: id PK int; name String(255) unique index; type String(64) null; currency String(8) null -->
<!-- transactions: id PK int; date DateTime index; amount Numeric(18,2); currency String(8);
     category String(255) null; raw_category String(255) null; merchant String(512) null;
     notes Text null; account_id FK accounts.id null index; is_transfer Boolean default False index -->
<!-- Precision decisions (D-09): money = Numeric(18,2); quantity = Numeric(28,8) -->
<!-- JSONB (D-10): audit_log.before/after, proposals.payload -->
<!-- proposals (D-11): id UUID PK; token String(64) unique (separate secret, NOT the id) -->
<!-- price_cache (D-12): source String(32) column carries 'manual'/'coingecko'/etc -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Alembic, env wiring, and the two migration files</name>
  <files>backend/requirements.txt, alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/001_baseline.py, alembic/versions/002_new_tables.py</files>
  <read_first>
    - .planning/phases/01-schema-foundation-auth/01-RESEARCH.md (Pattern 1, Pattern 2, the two Code Examples for 001_baseline.py and 002_new_tables.py, Pitfall 1/2/3)
    - .planning/phases/01-schema-foundation-auth/01-PATTERNS.md (alembic/env.py section, 001_baseline.py section, 002_new_tables.py section)
    - backend/config.py (DATABASE_URL env-var read pattern)
    - backend/db.py (the _DATE_HELPERS_VIEW SQL string — lifted verbatim into migration 002)
    - backend/models.py (current accounts + transactions column types the baseline MUST mirror exactly)
  </read_first>
  <behavior>
    - alembic check (after stamp) reports no schema diff between Base.metadata and the live DB
    - On a fresh empty DB, alembic upgrade head creates: accounts, transactions, audit_log, proposals, holdings, portfolio_events, price_cache, and the date_helpers view
    - Migration 002 down_revision chains to migration 001's revision id (linear history, alembic history shows 2 revisions)
  </behavior>
  <action>
    Add `alembic>=1.13.0` and `httpx>=0.27.0` to backend/requirements.txt (httpx is dev-only for TestClient used by Plan 03/02). Run `alembic init alembic` from the PROJECT ROOT (so alembic/ is a sibling of backend/ and alembic.ini sits at root). Edit alembic/env.py to: import `from backend.models import Base`, set `target_metadata = Base.metadata`, read `DATABASE_URL` from os.environ with the project default `postgresql+psycopg://monai:monai@localhost:5434/monai`, and set the sqlalchemy.url via `config.get_section(...)["sqlalchemy.url"] = db_url.replace("%", "%%")` (configparser % escape, Pitfall 3). Use the SYNC engine_from_config pattern with pool.NullPool — NOT the async env template. Leave alembic.ini sqlalchemy.url as a placeholder.

    Write alembic/versions/001_baseline.py BY HAND to mirror the current accounts + transactions schema exactly (columns and indexes from models.py: ix_accounts_name unique, ix_transactions_date, ix_transactions_account_id, ix_transactions_is_transfer). Set down_revision = None. Put a prominent comment at the top of upgrade() stating this migration is STAMPED (not run) on the live DB and exists only for fresh-DB setup. Do NOT include date_helpers in 001 (views are invisible to autogenerate and already exist live).

    Write alembic/versions/002_new_tables.py with down_revision chained to the 001 revision id. In upgrade(): first `op.execute(...)` the date_helpers view SQL (copy verbatim from backend/db.py _DATE_HELPERS_VIEW), then create the 5 tables per D-09/D-10/D-11/D-12: audit_log(id, entity String(64), entity_id Integer null, operation String(32), before JSONB null, after JSONB null, created_at timestamptz server_default now()); proposals(id UUID PK default uuid4, token String(64) unique, operation String(64), payload JSONB, status String(16) server_default 'pending', expires_at timestamptz, created_at timestamptz now(), confirmed_at timestamptz null) with unique index ix_proposals_token; holdings(id, ticker String(32) unique index, quantity Numeric(28,8), avg_cost Numeric(18,2), purchase_date Date null, currency String(8) server_default 'IDR', asset_type String(32) null); portfolio_events(id, date Date index, ticker String(32) index, event_type String(16), quantity Numeric(28,8), price Numeric(18,2)); price_cache(id, ticker String(32) index, price Numeric(18,2), currency String(8) server_default 'IDR', source String(32), fetched_at timestamptz now()) with index ix_price_cache_ticker_source. Use `from sqlalchemy.dialects.postgresql import JSONB, UUID`. Provide a downgrade() that drops the 5 tables and the view.
  </action>
  <verify>
    <automated>cd backend && python -c "import alembic, httpx" && grep -q "target_metadata = Base.metadata" ../alembic/env.py && grep -q "down_revision = None" ../alembic/versions/001_baseline.py && grep -q "CREATE OR REPLACE VIEW date_helpers" ../alembic/versions/002_new_tables.py && [ "$(grep -c 'create_table' ../alembic/versions/002_new_tables.py)" -eq 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `alembic>=1.13.0` and `httpx>=0.27.0` present in backend/requirements.txt
    - alembic/env.py contains `target_metadata = Base.metadata` and reads DATABASE_URL from os.environ, escaping `%` as `%%`
    - alembic/versions/001_baseline.py has `down_revision = None` and creates accounts + transactions with the 4 named indexes
    - alembic/versions/002_new_tables.py chains to 001, calls op.execute with `CREATE OR REPLACE VIEW date_helpers`, and contains exactly 5 `create_table` calls
    - `alembic history` lists 2 revisions in linear order (manual confirm in runbook or local sandbox)
  </acceptance_criteria>
  <done>Two migrations exist; env.py reads DATABASE_URL and targets Base.metadata; httpx + alembic added to requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 ORM models, fix Decimal annotation, remove create_all and startup hook</name>
  <files>backend/models.py, backend/db.py, backend/main.py</files>
  <read_first>
    - .planning/phases/01-schema-foundation-auth/01-PATTERNS.md (backend/models.py section, backend/db.py section, backend/main.py section, "SQLAlchemy 2.0 ORM Model Style")
    - backend/models.py (Account/Transaction Mapped[] style to replicate; line 49 amount annotation)
    - backend/db.py (init_db + _DATE_HELPERS_VIEW block to delete; engine/session/get_session to keep)
    - backend/main.py (line 24 import of init_db; lines 52-54 @app.on_event startup to delete)
  </read_first>
  <behavior>
    - Importing backend.models exposes classes Holding, Proposal, AuditLog, PortfolioEvent, PriceCache and they register on Base.metadata (Base.metadata.tables contains all 5 new table names)
    - backend.db imports cleanly with no reference to create_all or _DATE_HELPERS_VIEW
    - backend.main imports cleanly with no reference to init_db
  </behavior>
  <action>
    In backend/models.py: add imports `from datetime import datetime, date`, `from decimal import Decimal`, `from sqlalchemy import Date, Integer`, and `from sqlalchemy.dialects.postgresql import JSONB, UUID`. Change Transaction.amount annotation from `Mapped[float]` to `Mapped[Decimal]` (Pitfall 5; column stays Numeric(18,2)). Add 5 new models using SQLAlchemy 2.0 Mapped[] style mirroring the column definitions in migration 002 (same names, types, precision, nullability, server defaults): AuditLog, Proposal (UUID PK via `UUID(as_uuid=True)`, token String(64) unique), Holding (quantity Numeric(28,8), avg_cost Numeric(18,2)), PortfolioEvent, PriceCache. Use `Mapped[dict | None]` for JSONB columns, `Mapped[Decimal]` for Numeric, `Mapped[date | None]` for Date.

    In backend/db.py: delete the entire `_DATE_HELPERS_VIEW` string (lines ~14-28) and the entire `init_db()` function (lines ~31-35, including `Base.metadata.create_all(engine)`). Remove the now-unused `text` import if nothing else uses it. Keep engine, SessionLocal, and get_session() exactly as-is.

    In backend/main.py: remove `init_db` from the `from backend.db import ...` line (keep get_session). Delete the entire `@app.on_event("startup")` block (lines ~52-54) that calls init_db(). Do NOT touch any route decorators in this plan (auth deps are Plan 02's scope).
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.models import Base, Holding, Proposal, AuditLog, PortfolioEvent, PriceCache; names=set(Base.metadata.tables); assert {'holdings','proposals','audit_log','portfolio_events','price_cache'} <= names, names" && ! grep -q "create_all" db.py && ! grep -q "init_db" main.py && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `from backend.models import Holding, Proposal, AuditLog, PortfolioEvent, PriceCache` succeeds
    - Base.metadata.tables contains holdings, proposals, audit_log, portfolio_events, price_cache
    - `grep create_all backend/db.py` returns nothing
    - `grep init_db backend/main.py` returns nothing
    - backend/models.py line for Transaction.amount reads `Mapped[Decimal]`
    - `cd backend && python -m pytest -q` exits 0 (existing test_router/test_tools still pass)
  </acceptance_criteria>
  <done>5 models registered on Base.metadata; amount is Mapped[Decimal]; create_all and init_db startup removed; existing tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Docker entrypoint, Dockerfile CMD, and pg_dump + migration runbook</name>
  <files>backend/entrypoint.sh, backend/Dockerfile, README.md</files>
  <read_first>
    - .planning/phases/01-schema-foundation-auth/01-RESEARCH.md (Pattern 6 Docker entrypoint; Open Question 2 key generation; D-03 backup step)
    - .planning/phases/01-schema-foundation-auth/01-PATTERNS.md (backend/entrypoint.sh section, current Dockerfile CMD line 16)
    - backend/Dockerfile (current CMD to replace)
  </read_first>
  <action>
    Create backend/entrypoint.sh: `#!/bin/sh`, `set -e`, echo a migration banner, run `alembic upgrade head`, then `exec uvicorn backend.main:app --host 0.0.0.0 --port 8001`. In backend/Dockerfile, COPY backend/entrypoint.sh, `RUN chmod +x backend/entrypoint.sh`, and replace the CMD with `CMD ["./backend/entrypoint.sh"]`. (docker-compose already has depends_on db service_healthy, so Postgres is ready before migrations run.)

    Add a "Database migrations" section to README.md documenting the one-time introduction runbook in order: (1) `docker exec monai-db pg_dump -U monai monai > backup_pre_alembic.sql` BEFORE anything (D-03); (2) `alembic stamp <001_revision_id>` on the live DB to mark the baseline applied without running it; (3) `alembic upgrade head` to apply migration 002 only. Document `python3 -c "import secrets; print(secrets.token_hex(32))"` for generating MONAI_API_KEY. State explicitly that step 2 MUST precede step 3 on the existing volume or migration 001 will fail with "relation accounts already exists" (Pitfall 1).
  </action>
  <verify>
    <automated>test -f backend/entrypoint.sh && grep -q "alembic upgrade head" backend/entrypoint.sh && grep -q "entrypoint.sh" backend/Dockerfile && grep -qi "pg_dump" README.md && grep -q "alembic stamp" README.md</automated>
  </verify>
  <acceptance_criteria>
    - backend/entrypoint.sh exists, contains `alembic upgrade head` then `exec uvicorn backend.main:app`
    - backend/Dockerfile COPYs entrypoint.sh, chmod +x, and CMD is `["./backend/entrypoint.sh"]`
    - README.md "Database migrations" section documents pg_dump backup, alembic stamp, then alembic upgrade head, in that order, with the stamp-before-upgrade warning
  </acceptance_criteria>
  <done>Entrypoint runs migrations before uvicorn; Dockerfile invokes it; runbook documents backup + stamp + upgrade ordering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Run alembic stamp + upgrade head on the live volume and confirm data survives</name>
  <files>backup_pre_alembic.sql (generated backup artifact; not committed)</files>
  <action>
    [BLOCKING checkpoint — manual operator procedure against the live monai_pgdata volume; cannot be unit-tested and risks data loss if done out of order.] Execute in this exact order: (1) pg_dump backup to backup_pre_alembic.sql (D-03) and confirm it is non-empty; (2) `alembic stamp <001_revision_id>` (id from `alembic history`) so the baseline is marked applied WITHOUT running it — this MUST precede the upgrade or migration 001 fails with "relation accounts already exists" (Pitfall 1); confirm `alembic current` shows 001; (3) `alembic upgrade head` to apply migration 002 only; confirm `alembic current` shows 002 head. Then verify no data loss (transactions=5609, accounts=3) and that the 5 new tables + date_helpers view exist. Exact commands are in how-to-verify below.
  </action>
  <what-built>
    Tasks 1-3 produced: two migrations (baseline + new tables), env.py wired to DATABASE_URL, 5 ORM models, create_all/init_db removed, a Docker entrypoint running `alembic upgrade head`, and a runbook. This step actually applies the migration to the existing monai_pgdata volume (5,609 transactions, 3 accounts).
  </what-built>
  <how-to-verify>
    1. Backup first: `docker exec monai-db pg_dump -U monai monai > backup_pre_alembic.sql` — confirm the file is non-empty.
    2. Stamp the baseline on the live DB: from project root with DATABASE_URL pointing at the live DB, run `alembic stamp <001_revision_id>` (use the actual id from `alembic history`). Confirm `alembic current` shows the 001 revision.
    3. Apply new tables: `alembic upgrade head`. Confirm `alembic current` now shows the 002 revision (head).
    4. Verify NO data loss: `docker exec monai-db psql -U monai -d monai -c "SELECT count(*) FROM transactions;"` returns 5609 and `SELECT count(*) FROM accounts;` returns 3.
    5. Verify new tables exist: `docker exec monai-db psql -U monai -d monai -c "\dt"` lists audit_log, proposals, holdings, portfolio_events, price_cache; `\dv` lists date_helpers.
    Expected: row counts unchanged, 5 new tables + date_helpers view present, alembic current = 002 head.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms transactions=5609, accounts=3 unchanged; 5 new tables + date_helpers view present; alembic current = 002 head.</human-check>
  </verify>
  <done>Live volume migrated: existing data intact, 5 new tables + view present, alembic at head.</done>
  <resume-signal>Type "approved" once row counts are unchanged and the 5 new tables + view exist, or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/runbook → live DB | DDL migrations cross into the populated production volume |
| env (DATABASE_URL) → Alembic engine | The migration target is chosen entirely by an env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | alembic upgrade against live monai_pgdata volume | mitigate | Two-migration + stamp recipe (D-01): baseline is stamped not run; only migration 002 (additive CREATE TABLE) executes. Documented pg_dump backup (D-03) precedes first upgrade. BLOCKING human-verify task 4 confirms row counts unchanged. |
| T-01-02 | Tampering | migration runs against wrong DB (dev vs prod URL) | mitigate | env.py reads the same DATABASE_URL the app uses; runbook requires verifying `alembic current` and target before upgrade. |
| T-01-03 | Denial of Service | baseline run instead of stamped → "relation accounts already exists" | mitigate | Prominent comment in 001 upgrade() + runbook stamp-before-upgrade ordering with explicit Pitfall 1 warning. |
| T-01-SC | Tampering | pip installs (alembic, httpx) | mitigate | Package Legitimacy Audit in RESEARCH.md: both [ASSUMED] from SQLAlchemy/encode orgs at correct PyPI names. slopcheck was unavailable; packages are long-standing official projects — treat as approved. |
</threat_model>

<verification>
- `alembic history` shows 2 linear revisions; `alembic check` reports no diff after stamp
- On live volume after upgrade: transactions=5609, accounts=3, 5 new tables + date_helpers view present
- `grep create_all backend/db.py` and `grep init_db backend/main.py` both empty
- `cd backend && python -m pytest -q` exits 0
</verification>

<success_criteria>
- FND-01: `alembic upgrade head` applies new tables to the existing volume without destroying existing data (verified in Task 4)
- FND-03 (storage side): Transaction.amount is Mapped[Decimal] on a Numeric(18,2) column; all money/quantity columns use Numeric per D-09
- Roadmap criterion #4: Base.metadata.create_all() removed; schema fully Alembic-managed
</success_criteria>

## Truths — CONTEXT Decisions Covered

- **D-01:** Two migrations + stamp — baseline mirrors current `accounts`/`transactions`, stamped (not run) on the live DB; migration 002 creates the 5 new tables.
- **D-02:** `date_helpers` view moves into migration 002 (`op.execute`) so the whole schema is Alembic-managed.
- **D-03:** Documented `pg_dump` backup step in the README runbook + the BLOCKING human-verify checkpoint before first `alembic upgrade head` on real data.
- **D-04:** `Base.metadata.create_all()` removed from `db.py`; schema fully Alembic-managed.
- **D-09:** Numeric precision — quantity `Numeric(28,8)`, money `Numeric(18,2)` across new tables.
- **D-10:** `audit_log` before/after and `proposals` payload stored as JSONB columns.
- **D-11:** Proposal confirm token is a separate high-entropy column, not the row UUID id.
- **D-12:** Manual/overridden prices live in `price_cache` with `source='manual'`.

<output>
Create `.planning/phases/01-schema-foundation-auth/01-01-SUMMARY.md` when done
</output>
