---
phase: 14-rest-endpoints-agent-mcp-tool-registration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - backend/tools.py
  - backend/query.py
  - backend/main.py
autonomous: true
requirements: [CHAT-09]
must_haves:
  truths:
    - "Chat can propose a transfer, liquid→investment deposit, funded buy, funded sell, or balance adjustment, and confirming it writes the correct rows via the matching Phase-13 apply_* function"
    - "The 5 new propose_* names appear in tools.py TOOLS AND in query.py's FunctionTool list (no dual-registration gap)"
    - "The 5 new propose_* names are ABSENT from READ_TOOL_NAMES and from the external MCP tools/list surface"
    - "A malformed proposal payload for a new operation returns a clean 422, never a 500/KeyError"
  artifacts:
    - "5 propose_* functions in backend/tools.py, registered ONLY in the trailing TOOLS.update() call (after the READ_TOOL_NAMES snapshot)"
    - "5 imports + 5 FunctionTool.from_defaults entries in backend/query.py's write_tools block"
    - "5 elif dispatch branches + a KeyError/TypeError→ValueError guard in backend/main.py _execute_proposal_payload, plus 5 apply_* imports from backend.writes"
  key_links:
    - "each propose_* payload after-key set exactly matches its apply_* signature: leg_a/leg_b, cash_leg/event, flat funded dict, account_id/target_balance"
    - "TOOLS.update placement AFTER tools.py:628 READ_TOOL_NAMES snapshot is what keeps writes off the MCP surface"
    - "tools.py↔query.py dual registration is what makes the LLM able to see each tool"
    - "funded-buy/sell cash_amount/quantity/price stay JSON numbers in the payload because apply_add_funded_buy applies abs()/negation before Decimal conversion"
---

<objective>
Wire the agent (chat) path for the 5 new Phase-13 writes: add 5 propose_*
functions to tools.py, dual-register them in query.py, and add 5 confirm-time
dispatch branches to main.py that route through the existing apply_* primitives.
Turns the 5 propose→confirm tests and the named-tool registration test from
Plan 14-01 GREEN.

Purpose: CHAT-09 — user performs transfers, funded buy/sell, and balance
adjustments via chat through the existing confirm-before-write flow, with the
new write tools kept off the external MCP read-only surface.

Output: propose_add_transfer / propose_add_investment_transfer /
propose_add_funded_buy / propose_add_funded_sell / propose_add_balance_adjustment,
registered on the agent and dispatched at confirm, all routing through
backend/writes.py.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-RESEARCH.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-PATTERNS.md

# Source of truth — read the exact apply_* signatures BEFORE building payloads:
@backend/writes.py
@backend/tools.py
@backend/query.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 propose_* functions to tools.py + register in the trailing TOOLS.update()</name>
  <files>backend/tools.py</files>
  <read_first>
    - backend/tools.py lines 605-628 (initial `TOOLS = {...}` read-tool dict + the `READ_TOOL_NAMES = frozenset(TOOLS)` snapshot at 628 — NEVER add write tools here), line 635 (`_make_proposal(operation, payload)` helper), lines 708-740 (propose_add_transaction — simple before/after analog), lines 916-940 (propose_rename_category — custom-payload/scalar analog for balance adjustment), line 1124 (the trailing `TOOLS.update({...})` — the ONLY place the 5 new entries go)
    - backend/writes.py lines 54-107 (apply_add_transaction + apply_add_balance_adjustment), 150-267 (apply_add_transfer, apply_add_investment_transfer, apply_add_funded_buy, apply_add_funded_sell) — the exact after-dict keys each consumes; line 410-460 (apply_add_portfolio_event required keys: ticker, event_type, quantity, price, platform_id, currency, date, asset_type)
    - backend/portfolio.py lines 188-225 and prices.py line 57-58 (the existing asset_type=="cash" convention — cash positions are valued 1:1 and skip price_cache; the deposit sentinel builds on this)
  </read_first>
  <action>
    Add 5 propose_* functions to backend/tools.py immediately BEFORE the
    trailing `TOOLS.update({...})` call (after the last existing propose_*
    definition), each mirroring propose_add_transaction's shape: build a
    `payload` dict, call `_make_proposal(operation, payload)`, return
    `{"tool","proposal_id","proposal_token","summary","before","after"}`. Each
    gets a typed keyword signature and a clear docstring (LlamaIndex reads both
    for tool selection) that states the tool creates a proposal and does NOT
    move money. Add all 5 names to the trailing TOOLS.update() dict — NOWHERE
    else (adding to the line-605 dict would leak them onto the MCP surface,
    RESEARCH Pitfall 1).

    Exact payload shapes (verified against writes.py — do NOT guess keys):

    - propose_add_transfer(from_account, to_account, amount, currency="IDR",
      date=None, notes=None) → operation "add_transfer", payload
      rows=[{"before":None,"after":{"leg_a":{...},"leg_b":{...}}}] where leg_a =
      {"account":from_account,"amount":str(Decimal(str(-abs(amount)))),"currency":currency,"date":date,"notes":notes}
      and leg_b = same with to_account and +abs. (leg dicts feed
      apply_add_transaction, which does Decimal(str(amount)) → str is safe here.)
      Do NOT set is_transfer — apply_add_transfer forces it. Per CHAT-09/XFER-01.

    - propose_add_investment_transfer(from_account, platform_id, amount,
      currency="IDR", date=None, notes=None) → operation
      "add_investment_transfer", payload
      rows=[{"before":None,"after":{"cash_leg":{...},"event":{...}}}] where
      cash_leg = {"account":from_account,"amount":str(Decimal(str(-abs(amount)))),"currency":currency,"date":date,"notes":notes}
      and event = the DEPOSIT SENTINEL:
      {"ticker":"CASH","event_type":"deposit","quantity":str(Decimal(str(abs(amount)))),"price":"1","platform_id":platform_id,"currency":currency,"date":date,"asset_type":"cash"}.
      Document in the docstring: a liquid→investment deposit is recorded as a
      per-platform sentinel position ticker="CASH", asset_type="cash", price=1,
      quantity=deposited amount, currency=transfer currency — consistent with the
      existing asset_type=="cash" 1:1 valuation convention (portfolio.py/prices.py).
      platform_id is an int (resolve names→ids with the existing find_platforms
      read tool first). Per CHAT-09/XFER-02.

    - propose_add_funded_buy(source_account_name, platform_id, ticker, quantity,
      price, cash_amount, cash_currency="IDR", event_currency="IDR", date=None,
      notes=None, asset_type=None) → operation "add_funded_buy", payload
      rows=[{"before":None,"after":{...ONE flat dict...}}] with keys exactly:
      source_account_name, cash_currency, cash_amount, ticker, quantity, price,
      platform_id, event_currency, date, notes, asset_type.
      CRITICAL: cash_amount, quantity, price go into the payload as JSON NUMBERS
      (float(...)), NOT str — because apply_add_funded_buy does
      `-abs(after["cash_amount"])` and abs() on a str raises TypeError; JSON
      numbers survive the JSONB round-trip natively (the Decimal-JSON gotcha is
      only about bare Decimal objects, not floats). cash_amount is an unsigned
      positive magnitude — the primitive owns the sign (buy debits). Document
      the always-positive contract in the docstring. platform_id is int. Per
      CHAT-09/XFER-03.

    - propose_add_funded_sell(...same signature...) → operation
      "add_funded_sell", identical flat-dict payload shape and the same
      JSON-number rule; cash_amount stays a positive magnitude (the primitive
      credits on sell). Per CHAT-09/XFER-03.

    - propose_add_balance_adjustment(account_id, target_balance) → operation
      "add_balance_adjustment", payload
      rows=[{"account_id":account_id,"target_balance":str(target_balance)}]
      (custom shape, NOT before/after — mirrors propose_rename_category, since
      apply_add_balance_adjustment takes positional scalars). target_balance as
      str is safe (the primitive does Decimal(str(target_balance))). For a nicer
      summary + an early not-found signal, look the account up via
      `get_session_sync()` + `db.get(Account, account_id)` (import Account
      locally, matching the RESEARCH pattern); return
      {"tool":..., "error":...} if the account is missing. Per CHAT-09/ACCT-02.
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.tools import TOOLS, READ_TOOL_NAMES; names=['propose_add_transfer','propose_add_investment_transfer','propose_add_funded_buy','propose_add_funded_sell','propose_add_balance_adjustment']; assert all(n in TOOLS for n in names), 'missing from TOOLS'; assert not any(n in READ_TOOL_NAMES for n in names), 'leaked onto READ_TOOL_NAMES'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def propose_add_transfer\|def propose_add_investment_transfer\|def propose_add_funded_buy\|def propose_add_funded_sell\|def propose_add_balance_adjustment" backend/tools.py` returns 5
    - All 5 names appear in the trailing `TOOLS.update({...})` (after tools.py:628); a Python import shows each in `TOOLS` and NONE in `READ_TOOL_NAMES` (the verify command above prints `ok`)
    - `len(READ_TOOL_NAMES) == 15` still holds (`cd backend && python -c "from backend.tools import READ_TOOL_NAMES; assert len(READ_TOOL_NAMES)==15"`)
    - propose_add_funded_buy / propose_add_funded_sell payloads store cash_amount, quantity, price as numbers (float), not str — confirmed by reading the built payload; no bare `Decimal(` object is placed in any payload dict
    - propose_add_investment_transfer's event sub-dict uses ticker="CASH", event_type="deposit", asset_type="cash", price 1
  </acceptance_criteria>
  <done>5 propose_* functions exist, registered only in the trailing TOOLS.update(); READ_TOOL_NAMES still holds exactly the 15 read tools; funded payloads use JSON numbers for money.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-register in query.py + add 5 confirm-dispatch branches (with 422 guard) to main.py</name>
  <files>backend/query.py, backend/main.py</files>
  <behavior>
    - After this task, importing the agent and listing its tools shows all 5 new propose_* names (query.py registration).
    - Confirming each of the 5 proposal operations dispatches to the matching apply_* and writes the correct rows (Plan 14-01's 5 propose→confirm tests pass).
    - Confirming a proposal whose payload is missing a required key returns 422, not 500 (test_confirm_malformed_funded_buy_returns_422 passes).
  </behavior>
  <read_first>
    - backend/query.py lines 96-111 (the `from backend.tools import (...)` block) and lines 164-190 (the `write_tools = [...]` list, where category tools carry explicit `description=` overrides — follow that for the funded-buy/sell sign convention)
    - backend/main.py lines 58-78 (the `from backend.writes import (...)` block — currently missing the 5 new names) and lines 1014-1073 (`_execute_proposal_payload`'s if/elif chain ending in `else: raise ValueError(...)`), plus the confirm endpoint's existing `except ValueError as e: raise HTTPException(422, ...)` around line 1113
    - backend/writes.py lines 150-267 (the 5 apply_* call signatures the dispatch branches must match exactly)
    - .planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-PATTERNS.md lines 93-117 (the exact dispatch-branch shapes)
  </read_first>
  <action>
    In backend/query.py: add the 5 names to the `from backend.tools import (...)`
    block AND add 5 `FunctionTool.from_defaults(fn=propose_add_...)` entries to
    the `write_tools = [...]` list. For propose_add_funded_buy and
    propose_add_funded_sell, add an explicit `description=` override stating
    cash_amount must be a positive magnitude and platform_id is an int the agent
    should resolve via find_platforms first — mirroring how the category tools
    get explicit descriptions. This is the dual-registration gotcha
    (chat-tool-dual-registration): a tool in tools.py but not here is invisible
    to the LLM.

    In backend/main.py: add the 5 names to the `from backend.writes import (...)`
    block, then add 5 `elif operation == "...":` branches to
    _execute_proposal_payload, appended after the existing holding branches and
    before the final `else`:
    - "add_transfer": apply_add_transfer(db, after["leg_a"], after["leg_b"])
    - "add_investment_transfer": apply_add_investment_transfer(db, after["cash_leg"], after["event"])
    - "add_funded_buy": apply_add_funded_buy(db, rows[0]["after"])
    - "add_funded_sell": apply_add_funded_sell(db, rows[0]["after"])
    - "add_balance_adjustment": apply_add_balance_adjustment(db, rows[0]["account_id"], rows[0]["target_balance"])

    Guard the payload-shape hazard (autonomous decision 4 / RESEARCH Pitfall 3):
    wrap the dispatch so a missing/mismatched key raises ValueError, not a raw
    KeyError. The lazy correct fix is one guard: convert KeyError/TypeError from
    the dispatch into ValueError (which the confirm endpoint already maps to 422)
    — e.g. a `try/except (KeyError, TypeError) as e: raise ValueError(f"malformed
    payload for {operation!r}: {e}")` around the new branches' dict access. Do
    NOT add a second db.commit() — confirm_proposal owns the single commit; the
    apply_* functions never commit (never-commit contract, verified grep -c
    db.commit == 0 in writes.py).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_proposals.py tests/test_mcp.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The dual-registration grep loop reports nothing missing: `for fn in propose_add_transfer propose_add_investment_transfer propose_add_funded_buy propose_add_funded_sell propose_add_balance_adjustment; do grep -q "$fn" backend/tools.py || echo "MISSING tools.py:$fn"; grep -q "$fn" backend/query.py || echo "MISSING query.py:$fn"; done` prints nothing
    - `grep -c "add_transfer\|add_investment_transfer\|add_funded_buy\|add_funded_sell\|add_balance_adjustment" backend/main.py` shows the 5 operations appear in both the writes-import block and the dispatch chain
    - `cd backend && python -m pytest tests/test_proposals.py -q` — the 5 propose→confirm tests from 14-01 are now GREEN, and test_confirm_malformed_funded_buy_returns_422 returns 422 (not 500)
    - `cd backend && python -m pytest tests/test_mcp.py -q` — test_new_write_tools_registered_and_excluded and test_mcp_read_parity (len==15) both GREEN
    - No new `db.commit(` added inside any apply_* branch; dispatch branches call apply_* only
  </acceptance_criteria>
  <done>All 5 propose→confirm tests green; malformed-payload confirm returns 422; the 5 tools are visible to the agent and excluded from MCP; no double-commit introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external MCP client → tools/list surface | untrusted external clients must never see or call a write tool |
| chat LLM → proposal payload → confirm dispatch | LLM-built payloads reach apply_* and can be malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | new propose_* on MCP read-only surface | high | mitigate | Register ONLY in the trailing TOOLS.update() (after tools.py:628 READ_TOOL_NAMES snapshot); test_new_write_tools_registered_and_excluded + test_mcp_read_parity (len==15) assert absence — structural exclusion, verified by test |
| T-14-02 | Tampering / Information Disclosure | malformed proposal payload → KeyError → 500 stack-trace leak | high | mitigate | KeyError/TypeError→ValueError guard in the new dispatch branches maps to the confirm endpoint's existing 422; test_confirm_malformed_funded_buy_returns_422 pins 422-not-500; propose_* build exact shapes so the guard is defense-in-depth |
| T-14-03 | Tampering (data integrity) | double-commit / commit inside apply_* breaks atomicity | medium | mitigate | Dispatch branches call apply_* only; single commit owned by confirm_proposal; acceptance criterion forbids new db.commit in branches |
| T-14-04 | Tampering (data integrity) | transfer/funded leg missing is_transfer=True → double-count | low | accept | apply_* force is_transfer=True internally (T-13-07, closed Phase 13); propose_* must NOT re-derive it |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
Run `cd backend && python -m pytest tests/test_proposals.py tests/test_mcp.py -q`
then the full suite `cd backend && python -m pytest -q` (expect all green except
the pre-existing documented test_settings.py::test_put_settings_requires_key
failure). The 5 propose→confirm tests, the malformed-payload guard test, and the
named-tool registration test are the phase gate for the agent path.
</verification>

<success_criteria>
- 5 propose_* functions exist and are dual-registered (tools.py trailing TOOLS.update + query.py write_tools)
- 5 confirm-dispatch branches route through apply_*, with a KeyError→422 guard
- CHAT-09 success criteria 1, 2, 3 hold: chat can trigger each op via confirm-before-write; tools present on agent surface; absent from MCP surface
- All Plan 14-01 agent-path tests are GREEN; no regression in existing suite
</success_criteria>

<output>
Create `.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-02-SUMMARY.md` when done
</output>
