---
phase: 14-rest-endpoints-agent-mcp-tool-registration
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - backend/schemas.py
  - backend/main.py
autonomous: true
requirements: [CHAT-09]
must_haves:
  truths:
    - "5 direct REST endpoints exist, each routing through the matching Phase-13 apply_* function (not ad-hoc SQL), committing exactly once and calling reset_engine()"
    - "Bad input (negative/zero magnitude, missing field, nonexistent account/platform) returns 422, never a 500"
    - "Each new write endpoint is gated by require_api_key"
  artifacts:
    - "5 *Create Pydantic models in backend/schemas.py using MoneyDecimal with Field(gt=0) on positive magnitudes"
    - "5 REST route handlers in backend/main.py routing through apply_add_transfer / apply_add_investment_transfer / apply_add_funded_buy / apply_add_funded_sell / apply_add_balance_adjustment"
  key_links:
    - "ValueError→HTTPException(422) mapping (project-wide convention)"
    - "single db.commit() then reset_engine() after each write"
    - "Field(gt=0) rejects negative/zero magnitudes at the schema boundary before the primitive's sign normalization"
    - "REST path passes MoneyDecimal (Decimal) straight into apply_* after-dicts — abs(Decimal) and Decimal(str(Decimal)) both work, no JSONB round-trip so no float coercion needed"
---

<objective>
Wire the direct (non-agent) REST path for the 5 new Phase-13 writes: add 5
*Create request schemas and 5 route handlers that call the apply_* primitives
directly behind require_api_key. Turns Plan 14-01's 5 REST happy-path tests and
3 validation/auth tests GREEN.

Purpose: CHAT-09 success criterion 4 — REST endpoints for the new operations
exist and route through Phase 13's apply_* functions, not ad-hoc SQL. Gives
external API-key-authenticated clients (and later the Phase 16 UI) a
confirm-free write path parallel to the chat proposal flow.

Output: POST /transactions/transfer, /transactions/investment-transfer,
/portfolio-events/funded-buy, /portfolio-events/funded-sell, and
/accounts/{account_id}/adjust-balance.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-RESEARCH.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-PATTERNS.md

# Source of truth for schema + route + apply_* patterns:
@backend/schemas.py
@backend/main.py
@backend/writes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 *Create request schemas to schemas.py</name>
  <files>backend/schemas.py</files>
  <read_first>
    - backend/schemas.py lines 16-20 (the `MoneyDecimal` Annotated type — mandatory for every money field) and lines 107 (AccountCreate), 146-179 (PortfolioEventCreate with `Field(..., gt=0)` conventions)
    - backend/writes.py lines 150-267 (the apply_* signatures — the schema fields must supply exactly the keys each after-dict needs)
  </read_first>
  <action>
    Add 5 Pydantic v2 request models to backend/schemas.py, using MoneyDecimal
    for every money field and `Field(..., gt=0)` for every positive magnitude
    (the apply_* primitives own sign normalization — schemas reject
    negative/zero at the boundary, never pass a signed value through):

    - TransferCreate: from_account: str, to_account: str, amount: MoneyDecimal
      Field(gt=0), currency: str = "IDR", date: str | None = None, notes:
      str | None = None.
    - InvestmentTransferCreate: from_account: str, platform_id: int, amount:
      MoneyDecimal Field(gt=0), currency: str = "IDR", date: str | None = None,
      notes: str | None = None.
    - FundedBuyCreate: source_account_name: str, platform_id: int, ticker: str,
      quantity: MoneyDecimal Field(gt=0), price: MoneyDecimal Field(gt=0),
      cash_amount: MoneyDecimal Field(gt=0), cash_currency: str = "IDR",
      event_currency: str = "IDR", date: str | None = None, notes: str | None =
      None, asset_type: str | None = None.
    - FundedSellCreate: identical field set to FundedBuyCreate.
    - BalanceAdjustmentCreate: target_balance: MoneyDecimal (NO gt=0 — a target
      balance may legitimately be zero or negative). account_id comes from the
      path param, not the body.
  </action>
  <verify>
    <automated>cd backend && python -c "from backend.schemas import TransferCreate, InvestmentTransferCreate, FundedBuyCreate, FundedSellCreate, BalanceAdjustmentCreate; TransferCreate(from_account='a', to_account='b', amount=1000); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TransferCreate\|class InvestmentTransferCreate\|class FundedBuyCreate\|class FundedSellCreate\|class BalanceAdjustmentCreate" backend/schemas.py` returns 5
    - Every money field uses `MoneyDecimal`; transfer amount + funded cash_amount/quantity/price carry `Field(..., gt=0)`; BalanceAdjustmentCreate.target_balance has NO gt=0 constraint
    - Constructing TransferCreate(amount=-1) raises a Pydantic ValidationError (verify: `cd backend && python -c "from backend.schemas import TransferCreate; TransferCreate(from_account='a',to_account='b',amount=-1)"` exits non-zero)
  </acceptance_criteria>
  <done>5 *Create models importable; positive-magnitude fields reject negative/zero; target_balance allows any sign.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 direct REST route handlers to main.py routing through apply_*</name>
  <files>backend/main.py</files>
  <behavior>
    - POST to each of the 5 new routes with a valid body + api_key writes the correct rows via the matching apply_* and returns 201 (Plan 14-01's 5 happy-path REST tests pass).
    - A negative/zero magnitude body returns 422; a body naming a nonexistent account/platform returns 422 (not 500); a request with no api_key returns 401.
  </behavior>
  <read_first>
    - backend/main.py lines 58-78 (the `from backend.writes import (...)` block — the 5 apply_* names were added here in Plan 14-02; reuse, do not duplicate), lines 217-225 (create_account: apply_* → db.commit() → db.refresh() → reset_engine() idiom), lines 229-244 (update_account: ValueError→HTTPException(422) mapping), and create_transaction near line 648
    - backend/schemas.py (the 5 *Create models from Task 1)
    - backend/writes.py lines 150-267 (apply_* signatures) — note funded-buy/sell take ONE flat after dict; transfer/investment-transfer take two dicts; balance-adjustment takes positional account_id + target_balance
    - .planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-PATTERNS.md lines 121-154 (the create_transfer route example)
  </read_first>
  <action>
    Add 5 route handlers to backend/main.py, each decorated
    `dependencies=[Depends(require_api_key)]`, status_code=201, following the
    create_account/create_transfer idiom exactly: build the after-dict(s), call
    apply_* inside a `try/except ValueError as e: raise HTTPException(422,
    detail=str(e))`, then `db.commit()`, `db.refresh(...)` the returned rows,
    `from backend.query import reset_engine; reset_engine()`, and return the ids.
    Commit exactly once per handler; the apply_* functions never commit.

    - POST /transactions/transfer (TransferCreate): leg_a =
      {"account":payload.from_account,"amount":str(-abs(payload.amount)),"currency":payload.currency,"date":payload.date,"notes":payload.notes},
      leg_b = same with to_account and +abs; apply_add_transfer(db, leg_a, leg_b);
      return leg ids + transfer_pair_id. Per CHAT-09/XFER-01.
    - POST /transactions/investment-transfer (InvestmentTransferCreate): cash_leg
      = {"account":payload.from_account,"amount":str(-abs(payload.amount)),"currency":payload.currency,"date":payload.date,"notes":payload.notes};
      event = the deposit sentinel identical to Plan 14-02's propose tool
      ({"ticker":"CASH","event_type":"deposit","quantity":str(abs(payload.amount)),"price":"1","platform_id":payload.platform_id,"currency":payload.currency,"date":payload.date,"asset_type":"cash"});
      apply_add_investment_transfer(db, cash_leg, event). Per CHAT-09/XFER-02.
    - POST /portfolio-events/funded-buy (FundedBuyCreate): build ONE flat after
      dict from the payload fields (source_account_name, cash_currency,
      cash_amount, ticker, quantity, price, platform_id, event_currency, date,
      notes, asset_type). For the REST path pass the MoneyDecimal values
      straight through — abs(Decimal) and Decimal(str(Decimal)) both work and
      there is no JSONB round-trip, so NO float coercion and NO str() is needed
      here (unlike the proposal payload path). apply_add_funded_buy(db, after).
      Per CHAT-09/XFER-03.
    - POST /portfolio-events/funded-sell (FundedSellCreate): same flat-dict build;
      apply_add_funded_sell(db, after). Per CHAT-09/XFER-03.
    - POST /accounts/{account_id}/adjust-balance (BalanceAdjustmentCreate,
      account_id from path): apply_add_balance_adjustment(db, account_id,
      payload.target_balance); return the created Adjustment transaction id. Per
      CHAT-09/ACCT-02.

    Do NOT hand-roll any SQL/ORM insert or recompute holdings — every write
    routes through the Phase-13 apply_* primitive (SC #4; RESEARCH Don't
    Hand-Roll). Do NOT re-derive the balance-adjustment delta — pass
    target_balance straight through (the fresh unfiltered-SUM delta lives inside
    writes.py).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_write_endpoints.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@app.post("/transactions/transfer"\|@app.post("/transactions/investment-transfer"\|@app.post("/portfolio-events/funded-buy"\|@app.post("/portfolio-events/funded-sell"\|adjust-balance' backend/main.py` shows all 5 routes present
    - Every new route decorator includes `dependencies=[Depends(require_api_key)]`
    - `cd backend && python -m pytest tests/test_write_endpoints.py -q` — all 5 happy-path tests + the 3 validation/auth tests GREEN (201s, 422 on negative/zero + nonexistent account/platform, 401 without key)
    - Each handler calls its apply_* exactly once, commits exactly once, and calls reset_engine(); no raw `text(` SQL or direct `db.add(Transaction`/`db.add(PortfolioEvent` in the new handlers
    - `cd backend && python -m pytest -q` — full suite green except the pre-existing documented test_settings.py::test_put_settings_requires_key failure
  </acceptance_criteria>
  <done>All 5 REST routes exist behind require_api_key, route through apply_*, return 422 on bad input and 401 without a key; full suite green (minus the one documented pre-existing failure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST client → new write endpoints | untrusted request bodies cross into the mutation layer |
| request body → apply_* after-dict | malformed/negative/nonexistent-reference input must be rejected cleanly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Spoofing / Elevation of Privilege | new REST write endpoint missing auth gate | high | mitigate | Every route carries `dependencies=[Depends(require_api_key)]`; test_transfer_missing_api_key_401 asserts 401 without a key |
| T-14-06 | Tampering (data integrity) | unbounded/negative amounts bypass primitive sign normalization | medium | mitigate | `Field(..., gt=0)` on transfer amount + funded cash_amount/quantity/price rejects negative/zero at the schema boundary; test asserts 422 |
| T-14-07 | Information Disclosure | nonexistent account/platform → 500 stack-trace leak | medium | mitigate | apply_* raise ValueError on missing refs; handler maps ValueError→HTTPException(422); test asserts 422 not 500 |
| T-14-08 | Tampering (data integrity) | double-commit or hand-rolled SQL diverging from the shared mutation layer | medium | mitigate | Handlers call apply_* only, commit exactly once; acceptance criterion forbids raw SQL/ORM inserts and second commits |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
Run `cd backend && python -m pytest tests/test_write_endpoints.py -q` then the
full suite `cd backend && python -m pytest -q`. All 5 REST happy-path + 3
validation/auth tests green; full suite green except the pre-existing documented
test_settings.py failure. Live check (after `docker compose up -d --build`, per
deploy-requires-rebuild memory): `curl -X POST .../transactions/transfer` with
the api key returns 201 and both legs land paired.
</verification>

<success_criteria>
- 5 *Create schemas + 5 require_api_key-gated REST routes exist, routing through apply_*
- CHAT-09 success criterion 4 holds: REST endpoints route through Phase-13 apply_*, not ad-hoc SQL
- Bad input → 422, missing key → 401, no double-commit, reset_engine after each write
- All Plan 14-01 REST tests GREEN; full suite green (minus the documented pre-existing failure)
</success_criteria>

<output>
Create `.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-03-SUMMARY.md` when done
</output>
