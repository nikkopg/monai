---
phase: 14-rest-endpoints-agent-mcp-tool-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_proposals.py
  - backend/tests/test_write_endpoints.py
  - backend/tests/test_mcp.py
autonomous: true
requirements: [CHAT-09]
must_haves:
  truths:
    - "Running the phase-14 suite before any wiring shows the 5 propose→confirm tests, the 5 direct-REST tests, and the named-tool MCP-exclusion / agent-presence assertions all PRESENT and RED (failing or erroring, never missing)"
    - "Each new test asserts against the exact apply_* row/column outcome from writes.py, so a later payload-shape mismatch fails loudly instead of silently writing the wrong rows"
  artifacts:
    - "backend/tests/test_write_endpoints.py (new file) with 5 direct-REST endpoint tests"
    - "5 new propose→confirm integration tests in backend/tests/test_proposals.py"
    - "explicit named-tool assertions in backend/tests/test_mcp.py for the 5 new propose_* names"
  key_links:
    - "test payload shapes mirror the exact after-dict keys each apply_* consumes (leg_a/leg_b, cash_leg/event, flat funded dict, account_id/target_balance)"
    - "malformed-payload confirm test pins clean 422 (not 500/KeyError) for the shape hazard"
---

<objective>
Wave-0 RED validation scaffold for Phase 14. Create every automated test that
the two wiring plans (14-02 agent path, 14-03 REST path) must turn GREEN, so
the phase has a Nyquist-compliant feedback loop before a single line of
production wiring is written.

Purpose: this phase is 100% wiring — its entire risk is registration
completeness and payload-shape correctness (RESEARCH Pitfall 3). Tests-first
pins both risks so a shape mismatch or a missing registration fails a command,
not a manual chat probe.

Output: 5 propose→confirm tests (test_proposals.py), 5 direct-REST tests
(test_write_endpoints.py, new), and 5 named-tool surface assertions
(test_mcp.py). All RED at end of this plan.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-RESEARCH.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-PATTERNS.md
@.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-VALIDATION.md

# Source of truth for the exact after-dict shapes the tests must assert against:
@backend/writes.py
@backend/tests/test_proposals.py
@backend/tests/test_mcp.py
@backend/tests/test_write_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 propose→confirm integration tests to test_proposals.py (RED)</name>
  <files>backend/tests/test_proposals.py</files>
  <read_first>
    - backend/tests/test_proposals.py (fixtures `client`/`api_key`/`db_session` at lines 21-52; the model test `test_confirm_add_holding_persists_platform_id` at ~335 and `test_confirm_edit_holding_via_delegation` at ~404 are the exact propose→confirm delegation pattern to copy)
    - backend/writes.py lines 79-107 (apply_add_balance_adjustment), 150-167 (apply_add_transfer), 170-191 (apply_add_investment_transfer), 194-233 (apply_add_funded_buy), 236-267 (apply_add_funded_sell) — the after-dict keys each consumes
    - backend/tests/test_write_tools.py lines 1277-1517 (Phase-13 RED tests that already encode the exact canonical after-dict inputs and expected row outcomes for all 5 apply_* functions — reuse their seed idiom: `_make_account`, `_make_platform`, `zzNNtest-` prefixed names, `_cleanup_*` in a `finally` block)
  </read_first>
  <action>
    Add 5 integration tests to backend/tests/test_proposals.py, one per new
    operation, each: (1) seeds accounts/platform with `zz14test-` prefixed
    names via the existing seed helpers, (2) calls the corresponding propose_*
    tool via a lazy in-body import `from backend.tools import propose_add_transfer`
    (etc.) — this import raises ImportError until Plan 14-02 lands, which is the
    intended RED state, exactly the Phase-13 lazy-import RED idiom — (3) confirms
    via `POST /proposals/{id}/confirm` with the api_key token, (4) asserts the
    resulting rows match the apply_* contract, (5) cleans up in a `finally`.

    The five tests and their load-bearing assertions (per CHAT-09 → XFER-01..04 / ACCT-02):
    - `test_confirm_transfer_writes_both_legs`: propose_add_transfer(from,to,amount,currency,date,notes) → confirm → exactly 2 Transaction rows, both is_transfer=True, both sharing transfer_pair_id == leg A's id, leg amounts are -abs and +abs of the magnitude (source debited, dest credited). Mirrors test_write_tools.py::test_apply_add_transfer_pairs_both_legs.
    - `test_confirm_investment_transfer_links_event`: propose_add_investment_transfer(from_account, platform_id, amount, currency, date, notes) → confirm → 1 cash Transaction (is_transfer=True, negative amount on the liquid source) + 1 PortfolioEvent whose source_account_id == the cash leg's account_id, and NO new synthetic accounts row. Mirrors test_apply_add_investment_transfer. Assert the deposit sentinel: event ticker == "CASH", event_type == "deposit".
    - `test_confirm_funded_buy_writes_both_sides`: propose_add_funded_buy(source_account_name, platform_id, ticker, quantity, price, cash_amount, cash_currency, event_currency, date) → confirm → cash Transaction amount == Decimal(str(-cash_amount)) (DEBIT, is_transfer=True), 'buy' PortfolioEvent linked via source_account_id, and the (ticker, platform_id) Holding recomputed. Mirrors test_apply_add_funded_buy_one_commit_boundary.
    - `test_confirm_funded_sell_writes_both_sides`: propose_add_funded_sell(...) → confirm → cash Transaction amount positive (CREDIT), 'sell' PortfolioEvent linked via source_account_id. Mirrors test_apply_add_funded_sell_one_commit_boundary.
    - `test_confirm_balance_adjustment`: propose_add_balance_adjustment(account_id, target_balance) → confirm → exactly ONE new Transaction tagged category='Adjustment' and is_transfer=True whose amount == target_balance minus the account's current UNFILTERED SUM(amount). Mirrors test_apply_add_balance_adjustment_delta.

    Add one guard test `test_confirm_malformed_funded_buy_returns_422`: build a
    Proposal row DIRECTLY (not via the tool) with operation="add_funded_buy" and
    an after dict missing the "ticker" key, then confirm → assert status_code == 422
    (NOT 500). This pins the payload-shape hazard (autonomous decision 4): a
    missing/mismatched key must surface as a clean 422, never an unhandled
    KeyError→500. It stays green trivially pre-14-02 (unknown-operation → 422) and
    remains the regression guard once 14-02 adds the dispatch branch + KeyError
    guard.

    Do NOT set is_transfer in the transfer/investment-transfer/funded payloads —
    the apply_* primitives force it (writes.py:163-166,188,218,252). Do NOT put a
    bare Decimal in any payload dict. For the funded-buy/sell tool calls pass
    cash_amount/quantity/price as plain numbers (see Task-1 note in 14-02 on why
    these stay JSON numbers, not str).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_proposals.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_confirm_transfer_writes_both_legs\|def test_confirm_investment_transfer_links_event\|def test_confirm_funded_buy_writes_both_sides\|def test_confirm_funded_sell_writes_both_sides\|def test_confirm_balance_adjustment\|def test_confirm_malformed_funded_buy_returns_422" backend/tests/test_proposals.py` returns 6
    - `cd backend && python -m pytest tests/test_proposals.py -q` shows the 5 propose→confirm tests as RED (errored/failed via the lazy `from backend.tools import propose_*` ImportError), while the pre-existing test_proposals.py tests still pass
    - Each of the 5 new tests asserts on real row outcomes (transfer_pair_id equality, source_account_id linkage, Decimal amount signs, category='Adjustment', event ticker=='CASH') — not merely on HTTP 200
    - No test places a bare `Decimal(` object into a `payload=`/`after=` dict literal
  </acceptance_criteria>
  <done>test_proposals.py has 6 new tests; the 5 operation tests are RED via lazy import of not-yet-existent propose_* tools; existing proposal tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_write_endpoints.py + named-tool MCP assertions in test_mcp.py (RED)</name>
  <files>backend/tests/test_write_endpoints.py, backend/tests/test_mcp.py</files>
  <read_first>
    - backend/tests/test_account_crud.py (the `TestClient` + `require_api_key` header pattern for direct REST write tests)
    - backend/tests/test_mcp.py lines 99-145 (test_mcp_read_parity asserts len==15 and set==READ_TOOL_NAMES; test_agent_read_tools_count filters `not startswith("propose_")`; test_mcp_no_write_tools asserts no propose_* in tools/list) — add explicit per-name assertions alongside these
    - backend/writes.py (same 5 apply_* signatures as Task 1 — the REST endpoints route straight through these)
    - .planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-RESEARCH.md (proposed endpoint URLs: POST /transactions/transfer, /transactions/investment-transfer, /portfolio-events/funded-buy, /portfolio-events/funded-sell, /accounts/{id}/adjust-balance)
  </read_first>
  <action>
    Create backend/tests/test_write_endpoints.py (new file) following the
    test_account_crud.py TestClient + api_key-header idiom, with 5 happy-path
    tests + input-validation tests for the 5 direct REST endpoints (each RED
    until Plan 14-03 adds the route — a POST to a nonexistent route returns 404,
    which is the intended RED signal):
    - `test_post_transfer` → POST /transactions/transfer {from_account,to_account,amount,currency,date,notes} → 201, response carries leg_a_id/leg_b_id/transfer_pair_id, both legs exist and are paired.
    - `test_post_investment_transfer` → POST /transactions/investment-transfer {from_account,platform_id,amount,currency,date} → 201, cash leg + linked PortfolioEvent (ticker=="CASH").
    - `test_post_funded_buy` → POST /portfolio-events/funded-buy {source_account_name,platform_id,ticker,quantity,price,cash_amount,cash_currency,event_currency,date} → 201, cash leg debited + 'buy' event + holding recomputed.
    - `test_post_funded_sell` → POST /portfolio-events/funded-sell {...} → 201, cash leg credited + 'sell' event.
    - `test_post_adjust_balance` → POST /accounts/{account_id}/adjust-balance {target_balance} → 201, single Adjustment transaction with the correct delta.
    - Validation tests: `test_transfer_rejects_negative_amount` (amount=-1 → 422), `test_funded_buy_rejects_zero_cash_amount` (cash_amount=0 → 422), `test_transfer_missing_api_key_401` (no key header → 401). These pin the V5/auth threat controls (Field gt=0, require_api_key).

    Use `zz14test-` prefixed seed names + `finally` cleanup, matching the
    Phase-13 test idiom. Skip-if-DB-unavailable via the existing `db_available`
    pattern used across the suite.

    In backend/tests/test_mcp.py add ONE test `test_new_write_tools_registered_and_excluded`
    that names the 5 strings explicitly (propose_add_transfer,
    propose_add_investment_transfer, propose_add_funded_buy,
    propose_add_funded_sell, propose_add_balance_adjustment) and asserts, per
    CHAT-09 success criteria 2 & 3: each name is present in `TOOLS`, each name is
    ABSENT from `READ_TOOL_NAMES`, and each name is ABSENT from the live MCP
    tools/list surface. This catches an accidental rename/removal that the
    generic count/prefix checks would silently tolerate. It is RED until 14-02
    registers the tools (the `TOOLS` membership assertion fails today).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_write_endpoints.py tests/test_mcp.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_write_endpoints.py exists and defines the 5 happy-path tests plus the 3 validation/auth tests named above
    - `grep -c "def test_new_write_tools_registered_and_excluded" backend/tests/test_mcp.py` returns 1, and that test references all 5 new propose_* names as literal strings
    - `cd backend && python -m pytest tests/test_write_endpoints.py -q` shows the 5 happy-path REST tests RED (404 — routes not yet added)
    - `cd backend && python -m pytest tests/test_mcp.py::test_new_write_tools_registered_and_excluded -q` is RED (names not yet in TOOLS)
    - Pre-existing test_mcp.py tests (test_mcp_read_parity len==15, test_mcp_no_write_tools) remain green
  </acceptance_criteria>
  <done>New REST endpoint test file + explicit named-tool MCP test exist and are RED; existing MCP surface tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Postgres | Tests seed and clean up real rows; a leaked `zz14test-` row could pollute later assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-T1 | Tampering | test seed rows on live DB | low | mitigate | `finally`-block `_cleanup_*` + unique `zz14test-` prefixes + rollback, matching the Phase-13 test idiom |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A — pytest + existing fixtures only) |
</threat_model>

<verification>
Run `cd backend && python -m pytest tests/test_proposals.py tests/test_write_endpoints.py tests/test_mcp.py -q`.
Expected at end of this plan: the 5 propose→confirm tests, 5 REST happy-path
tests, and the named-tool registration test are RED; all pre-existing tests in
these files remain green. This is the correct Wave-0 RED baseline.
</verification>

<success_criteria>
- 11 new tests exist (6 in test_proposals.py incl. the malformed-payload guard, 5 happy-path + 3 validation in test_write_endpoints.py, 1 in test_mcp.py)
- The new behavior tests are RED (missing tools/routes), pre-existing tests green
- Every test asserts against the exact apply_* row/column outcomes from writes.py
</success_criteria>

<output>
Create `.planning/phases/14-rest-endpoints-agent-mcp-tool-registration/14-01-SUMMARY.md` when done
</output>
