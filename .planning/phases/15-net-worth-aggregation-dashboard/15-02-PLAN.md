---
phase: 15-net-worth-aggregation-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - ui/app/cashflow/page.tsx
autonomous: false
requirements: [NW-01, NW-02]
must_haves:
  truths:
    - "/cashflow hero shows one net-worth number sourced from GET /net-worth (not a client-side sum over all account rows) — fixes the live double-count, D-07, NW-01"
    - "Dashboard shows the liquid/investment split (two stat cards) plus a per-account liquid breakdown and a per-platform investment breakdown — D-08, NW-02"
    - "The breakdown renders whenever there is at least one liquid account OR one investment platform, even with zero period activity (net worth is not period-scoped)"
  artifacts:
    - ui/app/cashflow/page.tsx
  key_links:
    - "hero total binds to netWorthData.total from GET /net-worth (client-side net-worth summation removed)"
    - "'Liquid accounts' card filters to type==='liquid'; 'Investment platforms' card sourced from investment_groups (portfolio_summary groups)"
    - "422 from GET /net-worth surfaces the coverage-specific copy, distinct from the generic network-error copy"
---

<objective>
Rescope the existing `/cashflow` dashboard to lead with the server-computed net-worth headline, add a liquid/investment split row, and add per-side breakdowns (liquid = per-account, investment = per-platform). This fixes the live client-side double-count bug and delivers the NW-02 split UI — reusing existing card/token patterns, no new design system.

Purpose: User-facing half of NW-01 (one trustworthy number) and NW-02 (visible split + per-side breakdown) per D-07/D-08 and 15-UI-SPEC.md.
Output: Modified `ui/app/cashflow/page.tsx` consuming `GET /net-worth`.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-CONTEXT.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-UI-SPEC.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-01-PLAN.md

# GET /net-worth payload (from plan 15-01): { total, liquid_total, investment_total,
#   liquid_accounts:[{id,name,type,current_balance,period_net}],
#   investment_groups:[{platform_id,platform_name,kind,subtotal,holdings:[...]}],
#   accounts_covered, accounts_total }

# Repo rule: graphify-out/graph.json exists — run graphify query/explain BEFORE
# grepping/reading source; read raw lines only after graphify orients you.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch GET /net-worth, source hero from it, remove client-side net-worth calc + delta chip</name>
  <files>ui/app/cashflow/page.tsx</files>
  <read_first>
    - ui/app/cashflow/page.tsx (existing `/cashflow/summary` fetch pattern with useState/useEffect + summaryError; the client-side net-worth derivations at ~L179-186; hero card + ▲/▼ delta chip at ~L273-348; the AccountBalance TS type ~L28-33)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-UI-SPEC.md (Layout Contract §1 Hero row; error-state copy)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md (hero fix section)
  </read_first>
  <action>
    Add a `NetWorth` TypeScript type mirroring the plan-15-01 payload keys, and a `netWorthData`/`netWorthError` useState + useEffect that GETs `/net-worth` (via the same fetch/proxy convention the existing `/cashflow/summary` fetch uses in this file). Remove the client-side net-worth summation entirely — the two derived values that reduce over `summary.accounts` (current_balance and period_net) that fed the hero number and its delta; the hero must no longer compute net worth on the client (that summation over ALL account rows is the live double-count bug — D-07/NW-01). Bind the hero headline number to `netWorthData.total` (keep the exact existing hero visual: ink dark surface, radius 18, serif 52px, tabular-nums). Per 15-UI-SPEC §1, DELETE the ▲/▼ net-worth delta chip block (net-worth trend is deferred — do not invent a period delta). While `netWorthData` is loading, render the existing loading/skeleton treatment the page already uses for `summary`.
  </action>
  <verify>
    <automated>cd ui && npx tsc</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && npx tsc` passes (strict, noEmit) — no type errors from the new NetWorth type/fetch.
    - Source assertion: the hero number is bound to `netWorthData.total`; no client-side summation of `summary.accounts` remains for the net-worth number (grep the file for a reduce over account rows producing net worth — none remain).
    - The ▲/▼ hero delta chip block is removed.
  </acceptance_criteria>
  <done>Hero sources net worth from GET /net-worth; client-side net-worth reduce and delta chip removed; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add split row + per-side breakdowns (Liquid accounts / Investment platforms) with gate + empty/error states</name>
  <files>ui/app/cashflow/page.tsx</files>
  <read_first>
    - ui/app/cashflow/page.tsx (the "Stat cards" grid ~L419-446; the "Category + accounts" breakdown grid + Accounts card + account row markup ~L448-577; statCard/statLabel/statValue objects ~L754-769; `initials()` helper; `money()` helper; `hasActivity` gate ~L173-178; "No transactions yet." empty-state pattern ~L607-616)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-UI-SPEC.md (Layout Contract §2 Split row, §3 Breakdown row, Copywriting Contract, empty/error states)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md (split row + investment-platform row + gate-extension skeletons)
  </read_first>
  <action>
    Split row (NEW, insert directly below the hero row, above the existing Stat cards row): copy the existing Stat-cards grid shape (`repeat(auto-fit, minmax(min(100%, 200px), 1fr))`, gap 18) with two `statCard`s reusing statLabel/statValue verbatim (value 28px/600 tabular-nums in neutral `tokens.color.ink`, NOT green): Card A label "Liquid" → `money(netWorthData.liquid_total)` + 13px muted subtext "{N} account(s)" from `liquid_accounts.length`; Card B label "Investment" → `money(netWorthData.investment_total)` + subtext "{N} platform(s)" from `investment_groups.length`. Use singular/plural by count.
    Breakdown row (MODIFY the existing "Category + accounts" grid): rename the "Accounts" card title to "Liquid accounts" and source its rows from `netWorthData.liquid_accounts` (already type='liquid' from the endpoint) instead of `summary.accounts` — row markup (initials avatar + name + balance + period_net delta) stays byte-identical. Add a NEW third card "Investment platforms": one row per `netWorthData.investment_groups` entry reusing the exact row shell (34×34 initials badge via `initials(platform_name)`, name 14px/500, right-aligned `money(subtotal)` 14px/600 tabular-nums neutral ink) with NO delta/PnL line (groups carry no period-net). The `auto-fit minmax(300px)` grid becomes 3-up responsively with no manual breakpoint math.
    Gate extension: widen the breakdown-row render gate so it renders whenever `netWorthData.liquid_accounts.length > 0 || netWorthData.investment_groups.length > 0`, independent of the existing period-scoped `hasActivity` (net worth is not period-scoped — an account with a balance but no transactions this month must still show).
    Empty states: when `liquid_accounts` is empty render heading "No liquid accounts yet." / body "Add one below to start tracking cash balances."; when `investment_groups` is empty render heading "No investment platforms yet." / body "Add a platform and a holding on the Investments page to see it here." — reuse the existing empty-state visual (16px/600 heading + 14px muted body, no border).
    Error state: on `netWorthError` from a 422 (coverage-assertion failure) show the distinct copy "Couldn't verify net worth — some accounts aren't classified as liquid or investment. Check Settings › Accounts."; on a network/backend-down error reuse the existing generic summaryError copy. Never render the raw backend error body.
    Reuse only existing tokens/style objects (styles.ts + page-local statCard trio) — no new design tokens, no icon library, no new component.
  </action>
  <verify>
    <automated>cd ui && npx tsc</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && npx tsc` passes.
    - Source assertions: a split row with two statCards ("Liquid"/"Investment") exists between hero and the Stat-cards row; the breakdown card title reads "Liquid accounts" and maps `netWorthData.liquid_accounts`; an "Investment platforms" card maps `netWorthData.investment_groups` with `money(subtotal)` and no delta line.
    - The breakdown-row gate references `netWorthData.liquid_accounts.length`/`investment_groups.length` (not only `hasActivity`).
    - Both empty-state strings and the 422-specific error string are present verbatim per the UI-SPEC copy contract; no green used as a card background/border (green only as conditional positive-value text).
  </acceptance_criteria>
  <done>Split row + Liquid accounts + Investment platforms breakdowns render from the /net-worth payload with widened gate, empty states, and 422-distinct error copy; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — net-worth headline, split, and per-side breakdowns on /cashflow</name>
  <files>ui/app/cashflow/page.tsx</files>
  <read_first>
    - .planning/phases/15-net-worth-aggregation-dashboard/15-UI-SPEC.md (Layout Contract — the expected visual result)
    - Project memory: deploy-requires-rebuild (committed code != running container — must rebuild before UAT)
  </read_first>
  <action>Human verification checkpoint — no code changes. Rebuild/deploy the stack, then present the /cashflow dashboard for manual review per <how-to-verify>. Block until the user approves or reports issues.</action>
  <acceptance_criteria>
    - Hero net-worth number equals the Liquid subtotal + Investment subtotal shown in the split row.
    - No type='investment' account appears in the Liquid subtotal or under "Liquid accounts" — its value only appears under "Investment platforms".
    - No ▲/▼ delta chip on the hero; split + breakdown render for an account with a balance but no period activity.
    - User explicitly approves.
  </acceptance_criteria>
  <what-built>The /cashflow dashboard now leads with a server-computed net-worth headline sourced from GET /net-worth, a liquid/investment split row (two stat cards), and per-side breakdowns (Liquid accounts per-account, Investment platforms per-platform). The old client-side net-worth double-count and the hero delta chip are removed.</what-built>
  <how-to-verify>
    1. Rebuild the stack so the change is actually deployed (committed code != running container — deploy-requires-rebuild memory): `docker compose up -d --build` (or restart the ui + backend dev servers).
    2. Open http://localhost:3001/ (redirects to /cashflow).
    3. Confirm the hero "Net worth" number equals liquid subtotal + investment subtotal shown in the split row (do the addition by eye).
    4. Confirm NO investment-type account (e.g. legacy "Investments") is added into the Liquid subtotal or listed under "Liquid accounts" — its value should only appear under "Investment platforms".
    5. Confirm the "Investment platforms" card lists each platform with its subtotal and no delta line; confirm the hero has no ▲/▼ delta chip.
    6. Confirm the split + breakdown still show for an account that has a balance but no transactions this period.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's wrong (wrong total, double-count, missing card, layout).</resume-signal>
  <verify>
    <human-check>User confirms hero net worth == liquid + investment split, no investment account in the liquid side, no delta chip, breakdown shows with zero period activity.</human-check>
  </verify>
  <done>User types "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /net-worth | Client fetches aggregate net-worth data; backend error responses cross back here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Information Disclosure | Dashboard error rendering | low | mitigate | On a 422 (coverage failure) or network error, render the fixed UI-SPEC copy strings; never interpolate/render the raw backend error body or stack into the DOM. |
| T-15-06 | Tampering | Client-side net-worth number | medium | mitigate | Net worth is server-computed and only displayed; the client no longer sums account rows to derive it (removing the double-count divergence between client and server). No client-side aggregation logic remains for net worth. |

No new frontend packages installed (reuses existing next/react/recharts + inline style tokens); no legitimacy checkpoint required.
</threat_model>

<verification>
- `cd ui && npx tsc` — strict typecheck clean.
- Optional heavier gate: `cd ui && npm run build` compiles.
- Human-verify checkpoint: hero total == liquid + investment split, no investment account in the liquid side, no delta chip, breakdown shows with zero period activity.
</verification>

<success_criteria>
- NW-01 (UI): /cashflow hero shows one net-worth number sourced from GET /net-worth; the client-side double-count is gone.
- NW-02 (UI): liquid/investment split row + per-account liquid breakdown + per-platform investment breakdown render, reusing existing card/token patterns.
- Breakdown renders on balance-without-activity; empty and 422 states use the UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/15-net-worth-aggregation-dashboard/15-02-SUMMARY.md` when done.
</output>
