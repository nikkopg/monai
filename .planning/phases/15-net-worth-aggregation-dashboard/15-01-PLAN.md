---
phase: 15-net-worth-aggregation-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tools.py
  - backend/schemas.py
  - backend/main.py
  - backend/query.py
  - backend/mcp_server.py
  - backend/tests/test_net_worth.py
autonomous: true
requirements: [NW-01, NW-02]
must_haves:
  truths:
    - "GET /net-worth returns combined total == liquid_total + investment_total, with each account counted exactly once (type='investment' accounts excluded from the liquid sum) — D-01/D-03/D-04, NW-01"
    - "net_worth() raises ValueError (mapped to HTTP 422, never a raw 500) when liquid_count + investment_count != total account count — D-05, SC#3"
    - "test_net_worth.py proves the one-count sum for an all-liquid + all-investment mix AND proves an out-of-set/unexpected account type triggers the loud raise — D-06, SC#3"
    - "net_worth is callable as a zero-arg agent/MCP read tool, present in READ_TOOL_NAMES, and absent from every write surface — D-02"
    - "The payload exposes combined total + liquid subtotal + investment subtotal + per-side breakdown, and the subtotals reconcile to the combined total — D-08, NW-02"
  artifacts:
    - backend/tools.py
    - backend/schemas.py
    - backend/main.py
    - backend/query.py
    - backend/tests/test_net_worth.py
  key_links:
    - "net_worth registered in BOTH tools.py TOOLS/READ_TOOL_NAMES AND query.py read_tools (dual registration — chat-tool-dual-registration memory)"
    - "net_worth(db: Session | None = None) opens its own SessionLocal() when db is None so the agent/MCP zero-arg path works (Pitfall 3)"
    - "coverage-assertion ValueError caught in GET /net-worth handler → HTTPException(422, detail=str(exc)) (T-14-07 precedent)"
---

<objective>
Add a new composed read `net_worth` (tool + `GET /net-worth` endpoint) that returns one trustworthy net-worth number = liquid accounts + investment platforms, with each real account/holding counted exactly once, plus the liquid/investment split and per-side breakdown. Register it on the read-only agent/MCP surfaces and ship the coverage-assertion test suite.

Purpose: Delivers the backend for NW-01 (single net-worth number, no double-count) and NW-02 (split + per-side breakdown), and the SC#3 coverage assertion. Fixes the root cause of the live client-side double-count by making the server the single source of truth.
Output: `net_worth()` in tools.py, `NetWorth` schema, `GET /net-worth`, dual read-tool registration, and `backend/tests/test_net_worth.py` (green).
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-CONTEXT.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-RESEARCH.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md
@.planning/phases/15-net-worth-aggregation-dashboard/15-VALIDATION.md

# Repo rule: graphify-out/graph.json exists — run `graphify query "<question>"` /
# `graphify explain "<concept>"` BEFORE grepping/reading source; read raw files
# only after graphify orients you or to grab/modify exact lines.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create backend/tests/test_net_worth.py (RED)</name>
  <files>backend/tests/test_net_worth.py</files>
  <read_first>
    - backend/tests/test_cashflow_summary.py (fixture/seed-helper analog: db_available, db_session, _make_account, _make_transaction — L1-90)
    - backend/tests/test_portfolio.py (portfolio_summary DB-backed test style)
    - backend/tests/test_write_tools.py (write-surface / registry assertion style)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-RESEARCH.md (Open Question 2 — stub the coverage gap, do NOT insert a CHECK-violating row)
  </read_first>
  <action>
    Create backend/tests/test_net_worth.py mirroring test_cashflow_summary.py's fixtures (module-scoped `db_available` skip-if-Postgres-down, `db_session` rollback fixture) and add a `_make_account(db, name, type="liquid")` helper variant that accepts a `type` param (the existing helper hardcodes type="liquid") so it can seed both a `type="liquid"` and a `type="investment"` account. Write these cases, all initially failing because `net_worth` does not yet exist (this is the intended RED state):
    - `test_sum_counts_each_row_once`: seed one liquid account with a known non-transfer transaction balance L and one investment-typed account with its own balance X; assert `net_worth(db)["liquid_total"]` equals L only (X is excluded — D-04, counted-once by construction), and assert the investment account id does NOT appear in `liquid_accounts`.
    - `test_split_reconciles_to_total` (NW-02, D-08): assert `net_worth(db)["total"] == net_worth(db)["liquid_total"] + net_worth(db)["investment_total"]`, and that `investment_total == float(portfolio_summary(db)["total_value"])` (reconcile against the source of truth rather than hardcoding a price-dependent number).
    - `test_unclassified_type_raises` (D-05/D-06, SC#3): monkeypatch `backend.tools.account_balances` to return a stubbed `rows` list containing a row whose `type` is neither 'liquid' nor 'investment' (forcing `liquid_count + investment_count != len(rows)`); assert `pytest.raises(ValueError)` from `net_worth()`. Do NOT insert a real out-of-set row (the `ck_accounts_type` CHECK blocks it).
    - `test_net_worth_is_read_only` (D-02): assert `"net_worth" in READ_TOOL_NAMES` and `"net_worth" not in` the write-tool set added via `TOOLS.update()` (import the write-tool names / assert net_worth is absent from any propose_*/apply_* mapping) — the READ-ONLY safety contract.
    - `test_net_worth_registered_for_agent` (D-02, chat-tool-dual-registration memory — the query.py side is NOT auto-flowed and is the exact surface that bit the project in Phase 7): `from backend.query import read_tools` and assert some FunctionTool in that list has `fn` identity `net_worth` (e.g. `any(getattr(t, "fn", None) is net_worth or t.metadata.name == "net_worth" for t in read_tools)`). This makes the agent-registration surface RED if the FunctionTool line is ever removed — the one-time `python -c` check in Task 3 is not a regression guard.
    - `test_get_net_worth_endpoint` (NW-02 endpoint): use FastAPI `TestClient` (analog: test_cashflow_view.py if present) to `GET /net-worth`; assert 200 and that the JSON has keys total/liquid_total/investment_total/liquid_accounts/investment_groups/accounts_covered/accounts_total, and total == liquid_total + investment_total.
    Reference exact field names from Task 2's payload; keep them consistent.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_net_worth.py -q 2>&1 | grep -Eq "error|Error|failed|no tests ran|collected" </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_net_worth.py exists and pytest COLLECTS the 6 named test functions.
    - Running the file fails (RED) specifically because `net_worth` / the `NetWorth` payload keys are not yet importable/defined — not because of a fixture/import typo unrelated to net_worth.
    - `test_unclassified_type_raises` forces the coverage gap via a stub/monkeypatch of `account_balances`, never via a live CHECK-violating INSERT.
  </acceptance_criteria>
  <done>Test file present; all six cases collected and RED for the right reason (net_worth undefined).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: net_worth() read + extend account_balances + NetWorth schema + TOOLS registration</name>
  <files>backend/tools.py, backend/schemas.py</files>
  <read_first>
    - backend/tools.py (account_balances at ~L474-510 to extend; TOOLS dict + READ_TOOL_NAMES freeze at ~L604-628; TOOLS.update() write block ~L1320-1350)
    - backend/portfolio.py (portfolio_summary at ~L174, return shape ~L302-309 — the investment source of truth)
    - backend/schemas.py (CashflowSummary ~L82, PortfolioSummary ~L295 — DTO analogs)
    - backend/db.py (SessionLocal, Session — for the session shim)
    - backend/tests/test_net_worth.py (the RED cases Task 1 defined — this task makes them green)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md (net_worth skeleton + session shim, D-05 assertion)
  </read_first>
  <behavior>
    - net_worth(db=live session) with a mixed liquid+investment account set returns liquid_total = SUM of only type='liquid' derived balances; total = liquid_total + portfolio_summary.total_value.
    - net_worth() called with NO args (agent/MCP path) opens and closes its own SessionLocal and returns the same shape.
    - When the stubbed row set breaks coverage (liquid_count + investment_count != total), net_worth raises ValueError.
  </behavior>
  <action>
    In backend/tools.py, extend `account_balances()` ADDITIVELY: add `a.type` to the SELECT column list and to the GROUP BY, and include `"type": r[<idx>]` in each returned row dict. Keep it returning ALL accounts unfiltered — it is a shared MCP/agent/CashflowSummary contract (Pitfall 2); do NOT filter or rename existing keys (id/name/current_balance/period_net).
    Add `net_worth(db: Session | None = None) -> dict` in tools.py implementing D-01/D-03/D-04/D-05: open a `SessionLocal()` when `db is None` (track `owns_session`, `db.close()` in a `finally` only if it opened one — Pitfall 3, required by D-02). Compute the liquid side by calling the extended `account_balances()["rows"]` and partitioning in Python into `liquid_rows` (type == 'liquid') and `investment_rows` (type == 'investment'); `liquid_total = sum(current_balance for liquid_rows)`. Compute the investment side by calling `portfolio_summary(db)` and reading `total_value` and `groups` — never re-sum holdings/portfolio_events (Pitfall 4: CASH sentinel is already inside total_value). Coverage assertion (D-05): if `len(liquid_rows) + len(investment_rows) != len(rows)`, raise ValueError with a message stating the classified/total counts and that it refuses to silently drop or double-count. Return a dict with keys: tool, total, liquid_total, investment_total, liquid_accounts (liquid_rows), investment_groups (pf groups), accounts_covered, accounts_total. Filter liquid strictly by `type == 'liquid'` — never by name/id (anti-pattern).
    Register `net_worth` in the `TOOLS` dict literal BEFORE the `READ_TOOL_NAMES = frozenset(TOOLS)` freeze line so it auto-flows to MCP; do NOT add it to the `TOOLS.update()` write-tool block (D-02, read-only).
    In backend/schemas.py, add a `NetWorth(BaseModel)` mirroring CashflowSummary's loose style (plain float/list fields): total: float, liquid_total: float, investment_total: float, liquid_accounts: list, investment_groups: list, accounts_covered: int, accounts_total: int.
    NO fenced code / full impl beyond these identifiers — follow the PATTERNS.md skeleton.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_net_worth.py::test_sum_counts_each_row_once tests/test_net_worth.py::test_split_reconciles_to_total tests/test_net_worth.py::test_unclassified_type_raises tests/test_net_worth.py::test_net_worth_is_read_only -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from backend.tools import net_worth, account_balances; r=account_balances()['rows']; assert all('type' in x for x in r)"` succeeds (additive type field present).
    - net_worth() (zero-arg) returns a dict whose total == liquid_total + investment_total and whose liquid_total excludes any type='investment' account balance.
    - Forcing a coverage gap via the Task 1 stub raises ValueError.
    - `python -c "from backend.tools import READ_TOOL_NAMES; assert 'net_worth' in READ_TOOL_NAMES"` succeeds; net_worth appears in no write-tool mapping.
    - Existing account_balances consumers unaffected: `cd backend && python -m pytest tests/test_cashflow_summary.py tests/test_tools.py -q` stays green (row count/keys unchanged).
  </acceptance_criteria>
  <done>account_balances carries type additively; net_worth composes both sides with the session shim + loud coverage raise; NetWorth schema added; net_worth in TOOLS/READ_TOOL_NAMES only; the four unit cases from Task 1 pass.</done>
</task>

<task type="auto">
  <name>Task 3: GET /net-worth endpoint + query.py agent registration + MCP description</name>
  <files>backend/main.py, backend/query.py, backend/mcp_server.py</files>
  <read_first>
    - backend/main.py (cashflow_summary at ~L699-728 and investments_summary at ~L573-589 — composed-read + open-read + try/except→422 template; existing top import block for backend.tools)
    - backend/query.py (import block ~L97-115 and read_tools list ~L119-163 — FunctionTool.from_defaults entries)
    - backend/mcp_server.py (build_mcp loop ~L77-92 and MCP_DESCRIPTIONS ~L70-74)
    - .planning/phases/15-net-worth-aggregation-dashboard/15-PATTERNS.md (endpoint + dual-registration skeletons)
  </read_first>
  <action>
    In backend/main.py add `GET /net-worth` (response_model=NetWorth), an OPEN read with NO require_api_key dependency (matching cashflow_summary/investments_summary), taking `db: Session = Depends(get_session)`. Body: call `net_worth(db)` inside try/except; on `ValueError` raise `HTTPException(status_code=422, detail=str(exc))` (T-14-07 / apply-fk-integrityerror-not-422 precedent — the coverage-assertion ValueError must map to a clean 422, never bubble as a raw 500); return `NetWorth(**result)`. Import `net_worth` and `NetWorth` alongside the existing backend.tools / backend.schemas imports at the top of main.py (match how account_balances/cashflow_summary are imported).
    In backend/query.py add `net_worth` to the `from backend.tools import (...)` block AND append `FunctionTool.from_defaults(fn=net_worth)` to the `read_tools` list (the manual agent registration MCP does NOT do automatically — chat-tool-dual-registration memory).
    In backend/mcp_server.py add a `net_worth` entry to `MCP_DESCRIPTIONS` (a concise external-client description, matching the existing find_accounts/find_transactions entries). No other mcp_server.py change needed — build_mcp loops READ_TOOL_NAMES automatically.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_net_worth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /net-worth` returns 200 with the full NetWorth payload; `test_get_net_worth_endpoint` passes.
    - The coverage-assertion path maps to HTTP 422 (not 500): the endpoint wraps net_worth() in try/except ValueError → HTTPException(422).
    - `python -c "from backend.query import read_tools" ` (or the agent's tool-list builder) includes a FunctionTool whose fn is net_worth — the agent can call it.
    - Full backend suite green: `cd backend && python -m pytest tests/ -q`.
    - No require_api_key on the new endpoint (open read, matches siblings).
  </acceptance_criteria>
  <done>Endpoint live and 422-safe; net_worth dual-registered (TOOLS/READ_TOOL_NAMES from Task 2 + query.py read_tools here); MCP description added; entire backend test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/agent client → net_worth tool | Untrusted external client may invoke the read tool; it must stay read-only |
| HTTP client → GET /net-worth | Untrusted request; params (none beyond DB session) and error surface cross here |
| net_worth() → SQL (accounts/transactions) | Values partitioned by accounts.type reach the query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation/Tampering | net_worth registration surfaces (tools.py TOOLS/READ_TOOL_NAMES, query.py read_tools, mcp_server) | high | mitigate | Register net_worth ONLY in READ_TOOL_NAMES + query.py read_tools; never add it to the `TOOLS.update()` write-tool block. `test_net_worth_is_read_only` asserts membership in READ_TOOL_NAMES and absence from every write mapping. net_worth performs zero writes (composes two existing reads). |
| T-15-02 | Information Disclosure | GET /net-worth error path | high | mitigate | Wrap `net_worth(db)` in try/except in the endpoint; the coverage-assertion ValueError maps to `HTTPException(422, detail=str(exc))`, never bubbling as a raw 500 stack trace (T-14-07 / platform-existence 500 leak precedent). |
| T-15-03 | Tampering | net_worth SQL (liquid partition) | low | mitigate | The liquid filter is `type == 'liquid'` applied in Python over rows from the existing parameterized `account_balances()` query; no request-derived value is string-interpolated into SQL. LLM never emits SQL (correctness-by-construction). |
| T-15-04 | Denial of Service | portfolio_summary call on every /net-worth hit | low | accept | Single-user self-hosted app; portfolio_summary already backs investments_summary with the same cost profile. No new rate-limit needed. |

No package installs in this plan (composition of existing first-party code only — RESEARCH Package Legitimacy Audit = N/A); no T-15-SC / legitimacy checkpoint required.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_net_worth.py -q` — all net_worth cases green.
- `cd backend && python -m pytest tests/ -q` — full backend suite green (no regression in account_balances consumers).
- `python -c "from backend.tools import READ_TOOL_NAMES; assert 'net_worth' in READ_TOOL_NAMES"` — read-surface membership.
- Manual smoke: start backend, `curl -s localhost:8001/net-worth` returns the composed payload with total == liquid_total + investment_total.
</verification>

<success_criteria>
- NW-01: `GET /net-worth` returns one net-worth number = liquid (type='liquid' only) + investment (portfolio_summary.total_value), each account counted once.
- NW-02: payload carries liquid_total, investment_total, liquid_accounts, investment_groups; subtotals reconcile to total.
- SC#3: coverage assertion raises ValueError → 422 when classification is incomplete; accounts_covered/accounts_total exposed in the payload.
- net_worth is a read-only agent/MCP tool (dual-registered), off every write surface.
</success_criteria>

<output>
Create `.planning/phases/15-net-worth-aggregation-dashboard/15-01-SUMMARY.md` when done.
</output>
