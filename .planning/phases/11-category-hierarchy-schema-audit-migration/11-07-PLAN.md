---
phase: 11-category-hierarchy-schema-audit-migration
plan: 07
type: execute
wave: 5
depends_on: ["11-04", "11-05", "11-06"]
files_modified:
  - backend/schemas.py
  - backend/main.py
  - ui/app/cashflow/charts/CategoryDonut.tsx
  - ui/app/cashflow/page.tsx
autonomous: true
requirements: [CAT-04]
must_haves:
  truths:
    - "Dashboard donut shows the top-level groups, each slice colored by its category's own swatch — same group, same color, everywhere (D-09, D-14)"
    - "Clicking a slice drills into that group's subcategory breakdown; a Back link returns to the rollup (D-09)"
    - "Transfer never appears in the donut at either level — filtered in the query, not hidden in render (D-12)"
    - "Cashflow summary by_category carries id/name/color/icon/total plus children, sourced from the hierarchy (CAT-04)"
  artifacts:
    - path: "ui/app/cashflow/charts/CategoryDonut.tsx"
      provides: "Rollup + drill-down donut using category identity colors"
      contains: "Back"
    - path: "backend/schemas.py"
      provides: "CashflowSummary.by_category rich rollup shape"
      contains: "by_category"
  key_links:
    - from: "backend/main.py cashflow_summary"
      to: "backend/tools.py spending_by_category"
      via: "summary consumes the tool's rows + children rollup (was rows-only tuples)"
      pattern: "spending_by_category"
    - from: "ui/app/cashflow/page.tsx"
      to: "ui/app/cashflow/charts/CategoryDonut.tsx"
      via: "summary.by_category objects passed as donut data with drill state"
      pattern: "CategoryDonut"
---

<objective>
Rewire the dashboard category chart onto the hierarchy (CAT-04, ROADMAP criterion 4):
CashflowSummary.by_category becomes a rich top-group rollup (id, name, swatch color,
icon, total, children) fed by plan 11-04's hierarchy-aware spending_by_category, and
CategoryDonut gains drill-down + Back per UI-SPEC Component 3 (D-09), with Transfer
excluded at the query layer (D-12).

Purpose: the last surface still reading free-string category data moves onto the
hierarchy, completing the phase's read-path story.
Output: donut with identity-stable colors and drill-down; summary endpoint serving
the tree-shaped rollup.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-04-SUMMARY.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rich by_category rollup in CashflowSummary</name>
  <files>backend/schemas.py, backend/main.py</files>
  <read_first>
    - backend/main.py (cashflow_summary at ~556-585 — line 581 currently takes spending_by_category(...)["rows"] as tuples)
    - backend/tools.py (spending_by_category's new return shape from plan 11-04: rows + children keys)
    - backend/schemas.py (CashflowSummary at ~59 — by_category currently a tuple list)
    - backend/tests/test_cashflow_summary.py (existing summary assertions to update)
  </read_first>
  <behavior>
    - GET /cashflow/summary by_category entries are objects: {id, name, color, icon, total,
      children: [{id, name, color, icon, total}]} ordered by total desc
    - color is the group's stored swatch hex; children carry effective (inherited) color
    - No entry named Transfer at either level (D-12)
  </behavior>
  <action>
    schemas.py: add CategoryRollupChild {id, name, color, icon, total} and CategoryRollup
    (same + children list); CashflowSummary.by_category becomes list[CategoryRollup] —
    breaking change to the response shape, coordinated with Task 2 (the cashflow page is
    the only consumer). main.py cashflow_summary: consume both keys of the plan 11-04 tool
    return (rows for ordering/totals, children for the drill-down breakdown) and join
    category id/color/icon from one SELECT over categories; keep every other summary field
    untouched. Update test_cashflow_summary.py assertions to the new shape (write/adjust the
    failing assertions first per tdd flag). Commit as:
    feat(11-07): hierarchy rollup shape in cashflow summary
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt pytest backend/tests/test_cashflow_summary.py -q 2>&1 | tail -3 && uv run --with-requirements backend/requirements.txt pytest backend/tests -q 2>&1 | tail -2</automated>
  </verify>
  <done>Summary serves rich rollup objects; tests updated and green; full suite green.</done>
  <acceptance_criteria>
    - test_cashflow_summary.py asserts object-shaped by_category with children and passes
    - A summary response for all_time contains no rollup entry named Transfer (test assertion)
    - Full suite pytest backend/tests -q exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CategoryDonut drill-down + identity colors</name>
  <files>ui/app/cashflow/charts/CategoryDonut.tsx, ui/app/cashflow/page.tsx</files>
  <read_first>
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md (Component 3 — drill-down, Back affordance, monochrome-by-design note, tooltip unchanged)
    - ui/app/cashflow/charts/CategoryDonut.tsx (existing 150x150 ring, chartColors cycle being replaced, tooltip pattern)
    - ui/app/cashflow/page.tsx (by_category tuple mapping at ~162-171 and the legend list at ~473-492 — both move to the new object shape)
  </read_first>
  <action>
    cashflow/page.tsx: update the CashflowSummary type (~line 42) and categoryData mapping
    to the CategoryRollup object shape; legend swatches use each entry's color (identity
    colors, D-14) instead of positional chartColors; pass the full entries (with children)
    to CategoryDonut. CategoryDonut.tsx: default view renders top groups, slice fill = entry
    color; click a slice → re-render the same 150x150 ring scoped to that group's children
    (children inherit the parent swatch — a monochrome drilled ring is expected per UI-SPEC,
    not a bug); show a "‹ Back" text link (12px, muted2 token) only while drilled, returning
    to rollup; keep the existing tooltip pattern untouched; no Transfer handling needed
    client-side (excluded server-side, D-12). Local useState for drill state — no routing,
    no library. Rebuild for live check: docker compose up -d --build.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit</automated>
  </verify>
  <done>Donut rolls up, drills down, and returns; legend uses identity colors; tsc clean.</done>
  <acceptance_criteria>
    - tsc --noEmit exits 0
    - CategoryDonut renders slice fills from data colors (chartColors cycle no longer drives category slices)
    - Component contains the Back affordance rendered only in drilled state
    - Live check after docker compose up -d --build: donut shows top groups; clicking Food & Drinks shows its subcategories; Back returns (human-check at phase UAT)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB category data → summary JSON → browser | stored names/colors rendered in chart and legend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-23 | Tampering (integrity) | Transfer counted in spending chart | medium | mitigate | Exclusion lives in plan 11-04's SQL; Task 1 test asserts no Transfer entry in the response (D-12) |
| T-11-24 | Spoofing/XSS | category names in legend/tooltip | low | mitigate | React text-node rendering only, matching existing chart code |
| T-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- pytest backend/tests -q → green (summary shape tests included)
- cd ui && npx tsc --noEmit → clean
- Live: dashboard donut = top groups with palette colors; drill-down + Back work; no Transfer slice (human-check at /gsd-verify-work after docker compose up -d --build)
</verification>

<success_criteria>
- Dashboard charts read the hierarchy, not the free-string column (ROADMAP criterion 4)
- Drill-down per UI-SPEC Component 3; identity-stable group colors (D-09, D-14)
- Transfer excluded from chart data at the query layer (D-12)
</success_criteria>

<output>
Create `.planning/phases/11-category-hierarchy-schema-audit-migration/11-07-SUMMARY.md` when done
</output>
