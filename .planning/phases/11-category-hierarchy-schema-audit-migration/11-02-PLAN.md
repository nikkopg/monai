---
phase: 11-category-hierarchy-schema-audit-migration
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - alembic/data/category_mapping.csv
autonomous: false
requirements: [CAT-03]
must_haves:
  truths:
    - "All 74 distinct raw category strings (exact, untrimmed) appear as mapping keys reviewed by the user (D-05, D-06)"
    - "After migration: zero transactions have NULL category_id, and per-string row-count + sum-of-amount parity holds (CAT-03)"
    - "Re-running alembic upgrade head is a no-op (idempotent, D-07)"
    - "The 668 rows with the TRANSFER string point at the system Transfer category (D-04, D-12)"
  artifacts:
    - path: "alembic/data/category_mapping.csv"
      provides: "Human-reviewed 74-key mapping: raw_category,group,subcategory,emoji,color"
      contains: "raw_category"
  key_links:
    - from: "alembic/data/category_mapping.csv"
      to: "alembic/versions/009_category_hierarchy.py"
      via: "load_mapping consumes this file when upgrade() runs"
      pattern: "raw_category,group,subcategory,emoji,color"
---

<objective>
Draft the 74-string mapping file (D-05), pause for the mandatory human review
checkpoint (D-06), then execute migration 009 against the live database and prove
parity + idempotency (CAT-03, D-07). This is the in-database move the user asked to
have stated plainly: free-string `transactions.category` → `categories` table +
`transactions.category_id` FK, entirely inside PostgreSQL — nothing leaves the DB,
`raw_category` is never touched (D-08).

Purpose: the migration is the phase's zero-data-loss core; the human review is the
only non-automatable step (roadmap research flag).
Output: reviewed category_mapping.csv committed; live DB migrated with parity evidence.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-CONTEXT.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-RESEARCH.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the 74-string mapping CSV with emoji/color defaults</name>
  <files>alembic/data/category_mapping.csv</files>
  <read_first>
    - alembic/versions/009_category_hierarchy.py (GROUP_META group names the CSV must use verbatim; load_mapping's expected header and validation rules)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-RESEARCH.md (Runtime State Inventory: 74 strings, whitespace dupe, TRANSFER=668-row alignment)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md (Category Color Palette — the closed 13-swatch set; subcategories inherit by default per D-14)
  </read_first>
  <action>
    Query the live DB for the authoritative key list:
    psql "postgresql://monai:monai@localhost:5434/monai" -Atc "SELECT category FROM (SELECT DISTINCT category FROM transactions) d ORDER BY 1"
    — expect exactly 74 rows (abort and investigate if not; count drift means new imports
    happened since research). Write alembic/data/category_mapping.csv with header
    raw_category,group,subcategory,emoji,color and one row per EXACT raw string — no
    trimming, no case changes; both "Active sport, fitness" and its leading-space variant
    are separate rows targeting the same group+subcategory (Pitfall 1). Rules:
    - group must be one of the D-02 BudgetBakers top groups used in GROUP_META: Food & Drinks,
      Shopping, Housing, Transportation, Vehicle, Life & Entertainment, Communication/PC,
      Financial Expenses, Investments, Income, Others — plus Transfer for the "TRANSFER"
      string only (subcategory blank → maps to the system Transfer node, D-04/D-12)
    - subcategory = the human-readable node name (usually the trimmed raw string); blank
      subcategory means the string maps to the group node itself (any-level assignment, D-01)
    - emoji: one sensible default per subcategory (D-13 — Claude's discretion, user reviews)
    - color: blank = inherit parent group swatch (D-14 default); only set a hex when a
      subcategory genuinely warrants its own swatch, and only from the 13-value palette
    - kind is NOT a CSV column — it derives from the group (Income → income; Transfer →
      transfer; else expense, D-03)
    Then build the ambiguous-strings list for the checkpoint: any raw string whose group
    assignment required judgment (generic terms, Indonesian-language strings, overlapping
    candidates like fitness vs sport vs health). Verify the draft mechanically with a
    python one-liner: csv keys set == DB distinct set AND len(keys) == 74.
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt python -c "import csv,sqlalchemy as sa; ks={r['raw_category'] for r in csv.DictReader(open('alembic/data/category_mapping.csv'))}; e=sa.create_engine('postgresql+psycopg://monai:monai@localhost:5434/monai'); db={r[0] for r in e.connect().execute(sa.text('SELECT DISTINCT category FROM transactions'))}; assert ks==db and len(ks)==74, (len(ks), sorted(db-ks)[:5], sorted(ks-db)[:5]); print('74/74 exact-key coverage OK')"</automated>
  </verify>
  <done>CSV exists with exactly 74 data rows keyed by exact raw strings; coverage check prints OK; ambiguous list prepared for checkpoint.</done>
  <acceptance_criteria>
    - Coverage one-liner prints "74/74 exact-key coverage OK"
    - CSV contains a row whose raw_category begins with a space (the " Active sport, fitness" variant) mapping to the same group+subcategory as the trimmed variant
    - The row for raw_category TRANSFER has group Transfer and blank subcategory
    - Every non-empty color value in the CSV is one of the 13 UI-SPEC palette hexes
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of the mapping (D-05/D-06 — must NOT be auto-approved)</name>
  <files>alembic/data/category_mapping.csv</files>
  <action>Pause execution and present the drafted mapping for the mandated human review (D-06). Show the full mapping as a table grouped by target group, then walk through ONLY the ambiguous strings one at a time (D-05), applying any corrections the user gives directly to alembic/data/category_mapping.csv before proceeding. Do not run the migration until the user signals approval.</action>
  <what-built>Drafted alembic/data/category_mapping.csv: all 74 raw category strings mapped to BudgetBakers top groups with subcategory names, emoji defaults, and palette colors.</what-built>
  <how-to-verify>
    1. Review the mapping presented as a table grouped by target group (group → subcategory → raw string(s), emoji, color).
    2. Walk through the ambiguous strings only (D-05): for each, Claude states its proposed group + why, and the plausible alternative; confirm or correct each one in chat.
    3. Optionally hand-edit alembic/data/category_mapping.csv directly (keep the header and exact raw_category keys — edit only group/subcategory/emoji/color).
    4. Confirm the two system placements: TRANSFER → Transfer (668 rows), and that Uncategorized exists as forward-looking insurance (0 rows today, D-04).
  </how-to-verify>
  <resume-signal>Type "approved" to run the migration, or describe edits to apply first. This checkpoint is the phase's mandated human review (D-06) — the executor must not self-approve it even in yolo/auto-advance mode.</resume-signal>
  <verify>User has explicitly typed approval (or supplied edits that were applied and re-presented); every requested edit is reflected in the committed CSV.</verify>
  <done>User approval received in chat; CSV content matches the reviewed state; ambiguous strings each confirmed or corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Run migration 009 against the live DB; prove parity and idempotency</name>
  <files>alembic/data/category_mapping.csv</files>
  <read_first>
    - alembic/versions/009_category_hierarchy.py (what upgrade() prints/asserts)
    - alembic.ini (script location / config)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-01-SUMMARY.md (any invocation notes from plan 11-01)
  </read_first>
  <action>
    Safety first: take a pre-migration snapshot —
    pg_dump "postgresql://monai:monai@localhost:5434/monai" -Fc -f /tmp/monai-pre-009.dump
    and record pre-migration global totals via psql: SELECT COUNT(*), SUM(amount) FROM
    transactions. Then run: uv run --with-requirements backend/requirements.txt alembic
    upgrade head. Capture the parity summary output. Then prove idempotency (D-07): run
    alembic upgrade head a second time — it must complete without error and change 0 rows
    (alembic reports nothing to do at head; additionally re-run the psql checks below and
    confirm identical results). Post checks via psql, all against
    postgresql://monai:monai@localhost:5434/monai:
    - SELECT COUNT(*) FROM transactions WHERE category_id IS NULL → 0 (D-04)
    - SELECT COUNT(*), SUM(amount) FROM transactions → identical to pre-migration record
    - SELECT COUNT(*) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='Transfer' → 668
    - SELECT COUNT(*) FROM categories WHERE is_system → 2
    - SELECT COUNT(*) FROM categories WHERE parent_id IS NULL → number of distinct groups used + 2 system rows
    raw_category untouched: SELECT COUNT(*) FROM transactions WHERE raw_category IS NULL → 14
    (unchanged from research inventory). Commit the reviewed CSV. Note for later plans:
    the running backend container still serves pre-hierarchy code until
    `docker compose up -d --build` — expected until the phase's code plans land.
  </action>
  <verify>
    <automated>psql "postgresql://monai:monai@localhost:5434/monai" -Atc "SELECT (SELECT COUNT(*) FROM transactions WHERE category_id IS NULL)=0 AND (SELECT COUNT(*) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='Transfer')=668 AND (SELECT COUNT(*) FROM categories WHERE is_system)=2"</automated>
  </verify>
  <done>Migration applied to live DB; parity summary captured in SUMMARY.md; second run proven no-op; all psql checks pass; CSV committed.</done>
  <acceptance_criteria>
    - Verify query prints "t"
    - Global COUNT(*)/SUM(amount) identical before vs after (values recorded in SUMMARY.md)
    - Second `alembic upgrade head` run exits 0 with no row changes
    - /tmp/monai-pre-009.dump exists (rollback insurance)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human-edited CSV → migration SQL | user edits flow into UPDATEs on live financial data |
| host shell → live PostgreSQL | pg_dump/psql/alembic run directly against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering (data loss) | live backfill of 5,728 rows | critical | mitigate | pg_dump snapshot before run (Task 3) + single-transaction migration + abort-on-unknown + parity assertions (11-01's tested logic) |
| T-11-06 | Tampering | malformed human CSV edit | high | mitigate | load_mapping validation (empty key / unknown group → abort) + Task 3 re-runs the 74-key coverage check semantics inside upgrade() |
| T-11-07 | Elevation of Privilege | DB credentials in shell commands | low | accept | Local single-user dev DB with default creds; already the project's standing posture |
| T-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- All Task 3 psql checks pass against the live DB
- Parity summary (per-group counts/sums) recorded in SUMMARY.md — verified, not assumed
- CSV committed with the user's reviewed content
</verification>

<success_criteria>
- Every one of the 74 strings maps to a reviewed category node (ROADMAP criterion 1)
- Row-count and sum-of-amount parity verified pre vs post (ROADMAP criterion 2)
- Migration idempotent: second run is a no-op (D-07)
- Zero NULL category_id rows; Transfer and Uncategorized system rows exist (D-04)
</success_criteria>

<output>
Create `.planning/phases/11-category-hierarchy-schema-audit-migration/11-02-SUMMARY.md` when done
</output>
