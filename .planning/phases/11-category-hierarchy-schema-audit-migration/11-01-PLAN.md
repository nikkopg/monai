---
phase: 11-category-hierarchy-schema-audit-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - alembic/versions/009_category_hierarchy.py
  - backend/tests/test_category_migration.py
autonomous: true
requirements: [CAT-01, CAT-03]
must_haves:
  truths:
    - "A Category ORM model exists with name, parent_id (self-referential FK), kind, color, icon, is_system"
    - "Migration 009 aborts loudly (nothing partial) when any transaction category string is missing from the mapping CSV (D-07)"
    - "Migration 009's mapping/parity helpers are unit-tested without a database"
    - "Mapping is keyed by EXACT raw string — whitespace variants stay distinct keys (Pitfall 1)"
    - "D-01: transactions.category_id can reference a category at ANY level (parent or leaf) — no artificial General leaves exist"
  artifacts:
    - path: "backend/models.py"
      provides: "Category model + Transaction.category_id column"
      contains: "class Category"
    - path: "alembic/versions/009_category_hierarchy.py"
      provides: "Idempotent schema+data migration with testable helpers"
      exports: ["upgrade", "downgrade", "load_mapping", "find_unmapped", "assert_parity"]
    - path: "backend/tests/test_category_migration.py"
      provides: "Unit tests for mapping load, unknown-string abort, parity assertion"
  key_links:
    - from: "alembic/versions/009_category_hierarchy.py"
      to: "alembic/data/category_mapping.csv"
      via: "load_mapping reads CSV path relative to migration file (created by plan 11-02)"
      pattern: "category_mapping\\.csv"
    - from: "backend/tests/test_category_migration.py"
      to: "alembic/versions/009_category_hierarchy.py"
      via: "importlib.util.spec_from_file_location load of the migration module"
      pattern: "spec_from_file_location"
---

<objective>
Create the category hierarchy schema (CAT-01) and the Alembic revision 009 data
migration machinery (CAT-03): a self-referential `Category` model, a nullable
`transactions.category_id` FK, and a migration whose mapping-load / abort-on-unknown
(D-07) / parity-assertion logic is factored into pure, unit-testable functions.

This plan writes and tests the migration CODE. It does NOT run the migration and does
NOT create the mapping CSV — plan 11-02 drafts the CSV, pauses for human review (D-06),
then runs `alembic upgrade head`.

Purpose: TDD on the mapping/parity logic is the phase's core safety net — CAT-03's
"parity verified, not assumed" bar is met by tests that fail loudly on the exact
landmines found in research (whitespace-variant duplicate key, unknown string).
Output: Category model, migration 009 file, green test_category_migration.py.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-CONTEXT.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-RESEARCH.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing unit tests for migration mapping/parity helpers</name>
  <files>backend/tests/test_category_migration.py</files>
  <read_first>
    - backend/tests/test_category_management.py (fixture/assertion conventions, db_available pattern — these new tests are pure-unit and need NO db, but mirror style)
    - backend/tests/conftest.py (shared fixtures)
    - alembic/versions/006_multi_platform_holdings.py (migration idiom the helpers will live beside)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-RESEARCH.md (Pitfall 1 whitespace dupe; Common Pitfalls; Security Domain mapping-file row validation)
  </read_first>
  <behavior>
    Load the migration module via importlib.util.spec_from_file_location("migration_009",
    "alembic/versions/009_category_hierarchy.py") in a module-level fixture. Test cases
    (pure functions, tmp_path CSV fixtures, no DB):
    - load_mapping(csv_path) returns a dict keyed by EXACT raw string: a fixture CSV
      containing both "Active sport, fitness" and " Active sport, fitness" (leading
      space) yields 2 distinct keys mapping to the same (group, subcategory) target (Pitfall 1, D-07)
    - load_mapping rejects a row with empty raw_category by raising ValueError (mapping-file
      injection guard from RESEARCH.md Security Domain — an empty key must never reach SQL)
    - load_mapping rejects a row whose group is not a known GROUP_META key (raises ValueError naming the group)
    - find_unmapped(distinct_strings, mapping) returns sorted list of strings absent from
      mapping keys; empty list when all covered; exact match only — a trimmed variant of a
      key is still reported unmapped (no normalization, D-07)
    - assert_parity(pre, post) with dicts {raw_string: (count, amount_sum)}: passes when equal;
      raises RuntimeError naming the exact raw string and both (count, sum) pairs on mismatch
    - kind_for_group("Income") == "income"; kind_for_group("Food & Drinks") == "expense";
      kind_for_group("Transfer") == "transfer" (D-03 typing carried at top-level group)
  </behavior>
  <action>
    Write backend/tests/test_category_migration.py per the behavior block. The migration
    module does not exist yet — the importlib fixture must fail with a clear
    FileNotFoundError/ImportError so the RED run fails for the right reason. Use pytest
    tmp_path to write fixture CSVs with header raw_category,group,subcategory,emoji,color.
    Commit as: test(11-01): add failing tests for migration 009 mapping/parity helpers
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt pytest backend/tests/test_category_migration.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Test file exists, all tests FAIL (migration module missing), RED commit made with test(11-01) prefix.</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_category_migration.py` collects >= 6 tests and every test fails (no passes, no collection errors other than the intended missing-module failure path)
    - Test file contains the exact raw-string pair "Active sport, fitness" and " Active sport, fitness" as distinct CSV fixture keys
    - git log shows a commit whose subject starts with "test(11-01):"
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Category model, transactions.category_id, migration 009</name>
  <files>backend/models.py, alembic/versions/009_category_hierarchy.py</files>
  <read_first>
    - backend/models.py (Account lines ~44-54 and Transaction lines ~74-94 — exact Mapped/mapped_column idiom, docstring convention; raw_category at line ~84 stays untouched per D-08)
    - alembic/versions/006_multi_platform_holdings.py (full file — header docstring convention, nullable→backfill idiom, downgrade strict-reverse ordering)
    - alembic/versions/008_fx_rate_cache.py (revision id "d3e4f5a6b7c8" — 009's down_revision)
    - alembic/env.py (single-transaction online migration model)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md (Category Color Palette table — the 13 hex values for GROUP_META)
  </read_first>
  <action>
    Implements D-01, D-03, D-04, D-07, D-13, D-14 schema surface. Two changes:

    1. backend/models.py — add `Category` (tablename "categories") mirroring Account's
    Mapped style: id PK; name String(255) NOT NULL; parent_id ForeignKey("categories.id")
    nullable index (no ondelete clause — RESTRICT default so orphaning is impossible,
    Pitfall 3); kind String(16) NOT NULL (allowed values 'expense'|'income'|'transfer';
    'transfer' used only by the system Transfer row — discretion call under D-03, documented
    in the model docstring); color String(16) nullable (NULL = inherit parent per D-14);
    icon String(16) nullable (emoji as text, D-13); is_system Boolean NOT NULL default False
    (True for Transfer and Uncategorized, D-04). __table_args__: UniqueConstraint("name",
    "parent_id", name="uq_categories_name_parent") AND a partial unique Index
    "uq_categories_name_root" on name with postgresql_where parent_id IS NULL (Postgres
    treats NULLs as distinct in the composite constraint, so root names need the partial
    index to be unique). Add to `Transaction`: category_id Mapped[int | None] =
    mapped_column(ForeignKey("categories.id"), nullable=True, index=True). Deliberate
    deviation from the 006 idiom: category_id stays NULLABLE in DDL this phase — the
    NOT NULL tightening moves to the later legacy-column-drop migration (CAT-03 already
    defines that separate destructive step). Rationale in the migration docstring: making
    it NOT NULL now would break every insert path (POST /transactions, importer, agent
    writes) until plan 11-05 lands dual-write; the "no NULL categories remain" criterion
    (D-04) is enforced as a data assertion at the end of upgrade() instead. Depth cap is
    app-enforced (plan 11-03), not DDL — per RESEARCH Pattern 1.

    2. alembic/versions/009_category_hierarchy.py — revision "009_category_hierarchy"
    (or repo's hex convention), down_revision "d3e4f5a6b7c8". Module-level, pure,
    importable helpers (tested by Task 1): GROUP_META dict mapping each D-02 top-level
    group name to (kind, color_hex, default_emoji) using the UI-SPEC 13-swatch table
    exactly — Food & Drinks #d8b26a, Shopping #b5503f, Housing #5a8f73, Transportation
    #8fae9c, Vehicle #a8674a, Life & Entertainment #8a6a8f, Communication/PC #6b7f8f,
    Financial Expenses #6f6857, Investments #c9973f, Income #23543c (kind income),
    Others #c8c1b5, Transfer #a49c8c (kind transfer, is_system), Uncategorized #8b8474
    (kind expense, is_system); load_mapping(csv_path); find_unmapped(distinct_strings,
    mapping); assert_parity(pre, post); kind_for_group(group). upgrade() steps, all
    idempotent (sa.inspect guards: has_table, column list, seed via SELECT-before-INSERT):
    (a) create categories table if missing (matching the model, including both uniqueness
    rules); (b) load alembic/data/category_mapping.csv resolved relative to the migration
    file — clear FileNotFoundError naming plan 11-02 if absent; (c) seed top-level rows
    for every group used by the CSV plus Transfer and Uncategorized (always), then seed
    subcategory rows (name = subcategory value, parent = its group row, icon from CSV
    emoji, color from CSV color-or-NULL-inherit, kind inherited from group); rows where
    subcategory is blank resolve to the group node itself (any-level assignment, D-01);
    (d) add category_id column/FK "fk_transactions_category"/index
    "ix_transactions_category_id" if missing; (e) SELECT DISTINCT category FROM
    transactions, run find_unmapped, and raise RuntimeError listing every unmapped string
    if any (D-07 — env.py's single transaction guarantees nothing partial); (f) snapshot
    pre-parity per raw string (COUNT, SUM(amount) grouped by category), then backfill one
    bound-parameter UPDATE per mapping row: SET category_id = :cid WHERE category = :raw
    AND category_id IS NULL — exact string match, no TRIM/ILIKE (Pitfall 1); (g) post-parity
    per raw string (COUNT, SUM(amount) WHERE category = :raw AND category_id = :cid) and
    assert_parity against the snapshot; assert zero rows WHERE category_id IS NULL; assert
    the distinct-strings-seen count equals the CSV key count present in the DB (73/75
    drift = normalization crept in, Pitfall 1); print a per-group parity summary.
    raw_category is never read or written (D-08). All SQL uses bound parameters — never
    string interpolation, even though the CSV is trusted/reviewed input. downgrade():
    strict reverse — drop index, drop FK, drop category_id column, drop categories table
    (guarded). Commit as: feat(11-01): category schema + migration 009 with tested helpers
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt pytest backend/tests/test_category_migration.py backend/tests -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>All Task 1 tests pass; full existing suite still green; Category model importable; feat(11-01) commit made. Migration NOT run (CSV does not exist yet).</done>
  <acceptance_criteria>
    - `pytest backend/tests/test_category_migration.py -q` exits 0
    - `uv run --with-requirements backend/requirements.txt python -c "from backend.models import Category, Transaction; print(Category.__tablename__, Transaction.category_id is not None)"` prints "categories True"
    - Migration file's down_revision is "d3e4f5a6b7c8" and GROUP_META contains all 13 UI-SPEC hex values
    - Full suite `pytest backend/tests -q` exits 0 (no regression)
    - git log shows a commit whose subject starts with "feat(11-01):"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapping CSV → migration SQL | checked-in file drives UPDATE statements against live financial data |
| Alembic → live PostgreSQL | schema+data mutation of 5,728 live transaction rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | migration backfill UPDATE | high | mitigate | Bound parameters only (`:raw`, `:cid`); no string-formatted SQL anywhere in 009 (Task 2 action) |
| T-11-02 | Tampering | load_mapping CSV parsing | high | mitigate | Reject empty raw_category rows and unknown group names with ValueError before any SQL runs (Task 1 tests, Task 2 impl) |
| T-11-03 | Tampering (data integrity) | partial migration state | critical | mitigate | Single-transaction online migration (env.py) + abort-on-unknown RuntimeError + per-string parity assertion (D-07) |
| T-11-04 | Repudiation | untracked schema change | low | accept | Alembic revision history is the audit trail; per-row AuditLog for a one-time migration is overkill (single-user app) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none); any deviation requires a blocking legitimacy checkpoint |
</threat_model>

<verification>
- pytest backend/tests/test_category_migration.py -q → all green
- pytest backend/tests -q → full suite green (no regression from models.py change)
- Two commits present: test(11-01) then feat(11-01) (TDD gate order)
</verification>

<success_criteria>
- Failing tests written and committed before implementation (RED gate)
- Category model + Transaction.category_id exist matching the action spec exactly
- Migration 009 file exists with importable, tested helpers; abort/parity logic covered by unit tests
- Migration has NOT been executed against any database
</success_criteria>

<output>
Create `.planning/phases/11-category-hierarchy-schema-audit-migration/11-01-SUMMARY.md` when done
</output>
