---
phase: 11-category-hierarchy-schema-audit-migration
plan: 04
type: tdd
wave: 3
depends_on: ["11-02"]
files_modified:
  - backend/tools.py
  - backend/query.py
  - backend/tests/test_tools.py
autonomous: true
requirements: [CAT-04]
must_haves:
  truths:
    - "spending_by_category rolls up to top-level groups via the category_id join, excluding Transfer/system rows (D-09, D-12)"
    - "spending_in_category on a parent name includes all descendant categories' transactions (D-10)"
    - "list_categories returns the tree (id, name, kind, icon, effective color, children) — not a flat string list (D-10)"
    - "propose_rename_category / propose_merge_category validate against the categories table and keep working (D-11)"
    - "Rewritten tools are registered in BOTH tools.py TOOLS and query.py's FunctionTool list; propose_* stay off READ_TOOL_NAMES"
  artifacts:
    - path: "backend/tools.py"
      provides: "Hierarchy-aware category read tools + reworked propose rename/merge"
      contains: "category_id"
    - path: "backend/query.py"
      provides: "Updated FunctionTool registrations matching new tool behavior"
      contains: "list_categories"
  key_links:
    - from: "backend/tools.py"
      to: "backend/query.py"
      via: "dual registration — every rewritten tool has a matching FunctionTool.from_defaults entry"
      pattern: "FunctionTool.from_defaults"
    - from: "backend/tools.py"
      to: "categories table"
      via: "JOIN categories c ON c.id = t.category_id in rollup SQL"
      pattern: "t\\.category_id"
---

<objective>
Rewrite the agent's category tools onto the hierarchy (CAT-04, D-10): top-group
rollup with per-group children breakdown in spending_by_category (D-09), descendant-
inclusive spending_in_category, tree-shaped list_categories, and Transfer/system
exclusion from all spending totals (D-12). Rework propose_rename_category /
propose_merge_category validation onto the categories table (D-11). Honor the
dual-registration convention — the documented prior incident: a rewritten tool not
re-registered in query.py silently keeps serving stale pre-hierarchy behavior.

Purpose: these tools are the chat layer's only window onto categories; the LLM never
emits SQL (correctness-by-construction), so the hierarchy must live inside the
parameterized tools themselves.
Output: green category tests in test_tools.py; agent answers roll up correctly.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-CONTEXT.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-PATTERNS.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing hierarchy-awareness tests for category tools</name>
  <files>backend/tests/test_tools.py</files>
  <read_first>
    - backend/tests/test_tools.py (existing fixture and assertion conventions — extend this file, do not create a new one)
    - backend/tools.py lines ~160-210 and ~400 (current spending_by_category / spending_in_category / list_categories bodies and their structured-dict return shapes)
    - backend/tools.py lines ~790-850 (propose_rename_category / propose_merge_category current validation)
    - backend/models.py (Category model from plan 11-01)
  </read_first>
  <behavior>
    Fixtures: create a root group (kind expense, unique name) with a child and a grandchild
    category, plus transactions attached at each level via category_id; a Transfer-category
    transaction with is_transfer true. Cases (marked so `-k category` selects them):
    - spending_by_category: totals grouped under the ROOT group name even for grandchild-level
      transactions (3-level rollup); the returned dict keeps the {"tool", "rows", "period"} shape
      with rows as (top_name, total) AND gains a "children" key mapping top-group name to
      [(subcategory name, total)] for drill-down (D-09)
    - spending_by_category excludes transactions in system categories and is_transfer rows (D-12)
    - spending_in_category(parent name) total includes child + grandchild transactions (D-10);
      querying the child name includes only its own subtree
    - list_categories returns {"tool": "list_categories", "categories": [tree nodes]} where each
      node has id, name, kind, icon, color (effective — parent hex when own is NULL), children;
      the Transfer and Uncategorized system nodes are present (visible in lists per D-12)
    - propose_rename_category with a name not in the categories table returns the tools' standard
      error-shaped dict (match existing convention in tools.py, never an uncaught exception)
    - propose_merge_category counts affected transactions via category_id, not the legacy string
  </behavior>
  <action>
    Extend backend/tests/test_tools.py with the behavior block's cases, mirroring its
    existing db fixture/cleanup conventions. Run — new tests must fail against the current
    string-matching implementations. Commit as:
    test(11-04): add failing hierarchy-awareness tests for category tools
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt pytest backend/tests/test_tools.py -k category -q 2>&1 | tail -3</automated>
  </verify>
  <done>New category tests fail (old implementations); pre-existing test_tools.py tests untouched; RED commit made.</done>
  <acceptance_criteria>
    - pytest -k category collects >= 7 tests in test_tools.py with every NEW test failing
    - git log shows a commit whose subject starts with "test(11-04):"
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — rewrite tools onto hierarchy + dual registration</name>
  <files>backend/tools.py, backend/query.py</files>
  <read_first>
    - backend/tools.py lines ~493-517 (TOOLS dict + READ_TOOL_NAMES frozenset snapshot) and the TOOLS.update block near line ~962 (write tools added AFTER the snapshot — never move anything across that line)
    - backend/query.py lines ~117-146 (FunctionTool.from_defaults list — the second registration point)
    - backend/writes.py (reworked apply_rename_category/apply_merge_category signatures from plan 11-03 — name-based, unchanged)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-RESEARCH.md (Pattern 4 rollup SQL shape; Don't Hand-Roll dual-registration checklist)
  </read_first>
  <action>
    tools.py — rewrite keeping every tool's structured-dict return convention:
    - spending_by_category: JOIN transactions t ON t.category_id with two LEFT JOINs up the
      parent chain (p1 = c.parent, p2 = p1.parent — depth cap 3 makes two joins sufficient,
      simpler than a recursive CTE); top node id = COALESCE(p2.id, p1.id, c.id), top name via
      the matching COALESCE on names; WHERE amount < 0 AND is_transfer = false AND the resolved
      TOP-LEVEL node is not is_system (D-12); GROUP BY top, ORDER BY total DESC, LIMIT bound
      param. Second grouped query (same joins, GROUP BY top + c.id) fills the new "children"
      key {top_name: [(subcategory_name, total)]}. All SQL via text() + bound params.
    - spending_in_category(category, ...): resolve the name to a node, collect descendant ids
      with an in-Python tree walk (reuse the same tree list_categories builds — RESEARCH
      recommends the walk over a CTE at ~100 rows), filter t.category_id = ANY(:ids) via
      bound expanding param
    - list_categories: build the full tree from one SELECT over categories; return nodes with
      id, name, kind, icon, effective color (walk to parent when NULL, D-14), children; keep
      alphabetical ordering within a level
    - propose_rename_category / propose_merge_category: validate names against the categories
      table (exists; for rename, new name must not collide under the same parent; for merge,
      source must have no child categories — mirroring plan 11-03's write-layer rules so the
      proposal fails fast at propose time, not at confirm time); affected counts via
      category_id joins. These remain write tools registered via TOOLS.update AFTER the
      READ_TOOL_NAMES snapshot — do not relocate them (MCP read-only surface invariant).
    query.py — update the FunctionTool.from_defaults entries and descriptions for
    spending_by_category, spending_in_category, list_categories, propose_rename_category,
    propose_merge_category: descriptions must state that a parent category name includes all
    its descendants and that results roll up to top-level groups (D-10) — the LLM routes on
    these descriptions, so stale text means stale behavior even with correct code.
    Commit as: feat(11-04): hierarchy-aware category tools with dual registration
  </action>
  <verify>
    <automated>uv run --with-requirements backend/requirements.txt pytest backend/tests/test_tools.py -q 2>&1 | tail -3 && uv run --with-requirements backend/requirements.txt python -c "from backend.tools import TOOLS, READ_TOOL_NAMES; assert 'list_categories' in READ_TOOL_NAMES and 'spending_by_category' in READ_TOOL_NAMES; assert not any(n.startswith('propose_') for n in READ_TOOL_NAMES); assert 'propose_rename_category' in TOOLS and 'propose_merge_category' in TOOLS; print('registry OK', len(TOOLS))"</automated>
  </verify>
  <done>All test_tools.py tests green; registry invariants hold; both registration points updated; feat(11-04) commit made.</done>
  <acceptance_criteria>
    - pytest backend/tests/test_tools.py -q exits 0
    - Registry check prints "registry OK 26" (15 read + 11 write, unchanged counts)
    - grep of backend/query.py shows FunctionTool entries for list_categories, spending_by_category, spending_in_category with descendant-inclusive descriptions
    - Full suite pytest backend/tests -q exits 0
    - git log shows a commit whose subject starts with "feat(11-04):"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool args → parameterized SQL | model-chosen category names/periods enter tool queries |
| MCP read-only surface | external clients must never gain write tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Tampering | LLM-supplied category name in tool SQL | high | mitigate | text() + bound params everywhere incl. expanding id lists; LLM never emits SQL (standing constraint) |
| T-11-14 | Elevation of Privilege | propose_* leaking onto MCP read-only surface | high | mitigate | propose tools stay in TOOLS.update AFTER the READ_TOOL_NAMES snapshot; automated registry assertion in Task 2 verify |
| T-11-15 | Information Disclosure | wrong totals via stale registration (agent serves pre-hierarchy behavior) | medium | mitigate | Dual registration updated + description text updated; behavior pinned by test_tools.py rollup tests |
| T-11-16 | Tampering (integrity) | Transfer money counted as spending | medium | mitigate | is_transfer AND system-category exclusion in rollup SQL, pinned by test (D-12) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- pytest backend/tests/test_tools.py -q → green; full suite green
- Registry python assertion passes (READ_TOOL_NAMES excludes propose_*, TOOLS has 26 entries)
- TDD gate order: test(11-04) commit precedes feat(11-04)
</verification>

<success_criteria>
- Chat/agent category answers roll up to top groups and include descendants for parent queries (D-09, D-10)
- Transfer excluded from every spending total (D-12)
- propose rename/merge keep working, hierarchy-backed (D-11)
- No new agent tools added; registration counts unchanged (Pitfall 4)
</success_criteria>

<output>
Create `.planning/phases/11-category-hierarchy-schema-audit-migration/11-04-SUMMARY.md` when done
</output>
