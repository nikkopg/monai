---
phase: 11-category-hierarchy-schema-audit-migration
plan: 06
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - ui/app/styles.ts
  - ui/app/settings/CategoryManager.tsx
  - ui/app/settings/page.tsx
  - ui/app/cashflow/page.tsx
  - ui/app/cashflow/CategoryManager.tsx
autonomous: true
requirements: [CAT-02, CAT-04]
must_haves:
  truths:
    - "Settings shows an expandable category tree — top groups collapsed by default, 20px indent per level, inline add/edit/delete per node (D-16)"
    - "Deleting an in-use category opens the reassign flow showing transaction and subcategory counts; system rows show a disabled delete with the lock note (CAT-02, D-04)"
    - "Category colors come from the closed 13-swatch palette; subcategories inherit parent color by default with an override picker limited to the same set (D-14)"
    - "CategoryManager no longer lives on the Cashflow page (D-16 move)"
  artifacts:
    - path: "ui/app/styles.ts"
      provides: "categoryPalette — the 13 named swatches"
      contains: "categoryPalette"
    - path: "ui/app/settings/CategoryManager.tsx"
      provides: "Recursive tree manager with CRUD + delete-guard flows"
      min_lines: 200
  key_links:
    - from: "ui/app/settings/CategoryManager.tsx"
      to: "backend/main.py GET /categories"
      via: "fetch of the tree (id, name, kind, effective_color, icon, is_system, tx_count, children)"
      pattern: "/api/categories"
    - from: "ui/app/settings/page.tsx"
      to: "ui/app/settings/CategoryManager.tsx"
      via: "Categories card mounts the manager"
      pattern: "CategoryManager"
---

<objective>
Build the Settings > Categories expandable tree manager (D-16, CAT-02 UI): move
CategoryManager from Cashflow to Settings, replace the flat rename/merge table with a
recursive tree (no tree library — ~100 rows, plain useState), wire the block-or-reassign
delete flow to the two-count 422 payload from plan 11-03, and add the 13-swatch
category palette to the token layer (D-14). Implements the approved 11-UI-SPEC.md
Component 1 contract exactly — spacing, type, color, and copy are all specified there.

Purpose: this is the user's management surface for the new hierarchy (ROADMAP
criterion 3).
Output: working tree manager in Settings; Cashflow page no longer hosts it.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md
@.planning/phases/11-category-hierarchy-schema-audit-migration/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add categoryPalette to the token layer</name>
  <files>ui/app/styles.ts</files>
  <read_first>
    - ui/app/styles.ts (tokens object shape, chartColors, chipGreenBg opacity precedent — add alongside, change nothing existing)
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md (Category Color Palette table — source of truth for names and hexes)
  </read_first>
  <action>
    Export a `categoryPalette` constant from ui/app/styles.ts: an ordered array of
    { name, hex } for the 13 swatches exactly as the UI-SPEC table lists them —
    Food & Drinks #d8b26a, Shopping #b5503f, Housing #5a8f73, Transportation #8fae9c,
    Vehicle #a8674a, Life & Entertainment #8a6a8f, Communication / PC #6b7f8f,
    Financial Expenses #6f6857, Investments #c9973f, Income #23543c, Others #c8c1b5,
    Transfer #a49c8c, Uncategorized #8b8474. Where a hex already exists as a token
    (gold, terracotta, sage, sageLight, muted3, greenDark, muted2, muted, 6th chartColors
    entry) reference the token instead of duplicating the literal. No other styles.ts
    changes (D-14: one palette, not two).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit</automated>
  </verify>
  <done>categoryPalette exported with 13 entries; tsc clean; no existing token modified.</done>
  <acceptance_criteria>
    - ui/app/styles.ts exports categoryPalette with exactly 13 entries matching the UI-SPEC hexes
    - `cd ui && npx tsc --noEmit` exits 0
    - git diff of styles.ts shows only the addition (no existing token/value changed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Recursive tree manager component in Settings</name>
  <files>ui/app/settings/CategoryManager.tsx</files>
  <read_first>
    - .planning/phases/11-category-hierarchy-schema-audit-migration/11-UI-SPEC.md (Component 1 — full structure, states, and Copywriting Contract; follow verbatim)
    - ui/app/cashflow/CategoryManager.tsx (existing fetch/error/extractDetail/ConfirmDialog patterns and rename/merge flows to carry forward)
    - ui/app/cashflow/AccountManager.tsx (delete + 422-reassign flow at ~91-140 — the deleteFlow state machine to copy, extended for two counts)
    - ui/app/settings/page.tsx (cardTitle pattern, card layout the component will sit in)
    - backend/schemas.py (CategoryNode field names the fetch consumes)
  </read_first>
  <action>
    Create ui/app/settings/CategoryManager.tsx implementing UI-SPEC Component 1 (D-16):
    recursive TreeNode rendering from GET /api/categories; top groups collapsed by default
    with chevron expand/collapse; indent 20px per level (0/20/40); 36px min row height;
    per-row: chevron → emoji chip (circular, background = node's effective_color at 16%
    opacity matching the chipGreenBg convention, emoji unfiltered on top) → name (13px/400)
    → tx_count badge (12px muted pill) → hover-revealed inline actions Add subcategory /
    Edit / Delete. Add-subcategory hidden on depth-3 rows (CAT-01). Inline add/edit swaps
    the name cell for input + swatch picker (13 circular buttons from categoryPalette,
    default = inherit parent) + emoji text field, Save/Cancel inline. Delete flow: DELETE
    /api/categories/{id}; on 422 read detail.affected_count and detail.child_count and open
    the reassign ConfirmDialog with a destination select excluding the node and its
    descendants; confirm re-calls DELETE with ?reassign_to=. Keep rename (inline edit) and
    merge flows working against the existing /api/categories/rename and /api/categories/merge
    endpoints (D-11). System rows (is_system): delete disabled/grayed with the exact copy
    "System category — can't be deleted."; edit allows color/icon only. All copy strings from
    the UI-SPEC Copywriting Contract; error pattern via extractDetail exactly as today.
    Styling only from ui/app/styles.ts tokens + categoryPalette (inline CSSProperties, no
    new styling approach). Type the fetch payload to CategoryNode (id, name, parent_id,
    kind, color, effective_color, icon, is_system, tx_count, children).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit</automated>
  </verify>
  <done>Component compiles; implements tree, inline CRUD, two-count reassign flow, system locks, palette picker per UI-SPEC.</done>
  <acceptance_criteria>
    - tsc --noEmit exits 0
    - Component contains the literal copy "System category — can't be deleted." and "No categories yet."
    - Delete flow reads both affected_count and child_count from the 422 detail
    - Reassign select excludes the deleted node's own subtree (descendant-id set computed client-side)
    - No import of any tree/icon library; imports styles from ../styles (tokens + categoryPalette)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mount in Settings, remove from Cashflow</name>
  <files>ui/app/settings/page.tsx, ui/app/cashflow/page.tsx, ui/app/cashflow/CategoryManager.tsx</files>
  <read_first>
    - ui/app/settings/page.tsx (card/section structure — add a Categories card in the same pattern)
    - ui/app/cashflow/page.tsx (where CategoryManager is imported/rendered today — remove cleanly)
  </read_first>
  <action>
    settings/page.tsx: add a "Categories" card (existing cardTitle 15px/600 style) mounting
    the new CategoryManager. cashflow/page.tsx: remove the CategoryManager import and its
    render site (leave everything else untouched). Delete ui/app/cashflow/CategoryManager.tsx
    (fully superseded; git history preserves it). Rebuild the stack for live verification:
    docker compose up -d --build (committed code is not the running container — prior-phase
    lesson).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit && test ! -f app/cashflow/CategoryManager.tsx && ! grep -rn "cashflow/CategoryManager" app --include=*.tsx && echo "move complete"</automated>
  </verify>
  <done>Settings hosts the tree; Cashflow references removed; old file deleted; tsc clean.</done>
  <acceptance_criteria>
    - Verify command prints "move complete"
    - settings/page.tsx imports and renders CategoryManager inside a Categories card
    - Live check after docker compose up -d --build: Settings page shows the collapsed tree with the ~11 groups + system rows (human-check during phase UAT: expand/collapse, add/edit/delete, reassign flow, system lock)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/categories proxy | user input (names, emoji) posted to mutating endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-20 | Spoofing/XSS | category name/emoji rendered in tree | medium | mitigate | React JSX text rendering only (no dangerouslySetInnerHTML); names rendered as text nodes |
| T-11-21 | Tampering (integrity) | destructive delete without user intent | medium | mitigate | ConfirmDialog on every delete/merge; reassign flow surfaces counts before action (UI-SPEC copy) |
| T-11-22 | Elevation of Privilege | mutating calls without auth | high | mitigate | Calls go through the existing server-side /api proxy that injects the API key — same as AccountManager; no new auth surface |
| T-11-SC | Tampering | npm/pip/cargo installs | low | mitigate | No packages installed in this plan (no tree/icon lib — UI-SPEC mandate) |
</threat_model>

<verification>
- cd ui && npx tsc --noEmit → clean
- No references to the old cashflow CategoryManager remain
- Live (post docker compose up -d --build): tree renders, CRUD + reassign flows work — human-check at phase UAT (/gsd-verify-work)
</verification>

<success_criteria>
- Settings > Categories manages the hierarchy per UI-SPEC Component 1 (ROADMAP criterion 3)
- Delete guard UI distinguishes transactions vs subcategories (Pitfall 3)
- One closed 13-swatch palette; inheritance default (D-14)
</success_criteria>

<output>
Create `.planning/phases/11-category-hierarchy-schema-audit-migration/11-06-SUMMARY.md` when done
</output>
