---
phase: 04-cashflow-dashboard-crud
plan: 07
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - ui/app/cashflow/TransactionModal.tsx
  - ui/e2e/cashflow-crud.spec.ts
autonomous: true
gap_closure: true
requirements: [CASH-04, CASH-06]
user_setup: []

must_haves:
  truths:
    - "The Category field in the Add/Edit Transaction modal is a select populated from GET /categories — picking an existing category is the default path, so 'jajan' vs 'Jajan' case-variant duplicates can no longer happen by accident"
    - "Picking an existing category submits exactly the stored name (byte-identical), and picking '(no category)' still submits null"
    - "A user can still deliberately create a new category via an explicit '+ New category…' affordance that reveals a text input"
    - "Edit mode pre-selects the transaction's current category, even if the categories fetch is slow or that name is missing from the fetched list"
  artifacts:
    - "ui/app/cashflow/TransactionModal.tsx — category select sourced from /api/categories, with (no category) and + New category… options"
    - "ui/e2e/cashflow-crud.spec.ts — updated create/edit specs driving the select, plus a new-category-affordance spec"
  key_links:
    - "TransactionModal fetches GET /api/categories on mount through the Next.js proxy; response shape {\"categories\": [name, ...]} per backend/main.py:316-326"
    - "Submit body keeps the existing category-or-null contract (TransactionModal.tsx:86) — no backend or schema change"
---

<objective>
Close UAT gap 2 (minor, test 10): category is a free-text input in the Add/Edit Transaction modal (ui/app/cashflow/TransactionModal.tsx:183-191), so typing a spelling/case variant silently creates a duplicate category — the user observed 'jajan' and 'Jajan' fragmenting their spending. Per the USER-CHOSEN fix direction recorded in 04-UAT.md (user_fix_direction), replace the free-text field with a select populated from GET /categories so users pick an existing category by default, keeping a deliberate "+ New category…" affordance for genuinely new categories. No data migration: existing duplicates are consolidated with the already-shipped category merge feature (Plan 05 / D-09).

Purpose: restores the UAT truth "The same category is not duplicated by a typo or case difference" for all future entry, while preserving the ability to introduce new categories on purpose.

Output: a select-based category field in the shared create/edit TransactionModal (D-10 component stays single) and updated Playwright coverage in ui/e2e/cashflow-crud.spec.ts.
</objective>

<execution_context>
@/home/nikko/nikko/projects/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/nikko/nikko/projects/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cashflow-dashboard-crud/04-UAT.md
@.planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md

# Source files this plan modifies / mirrors
@ui/app/cashflow/TransactionModal.tsx
@ui/e2e/cashflow-crud.spec.ts
@ui/app/styles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the free-text category field in TransactionModal with a select sourced from GET /categories</name>
  <files>ui/app/cashflow/TransactionModal.tsx</files>
  <read_first>
    - ui/app/cashflow/TransactionModal.tsx (whole file, ~273 lines: category state at line 60, submit body category-or-null at line 86, the category text input at 183-191, the Account select at 201-214 — the in-file styling analog for the new select)
    - backend/main.py:316-326 (GET /categories — open read, returns {"categories": [name, ...]}; the enumeration source, same one CategoryManager consumes)
    - ui/app/cashflow/CategoryManager.tsx (existing /api/categories fetch pattern through the proxy, for consistency)
  </read_first>
  <action>
Rework the Category field in ui/app/cashflow/TransactionModal.tsx per the user-chosen fix direction (04-UAT.md gap 2 user_fix_direction; component stays the single shared create/edit modal per D-10):

1. On mount (useEffect, runs each time the modal opens since it is conditionally rendered from page.tsx:487), fetch `/api/categories` through the proxy and store the returned `categories` string array in state. On fetch failure, degrade to an empty list — the field must still work (options 3 and 4 below remain usable).
2. Replace the category text input (lines 183-191) with a `<select>` styled with the shared `input` style object, exactly like the Account select at lines 201-214. Options, in order:
   - "(no category)" with value "" — preserves the existing category-or-null submit contract at line 86 (empty string submits null; no backend change).
   - One option per fetched category name, value = the exact name (byte-identical to what is stored, so selecting can never introduce a case variant).
   - A final "+ New category…" option with a sentinel value that cannot collide with a real category name (e.g. a non-printable-prefixed constant defined at module scope).
3. When the sentinel option is selected, reveal a text input (shared `input` style) directly beneath the select for the deliberate new-category name. The submitted category is then the trimmed typed value, or null if left blank. This is the explicit add-new affordance the user asked to keep — selection is the default, typing is a deliberate act.
4. Edit-mode pre-fill: initialize the selection from editingTx.category (line 60 currently seeds free text). If that name is non-null but absent from the fetched list (fetch in flight, fetch failed, or list changed), include it as an extra option so opening the edit modal never blanks or mutates the transaction's current category.
5. Submit logic (handleSubmit, lines 83-96): compute the category value from select-state — sentinel → trimmed new-name-or-null, "" → null, otherwise the selected name — and keep the rest of the body construction untouched. No prop changes, so page.tsx needs no edit.

Keep the label "Category" and the existing grid cell position so the modal layout is unchanged apart from the control type (UI-SPEC layout contract from Plan 05).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The Category field renders a `<select>` whose options come from a `/api/categories` fetch (grep confirms both the fetch URL and a category select in TransactionModal.tsx).
    - The select includes a "(no category)" empty-value option and a "+ New category…" sentinel option that reveals a text input when chosen (grep confirms both option labels).
    - Edit mode seeds the selection from editingTx.category and injects it as an option when it is not in the fetched list.
    - `npx tsc --noEmit` reports no errors in TransactionModal.tsx, and no props changed (page.tsx untouched).
  </acceptance_criteria>
  <done>Category selection from existing names is the default path in both create and edit mode; new categories require the deliberate "+ New category…" affordance; null-category and edit pre-fill behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Playwright CRUD spec for the select-based category field</name>
  <files>ui/e2e/cashflow-crud.spec.ts</files>
  <read_first>
    - ui/e2e/cashflow-crud.spec.ts (mockDashboard at ~L40-84 — already mocks GET **/api/categories to {"categories": ["Food & Drinks", "Transport"]}, which will serve the modal's new fetch; the create test at ~L87-126 and edit test at ~L128+ that fill the modal)
    - ui/app/cashflow/TransactionModal.tsx (Task 1 result — the select structure, option labels, and sentinel behavior under test)
  </read_first>
  <action>
Update ui/e2e/cashflow-crud.spec.ts to exercise the new control using the existing route-interception mocking pattern (backend never required, spec stays deterministic):

1. Create test (~L87): after opening the modal, assert the category `<select>` is populated with the mocked names ("Food & Drinks", "Transport") — this proves the modal consumed GET /api/categories. Select "Transport" via selectOption, submit, and strengthen the existing POST interception to assert `route.request().postDataJSON().category === "Transport"` — proving the byte-identical stored name is submitted, which is the mechanism that prevents case-variant duplicates.
2. Edit test: the tx fixture row being edited has a category — assert the select's value equals that fixture category when the modal opens (pre-fill contract), then save and keep the existing PUT assertion.
3. New test in the same describe block: open the create modal, choose the "+ New category…" option, assert a text input is revealed, type a fresh name, submit, and assert the POSTed body carries that typed name — locking the deliberate add-new affordance.
4. New test: choose "(no category)" and assert the POSTed body has `category: null` — locking the null contract.
5. Remove/replace any interaction that still targets the old free-text category input so the suite matches the new control (the amount placeholder "-25000" interactions are unrelated — keep them).
  </action>
  <verify>
    <automated>cd ui && npm run e2e -- cashflow-crud.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec asserts the category select is populated from the mocked GET /api/categories payload.
    - POST body assertions cover all three submission paths: existing name ("Transport"), deliberate new name via the sentinel affordance, and null via "(no category)".
    - The edit test asserts pre-selection of the fixture transaction's category.
    - `cd ui && npm run e2e -- cashflow-crud.spec.ts` green; `cd ui && npx tsc --noEmit -p tsconfig.json` reports no errors in the spec.
  </acceptance_criteria>
  <done>cashflow-crud.spec.ts deterministically covers select-population, existing-name submission, deliberate new-category creation, null category, and edit pre-fill — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser modal → Next proxy → POST/PUT /transactions | The category value (selected or deliberately typed) crosses into the DB write path; the proxy injects the API key server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering / Input Validation | category value on transaction writes | low | mitigate | Default path constrains input to names already stored (select); the deliberate new-name path is trimmed client-side and, as before, persisted via Pydantic DTO + SQLAlchemy bound parameters (Plan 03 endpoints, unchanged) — no string interpolation into SQL |
| T-04-23 | Elevation of Privilege | modal write calls | low | accept | Unchanged from Plan 05 (T-04-16): writes go through /api/[...proxy] with server-side MONAI_API_KEY injection; this plan alters only which control produces the category string |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO new packages — pure edits to an existing component and spec |
</threat_model>

<verification>
- `cd ui && npx tsc --noEmit -p tsconfig.json` reports no errors in TransactionModal.tsx or cashflow-crud.spec.ts.
- `cd ui && npm run e2e -- cashflow-crud.spec.ts` green (select population, existing-name/new-name/null submission paths, edit pre-fill).
- Full e2e regression: `cd ui && npm run e2e` green (smoke + settings + dashboard + crud).
- Manual (UAT truth): open Add transaction — Category is a dropdown listing existing categories; picking "Jajan" cannot create "jajan"; "+ New category…" still allows a deliberate new name; consolidate the pre-existing jajan/Jajan pair via CategoryManager merge (already shipped, D-09 — no migration in this plan).
</verification>

<success_criteria>
- UAT gap 2 truth restored: accidental case/typo category duplicates are structurally prevented at the entry point — selection from GET /categories is the default, new names require a deliberate affordance.
- The shared create/edit modal contract (D-10), the category-or-null submit shape, and all Plan 05 CRUD flows remain intact (full e2e green).
</success_criteria>

<output>
Create `.planning/phases/04-cashflow-dashboard-crud/04-07-SUMMARY.md` when done
</output>
