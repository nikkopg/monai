---
phase: 04-cashflow-dashboard-crud
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/app/styles.ts
  - ui/app/cashflow/charts/CategoryDonut.tsx
  - ui/app/cashflow/charts/IncomeExpenseBar.tsx
  - ui/app/cashflow/charts/TrendChart.tsx
  - ui/app/cashflow/page.tsx
  - ui/e2e/cashflow-dashboard.spec.ts
autonomous: true
requirements: [CASH-01, CASH-02, CASH-03]
user_setup: []

must_haves:
  truths:
    - "The /cashflow page shows total income, total expenses, and net for the selected period, plus per-account current balance and period net"
    - "The page renders a spending-by-category donut, an income-vs-expense bar, and a >=6-month trend chart, all from GET /cashflow/summary"
    - "Changing the period selector refetches the summary and updates every figure and chart"
  artifacts:
    - "recharts added to ui/package.json (D-07)"
    - "ui/app/cashflow/charts/CategoryDonut.tsx, IncomeExpenseBar.tsx, TrendChart.tsx"
    - "ui/app/styles.ts extended with dangerBtn and chartColors"
    - "ui/app/cashflow/page.tsx grown with a summary row, per-account balances, charts row, trend row, and a period selector"
  key_links:
    - "page.tsx fetches /api/cashflow/summary through the existing proxy and passes shaped arrays directly to the Recharts components"
    - "each chart is wrapped in a div with explicit height 280 before ResponsiveContainer (Pitfall 3)"
    - "income series uses Success #4ade80, expense series uses Destructive #f87171 (UI-SPEC color contract)"
---

<objective>
Grow the interim `/cashflow` page into the read-side dashboard: install Recharts (D-07), add the three chart components (spending-by-category donut, income-vs-expense bar, >=6-month trend), extend `ui/app/styles.ts` with the two new shared constants (`dangerBtn`, `chartColors`), and lay out the dashboard — a period selector, the totals summary row (income/expense/net), the per-account dual-balance row (current_balance + period_net, D-04), and the two chart rows — all populated from the single `GET /cashflow/summary` payload (D-08) fetched through the existing `/api/[...proxy]`.

Purpose: This is the first user-visible vertical slice — after it, a user can open `/cashflow` and understand their spending/income at a glance from real data (CASH-01/02/03). It is the read half of the phase; the write CRUD half is Plan 05, which builds on the same page and styles.

Output: three Recharts components, two new style constants, a grown `page.tsx` with the dashboard sections, and a Playwright spec proving the dashboard renders figures + charts and that the period selector refetches.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cashflow-dashboard-crud/04-CONTEXT.md
@.planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md
@.planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md
@.planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md

# Source files this plan modifies / mirrors
@ui/app/cashflow/page.tsx
@ui/app/styles.ts
@ui/app/settings/page.tsx
@ui/app/api/[...proxy]/route.ts
@ui/e2e/smoke.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and add dangerBtn + chartColors to ui/app/styles.ts</name>
  <files>ui/package.json, ui/package-lock.json, ui/app/styles.ts</files>
  <read_first>
    - ui/package.json (current dependency set — verify React 18.3.1 pin before installing)
    - ui/app/styles.ts (existing btn L34-43 constant to spread for dangerBtn; existing card/input/label constants)
    - .planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md (§ Standard Stack / § Package Legitimacy Audit — recharts 3.9.2 APPROVED, official repo, no postinstall; § Installation)
    - .planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md (§ Color — chartColors array; § dangerBtn constant)
    - .planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md (§ Inline style constants — add exactly two constants, do not redefine existing four)
  </read_first>
  <action>
Install Recharts pinned to `^3.9.2` in the `ui` workspace: `cd ui && npm install recharts@^3.9.2`. This package was vetted APPROVED in RESEARCH.md's Package Legitimacy Audit (official `recharts/recharts` repo, ~51.5M weekly downloads, no postinstall script) — it is the phase's one locked new dependency (D-07). Do not install any other new package.

Extend `ui/app/styles.ts` with exactly two new exported constants (do NOT redefine `card`/`input`/`btn`/`label`): `export const dangerBtn: React.CSSProperties = { ...btn, background: "#f87171" };` (Destructive color, for delete/merge-confirm buttons — consumed by Plan 05) and `export const chartColors = ["#3b82f6", "#f87171", "#4ade80", "#fbbf24", "#a78bfa", "#22d3ee"];` (the category-donut palette from the UI-SPEC Color section). Keep them at module scope alongside the existing constants.
  </action>
  <verify>
    <automated>cd ui && node -e "const p=require('./package.json'); if(!p.dependencies.recharts) throw new Error('recharts not in dependencies'); console.log('recharts', p.dependencies.recharts)" && grep -c "export const dangerBtn\|export const chartColors" app/styles.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recharts` appears in `ui/package.json` dependencies at `^3.9.2` and `ui/package-lock.json` is updated.
    - `grep -c "export const dangerBtn" ui/app/styles.ts` returns 1 and `grep -c "export const chartColors" ui/app/styles.ts` returns 1.
    - `dangerBtn` spreads the existing `btn` (grep confirms `...btn` in its definition) — no duplicated button base.
    - `card`/`input`/`btn`/`label` are NOT redefined (still exactly one definition each).
  </acceptance_criteria>
  <done>recharts@^3.9.2 installed; styles.ts has dangerBtn (spread over btn) and chartColors; existing constants untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build the three Recharts chart components</name>
  <files>ui/app/cashflow/charts/CategoryDonut.tsx, ui/app/cashflow/charts/IncomeExpenseBar.tsx, ui/app/cashflow/charts/TrendChart.tsx</files>
  <read_first>
    - .planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md (§ Code Examples — verified Recharts CategoryDonut pattern; § Pitfall 3 explicit-height wrapper)
    - .planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md (§ charts/*.tsx — no in-repo analog, use RESEARCH example; income=#4ade80, expense=#f87171)
    - .planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md (§ Color — income Success / expense Destructive, chartColors for donut slices; § Layout & Component Notes → Charts shared conventions — CartesianGrid #2a2e37 strokeDasharray 3 3, axis ticks #9aa0a6 12px, height 280, no gradients/shadows)
    - ui/app/styles.ts (chartColors from Task 1)
  </read_first>
  <action>
Create three files under `ui/app/cashflow/charts/`, each a `"use client"`-free child component (the parent page is already `"use client"`), importing from `recharts` and `chartColors` from `../../styles`. Every chart's outer element is `<div style={{ width: "100%", height: 280 }}>` wrapping a `<ResponsiveContainer>` — the explicit 280px height is load-bearing (Pitfall 3: ResponsiveContainer renders blank with no resolvable parent height).

`CategoryDonut.tsx`: props `{ data: { category: string; total: number }[] }`. Render `PieChart` > `Pie` (dataKey="total", nameKey="category", innerRadius 60, outerRadius 100, paddingAngle 2) with one `<Cell fill={chartColors[i % chartColors.length]}>` per slice (cycle the palette for >6 categories), plus a `<Tooltip>` whose formatter runs `Intl.NumberFormat` grouping.

`IncomeExpenseBar.tsx`: props `{ data: { income: number; expense: number }[] }` (or a single-period summary shaped for a two-bar chart). Render a `BarChart` with two `<Bar>` series: income filled `#4ade80` (Success), expense filled `#f87171` (Destructive) — NOT the accent blue (UI-SPEC Color contract). Include `<CartesianGrid stroke="#2a2e37" strokeDasharray="3 3">`, `<XAxis>`/`<YAxis>` with `tick={{ fill: "#9aa0a6", fontSize: 12 }}`, and a `<Tooltip>`.

`TrendChart.tsx`: props `{ data: { month: string; income: number; expense: number }[] }`. Render a `BarChart` (grouped) or `LineChart` with two series — income `#4ade80`, expense `#f87171` — over the `month` XAxis; do NOT plot net as a third series (UI-SPEC: net is read from the totals row, avoid a 3rd chart color). Same CartesianGrid/axis-tick/tooltip conventions. This chart must display all months returned (>=6, CASH-02).

No gradients, drop-shadows, or 3D — flat fills only (UI-SPEC).
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "charts/(CategoryDonut|IncomeExpenseBar|TrendChart)" || echo "no type errors in chart components"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `ui/app/cashflow/charts/` and typecheck (no TS errors referencing them in `npx tsc --noEmit`).
    - Each chart wraps its `ResponsiveContainer` in a `div` with explicit `height: 280` — `grep -c "height: 280" ui/app/cashflow/charts/*.tsx` returns >= 3 (Pitfall 3).
    - IncomeExpenseBar and TrendChart use `#4ade80` for income and `#f87171` for expense (grep confirms both hex values present in each; the accent `#3b82f6` is NOT used for a bar/line series in these two files).
    - `TrendChart` renders a series over a `month` key (grep confirms `dataKey="month"` or `"month"` axis reference).
  </acceptance_criteria>
  <done>CategoryDonut, IncomeExpenseBar, TrendChart exist, typecheck, use the explicit-height wrapper, and follow the income=green/expense=red color contract with flat fills.</done>
</task>

<task type="auto">
  <name>Task 3: Grow page.tsx with the dashboard (period selector, totals, per-account balances, charts) and add a Playwright spec</name>
  <files>ui/app/cashflow/page.tsx, ui/e2e/cashflow-dashboard.spec.ts</files>
  <read_first>
    - ui/app/cashflow/page.tsx (existing 247-line page: imports L1-5, loadTxs refetch L51-57, error-surfacing L86-95, fmt/color-by-sign L103-104 L229-238, existing recent-transactions table L204-244 — grow, don't rewrite)
    - ui/app/settings/page.tsx (SaveState per-section pattern for reference; GET-on-load useEffect)
    - ui/app/cashflow/charts/CategoryDonut.tsx, IncomeExpenseBar.tsx, TrendChart.tsx (Task 2 — the components to mount)
    - ui/app/api/[...proxy]/route.ts (proxy forwards GET untouched — no change needed; fetch path is /api/cashflow/summary)
    - .planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md (§ Layout & Component Notes → Page structure items 1-5: heading + period selector, summary row 3-col grid, per-account balances row, charts row 2-col grid, trend full-width row; § Typography → dashboard figure = Heading 20px/600 in semantic color; § Copywriting → period selector default "This month", empty/error states)
    - .planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md (§ ui/app/cashflow/page.tsx — refetch-after-write, error-surfacing, SaveState, fmt, datetime helper patterns)
    - ui/e2e/smoke.spec.ts (Playwright spec shape/selectors convention to mirror)
  </read_first>
  <action>
Grow `ui/app/cashflow/page.tsx` (keep it `"use client"`; extend the existing file, preserving the recent-transactions table and entry form for now — Plan 05 refactors the form into a modal). Add above the existing content, in UI-SPEC page-structure order:

1. Page heading "Cashflow" (Display 28px/600) + a period selector: a row of pill buttons for `["This week","This month","Last month","This year"]` mapping to resolve_period names (`this_week`,`this_month`,`last_month`,`this_year`); default `this_month` ("This month"). Active pill uses accent `#3b82f6`; selecting one sets state and triggers a summary refetch.
2. A `loadSummary()` async function that fetches `/api/cashflow/summary?period=${period}` through the existing proxy, sets summary state on ok, and on failure surfaces the UI-SPEC copy "Couldn't load the dashboard — check the backend is running and reload the page." Call it in a `useEffect` on mount AND whenever `period` changes.
3. Summary row — CSS grid `gridTemplateColumns: "repeat(3, 1fr)"`, `gap: 16`: three `card`s (Total Income, Total Expenses, Net). Each: a muted Label caption above a Heading-styled (20px/600) figure; income green `#4ade80`, expense red `#f87171`, net green if >=0 else red. Format figures with the existing `fmt`/`Intl.NumberFormat` helper (no currency prefix — UI-SPEC currency note).
4. Per-account balances row — one `card` with a compact list/table: account name, `current_balance` (Body/600 tabular-nums), `period_net` (Body/600 tabular-nums, green/red by sign). Captions "Balance" and "This period" per D-04.
5. Charts row — grid `repeat(2,1fr)` `gap: 16`: `CategoryDonut` (data = `summary.by_category` mapped to `{category,total}`) and `IncomeExpenseBar` (data from `summary.totals`). Trend row — full-width `card` header "6-Month Trend" containing `TrendChart` (data = `summary.trend`).

Handle the empty/zero-activity state with the UI-SPEC copy "Nothing here for this period." Reuse the existing `fmt` and color-by-sign helpers; do not introduce a new formatting approach.

Also create `ui/e2e/cashflow-dashboard.spec.ts` mirroring `smoke.spec.ts`: navigate to `/cashflow`, assert the page heading and the three summary figure captions render, assert at least one chart SVG (`svg` inside the charts row) appears, and assert clicking a different period pill issues a new `/api/cashflow/summary` request (route interception or waiting for the figures to update). Gate on the dev server the existing e2e config already starts.
  </action>
  <verify>
    <automated>cd ui && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "cashflow/page.tsx" || echo "page.tsx typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` renders a period selector with the four periods, a 3-card summary row, a per-account balances card (current_balance + period_net per D-04), a 2-chart row, and a full-width trend row.
    - `loadSummary()` fetches `/api/cashflow/summary?period=` and re-runs on period change — grep confirms `/api/cashflow/summary` and a `useEffect` dependency on the period state.
    - The three chart components are imported and mounted (grep confirms `CategoryDonut`, `IncomeExpenseBar`, `TrendChart` usages in page.tsx).
    - `ui/e2e/cashflow-dashboard.spec.ts` exists and asserts the heading, the summary captions, a chart `svg`, and a period-change refetch.
    - `npx tsc --noEmit` reports no errors in `cashflow/page.tsx`.
  </acceptance_criteria>
  <done>The /cashflow page shows totals, per-account dual balances, donut, income/expense bar, and a >=6-month trend from GET /cashflow/summary, with a working period selector; a Playwright spec covers the dashboard render + period refetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next proxy → backend | Dashboard read requests carry the server-injected API key; no user-supplied secrets in the browser |
| npm registry → build | Recharts is the one new dependency pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | npm install recharts | high | mitigate | Recharts vetted APPROVED in RESEARCH.md Package Legitimacy Audit (official recharts/recharts repo, ~51.5M weekly downloads, no postinstall script verified) — pinned `^3.9.2`; no other new package installed |
| T-04-14 | Information Disclosure | dashboard figures rendered client-side | low | accept | Single-user self-hosted app; all data belongs to the one user; API key injected server-side by the existing proxy, never exposed to the browser |
| T-04-15 | Denial of Service | ResponsiveContainer blank-render / infinite resize | low | mitigate | Explicit 280px height wrapper (Pitfall 3) prevents the zero-height blank-render failure mode |
| T-04-SC | Tampering | npm install (recharts) | high | mitigate | Only recharts (APPROVED, not [ASSUMED]/[SUS]) installed; audit table present in RESEARCH.md — no blocking human checkpoint required |
</threat_model>

<verification>
- `cd ui && npx tsc --noEmit -p tsconfig.json` reports no errors in the new/grown files.
- `cd ui && npm run build` succeeds (Recharts bundles, page compiles).
- `cd ui && npm run e2e -- cashflow-dashboard.spec.ts` passes (dashboard renders + period refetch), assuming backend + dev server are up per the existing e2e config.
- Manual: `/cashflow` shows totals, per-account dual balances, donut, income/expense bar, and a >=6-bar trend from seeded data.
</verification>

<success_criteria>
- recharts installed; styles.ts extended with dangerBtn + chartColors (existing constants intact).
- Three chart components with explicit-height wrappers and the income=green/expense=red contract.
- page.tsx dashboard: period selector, totals row, per-account dual-balance row, charts row, >=6-month trend — all from GET /cashflow/summary.
- cashflow-dashboard.spec.ts green.
</success_criteria>

<output>
Create `.planning/phases/04-cashflow-dashboard-crud/04-04-SUMMARY.md` when done
</output>
