---
phase: 04-cashflow-dashboard-crud
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - backend/main.py
  - backend/tests/test_transaction_crud.py
  - backend/tests/test_account_crud.py
  - backend/tests/test_category_management.py
autonomous: true
requirements: [CASH-01, CASH-02, CASH-03, CASH-04, CASH-05, CASH-06, CASH-07]
user_setup: []

must_haves:
  truths:
    - "GET /cashflow/summary returns totals, per-account balances, spending-by-category, and a >=6-month trend in one payload (D-08)"
    - "A user (via the UI proxy) can create, edit, and delete a transaction through direct auth-protected REST endpoints (D-01)"
    - "A user can create, edit, and delete accounts; deleting an account with transactions returns 422-with-count unless reassign_to is supplied (D-05)"
    - "A user can rename a category (all matching transactions remapped) and merge one category into another; the affected count is returned"
    - "Every direct write endpoint carries require_api_key, calls a backend/writes.py apply_* helper, writes an AuditLog row, and calls reset_engine()"
  artifacts:
    - "backend/main.py: GET /cashflow/summary; PUT and DELETE /transactions/{id}; POST/PUT/DELETE /accounts; GET /categories (list); GET /categories/{name}/affected-count; POST /categories/rename; POST /categories/merge"
    - "backend/tests/test_transaction_crud.py, test_account_crud.py, test_category_management.py"
  key_links:
    - "GET /cashflow/summary resolves the period once via resolve_period and composes existing tools.py aggregations + Plan 02's monthly_trend/account_balances"
    - "Every write endpoint calls a backend/writes.py apply_* helper (never inline ORM mutations) then db.commit() then reset_engine()"
    - "DELETE /accounts/{id} does reassign-then-delete via apply_delete_account(reassign_to=...): 422+affected_count when transactions exist and reassign_to is absent (D-06); the reassignment is audited inside the helper (not an inline bulk update)"
    - "GET /categories returns the list of distinct category names (reusing list_categories logic) so the UI can enumerate categories deterministically"
---

<objective>
Wire the new backend REST surface on top of the shared write helpers (Plan 01) and read aggregations + schemas (Plan 02): the single aggregate dashboard endpoint `GET /cashflow/summary` (D-08), full transaction CRUD (`PUT`/`DELETE /transactions/{id}` — `POST /transactions` already exists), full account CRUD (`POST`/`PUT`/`DELETE /accounts` — only `GET /accounts` exists today, D-05), reassign-then-delete for accounts (D-06), and category rename/merge with an affected-count read (D-09). Every write endpoint rides the existing `require_api_key` auth dependency, calls a `backend/writes.py` `apply_*` helper (never inline ORM), and invalidates the query engine cache — so the UI write path (D-01) shares one implementation with the agent path and never skips the audit log.

Purpose: This is the API contract the frontend (Plans 04 and 05) consumes. It closes CASH-01/02/03 (dashboard data), CASH-04 (transaction CRUD), CASH-05 (account CRUD + reassign-then-delete), CASH-06/07 (category rename/merge) at the backend layer.

Output: ~7 new/extended endpoints in `backend/main.py` + three backend test files pinning the CRUD and reassign-then-delete behaviors, all green, with the Phase 2 regression suite still green.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cashflow-dashboard-crud/04-CONTEXT.md
@.planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md
@.planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md

# Prior-plan outputs this plan builds on
@backend/writes.py
@backend/tools.py
@backend/schemas.py
@backend/main.py
@backend/auth.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /cashflow/summary aggregate endpoint (D-08, CASH-01/02/03)</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (read_settings L119 for a GET-endpoint shape; create_transaction L96 for Depends(get_session) DI; import block L23-56)
    - backend/tools.py (resolve_period L40; spending_total L107, income_total L125, net_total L142, spending_by_category L159; and Plan 02's monthly_trend / account_balances)
    - backend/schemas.py (CashflowSummary from Plan 02 — the response_model)
    - .planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md (§ Pattern 2 cashflow_summary example; § Pitfall 4 rolling trend)
    - .planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md (§ GET /cashflow/summary analog — compose tools.py, return one Pydantic model)
  </read_first>
  <behavior>
    - GET /cashflow/summary?period=this_month returns 200 with a CashflowSummary body containing: totals {income, expense, net}; accounts [{id,name,current_balance,period_net}]; by_category [...]; trend [...] with >=6 month rows.
    - The period is resolved exactly once; account_balances receives the resolved (start,end); trend always covers >=6 months regardless of the selected period.
    - It is a read endpoint (no require_api_key needed — matches existing GET /accounts, GET /transactions being open reads).
  </behavior>
  <action>
Add `GET /cashflow/summary` to `backend/main.py` with `response_model=CashflowSummary`. Signature: `def cashflow_summary(period: str = "this_month", start_date: str | None = None, end_date: str | None = None, db: Session = Depends(get_session))`. Resolve the period ONCE: `s, e = resolve_period(period, start_date, end_date)`. Compose the payload:
- totals from `income_total(period, start_date, end_date)["total"]`, `spending_total(...)["total"]`, `net_total(...)["net"]` (reuse existing functions — do not reinvent).
- by_category from `spending_by_category(period, start_date, end_date, limit=10)["rows"]`.
- accounts from `account_balances(s, e)["rows"]` (Plan 02 — pass the already-resolved bounds).
- trend from `monthly_trend(6)["rows"]` (Plan 02 — always >=6 months, independent of the selected period, per Pitfall 4).
Return `CashflowSummary(totals=..., by_category=..., accounts=..., trend=...)`. Add lazy imports of the new tools functions at the top-of-file import block alongside the existing tools imports (extend, do not duplicate). This is a READ endpoint: do NOT attach require_api_key (consistent with existing open GET reads).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cashflow_summary.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A TestClient GET to `/cashflow/summary?period=this_month` returns 200 with JSON containing keys `totals`, `accounts`, `by_category`, `trend`.
    - `trend` has `len >= 6` (CASH-02).
    - `accounts` rows carry both `current_balance` and `period_net` (CASH-03/D-04).
    - `resolve_period` is called exactly once in the handler (grep: one `resolve_period(` call in the summary function body).
    - The endpoint is NOT decorated with `require_api_key` (it is an open read, matching existing GET reads).
  </acceptance_criteria>
  <done>GET /cashflow/summary returns the full dashboard payload (totals + accounts dual-balance + by_category + >=6-month trend) in one request, composing existing + Plan-02 aggregations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transaction CRUD (PUT/DELETE), GET /categories list, and category rename/merge + affected-count endpoints (CASH-04/06/07)</name>
  <files>backend/main.py, backend/tests/test_transaction_crud.py, backend/tests/test_category_management.py</files>
  <read_first>
    - backend/main.py (create_transaction L96 — the auth+write+commit+refresh+reset_engine pattern to replicate; import_csv L157 for ValueError→422 error mapping)
    - backend/writes.py (apply_edit_transaction, apply_delete_transaction, apply_rename_category, apply_merge_category from Plan 01 — the helpers to call)
    - backend/tools.py (list_categories L269 — the existing distinct-category parameterized SQL to reuse for the GET /categories list endpoint; note it currently returns expense categories with totals — the list endpoint needs distinct NAMES across all transactions)
    - backend/schemas.py (TransactionUpdate, CategoryRenameRequest, CategoryMergeRequest, AffectedCountResponse from Plan 02)
    - backend/auth.py (require_api_key L30 — the dependency contract)
    - .planning/phases/04-cashflow-dashboard-crud/04-PATTERNS.md (§ new direct-write endpoints — auth+write+reset_engine; § Pattern 4 category bulk-update; § Shared Patterns audit-log + cache-invalidate)
    - .planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md (§ Pitfall 2 direct endpoint must call apply_* not inline; § Pattern 4)
  </read_first>
  <behavior>
    - PUT /transactions/{id} with a partial TransactionUpdate body updates only provided fields, writes an AuditLog edit row, returns 200 with the updated transaction; unknown id → 404.
    - DELETE /transactions/{id} deletes the row, writes an AuditLog delete row, returns 200/204; unknown id → 404.
    - GET /categories returns {"categories": [name, ...]} — the distinct category names across all transactions (the enumeration source the UI CategoryManager consumes, WARNING 2 fix).
    - GET /categories/{name}/affected-count returns {category, affected_count} = count of transactions with that category.
    - POST /categories/rename remaps all matching transactions and returns the affected count; POST /categories/merge moves all from_name rows to into_name and returns the affected count.
    - Every mutating endpoint requires the API key (401 without it) and calls reset_engine() after commit.
  </behavior>
  <action>
Add to `backend/main.py`, each mutating route decorated `dependencies=[Depends(require_api_key)]` and following the create_transaction pattern (call helper → db.commit() → reset_engine() via the lazy `from backend.query import reset_engine` import inside the handler):

`PUT /transactions/{tx_id}` (response_model=TransactionOut): body `payload: TransactionUpdate`. Look up the transaction; if None raise HTTPException(404). Build the `after` dict from `payload.model_dump(mode="json", exclude_none=True)` and the `before` dict from the current row, then call `apply_edit_transaction(db, tx_id, after, before)`; db.commit(); db.refresh(tx); reset_engine(); return tx. Map any ValueError from the helper to HTTPException(422, str(e)).

`DELETE /transactions/{tx_id}`: look up; 404 if missing; capture `before`; call `apply_delete_transaction(db, tx_id, before)`; db.commit(); reset_engine(); return {"status": "deleted"}.

`GET /categories` (open read — no auth, matching existing open GET reads): return `{"categories": [...]}` where the list is the DISTINCT category names across all transactions, sorted. Reuse the existing `list_categories` tool's parameterized-SQL approach from `backend/tools.py` (do NOT hand-build SQL in the handler) — either call a small tool helper that runs `SELECT DISTINCT category FROM transactions WHERE category IS NOT NULL ORDER BY category` via `text()`, or extend/reuse `list_categories` to expose the name list. Return just the names (the UI needs the full enumerable set to render the CategoryManager rows, including categories with no expense rows). This is the deterministic enumeration source Plan 05's CategoryManager consumes (WARNING 2 fix).

`GET /categories/{name}/affected-count` (response_model=AffectedCountResponse, open read — no auth): count transactions where category == name; return AffectedCountResponse(category=name, affected_count=count).

`POST /categories/rename`: body `req: CategoryRenameRequest`; call `count = apply_rename_category(db, req.old_name, req.new_name)`; db.commit(); reset_engine(); return {"old_name":..., "new_name":..., "affected_count": count}.

`POST /categories/merge`: body `req: CategoryMergeRequest`; call `count = apply_merge_category(db, req.from_name, req.into_name)`; db.commit(); reset_engine(); return {"from_name":..., "into_name":..., "affected_count": count}.

Do NOT inline any ORM mutation in these handlers — every write goes through a backend/writes.py apply_* helper (Pitfall 2). Category names are user input reaching SQL only via the parameterized helpers from Plan 01 — never build SQL strings here.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_transaction_crud.py tests/test_category_management.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_transaction_crud.py` proves PUT edits only supplied fields + writes an AuditLog edit row; DELETE removes the row + writes an AuditLog delete row; unknown id → 404.
    - `GET /categories` returns a JSON body with a `categories` list of distinct category names (a TestClient GET to `/categories` returns 200 and the list contains every seeded category name); it reuses the parameterized `list_categories`/`text()` SQL (no hand-built SQL string in the handler).
    - `test_category_management.py::test_rename` proves all matching transactions are remapped (CASH-06); `::test_merge` proves all from_name rows move to into_name (CASH-07); both return the correct affected_count.
    - Every new mutating route is decorated with `Depends(require_api_key)`: `grep -c "Depends(require_api_key)" backend/main.py` increased by the number of mutating endpoints added across Tasks 2+3.
    - No inline `Transaction(`/`db.execute(text("UPDATE` in these handlers — writes go through apply_* helpers (grep the new handlers).
    - Each mutating handler calls `reset_engine()` after `db.commit()`.
  </acceptance_criteria>
  <done>PUT/DELETE /transactions/{id}, GET /categories (distinct name list), GET /categories/{name}/affected-count, POST /categories/rename, POST /categories/merge exist, auth-protected where mutating, all routed through apply_* helpers with audit + cache invalidation; CASH-04/06/07 backend tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add account CRUD with reassign-then-delete (CASH-05, D-05/D-06)</name>
  <files>backend/main.py, backend/tests/test_account_crud.py</files>
  <read_first>
    - backend/main.py (create_transaction L96 pattern; the accounts GET endpoint; import block)
    - backend/writes.py (apply_add_account, apply_edit_account, apply_delete_account from Plan 01 — apply_delete_account takes an optional `reassign_to` and audits the reassignment inside the helper)
    - backend/schemas.py (AccountCreate, AccountUpdate, AccountOut)
    - backend/tools.py (propose_delete_account L579 — the existing BLOCK-only precedent to extend into reassign-then-delete; DO NOT modify it, only mirror the count-check structure)
    - backend/models.py (Account L43, Transaction L56 — Transaction.account_id FK)
    - .planning/phases/04-cashflow-dashboard-crud/04-RESEARCH.md (§ Pattern 3 reassign-then-delete 422-with-count; § Open Question 2 — reassignment logic lives in the endpoint, propose_delete_account stays block-only)
    - .planning/phases/04-cashflow-dashboard-crud/04-UI-SPEC.md (§ Copywriting — the 422 message surfaces affected_count)
  </read_first>
  <behavior>
    - POST /accounts creates an account (writes AuditLog), returns 201 with AccountOut.
    - PUT /accounts/{id} updates the account (writes AuditLog), returns 200; unknown id → 404.
    - DELETE /accounts/{id} with no transactions deletes the account (writes AuditLog), returns 200.
    - DELETE /accounts/{id} when the account HAS transactions and reassign_to is absent returns 422 with detail carrying affected_count and a human message (D-06).
    - DELETE /accounts/{id}?reassign_to={other_id} reassigns those transactions to the target account and deletes the source account in ONE audited helper call (apply_delete_account routes the reassignment through the audit trail); unknown reassign_to → 404.
  </behavior>
  <action>
Add to `backend/main.py`, each mutating route decorated `dependencies=[Depends(require_api_key)]`, following the create_transaction pattern (helper → commit → reset_engine):

`POST /accounts` (response_model=AccountOut, status_code=201): body `payload: AccountCreate`; call `acc = apply_add_account(db, payload.model_dump(mode="json"))`; db.commit(); db.refresh(acc); reset_engine(); return acc.

`PUT /accounts/{account_id}` (response_model=AccountOut): body `payload: AccountUpdate`; look up account, 404 if missing; capture before; call `apply_edit_account(db, account_id, payload.model_dump(mode="json", exclude_none=True), before)`; db.commit(); db.refresh(acc); reset_engine(); return acc.

`DELETE /accounts/{account_id}` with query param `reassign_to: int | None = None`: look up account, 404 if missing. Count transactions where account_id == account_id. If tx_count > 0: if reassign_to is None → raise HTTPException(422, detail={"message": f"{tx_count} transactions use this account — reassign or delete them first", "affected_count": tx_count}) (D-06 exact shape the UI copy consumes). Else look up the target account (404 if missing). Then perform the reassignment-and-delete through the audited helper — do NOT bulk-update transactions inline in this handler (WARNING 1 fix: an inline `db.query(Transaction)...update({"account_id": reassign_to})` writes no AuditLog row, so the reassignment would be invisible in the audit trail). Instead call `reassigned = apply_delete_account(db, account_id, before, reassign_to=reassign_to)` — the Plan-01 helper performs the parameterized reassignment UPDATE and the account delete, and writes the single AuditLog row capturing the reassignment target + count. Then db.commit(); reset_engine(); return {"status": "deleted", "reassigned": reassigned}. For the no-transaction path (tx_count == 0), call `apply_delete_account(db, account_id, before)` (reassign_to defaults to None). Keep the reassignment DECISION (whether/where to reassign) in THIS endpoint but the reassignment WRITE in the audited helper (Open Question 2 resolution — do not add reassign to propose_delete_account; leave the agent tool block-only).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_account_crud.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_account_crud.py` proves: POST creates + audits; PUT updates + audits; DELETE with no transactions succeeds + audits.
    - `test_account_crud.py::test_delete_blocked_without_reassign` (or equivalent) proves DELETE on an account WITH transactions and no `reassign_to` returns 422 and the detail contains `affected_count` equal to the transaction count (D-06).
    - `test_account_crud.py::test_reassign_then_delete` proves DELETE with `reassign_to` set moves the transactions to the target account (their account_id becomes reassign_to) and then removes the source account.
    - The reassignment is routed through `apply_delete_account(..., reassign_to=...)` — the DELETE handler contains NO inline `db.query(Transaction)...update({"account_id"` bulk update (grep the handler confirms the reassignment goes through the helper, not inline), so the reassignment is audited (WARNING 1 fix).
    - `test_account_crud.py::test_reassign_then_delete` also asserts an AuditLog row is written recording the reassignment (target + count), proving the reassignment is captured in the audit trail.
    - `propose_delete_account` in tools.py is unchanged (grep confirms no `reassign_to` added there — Open Question 2).
    - Account writes route through apply_* helpers (no inline `Account(` construction in the endpoints).
  </acceptance_criteria>
  <done>POST/PUT/DELETE /accounts exist with reassign-then-delete (422+affected_count when unreassigned; reassignment audited inside apply_delete_account), all auth-protected, routed through apply_* helpers with audit + cache invalidation; CASH-05 backend tests green and propose_delete_account left block-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI proxy (with MONAI_API_KEY) → write endpoints | Direct CRUD payloads (tx/account/category) cross into the DB, bypassing the propose→confirm token path (D-01) |
| client → summary read | period query param reaches SQL bounds via resolve_period |
| account delete → foreign keys | deleting an account risks orphaning transactions unless reassigned first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | new PUT/DELETE /transactions, POST/PUT/DELETE /accounts, POST /categories/* | high | mitigate | Every mutating route declared with `dependencies=[Depends(require_api_key)]`; acceptance grep asserts the require_api_key count rose by the number of mutating endpoints — a forgotten guard fails the gate |
| T-04-09 | Tampering | category rename/merge SQL via user-supplied names | high | mitigate | Endpoints call parameterized `apply_rename_category`/`apply_merge_category` (Plan 01) only; no SQL built in the handlers; grep asserts no inline UPDATE |
| T-04-10 | Repudiation | direct UI writes skipping audit log | high | mitigate | Every write endpoint calls a `backend/writes.py` apply_* helper which writes an AuditLog row (Pitfall 2); tests assert an audit row appears after each CRUD op |
| T-04-11 | Tampering / Repudiation | account delete orphaning transactions | high | mitigate | Reassign-then-delete (D-06): 422+affected_count blocks silent data loss; the reassignment runs INSIDE apply_delete_account (reassign_to param) via a parameterized UPDATE and is captured in a single AuditLog row (target + count) — no un-audited inline bulk update (WARNING 1 fix); test proves both branches and asserts the audit row |
| T-04-12 | Input Validation | malformed CRUD payloads | medium | mitigate | Pydantic v2 schemas (TransactionUpdate/AccountCreate/Update, CategoryRename/Merge) validate every body; ValueError from helpers → HTTPException(422) |
| T-04-SC | Tampering | npm/pip installs | high | mitigate | This plan installs NO packages (existing backend deps only); no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_transaction_crud.py tests/test_account_crud.py tests/test_category_management.py tests/test_cashflow_summary.py -x` all green.
- Regression gate: `cd backend && python -m pytest tests/test_proposals.py tests/test_write_tools.py -x` still green (D-02 refactor from Plan 01 untouched).
- `grep -c "Depends(require_api_key)" backend/main.py` increased by exactly the count of new mutating endpoints (transaction PUT/DELETE, account POST/PUT/DELETE, category rename/merge = 7 mutating routes).
- Full backend suite: `cd backend && python -m pytest tests/ -x`.
</verification>

<success_criteria>
- GET /cashflow/summary returns the one-payload dashboard (CASH-01/02/03).
- Transaction PUT/DELETE, account POST/PUT/DELETE (with reassign-then-delete), category rename/merge + affected-count endpoints exist, all auth-protected where mutating, all routed through apply_* helpers with audit + reset_engine.
- Three new backend test files green; Phase 2 regression suite green.
</success_criteria>

<output>
Create `.planning/phases/04-cashflow-dashboard-crud/04-03-SUMMARY.md` when done
</output>
