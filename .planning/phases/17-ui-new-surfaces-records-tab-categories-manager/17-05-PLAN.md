---
phase: 17-ui-new-surfaces-records-tab-categories-manager
plan: 05
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - ui/app/investments/[platformId]/page.tsx
  - ui/app/investments/page.tsx
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "A dynamic route /investments/[platformId] renders a platform detail page reached by clicking a platform on the Investments page (PLAT-01)"
    - "The page shows a back-link, eyebrow, platform name + kind tag, and Subtotal/Realized/Unrealized stat cards from GET /api/platforms/{id}/detail (PLAT-01)"
    - "A PnL / Buy & Sell segmented control (PnL default) switches between a holdings PnL table and a buy/sell event table (PLAT-01)"
    - "The PnL table shows Ticker/Qty/Avg cost/Price/Value/Realized/Unrealized; the Buy/Sell table shows Date/Ticker/Side/Qty/Price from GET /api/portfolio-events?platform_id= (PLAT-01)"
    - "Loading / error / not-found (404) / per-tab empty states render with the locked copy; the back-link stays present on 404 so the user is never stranded"
    - "The Investments page platform group-header name becomes a link only when platform_id is non-null; the Unassigned bucket stays plain text"
  artifacts:
    - ui/app/investments/[platformId]/page.tsx
    - ui/app/investments/page.tsx
  key_links:
    - "Reuse verbatim: the TransactionModal/settings segmented-control markup, investments/page.tsx pnlColor()/badgeColor()/fmtQty()/fmtPlain() helpers, HoldingModal, styles.ts tokens, Cashflow stat-card grid"
    - "Fetches GET /api/platforms/{id}/detail + GET /api/portfolio-events?platform_id= in parallel (Promise.all) on mount"
---

<objective>
Build the Platform detail surface: a new dynamic route `ui/app/investments/[platformId]/page.tsx`
with a PnL tab and a Buy/Sell history tab behind a segmented control, plus the link-out from the
Investments page - turning Plan 17-02's platform-detail.spec.ts GREEN. Composition only: reuses the
two platform-detail reads from 17-03 and the existing segmented control, PnL-table helpers, and
tokens.

Purpose: PLAT-01 - the user can drill into one platform's performance and trade history.
Output: new platform detail route + a link on the Investments page; platform-detail.spec.ts passes.

Implements: D-08 (platform detail route + PnL/Buy-Sell segmented tabs + Investments link-out), consuming D-05 (the 17-03 platform-detail + portfolio-events reads).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-UI-SPEC.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-PATTERNS.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-RESEARCH.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "investments page platform
group header holdings table pnlColor badgeColor fmtQty"`, `graphify query "TransactionModal
segmented control settings provider selector"`, and `graphify query "HoldingModal investments load
Promise.all"` to orient BEFORE reading raw source; then open only the ranges you need.

17-UI-SPEC.md is the visual/copy/interaction source of truth (Copywriting Contract + Components 9-13,
15). Match every locked string. Inline-style + styles.ts tokens only (no Tailwind). The two
platform-detail reads (17-03) are the backend contract this consumes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic route + page shell (back-link, header, stat cards, segmented control) + Investments link-out</name>
  <files>ui/app/investments/[platformId]/page.tsx, ui/app/investments/page.tsx</files>
  <read_first>
    - ui/app/investments/page.tsx (group-header markup ~L534-550 for name+kind tag; load()/Promise.all fetch pattern; loading/error card shells; the group-header name span to convert to a Link)
    - ui/app/cashflow/TransactionModal.tsx:75-79, 356-397 (segmented-control markup to copy verbatim)
    - ui/app/cashflow/page.tsx (stat-card grid: gridTemplateColumns repeat(auto-fit, minmax(min(100%,200px),1fr)), gap 18; statLabel/statValue)
    - ui/app/styles.ts (tokens, card)
    - 17-UI-SPEC.md Components 9, 10, 13, 15 + Copywriting Contract (Platform detail) + Color (segmented active = ink-on-white, never green)
  </read_first>
  <action>
    Create ui/app/investments/[platformId]/page.tsx as a "use client" page reading platformId via useParams().
    On mount fetch GET /api/platforms/{id}/detail and GET /api/portfolio-events?platform_id={id} in parallel with
    Promise.all (mirroring investments/page.tsx load()). Header: a "left-arrow Investments" back-link (muted,
    returns to /investments), eyebrow "Platform", h1 platform name (40px serif) with the kind tag inline (copied
    from the investments group header), then a 3-stat-card row Subtotal / Realized / Unrealized using the
    Cashflow stat-card grid and statLabel/statValue objects. Add the segmented control (Component 10) copied
    verbatim from the TransactionModal/settings markup with options for PnL and Buy & Sell (labels "PnL" /
    "Buy & Sell"), PnL active by default, ink-on-white active styling (never green). Wire loading ("Loading
    platform..."), error, and not-found (404 -> "Platform not found. It may have been deleted." with the
    back-link still rendered above) states per Component 13. In ui/app/investments/page.tsx, convert the group
    header name span to a link to /investments/{platform_id} ONLY when platform_id is non-null (cursor pointer,
    hover underline); the Unassigned bucket (platform_id null) stays plain text, never a link.
  </action>
  <acceptance_criteria>
    - `test -f "ui/app/investments/[platformId]/page.tsx"` succeeds and it begins with a "use client" directive
    - `grep -c 'Buy & Sell\|Platform not found\|Subtotal\|Realized\|Unrealized' "ui/app/investments/[platformId]/page.tsx"` returns >= 3 (locked copy present)
    - `grep -c 'platforms/.*detail\|portfolio-events' "ui/app/investments/[platformId]/page.tsx"` returns >= 2 (both reads fetched)
    - `grep -c 'investments/\|Link' ui/app/investments/page.tsx` shows the group-header name links to a platform detail route; source review confirms the null (Unassigned) platform_id stays plain text
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts -g "shell\|stat\|not found" --reporter=line` passes the shell/stat/404 test(s)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts -g "shell|stat|not found" --reporter=line</automated>
  </verify>
  <done>The dynamic route renders the locked shell (back-link, header, stat cards, segmented control) fed by the two parallel reads, handles loading/error/404, and the Investments page links non-null platform rows out to it; the shell e2e test(s) pass.</done>
</task>

<task type="auto">
  <name>Task 2: PnL tab (holdings table) + Buy/Sell history tab (event table) + empty states</name>
  <files>ui/app/investments/[platformId]/page.tsx</files>
  <read_first>
    - ui/app/investments/page.tsx (per-holding column-header + row markup; pnlColor()/badgeColor()/fmtQty()/fmtPlain() helpers to reuse directly)
    - 17-UI-SPEC.md Components 11, 12, 13 + Copywriting Contract (PnL headers, Buy/Sell headers, both empty states) + Color (Side column: green buy/deposit, terracotta sell/withdrawal, ink other)
    - 17-RESEARCH.md Pattern 4 (group.holdings shape) & Pattern 5 (PortfolioEventOut fields: date, ticker, event_type, quantity, price)
  </read_first>
  <action>
    When the PnL tab is active, render a holdings table over the detail group's holdings with the locked headers
    Ticker / Qty / Avg cost / Price / Value / Realized / Unrealized (12px muted2 header row, borderBottom
    hairline), reusing badgeColor() for the ticker badge, fmtQty() for quantity, and pnlColor() for the Realized
    and Unrealized cell colors (both figures shown as explicit columns, not the main page's combined Return).
    Empty state: "No holdings on this platform yet." When the Buy & Sell tab is active, render an event table
    over the portfolio-events response with headers Date / Ticker / Side / Qty / Price; Side renders the
    event_type Title-cased ("Buy"/"Sell"/"Deposit"/"Withdrawal"/"Dividend") colored per the Color table (green
    for buy/deposit, terracotta for sell/withdrawal, ink otherwise); Qty via fmtQty(), Price via fmtPlain().
    Empty state: "No buy/sell history on this platform yet." Same header/row styling convention across both
    tables. Reuse HoldingModal and styles.ts tokens; no Tailwind.
  </action>
  <acceptance_criteria>
    - `grep -c 'Avg cost\|Unrealized\|No holdings on this platform\|No buy/sell history' "ui/app/investments/[platformId]/page.tsx"` returns >= 3 (locked headers + empty copy)
    - `grep -c 'pnlColor\|badgeColor\|fmtQty' "ui/app/investments/[platformId]/page.tsx"` returns >= 2 (helpers reused, not re-implemented)
    - Source review: Side column maps event_type to the locked colors (green buy/deposit, terracotta sell/withdrawal, ink other) and Title-cases the label
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts --reporter=line` passes the full platform-detail spec (PnL tab + Buy/Sell tab switch)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts --reporter=line</automated>
  </verify>
  <done>Both tabs render their locked tables (PnL with explicit Realized/Unrealized columns; Buy/Sell with colored Title-cased Side) reusing the investments-page helpers, with the locked empty states; platform-detail.spec.ts is fully GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client -> REST reads | The page consumes two open GET reads; no writes originate here |
| rendered ticker/event text -> DOM | ticker/event_type strings from stored data are rendered into tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-12 | Tampering (XSS) | rendering ticker/event_type into tables | low | accept | React escapes interpolated text by default; no dangerouslySetInnerHTML is introduced |
| T-17-13 | Information Disclosure | 404/invalid platformId handling | low | mitigate | An invalid platformId renders the locked "Platform not found" state (no stack/detail leak) with the back-link, so the user is never stranded and no server error surfaces to the UI |

No package installs this phase (pure React/Next.js composition) - no supply-chain (T-*-SC) threat applies. No writes on this surface, so no api-key/audit concern.
</threat_model>

<verification>
- `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts` - the full platform-detail spec passes (GREEN).
- Source review: only ui/app/investments/[platformId]/page.tsx (new) + ui/app/investments/page.tsx (link-out) changed; segmented control + PnL helpers reused unchanged; inline styles + styles.ts tokens only.
</verification>

<success_criteria>
The user can open a platform's detail page from the Investments list and switch between a PnL tab
(explicit Realized/Unrealized columns) and a Buy/Sell history tab, with loading/error/404/empty states
handled and the Unassigned bucket correctly not linked. platform-detail.spec.ts is fully GREEN.
</success_criteria>

<output>
Create `.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-05-SUMMARY.md` when done.
</output>
