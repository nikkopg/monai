---
phase: 17-ui-new-surfaces-records-tab-categories-manager
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - ui/app/records/page.tsx
  - ui/app/components/Nav.tsx
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-05]

must_haves:
  truths:
    - "A new 'Records' nav item (inserted after Cashflow) routes to /records with a ledger glyph icon (REC-01)"
    - "The Records page shows a date-grouped ledger with a per-group 'Net {signed}' header; the daily net sums only rows where transfer_pair_id IS NULL (transfer pairs contribute zero) (REC-01, critical item #5)"
    - "A filter bar (search, account, category, type, amount range, Show-transfers) drives a debounced GET /api/transactions with the matching query params; offset resets to 0 on any filter change (REC-02)"
    - "Rows sharing a transfer_pair_id collapse into one 'Transfer: From to To' row (unsigned, ink); a single visible leg degrades to a normal row with a (transfer) tag and never throws (REC-05)"
    - "Multi-select checkboxes + a bulk bar do bulk-delete (via ConfirmDialog, POST /api/transactions/bulk-delete) and recategorize (immediate, POST /api/transactions/bulk-recategorize); selecting a collapsed pair selects BOTH ids (REC-03/05)"
    - "Editing a transfer pair opens TransactionModal in the Phase-16 transfer-leg-locked mode; no single-leg edit is offered (REC-05)"
    - "Load-more pagination (limit=100, offset increments) appends rows; hidden when a short page returns (REC-01)"
  artifacts:
    - ui/app/records/page.tsx
    - ui/app/components/Nav.tsx
  key_links:
    - "Reuse verbatim: TransactionModal (row + transfer-leg-locked edit), ConfirmDialog (bulk/pair delete), CategoryManager flattenCategories (category filter + recategorize picker), styles.ts tokens, cashflow recent-list row markup"
    - "collapseTransferPairs() copied from 17-RESEARCH Code Examples; degrades gracefully when sibling is undefined"
    - "onSaved/onChanged refetch the current filtered page after any mutation"
---

<objective>
Build the Records ledger surface (`ui/app/records/page.tsx`) and add its nav entry: a date-grouped
transaction ledger with a daily net, a full filter bar, transfer-pair collapse, multi-select bulk
delete/recategorize, and load-more paging - turning Plan 17-02's records.spec.ts GREEN. Composition
only: reuses the extended `GET /transactions` + bulk endpoints from 17-03 and the existing
TransactionModal / ConfirmDialog / CategoryManager picker / styles.ts tokens.

Purpose: REC-01/02/03/05 - the user can browse, filter, and bulk-manage their full history with
transfer pairs shown as one unit.
Output: new records page + Nav.tsx entry; records.spec.ts passes.

Implements: D-06 (Records nav + route + filter bar + bulk bar), D-07 (transfer-pair collapse + locked-modal edit + pair-aware delete from the UI), and consumes D-01/D-03/D-04 (the 17-03 filter + bulk + pair-delete contract).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-UI-SPEC.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-PATTERNS.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-RESEARCH.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "cashflow page recent
transactions row TransactionModal ConfirmDialog wiring"`, `graphify query "Nav NAV_LINKS Icon
switch"`, and `graphify query "CategoryManager flattenCategories picker"` to orient BEFORE reading
raw source; then open only the ranges you need.

17-UI-SPEC.md is the visual/copy/interaction source of truth (Copywriting Contract + Components 1-8,
14). Match every locked string and interaction. Inline-style + styles.ts tokens only (no Tailwind).
The extended GET /transactions + bulk endpoints (17-03) are the backend contract this consumes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav "Records" entry + Records page shell (header, filter bar, paged fetch, states)</name>
  <files>ui/app/components/Nav.tsx, ui/app/records/page.tsx</files>
  <read_first>
    - ui/app/components/Nav.tsx:17-67 (NAV_LINKS array + Icon() switch - add "records" entry after Cashflow + a ledger-glyph case using the same `common` props)
    - ui/app/cashflow/page.tsx (page header eyebrow+h1 + justifyContent:space-between, load()/fetch pattern, statLabel/statValue objects, loading/empty/error card shells, "+ Add transaction" btnDark button)
    - ui/app/settings/CategoryManager.tsx flattenCategories (for the category filter select) and how accounts are fetched
    - ui/app/styles.ts (tokens, card, input, btn, btnDark, label)
    - 17-UI-SPEC.md Components 1, 2, 7, 8 + Copywriting Contract (Records) + Component 14 (Nav)
  </read_first>
  <action>
    In Nav.tsx insert an entry with href "/records", label "Records", icon "records" directly after the
    /cashflow entry and add a "records" case to Icon() with a three-line ledger glyph path using the shared
    `common` props object (IconName auto-widens). Create ui/app/records/page.tsx as a "use client" page with the
    locked header (eyebrow "Ledger", h1 "Records", a "+ Add record" btnDark button in the header right that opens
    TransactionModal in create mode). Build the filter bar (Component 2): single flex-wrap row inside the ledger
    card, sidebar-band background, fields in the locked order (Search flex:2 min180, Account select, Category
    select using flattenCategories indent, Type select, Min/Max number inputs, "Show transfers" checkbox
    default-checked). On any filter change, debounce 300ms and refetch GET /api/transactions with the mapped
    params (q, account_id, category, type, amount_min, amount_max, include_transfers, date_from/to, limit=100,
    offset=0) - offset ALWAYS resets to 0 on a filter change. Implement load-more (Component 7): "Load 100 more"
    increments offset by 100 and appends; hide it when a fetch returns fewer than limit rows. Wire the loading /
    empty (no records) / filtered-to-empty / error states with the locked copy (Component 8). Fetch the account +
    category lists for the selects. Reuse styles.ts tokens; no Tailwind.
  </action>
  <acceptance_criteria>
    - `grep -c 'href: "/records"' ui/app/components/Nav.tsx` returns 1 and `grep -c 'case "records"' ui/app/components/Nav.tsx` returns 1
    - `test -f ui/app/records/page.tsx` succeeds and it begins with a "use client" directive
    - `grep -c 'Show transfers\|Search merchant or notes\|Load 100 more\|Add record' ui/app/records/page.tsx` returns >= 3 (locked copy present)
    - The filter fetch resets offset to 0 on filter change and sends limit=100 (source review)
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "filter" --reporter=line` passes the filter-bar test(s)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "filter" --reporter=line</automated>
  </verify>
  <done>Records nav item + page shell exist with the locked header, a working debounced filter bar mapping to the 17-03 query params, load-more paging, and all four list states; the filter e2e test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Date-grouped ledger + daily net + transfer-pair collapse</name>
  <files>ui/app/records/page.tsx</files>
  <read_first>
    - ui/app/cashflow/page.tsx:742-828 (recent-transactions row markup to copy verbatim, plus signed() helper and tint logic)
    - 17-RESEARCH.md "Code Examples > Transfer-pair collapse" (collapseTransferPairs - copy verbatim, degrade when sibling undefined)
    - 17-UI-SPEC.md Components 3, 4, 5 + Color table (transfer tint tintNeutral, net-neutral ink amount) + Copywriting (day-group labels "Today"/"Yesterday"/weekday, "Net {signed}")
    - ui/app/styles.ts tokens (tintNeutral, ink, green, terracotta, borderInner)
  </read_first>
  <action>
    Group the server-filtered, date-desc flat list client-side by local calendar date. For each group render a
    header row (day label per Copywriting: "Today"/"Yesterday"/"{Weekday}, {Mon} {D}") with a right-aligned
    "Net {signed}" figure colored green if the value is >= 0, terracotta if below 0. LOCKED daily-net rule: sum
    the amount only over that day's visible rows where transfer_pair_id is null - collapsed transfer pairs
    contribute ZERO, whether one or both legs are visible. Run collapseTransferPairs() (copied verbatim from
    RESEARCH) over each day's rows: rows sharing a non-null transfer_pair_id render as ONE collapsed row -
    tintNeutral chip with a swap glyph, primary text naming both accounts as a transfer from one to the other
    (account names resolved from the accounts prop by account_id), meta = date only, amount = unsigned magnitude
    in ink (no sign). Degraded case (sibling not in the filtered set): render the single leg as a NORMAL row
    (Component 4 shell) with a muted "(transfer)" tag appended - must never throw on a missing sibling.
    Non-transfer rows use the cashflow recent-list row shell verbatim (38px tint chip, primary/meta stack,
    signed+colored amount, Edit/Delete action links).
  </action>
  <acceptance_criteria>
    - `grep -c 'collapseTransferPairs\|transfer_pair_id' ui/app/records/page.tsx` returns >= 2
    - `grep -c 'Transfer' ui/app/records/page.tsx` returns >= 1 (collapsed-row primary text)
    - Source review: the daily-net reducer filters on transfer_pair_id being null before summing; collapse code guards a missing sibling (no non-null assertion that could throw)
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "transfer pair" --reporter=line` passes the collapse test, and the day-group/daily-net test passes (net excludes the transfer pair)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "transfer pair" --reporter=line; cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "date-grouped" --reporter=line</automated>
  </verify>
  <done>The ledger groups by day with a per-day net that excludes transfer pairs, collapses paired rows into one net-neutral transfer row, and degrades gracefully to a normal-row-with-tag when only one leg is visible; the collapse + daily-net e2e tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-select + bulk action bar (delete via ConfirmDialog, recategorize) + transfer-pair edit</name>
  <files>ui/app/records/page.tsx</files>
  <read_first>
    - ui/app/cashflow/ConfirmDialog.tsx (destructive-confirm modal shape and props)
    - ui/app/cashflow/TransactionModal.tsx (create + transfer-leg-locked edit modes; onSaved refetch prop)
    - ui/app/settings/CategoryManager.tsx flattenCategories (recategorize target select)
    - 17-UI-SPEC.md Components 5, 6 + Copywriting Contract (bulk bar, partial-skip note, both confirm dialogs) + Color (Recategorize green btn = the one accent target; Delete dangerBtn terracotta)
    - 17-RESEARCH.md Pattern 2 (bulk endpoint response {deleted|recategorized, skipped})
  </read_first>
  <action>
    Add persistent leading-gutter (28px) checkboxes to every ledger row. Selecting a collapsed transfer-pair
    row's checkbox marks BOTH underlying transaction ids as selected. When selectedIds.size > 0 render the
    bulk-action bar (Component 6) directly below the filter bar: left "{N} selected"; right a compact category
    select (flattenCategories) followed by "Recategorize" (green btn), then "Delete" (dangerBtn terracotta), then
    "Cancel selection" (muted text link that clears selection). "Recategorize" applies immediately (no dialog):
    POST /api/transactions/bulk-recategorize with the selected ids + chosen category; on success clear selection,
    refetch, and if the response skipped[] is non-empty show the locked partial-skip note. "Delete" opens
    ConfirmDialog with the locked bulk-delete copy; confirming POSTs /api/transactions/bulk-delete with the full
    selected id array (including both legs of any selected pair) then clears selection and refetches. Row-level
    Edit on a collapsed transfer pair opens TransactionModal seeded with legA and is_transfer true so the
    Phase-16 transfer-leg-locked mode engages (segment locked); Delete on a collapsed pair opens ConfirmDialog
    with the single-transfer-pair copy and calls the pair-aware delete with exactly one id (backend removes both
    per 17-03). No single-leg edit is ever offered. Bulk-action failures render the locked inline terracotta
    error line; onSaved/onChanged refetch the current filtered page after any mutation.
  </action>
  <acceptance_criteria>
    - `grep -c 'bulk-delete\|bulk-recategorize' ui/app/records/page.tsx` returns >= 2 (both endpoints wired)
    - `grep -c 'selected\|Recategorize\|Cancel selection' ui/app/records/page.tsx` returns >= 2 (bulk bar copy)
    - Source review: selecting a collapsed pair adds both leg ids to the selection set; bulk-delete sends the full id array; recategorize is immediate (no ConfirmDialog) while delete goes through ConfirmDialog
    - Transfer-pair Edit seeds the modal with is_transfer true (locked mode); no code path offers a single-leg edit
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "bulk" --reporter=line` passes the multi-select/bulk tests
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts -g "bulk" --reporter=line</automated>
  </verify>
  <done>Persistent multi-select drives an accent-scoped bulk bar: recategorize applies immediately (skips surfaced), delete is ConfirmDialog-gated and atomic; selecting a pair selects both legs; transfer-pair edit uses the locked modal; the bulk e2e tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click -> destructive bulk-delete | The one destructive action on this page; must be confirmed before firing |
| rendered transaction text -> DOM | merchant/notes/category strings come from stored data and are rendered into the ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-09 | Denial of Service (self-inflicted data loss) | bulk-delete of a large selection | high | mitigate | ConfirmDialog gate with the locked "Delete {N} records? ... This can't be undone." copy before the POST fires; recategorize is non-destructive so no dialog needed |
| T-17-10 | Tampering (XSS) | rendering merchant/notes into rows | low | accept | React escapes interpolated text by default; no dangerouslySetInnerHTML is introduced |
| T-17-11 | Information Disclosure | API key exposure from the client | low | accept | The page calls the Next.js /api proxy which injects MONAI_API_KEY server-side; no key is present in client code |

No package installs this phase (pure React/Next.js composition) - no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts` - the full Records spec passes (GREEN).
- Source review: only ui/app/records/page.tsx + ui/app/components/Nav.tsx changed; TransactionModal/ConfirmDialog/CategoryManager reused unchanged; inline styles + styles.ts tokens only (no Tailwind, no new component library).
</verification>

<success_criteria>
The user can browse a date-grouped ledger with a transfer-excluding daily net, filter by every locked
param, multi-select and bulk delete (confirmed, atomic, pair-cascading) or recategorize (immediate,
transfer-legs skipped), and edit a transfer pair through the locked modal - all reusing existing
components. records.spec.ts is fully GREEN.
</success_criteria>

<output>
Create `.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-04-SUMMARY.md` when done.
</output>
