---
phase: 17-ui-new-surfaces-records-tab-categories-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_write_endpoints.py
  - backend/tests/test_portfolio.py
  - backend/tests/test_write_tools.py
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-05, PLAT-01]

must_haves:
  truths:
    - "A RED backend test exists for every phase requirement (REC-01/02/03/05, PLAT-01) and each fails BEFORE Plan 17-03 lands (route 404 / missing field / unnarrowed result)"
    - "The category-filter test asserts a parent category matches its descendant rows (hierarchy), not exact-string only — pins critical item #3"
    - "The type=transfer test asserts the filter keys off transfer_pair_id IS NOT NULL, and an Adjustment/Investment (is_transfer=true, transfer_pair_id=null) row is NOT returned by type=transfer — pins critical item #4"
    - "The pair-aware-delete test asserts deleting ONE transfer leg (single-delete AND bulk-delete) removes BOTH rows with no orphan — pins critical item #1"
    - "The bulk-recategorize test asserts transfer legs are skipped (reported in skipped[], not mutated, not raised) — pins critical item #6"
  artifacts:
    - backend/tests/test_write_endpoints.py
    - backend/tests/test_portfolio.py
    - backend/tests/test_write_tools.py
  key_links:
    - "Tests target the exact endpoint contract Plan 17-03 implements: GET /transactions (extended params), POST /transactions/bulk-delete, POST /transactions/bulk-recategorize, GET /platforms/{id}/detail, GET /portfolio-events?platform_id="
    - "Tests follow the live-Postgres + TestClient + rollback-probe-inserts idiom already used in test_write_endpoints.py / test_write_tools.py (no mocking, never mutate live data permanently)"
---

<objective>
Create the RED backend test baseline for Phase 17 so every downstream endpoint has an
`<automated>` sampling command that fails now and turns green when Plan 17-03 lands. Covers
the extended `GET /transactions` filter/paging surface, the two bulk endpoints, the
`transfer_pair_id` exposure, pair-aware delete (single + bulk), and the two platform-detail
reads.

Purpose: Nyquist / correctness-by-construction — pin the 4 backend-side critical-correctness
items (hierarchy filter, transfer_pair_id semantics, pair-aware delete, recategorize skip) as
executable assertions BEFORE any implementation, matching the v1.2 Wave-0 RED-scaffold
convention (12-01, 13-01, 14-01, 16-01).
Output: three extended test files whose new tests collect and fail RED.

Implements (RED coverage for): D-01 (filters), D-02 (transfer_pair_id exposure), D-03 (bulk endpoints), D-04 (pair-aware delete), D-05 (platform-detail reads).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-RESEARCH.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-PATTERNS.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-CONTEXT.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "test_write_endpoints
TestClient live Postgres rollback probe insert idiom"` and `graphify query "GET /transactions
delete_transaction apply_delete_transaction transfer_pair_id"` to orient BEFORE reading raw
test/source files; then open only the specific ranges you need.

RESEARCH.md "Validation Architecture > Phase Requirements → Test Map" lists the exact `-k`
selector names to use for each test. RESEARCH.md Pattern 1/2 and Pitfalls 1–5 define the
behaviors under test. This is a NEW-TEST-ONLY plan — do not implement any endpoint here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for extended GET /transactions (filters, paging, transfer_pair_id exposure)</name>
  <files>backend/tests/test_write_endpoints.py</files>
  <read_first>
    - backend/tests/test_write_endpoints.py (the file to extend — follow its TestClient + live-DB + rollback-probe-inserts idiom exactly; read its existing setup/fixtures)
    - backend/main.py:641-648 (current limit-only list_transactions — the target under test)
    - backend/tools.py `_find_category_node` / `_descendant_ids` / `spending_in_category` (hierarchy semantics the category-filter test must mirror)
    - 17-RESEARCH.md Pattern 1 (exact param list + type-filter semantics), Pitfall 3 (hierarchy), Pitfall 4 (500-cap/offset), Pitfall 5 (transfer_pair_id vs is_transfer)
    - 17-PATTERNS.md "extend GET /transactions" block (locked type=transfer → transfer_pair_id IS NOT NULL)
  </read_first>
  <action>
    Add tests named so `-k transactions_filter`, `-k transaction_paging`, `-k category_filter_hierarchy`,
    and `-k transfer_pair_id_exposed` select them. Insert probe rows in a transaction that is rolled
    back at test end (never leave live data mutated), then drive `GET /transactions` via TestClient:
    (a) each filter param narrows results — `q` (merchant/notes ilike), `account_id`, `type=expense`
    (amount<0 AND transfer_pair_id null), `type=income` (amount>0 AND transfer_pair_id null),
    `type=transfer` (transfer_pair_id IS NOT NULL only), `amount_min`/`amount_max` on abs(amount),
    `date_from`/`date_to`, `include_transfers=false`;
    (b) `offset`+`limit` page correctly and the 500 hard cap holds;
    (c) category filter by a PARENT category name returns rows in descendant subcategories (assert the
    parent-filter count equals the node+descendants count, NOT the exact-string-only count) — this is the
    anti-undercount assertion vs the dashboard rollup;
    (d) an Adjustment/Investment probe row (is_transfer=true, transfer_pair_id=null) is returned under
    type=expense/income by its sign and is NOT returned under type=transfer;
    (e) `transfer_pair_id` appears as a key in each `TransactionOut` JSON object.
    These are RED now: unknown query params are ignored by the current endpoint so filters do not narrow,
    and transfer_pair_id is absent from the response — assertions fail until 17-03.
  </action>
  <acceptance_criteria>
    - `grep -c 'def test_transactions_filter\|def test_transaction_paging\|def test_category_filter_hierarchy\|def test_transfer_pair_id_exposed' backend/tests/test_write_endpoints.py` returns >= 4
    - `cd backend && python -m pytest tests/test_write_endpoints.py -k 'transactions_filter or transaction_paging or category_filter_hierarchy or transfer_pair_id_exposed' -q` COLLECTS the new tests and reports failures (RED baseline — endpoints/params/field not yet present)
    - The category_filter_hierarchy test asserts a parent-category filter count == node+descendant-id count (derived live from the category tree), never a hard-coded number
    - The type=transfer test explicitly asserts an is_transfer=true / transfer_pair_id=null probe row is excluded
    - Probe inserts are rolled back: running the test twice leaves `SELECT count(*) FROM transactions` unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_write_endpoints.py -k 'transactions_filter or transaction_paging or category_filter_hierarchy or transfer_pair_id_exposed' -q</automated>
  </verify>
  <done>Four+ RED tests exist covering every GET /transactions filter param, paging, hierarchy category match, and transfer_pair_id exposure; all fail before 17-03 and leave the live DB unmutated.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for bulk-delete, bulk-recategorize, and pair-aware delete (single + bulk)</name>
  <files>backend/tests/test_write_endpoints.py, backend/tests/test_write_tools.py</files>
  <read_first>
    - backend/tests/test_write_endpoints.py (bulk endpoints — follow its POST-via-TestClient + require_api_key header idiom)
    - backend/tests/test_write_tools.py (extend near existing `test_paired_leg_delete_blocked` ~L1618 — the single-row pair guard test; the new test extends coverage to endpoint-level cascade)
    - backend/main.py:773-799 (update_transaction analog), 802-823 (delete_transaction analog — before-dict shape, no allow_paired today)
    - backend/writes.py:136-148 apply_delete_transaction (allow_paired only SUPPRESSES the guard — does NOT delete the sibling), :150-167 apply_add_transfer (pairing convention: both legs' transfer_pair_id = leg A id)
    - 17-RESEARCH.md Pattern 2 (bulk loop + pair-lookup), Pitfall 1 & 2 (no cascade in primitive; existing single-delete 422s on a leg today), Pattern 3 (Decimal-JSON: str(tx.amount))
  </read_first>
  <action>
    Add tests named so `-k bulk_delete`, `-k bulk_recategorize`, and `-k pair_aware_delete` select them.
    Using rolled-back probe rows including one liquid to liquid transfer PAIR (two rows sharing a non-null
    transfer_pair_id) created via the existing transfer path:
    (a) bulk_delete: POST /transactions/bulk-delete with `{ids:[...]}` (api-key header) removes every listed
    id, returns `{deleted:[...], skipped:[...]}`, and when a listed id is one transfer leg it ALSO removes the
    sibling leg (assert BOTH rows absent, no orphan) inside one commit; assert one AuditLog row per deleted
    entity; assert a non-existent id lands in skipped[] with a reason (never a 500);
    (b) pair_aware_delete (in test_write_tools.py): deleting ONE leg via the single DELETE /transactions/{id}
    endpoint removes BOTH legs (this is the retrofit assertion — RED today because the current endpoint 422s on
    a paired leg);
    (c) bulk_recategorize: POST /transactions/bulk-recategorize with `{ids:[...], category:"X"}` sets category
    on non-transfer rows, and SKIPS transfer legs — a selected transfer-leg id appears in skipped[] with a
    transfer-leg reason, its category is unchanged, and no error is raised;
    (d) both bulk endpoints require the API key (401 without it).
    RED now: both bulk routes 404, and single-delete 422s on a transfer leg.
  </action>
  <acceptance_criteria>
    - `grep -c 'def test_bulk_delete\|def test_bulk_recategorize' backend/tests/test_write_endpoints.py` returns >= 2
    - `grep -c 'def test_pair_aware_delete' backend/tests/test_write_tools.py` returns >= 1
    - `cd backend && python -m pytest tests/test_write_endpoints.py -k 'bulk_delete or bulk_recategorize' tests/test_write_tools.py -k pair_aware_delete -q` collects and fails RED
    - bulk_delete test asserts BOTH legs of a selected transfer pair are gone AND that a bad id is reported in skipped[] (not a 500)
    - bulk_recategorize test asserts a transfer-leg id is in skipped[] and its category is unchanged
    - Both bulk endpoint tests assert 401 when the api-key header is omitted
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_write_endpoints.py -k 'bulk_delete or bulk_recategorize' -q; cd backend && python -m pytest tests/test_write_tools.py -k pair_aware_delete -q</automated>
  </verify>
  <done>RED tests pin: bulk-delete atomicity + pair cascade + partial-skip, single-delete pair cascade retrofit, bulk-recategorize transfer-leg skip, and api-key gating; all fail before 17-03.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for platform-detail and portfolio-events reads</name>
  <files>backend/tests/test_portfolio.py</files>
  <read_first>
    - backend/tests/test_portfolio.py (the file to extend — follow its portfolio_summary + live-DB idiom)
    - backend/portfolio.py:174 portfolio_summary (per-platform group shape: platform_id, platform_name, kind, subtotal, holdings[] with realized_pnl/unrealized_pnl/current_value)
    - backend/main.py:575 investments_summary (lazy refresh_all_prices idiom the new read reuses), :427 POST /portfolio-events (model/schema import site)
    - backend/schemas.py PortfolioEventOut (id, date, ticker, event_type, quantity, price)
    - 17-RESEARCH.md Pattern 4 (GET /platforms/{id}/detail) & Pattern 5 (GET /portfolio-events?platform_id=)
  </read_first>
  <action>
    Add tests named so `-k platform_detail` and `-k portfolio_events_by_platform` select them, via TestClient:
    (a) platform_detail: GET /platforms/{id}/detail returns the group whose platform_id == id, containing
    subtotal + holdings with realized_pnl and unrealized_pnl present; a non-existent platform id returns 404;
    (b) portfolio_events_by_platform: GET /portfolio-events?platform_id={id} returns ONLY that platform's events,
    ordered date-desc, each row carrying date/ticker/event_type/quantity/price. Use existing platform/holding
    fixtures or rolled-back probe inserts. RED now: both routes 404 (GET forms do not exist — only POST
    /portfolio-events exists today).
  </action>
  <acceptance_criteria>
    - `grep -c 'def test_platform_detail\|def test_portfolio_events_by_platform' backend/tests/test_portfolio.py` returns >= 2
    - `cd backend && python -m pytest tests/test_portfolio.py -k 'platform_detail or portfolio_events_by_platform' -q` collects and fails RED (both GET routes 404)
    - platform_detail test asserts the returned group's platform_id equals the requested id AND holdings carry realized_pnl + unrealized_pnl
    - portfolio_events test asserts every returned row's platform matches the query and ordering is date-desc
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_portfolio.py -k 'platform_detail or portfolio_events_by_platform' -q</automated>
  </verify>
  <done>RED tests pin the two platform-detail reads (scoped PnL group + platform-filtered date-desc event ledger, 404 on bad id); both fail before 17-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Postgres | Probe inserts touch the real DB; must be rolled back, never permanently mutate live financial rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | probe inserts leaking into live data | medium | mitigate | Every probe insert wrapped in a transaction rolled back at test end (test_write_tools.py idiom); acceptance criteria assert `count(*)` unchanged after a run |
| T-17-02 | Repudiation | tests asserting on AuditLog | low | accept | Tests read AuditLog rows to assert one-per-entity; read-only, no risk |

No package installs this phase (test frameworks pytest/Playwright already present) — no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_write_endpoints.py tests/test_portfolio.py tests/test_write_tools.py -k '17 or filter or bulk or pair_aware or platform_detail or portfolio_events or transfer_pair_id' -q` collects all new tests; they fail RED (nothing implemented yet).
- Live `transactions` / `audit_log` row counts unchanged after the suite runs (probe rollback holds).
</verification>

<success_criteria>
Every Phase 17 requirement (REC-01/02/03/05, PLAT-01) has at least one RED backend test with a named `-k` selector; all fail before 17-03 and none leaves the live DB permanently mutated. The 4 backend critical-correctness items (hierarchy filter, transfer_pair_id semantics, pair-aware delete, recategorize skip) are each pinned by an assertion.
</success_criteria>

<output>
Create `.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-01-SUMMARY.md` when done.
</output>
