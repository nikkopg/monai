---
phase: 17-ui-new-surfaces-records-tab-categories-manager
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/e2e/records.spec.ts
  - ui/e2e/platform-detail.spec.ts
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-05, PLAT-01]

must_haves:
  truths:
    - "A route-mocked RED e2e spec exists for the Records ledger (date-grouped rows, daily net, filter bar, multi-select bulk delete/recategorize, transfer-pair collapse + edit + delete) — fails before 17-04"
    - "A route-mocked RED e2e spec exists for Platform detail (route reachable, PnL tab, Buy/Sell tab via segmented control) — fails before 17-05"
    - "Specs mock the exact endpoint contract 17-03 implements and assert the exact copy locked in 17-UI-SPEC.md"
  artifacts:
    - ui/e2e/records.spec.ts
    - ui/e2e/platform-detail.spec.ts
  key_links:
    - "Specs run with PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome (bundled chromium absent — Pitfall 6)"
    - "Mock shapes match backend contract: GET /api/transactions (with transfer_pair_id), POST /api/transactions/bulk-delete, POST /api/transactions/bulk-recategorize, GET /api/platforms/{id}/detail, GET /api/portfolio-events?platform_id="
---

<objective>
Create the RED Playwright (route-mocked) baseline for the two new frontend surfaces so
Plans 17-04 (Records) and 17-05 (Platform detail) have a concrete GREEN target that also
locks the 17-UI-SPEC copy and interaction contract.

Purpose: Nyquist — every frontend behavior gets an `<automated>` spec that fails now and
turns green on implementation, matching the Phase 16 e2e-scaffold convention (16-01).
Output: two new spec files whose tests fail RED (routes/pages do not exist yet).

Implements (e2e coverage for): D-06 (Records tab), D-07 (transfer-pair collapse in ledger), D-08 (platform detail route + tabs).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-UI-SPEC.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-PATTERNS.md
@.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-RESEARCH.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "cashflow-crud e2e
route mock fulfill pattern"` and `graphify query "platform-crud spec platform page mock"` to
orient BEFORE reading raw spec files; then open only the ranges you need.

17-UI-SPEC.md is the copy/interaction source of truth (Copywriting Contract + Component
Contracts 1–15). Assert on the exact strings there. This is a NEW-SPEC-ONLY plan — do not
build any page here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED e2e spec for the Records ledger</name>
  <files>ui/e2e/records.spec.ts</files>
  <read_first>
    - ui/e2e/cashflow-crud.spec.ts (route-mock idiom: page.route("**/api/...", route.fulfill(...)), per-test scoped mocks, method-branching for POST)
    - ui/e2e/platform-crud.spec.ts (platform-page mock conventions)
    - ui/playwright.config.ts:6-33 (PLAYWRIGHT_CHROMIUM_PATH fallback)
    - 17-UI-SPEC.md Copywriting Contract (Records) + Components 1–8 (filter bar, date-group, daily-net, transfer-pair collapse, degraded case, multi-select bulk bar, pagination, states)
    - 17-PATTERNS.md "records.spec.ts" analog note + collapseTransferPairs reference
  </read_first>
  <action>
    Create ui/e2e/records.spec.ts. Mock `**/api/transactions*` to return a fixed list that includes:
    normal expense/income rows across two calendar days, one transfer PAIR (two rows sharing a non-null
    transfer_pair_id), and one Adjustment row (is_transfer true, transfer_pair_id null). Assert:
    (a) navigating to /records shows day-group headers with the locked labels ("Today"/"Yesterday"/weekday
    form) and a per-group "Net {signed}" figure that EXCLUDES the transfer pair from the sum;
    (b) the filter bar renders Search / Account / Category / Type / Min / Max / "Show transfers" with the
    locked placeholders and default-checked transfers toggle; changing a filter refetches with the matching
    query param (assert the outgoing request URL);
    (c) the transfer pair renders as ONE collapsed row "Transfer: {From} → {To}" with an unsigned amount;
    (d) selecting rows shows the bulk bar "{N} selected" with Recategorize / Delete / Cancel selection;
    Delete opens the ConfirmDialog with the locked bulk-delete copy and on confirm POSTs
    /api/transactions/bulk-delete with the selected ids (mock the POST); Recategorize POSTs
    /api/transactions/bulk-recategorize;
    (e) "Load 100 more" appears when a full page returns and is hidden when a short page returns.
    RED now: /records route + page do not exist.
  </action>
  <acceptance_criteria>
    - `test -f ui/e2e/records.spec.ts` succeeds
    - `grep -c "test(" ui/e2e/records.spec.ts` returns >= 5 (date-group+net, filters, transfer-pair collapse, bulk select/delete/recategorize, load-more)
    - `grep -c 'bulk-delete\|bulk-recategorize\|Show transfers\|Transfer:' ui/e2e/records.spec.ts` returns >= 3 (asserts locked copy + endpoints)
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts` runs (chromium launches) and FAILS RED (page not built)
    - A test asserts the daily-net figure equals the sum of non-transfer rows only (transfer pair contributes zero)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/records.spec.ts --reporter=line || true</automated>
  </verify>
  <done>records.spec.ts exists with >=5 route-mocked tests locking the ledger copy, filter params, transfer-pair collapse, daily-net exclusion, and bulk actions; all fail RED before 17-04.</done>
</task>

<task type="auto">
  <name>Task 2: RED e2e spec for Platform detail</name>
  <files>ui/e2e/platform-detail.spec.ts</files>
  <read_first>
    - ui/e2e/platform-crud.spec.ts (platform-page route-mock idiom)
    - ui/e2e/cashflow-crud.spec.ts (general fulfill pattern + segmented-control interaction reference)
    - ui/playwright.config.ts:6-33 (chromium fallback)
    - 17-UI-SPEC.md Copywriting Contract (Platform detail) + Components 9–13 (shell, segmented control, PnL table, Buy/Sell table, states)
  </read_first>
  <action>
    Create ui/e2e/platform-detail.spec.ts. Mock `**/api/platforms/*/detail` to return one group
    (platform_name, kind, subtotal, holdings[] with realized_pnl/unrealized_pnl/current_value) and
    `**/api/portfolio-events*` to return a few events (date, ticker, event_type buy/sell/deposit, quantity,
    price). Assert:
    (a) navigating to /investments/{id} shows the "← Investments" back-link, the "Platform" eyebrow, the
    platform name heading + kind tag, and the 3 stat cards Subtotal/Realized/Unrealized;
    (b) the PnL / Buy & Sell segmented control renders with PnL active by default; the PnL table shows the
    locked headers (Ticker · Qty · Avg cost · Price · Value · Realized · Unrealized) with realized/unrealized
    values;
    (c) clicking "Buy & Sell" switches to the event table with the locked headers (Date · Ticker · Side · Qty ·
    Price) and Side rendered Title-cased and colored;
    (d) a 404 detail mock shows the "Platform not found" copy with the back-link still present.
    RED now: the dynamic route + page do not exist.
  </action>
  <acceptance_criteria>
    - `test -f ui/e2e/platform-detail.spec.ts` succeeds
    - `grep -c "test(" ui/e2e/platform-detail.spec.ts` returns >= 3 (shell+stats, PnL tab, Buy/Sell tab, and/or 404)
    - `grep -c 'Buy & Sell\|Unrealized\|← Investments\|platforms/.*detail\|portfolio-events' ui/e2e/platform-detail.spec.ts` returns >= 3
    - `cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts` runs and FAILS RED (route not built)
  </acceptance_criteria>
  <verify>
    <automated>cd ui && PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome npx playwright test e2e/platform-detail.spec.ts --reporter=line || true</automated>
  </verify>
  <done>platform-detail.spec.ts exists with >=3 route-mocked tests locking the shell, segmented-control tab switch, both table header sets, and the 404 state; all fail RED before 17-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → mocked network | All API traffic is route-mocked; no real backend or DB is touched by e2e |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering | e2e hitting real backend by accident | low | mitigate | Every network call is `page.route`-mocked; specs assert on mocked payloads, never a live DB |

No package installs this phase (Playwright already configured) — no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- Both specs launch chromium via `PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/google-chrome` and run to a RED result (surfaces exist as tests, fail because pages are unbuilt).
- No spec issues an un-mocked network request (all `**/api/...` routes are fulfilled locally).
</verification>

<success_criteria>
Both new e2e spec files exist, run under the documented chromium path, and fail RED before their implementing plans; they encode the 17-UI-SPEC copy + interaction contract (including the daily-net transfer-exclusion and segmented-control tab switch) as executable assertions.
</success_criteria>

<output>
Create `.planning/phases/17-ui-new-surfaces-records-tab-categories-manager/17-02-SUMMARY.md` when done.
</output>
