---
phase: 05-investment-subsystem
plan: 03
type: tdd
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - backend/portfolio.py
  - backend/writes.py
  - backend/schemas.py
  - backend/main.py
  - backend/tests/test_portfolio.py
  - backend/tests/test_write_tools.py
  - ui/app/investments/HoldingModal.tsx
  - ui/app/investments/HoldingOverrideModal.tsx
  - ui/app/investments/page.tsx
autonomous: true
requirements: [INV-01, INV-06, INV-07]
user_setup: []

must_haves:
  truths:
    - "Logging buy/sell/dividend events recomputes the holding's quantity and avg_cost from the event ledger (portfolio_events is source of truth, D-01)"
    - "A sell realizes (sell_price − avg_cost) × sold_qty and leaves avg_cost unchanged; dividends fold into realized return (D-02)"
    - "GET /investments/summary returns platform-grouped holdings with per-holding unrealized P&L, realized P&L, total portfolio value, and an 'as of' timestamp (D-05, INV-06)"
    - "A direct holding override (D-03 escape hatch) edits/deletes the holding row and writes an audit_log entry"
    - "Every buy/sell/dividend write inserts a portfolio_events row (INV-07)"
  artifacts:
    - backend/portfolio.py (recompute_holding_from_events, realized/unrealized P&L calculators)
    - backend/writes.py (apply_add_portfolio_event, apply_add_holding, apply_edit_holding, apply_delete_holding)
    - backend/schemas.py (HoldingCreate/Out, PortfolioEventCreate/Out, PortfolioSummary)
    - backend/main.py (holdings + portfolio-events CRUD routes, GET /investments/summary)
    - ui/app/investments/HoldingModal.tsx
    - ui/app/investments/HoldingOverrideModal.tsx
  key_links:
    - "apply_add_portfolio_event calls recompute_holding_from_events(db, ticker) before returning so the position falls out of the ledger (D-01)"
    - "PortfolioEventCreate.event_type constrained to Literal['buy','sell','dividend']; quantity/price positive Decimal (V5)"
    - "GET /investments/summary is an open read composing holdings + price_cache current price + portfolio.py P&L calculators"
    - "recompute keys on Holding.ticker (unique) — one holding row per ticker"
---

<objective>
Deliver the keystone vertical slice: the buy/sell/dividend event ledger and average-cost accounting (D-01/D-02), realized+unrealized P&L and total portfolio value with an "as of" timestamp (D-05/INV-06), the direct holding-override escape hatch (D-03), and the composed `GET /investments/summary` read — surfaced through `HoldingModal` (event entry) and `HoldingOverrideModal`, rendered as platform-grouped holding cards on `/investments`.

Purpose: This is the heart of INV-01/06/07 — positions derive from an audited event ledger, and the page shows what the portfolio is worth and how each position is doing. Prices come from cache/manual only in this slice (Plan 04 adds live fetch + staleness); this slice reads whatever `price_cache` currently holds (or shows a "no price yet" state) so P&L math is demonstrable independent of the external APIs.
Output: `backend/portfolio.py`, four holding/event write helpers, holding/event/summary DTOs, holdings+events REST + `GET /investments/summary`, two modals, and the grouped-holdings render on the page.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-investment-subsystem/05-CONTEXT.md
@.planning/phases/05-investment-subsystem/05-RESEARCH.md
@.planning/phases/05-investment-subsystem/05-PATTERNS.md
@.planning/phases/05-investment-subsystem/05-UI-SPEC.md
@backend/writes.py
@backend/models.py
@ui/app/cashflow/TransactionModal.tsx
@ui/app/cashflow/page.tsx
</context>

<feature>
  <name>Average-cost recompute from event ledger (D-01/D-02)</name>
  <files>backend/portfolio.py, backend/tests/test_portfolio.py</files>
  <behavior>
    RED first — write these against `recompute_holding_from_events(db, ticker)` before implementing:
    - buy 10 @ 100 → qty 10, avg_cost 100, realized 0.
    - buy 10 @ 100 then buy 10 @ 200 → qty 20, avg_cost 150.
    - buy 10 @ 100 then sell 4 @ 250 → qty 6, avg_cost 100 (UNCHANGED by the sell, D-02), realized (250−100)×4 = 600.
    - dividend event (quantity=1, price=amount) folds into realized return, does not change qty/avg_cost.
    - selling the full position → qty 0 (holding row retained, D-04 "drops off active list" is a query-time filter, not a delete).
    - all arithmetic in Decimal; no float artifacts.
  </behavior>
  <implementation>Implement `recompute_holding_from_events(db, ticker) -> dict` per 05-RESEARCH.md Pattern 2 (lines 308-370): scan `portfolio_events` ordered by (date, id); running qty + total_cost; on buy add cost+qty; on sell realize `(price − avg_cost) × qty` and reduce total_cost by `avg_cost × qty` (leaving avg_cost unchanged); on dividend add `price × quantity` to realized. Upsert the `holdings` row (key on unique `ticker`). Return dict with ticker, quantity, avg_cost, realized_pnl, dividend_total. Add `unrealized_pnl(current_price, avg_cost, qty)` and a `portfolio_summary(db)` helper that composes per-holding + totals. All Decimal. Do NOT recompute avg_cost from a reduced pool (05-RESEARCH.md Pitfall 4 / Anti-Patterns).</implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED→GREEN recompute + P&L calculators in backend/portfolio.py</name>
  <read_first>
    - .planning/phases/05-investment-subsystem/05-RESEARCH.md § Architecture Patterns Pattern 2 (full recompute algorithm) + § Common Pitfalls 4 (avg_cost-after-sell) + § Anti-Patterns
    - backend/tools.py (resolve_period + aggregation style — the pure-derivation analog per 05-PATTERNS.md § portfolio.py)
    - backend/models.py (Holding, PortfolioEvent columns — quantity Numeric(28,8), price/avg_cost Numeric(18,2))
    - backend/tests/test_portfolio.py (the red scaffold from Plan 01 — fill test_recompute_holding_from_events and test_avg_cost_realized_pnl)
  </read_first>
  <action>RED: implement the buy/sell/dividend cases from `<behavior>` in `test_recompute_holding_from_events` and `test_avg_cost_realized_pnl`, asserting exact Decimal values; run to confirm they fail. GREEN: create `backend/portfolio.py` with `recompute_holding_from_events(db, ticker)`, `unrealized_pnl(...)`, and `portfolio_summary(db)` per 05-RESEARCH.md Pattern 2; run until the two tests pass. REFACTOR: keep the "avg_cost unchanged by sell" invariant expressed via total_cost reduction (not pool re-derivation). Commit test then implementation separately.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_portfolio.py::test_recompute_holding_from_events backend/tests/test_portfolio.py::test_avg_cost_realized_pnl -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Both recompute/P&L tests green; avg_cost provably unchanged across a profitable and a losing sell; dividends fold into realized only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Holding + portfolio-event write helpers, DTOs, and REST routes</name>
  <read_first>
    - backend/writes.py (the file being modified — `apply_add_account` L76-87 add-pattern with `db.flush()` before AuditLog; `apply_edit_account`/`apply_delete_account` for edit/delete + before-snapshot; Decimal(str(x)) money invariant L33)
    - backend/schemas.py (the file being modified — MoneyDecimal L18; AccountCreate/Out/Update DTO-by-role; use Literal for event_type)
    - backend/main.py (the file being modified — create/update/delete_account L121-201 auth+commit+reset_engine+422; cashflow_summary L214-243 composed-read analog for GET /investments/summary)
    - backend/portfolio.py (recompute_holding_from_events — call it from apply_add_portfolio_event)
    - .planning/phases/05-investment-subsystem/05-PATTERNS.md § writes.py (apply_add_portfolio_event calls recompute), § schemas.py, § main.py (composed-read)
    - .planning/phases/05-investment-subsystem/05-UI-SPEC.md § Layout (GET /investments/summary payload: platform groups, per-holding rows, unrealized/realized totals, "as of" timestamp)
  </read_first>
  <behavior>
    - Test: POST /portfolio-events {ticker, event_type:"buy", quantity, price, date} inserts a portfolio_events row (INV-07), writes one AuditLog, and the holding's qty/avg_cost recompute (D-01).
    - Test: PortfolioEventCreate rejects event_type "gift" (not in the buy/sell/dividend literal set) with a 422 (V5).
    - Test: apply_edit_holding / apply_delete_holding (D-03 direct override) each write an AuditLog row.
    - Test: GET /investments/summary returns holdings grouped by platform_id (including an "unassigned" group for null platform_id) with unrealized_pnl, realized_pnl, total_value, and an as_of timestamp.
  </behavior>
  <action>In backend/writes.py add `apply_add_portfolio_event(db, after)` (insert portfolio_events row with `Decimal(str(...))` money, AuditLog entity="portfolio_event", then call `recompute_holding_from_events(db, after["ticker"])` before returning, D-01), plus `apply_add_holding`/`apply_edit_holding`/`apply_delete_holding` (D-03 direct override, entity="holding", audited). In backend/schemas.py add `PortfolioEventCreate` (event_type: Literal["buy","sell","dividend"]; quantity/price MoneyDecimal validated positive; date; ticker), `PortfolioEventOut`, `HoldingCreate`/`HoldingOut`, and `PortfolioSummary` (composed payload: list of platform groups each with holdings rows + subtotal, plus totals + as_of). In backend/main.py add write routes `POST /portfolio-events`, `POST /holdings`, `PUT /holdings/{id}`, `DELETE /holdings/{id}` (all `dependencies=[Depends(require_api_key)]`, apply→commit→refresh→reset_engine, ValueError→422) and open read `GET /investments/summary` composing holdings + latest `price_cache` price per ticker + `portfolio.py` calculators; when no price_cache row exists for a ticker, current price is null and that holding's unrealized P&L is reported as null (Plan 04 backfills live prices). Group by platform_id; null platform_id → "unassigned" group.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_write_tools.py -x -q 2>&1 | tail -6</automated>
  </verify>
  <done>Event/holding writes audited; event_type constrained to the literal set; recompute fires on event write; GET /investments/summary returns grouped P&L payload with as_of.</done>
</task>

<task type="auto">
  <name>Task 3: HoldingModal + HoldingOverrideModal + grouped-holdings render</name>
  <read_first>
    - ui/app/cashflow/TransactionModal.tsx (dual-mode create/edit L51-57; overlay+card shell L183-202; toLocalDatetimeInputValue L44-49; handleSubmit + error-copy L118-181)
    - ui/app/cashflow/page.tsx (fmt() Intl.NumberFormat signDisplay:"always" for signed/colored P&L; refetch-on-save wiring)
    - ui/app/investments/page.tsx (the file being modified — hosts PlatformManager from Plan 02; add banner + P&L summary + platform-grouped cards)
    - .planning/phases/05-investment-subsystem/05-UI-SPEC.md § Layout notes 1-6 (page structure), § "HoldingModal.tsx", § "HoldingOverrideModal.tsx", § Copywriting (Log event / Add holding directly / event-type select / empty states / error copy)
  </read_first>
  <action>Create ui/app/investments/HoldingModal.tsx (event-entry, dual-mode via `editingHolding == null`) with fields per UI-SPEC (Ticker, Asset type select crypto/idx_stock/mutual_fund/other, Platform select from platforms list with "(unassigned)" option, Event type select Buy/Sell/Dividend with lowercase values, Quantity, Price per unit (IDR), Date via toLocalDatetimeInputValue, Notes); on Dividend, relabel Price to "Dividend amount (IDR)" and default Quantity to 1. POST /api/portfolio-events on submit, refetch summary on success, error copy "Couldn't log event: {detail}. Nothing was changed." Create ui/app/investments/HoldingOverrideModal.tsx (D-03 direct override; visually de-emphasized via a smaller "Add holding directly" text-link; italic warning caption "This directly sets the holding's position, bypassing the event ledger…"; fields Ticker/Asset type/Platform/Quantity/Avg cost (IDR)/Purchase date/Currency read-only "IDR"; POST/PUT /api/holdings). Grow ui/app/investments/page.tsx: fetch `GET /api/investments/summary`, render the portfolio-total banner (Display figure + "as of" caption + a placeholder "Refresh prices" button — Plan 04 wires its handler), the 2-column Unrealized/Realized P&L summary row (signed/colored via fmt()), and platform-grouped holding cards (one card per platform + an "Unassigned" card, zero-qty holdings filtered out per D-04, empty-state copy from UI-SPEC). Reuse styles from `../styles`.</action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -qiE "investments/(HoldingModal|HoldingOverrideModal|page)" && echo "TS ERROR" || echo "OK"</automated>
    <human-check>Log a buy then a sell for one ticker; confirm the holding card shows recomputed qty/avg cost and realized P&L; use "Add holding directly" to seed a position and confirm it appears; confirm platform grouping + Unassigned card + empty states render per UI-SPEC.</human-check>
  </verify>
  <done>Event entry + direct override work through the /api proxy; /investments shows portfolio banner, realized/unrealized P&L summary, and platform-grouped holding cards; tsc clean for new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /portfolio-events, /holdings write routes | Untrusted event/holding input (event_type, quantity, price, ticker) crosses into the ledger and drives the recompute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-AC | Elevation of Privilege | POST/PUT/DELETE /holdings, POST /portfolio-events | high | mitigate | Every write route carries `dependencies=[Depends(require_api_key)]` (V4); GET /investments/summary stays open |
| T-05-03-EVT | Tampering | PortfolioEventCreate → recompute | high | mitigate | `event_type` constrained to `Literal["buy","sell","dividend"]` and quantity/price validated positive Decimal at the schema boundary BEFORE apply_add_portfolio_event runs the recompute — the recompute never sanitizes its own inputs (V5) |
| T-05-03-REP | Repudiation | apply_* holding/event incl. D-03 override | high | mitigate | Every holding/event mutation (including the direct-override escape hatch) writes one AuditLog row via writes.py — no write path bypasses the audit helper (D-16) |
| T-05-03-PNL | Tampering | portfolio.py recompute | medium | mitigate | Average-cost math is unit-tested (Task 1) against hand-computed values incl. the avg_cost-unchanged-by-sell invariant; zero-qty division guarded (Pitfall 4) |
</threat_model>

<verification>
- `pytest backend/tests/test_portfolio.py backend/tests/test_write_tools.py -x` green.
- event_type outside {buy,sell,dividend} → 422.
- Writes without API key → 401.
- GET /investments/summary returns platform-grouped payload with unrealized/realized/total/as_of.
- `npx tsc --noEmit` clean for the three new/modified UI files.
- Human: buy→sell recompute + direct override + grouping render correctly.
</verification>

<success_criteria>
Positions derive from an audited buy/sell/dividend ledger with correct average-cost realized/unrealized P&L; `/investments` shows portfolio value + per-holding P&L grouped by platform; the D-03 override path is audited and reachable. Live prices/staleness (Plan 04) and the correlation tool (Plan 05) build on this.
</success_criteria>

<output>
Create `.planning/phases/05-investment-subsystem/05-03-SUMMARY.md` when done
</output>
