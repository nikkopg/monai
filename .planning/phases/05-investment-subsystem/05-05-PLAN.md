---
phase: 05-investment-subsystem
plan: 05
type: tdd
wave: 4
depends_on: ["05-03"]
files_modified:
  - backend/tools.py
  - backend/tests/test_tools.py
autonomous: true
requirements: [CHAT-03, INV-07]
user_setup: []

must_haves:
  truths:
    - "The agent can answer 'since I bought BBCA, how has my eating-out spending changed?' with a concrete before/after number (CHAT-03)"
    - "The pivot date is the earliest 'buy' event for the ticker in portfolio_events; before/after windows are equal-length (D-15)"
    - "The tool returns an honest error when there is no buy event for the ticker (no fabricated number)"
    - "The tool is registered in the TOOLS dict and emits no raw SQL to the LLM (parameterized queries only)"
  artifacts:
    - backend/tools.py (spending_before_after_purchase, registered in TOOLS)
    - backend/tests/test_tools.py (test_spending_before_after_purchase)
  key_links:
    - "pivot = MIN(date) WHERE ticker=:ticker AND event_type='buy' (parameterized, from portfolio_events written in Plan 03)"
    - "reuses spending_in_category(category, period='custom', start_date=..., end_date=...) — no new date-range SQL"
    - "registered in the TOOLS registry so the agent /query path exposes it"
---

<objective>
Deliver the spending↔portfolio correlation vertical slice (CHAT-03, D-15): a new read-only tool `spending_before_after_purchase(ticker, category)` in `backend/tools.py` that resolves the earliest buy event for a ticker as the pivot date and compares category spending in equal-length windows before vs after, returning before/after totals + delta so the agent can state a concrete number. Registered in the `TOOLS` dict so the existing agent `/query` path picks it up.

Purpose: CHAT-03 is the payoff of the INV-07 event ledger — it makes the portfolio and spending data answer a single natural-language question. The LLM never emits SQL; this tool does the parameterized queries (correctness-by-construction).
Output: The correlation tool + its registration + a unit test following the existing `spending_in_category` test pattern.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-investment-subsystem/05-CONTEXT.md
@.planning/phases/05-investment-subsystem/05-RESEARCH.md
@.planning/phases/05-investment-subsystem/05-PATTERNS.md
@backend/tools.py
</context>

<feature>
  <name>Correlation tool: spending before vs after a purchase (D-15 / CHAT-03)</name>
  <files>backend/tools.py, backend/tests/test_tools.py</files>
  <behavior>
    RED first — write against `spending_before_after_purchase(ticker, category)` before implementing:
    - Given a buy event for TICKER on a known pivot date and category spend rows in both windows, the tool returns before_total, after_total, delta (= after − before), and delta_pct.
    - The before/after windows are equal length (N = days since the pivot), so the comparison isn't skewed as time passes.
    - No buy event for the ticker → returns a structured error dict (keyed "tool" + "error"), NOT a fabricated number.
    - A purchase dated today/future → returns an "no after window yet" error (n_days < 1 guard).
    - The return dict is keyed "tool" == "spending_before_after_purchase" (structured-dict convention).
  </behavior>
  <implementation>Implement per 05-RESEARCH.md § Code Examples "Correlation tool (D-15 / CHAT-03)" (lines 584-648): resolve pivot via parameterized `SELECT MIN(date) FROM portfolio_events WHERE ticker=:ticker AND event_type='buy'`; compute n_days = today − pivot; build equal-length before (pivot−n_days … pivot−1) and after (pivot … today) windows; call the existing `spending_in_category(category, period="custom", start_date=..., end_date=...)` for each (reuse its custom-period contract, mind resolve_period's exclusive end_date); return before/after/delta/delta_pct. Register in the TOOLS dict alongside spending_in_category.</implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED→GREEN spending_before_after_purchase + TOOLS registration</name>
  <read_first>
    - backend/tools.py (the file being modified — module docstring L1-18 no-raw-SQL/structured-dict/registry rules; `spending_in_category` L188 (the custom-period callee); `resolve_period` L41-58 (custom period, exclusive end_date); `_currency` L99-102 parameterized connect+bound-param shape; the TOOLS dict where the tool registers)
    - backend/tests/test_tools.py (the file being extended — copy the existing spending_in_category test's DB-fixture + assertion structure)
    - backend/tests/conftest.py (db_available fixture to reuse)
    - .planning/phases/05-investment-subsystem/05-RESEARCH.md § Code Examples "Correlation tool" (full skeleton) + § user_constraints D-15
    - .planning/phases/05-investment-subsystem/05-PATTERNS.md § tools.py (analog + period-window reuse)
  </read_first>
  <action>RED: add `test_spending_before_after_purchase` to backend/tests/test_tools.py — seed a portfolio_events buy row (pivot date) + transactions in both the before and after windows for a category, assert before_total/after_total/delta and the no-buy-event error path; run to confirm failure. GREEN: implement `spending_before_after_purchase(ticker, category)` in backend/tools.py per the RESEARCH skeleton (parameterized pivot lookup on portfolio_events, equal-length windows, reuse `spending_in_category(period="custom", ...)`), register it in the TOOLS dict; run until green. Return a structured dict keyed "tool"; on no-buy-event or future-purchase return an error dict rather than raising or fabricating (CHAT-08 honesty carries over). No raw SQL leaves the tool boundary — the LLM only sees the tool name/args.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_tools.py::test_spending_before_after_purchase -x -q 2>&1 | tail -5</automated>
    <automated>python -c "from backend.tools import TOOLS; assert 'spending_before_after_purchase' in TOOLS; print('registered')"</automated>
  </verify>
  <done>Correlation test green; tool registered in TOOLS; equal-length windows; honest error on missing buy event; parameterized SQL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/agent → tools.py correlation tool | Agent-supplied ticker/category args reach parameterized queries; the LLM never emits SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-SQL | Tampering | spending_before_after_purchase | medium | mitigate | Ticker/category reach only bound parameters in `text(... :ticker ...)` queries and the existing `spending_in_category` custom-period contract — no string-built SQL; correctness-by-construction (CHAT-02) preserved |
| T-05-05-FAB | Spoofing (fabricated data) | correlation answer | medium | mitigate | Missing buy event / future purchase returns a structured error dict, never a made-up number — the agent surfaces the honest error (CHAT-08 pattern) |
</threat_model>

<verification>
- `pytest backend/tests/test_tools.py::test_spending_before_after_purchase -x` green.
- `spending_before_after_purchase` present in the TOOLS dict.
- No-buy-event and future-purchase paths return error dicts (no fabricated number).
- Windows are equal length; parameterized SQL only.
</verification>

<success_criteria>
The agent can answer a spending↔portfolio correlation question with a concrete before/after number derived from the event ledger and category spending, or honestly report when it can't — CHAT-03 delivered, closing the last Phase-5 requirement.
</success_criteria>

<output>
Create `.planning/phases/05-investment-subsystem/05-05-SUMMARY.md` when done
</output>
