---
phase: 05-investment-subsystem
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/writes.py
  - backend/schemas.py
  - backend/main.py
  - backend/tests/test_write_tools.py
  - ui/app/investments/PlatformManager.tsx
  - ui/app/investments/page.tsx
autonomous: true
requirements: [INV-01]
user_setup: []

must_haves:
  truths:
    - "A user can create a platform (name + optional kind), see it listed, rename it, and delete it from the Investments page"
    - "Deleting a platform that still has holdings returns 422 with affected_count; the user picks a destination platform and the holdings are reassigned via ?reassign_to="
    - "Every platform add/edit/delete/reassign writes exactly one audit_log row (before/after)"
  artifacts:
    - backend/writes.py (apply_add_platform, apply_edit_platform, apply_delete_platform)
    - backend/schemas.py (PlatformCreate, PlatformUpdate, PlatformOut)
    - backend/main.py (POST/PUT/DELETE /platforms, GET /platforms)
    - ui/app/investments/PlatformManager.tsx
  key_links:
    - "DELETE /platforms/{id} 422 detail uses key `affected_count` (holdings count) — PlatformManager reads detail.affected_count"
    - "apply_delete_platform reassigns via UPDATE holdings SET platform_id = :reassign_to (matches the FK added in Plan 01)"
    - "write routes carry dependencies=[Depends(require_api_key)]; GET /platforms is an open read"
---

<objective>
Deliver the platform-management vertical slice (D-12): a managed `platforms` entity with full CRUD, mirroring the Phase-4 account manager end to end (backend writes.py helpers + audited writes, REST routes, and the `PlatformManager.tsx` UI), wired into `ui/app/investments/page.tsx`. After this slice a real user can add/rename/delete platforms and reassign holdings on delete — the "buckets" grouping every holding will later attach to.

Purpose: Platforms are the grouping substrate for the holdings slice (Plan 03 assigns holdings to a platform). Shipping it first as its own demonstrable slice keeps the keystone ledger plan focused.
Output: Three `apply_*_platform` helpers, three platform DTOs, four platform routes, `PlatformManager.tsx`, and a first real render of `/investments` as a `"use client"` page.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-investment-subsystem/05-CONTEXT.md
@.planning/phases/05-investment-subsystem/05-PATTERNS.md
@.planning/phases/05-investment-subsystem/05-UI-SPEC.md
@backend/writes.py
@backend/schemas.py
@ui/app/cashflow/AccountManager.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Platform write helpers + DTOs + REST routes</name>
  <read_first>
    - backend/writes.py (the file being modified — `apply_add_account` L76-87 template for apply_add_platform; `apply_edit_account` L90-105; `apply_delete_account` L106-133 reassign-then-delete template; module docstring L10-14: one mutation, one AuditLog, never commit)
    - backend/schemas.py (the file being modified — `AccountCreate`/`AccountUpdate`/`AccountOut` L24-95 for the DTO-by-role pattern; `ConfigDict(from_attributes=True)`)
    - backend/main.py (the file being modified — `create_account`/`update_account`/`delete_account` L121-201 for auth + apply + commit + reset_engine + ValueError→422 + the 422 detail dict shape; `from backend.auth import require_api_key` L44)
    - .planning/phases/05-investment-subsystem/05-PATTERNS.md § writes.py and § main.py (exact analog line refs) and § "Reassign-then-delete"
  </read_first>
  <behavior>
    - Test: `apply_add_platform(db, {"name":"BCA Sekuritas","kind":"brokerage"})` inserts a platforms row and writes one AuditLog(entity="platform", operation="add", before=None).
    - Test: `apply_delete_platform` with `reassign_to` set runs `UPDATE holdings SET platform_id=:reassign_to WHERE platform_id=:pid`, records reassigned_count in the AuditLog after-dict, then deletes the platform.
    - Test: POST /platforms without the API key header returns 401; with it returns 201 and the created row.
  </behavior>
  <action>In backend/writes.py add `apply_add_platform(db, after)`, `apply_edit_platform(db, platform_id, after, before)`, `apply_delete_platform(db, platform_id, before, reassign_to=None) -> int` copying the account helpers verbatim with `entity="platform"` and reassigning the `holdings.platform_id` column (not accounts). In backend/schemas.py add `PlatformCreate` (name: str, kind: str | None = None), `PlatformUpdate` (all-optional: name, kind), `PlatformOut` (ConfigDict from_attributes; id, name, kind). In backend/main.py add `GET /platforms` (open read, returns list[PlatformOut]), `POST /platforms` (201), `PUT /platforms/{platform_id}`, `DELETE /platforms/{platform_id}` (accepts `reassign_to: int | None` query param) — each write route `dependencies=[Depends(require_api_key)]`, calling the apply_* helper then `db.commit()`/`db.refresh()`/`reset_engine()`; DELETE returns 422 `detail={"message": f"{count} holdings use this platform — reassign or delete them first", "affected_count": count}` when holdings remain and no reassign target given (mirror `delete_account` L182-196 shape exactly — frontend reads `affected_count`). Map `ValueError → HTTPException(422)`.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_write_tools.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>apply_*_platform helpers audit-log every mutation; platform DTOs validate; routes enforce auth on writes; delete returns affected_count 422 and honors reassign_to.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_write_tools.py with platform write cases</name>
  <read_first>
    - backend/tests/test_write_tools.py (the file being extended — copy the existing propose/apply-creates-row assertion structure and DB fixture usage)
    - backend/tests/conftest.py (db_available fixture — reuse the skip pattern)
    - .planning/phases/05-investment-subsystem/05-VALIDATION.md § Per-Task Verification Map (INV-01 direct-override + platform rows)
  </read_first>
  <action>Add `test_apply_add_platform_creates_row_and_audit` (asserts a platforms row + one AuditLog entity="platform" after add) and `test_apply_delete_platform_reassigns_holdings` (seed a holding with platform_id=A, delete A with reassign_to=B, assert the holding now points at B and the AuditLog after-dict carries reassigned_count) following the existing test file's DB-fixture pattern. Reuse `db_available`.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_write_tools.py::test_apply_add_platform_creates_row_and_audit -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Both new platform tests pass against a live DB (skip cleanly when DB down).</done>
</task>

<task type="auto">
  <name>Task 3: PlatformManager.tsx + first real /investments client page</name>
  <read_first>
    - ui/app/cashflow/AccountManager.tsx (direct mirror per D-12 — imports/state L1-39, reassign-on-422 delete flow L91-150, inline-edit rows L155-217, ConfirmDialog-with-select L268-291, extractDetail L296-308)
    - ui/app/cashflow/page.tsx (the `"use client"` tracker shape + fmt() pattern the page adopts)
    - ui/app/investments/page.tsx (the file being modified — Phase-3 server-component skeleton to convert to "use client" per RESEARCH Pitfall 5)
    - .planning/phases/05-investment-subsystem/05-UI-SPEC.md § "PlatformManager.tsx" (adds optional `kind` input via placeholder) + § Copywriting (platform CTAs, delete/reassign copy) + § Layout note 6
  </read_first>
  <action>Create ui/app/investments/PlatformManager.tsx mirroring AccountManager.tsx structurally: `editingId`/`editName` inline-edit state, add-row with a `name` input plus a second smaller optional `kind` input (placeholder "e.g. brokerage, crypto app"), Edit/Delete text-links, delete via `ConfirmDialog` imported from `../cashflow/ConfirmDialog`; on 422 read `detail.affected_count`, swap to a destination-platform `<select>` and re-issue `DELETE /api/platforms/{id}?reassign_to=${targetId}`. Copy `extractDetail(r)` verbatim. Use copy from UI-SPEC ("Add platform"/"Save platform", "{affected_count} holdings use this platform — choose a destination platform to reassign them, or cancel."). Convert ui/app/investments/page.tsx to `"use client"`, fetch `GET /api/platforms`, render the page heading "Investments" (Display 28px/600) and a "Platforms" card hosting `<PlatformManager>`; all styles from `../styles` (card, input, btn, label, dangerBtn) reused verbatim. This page grows in Plans 03/04; this slice renders the platform-manager section only.</action>
  <verify>
    <automated>cd ui && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -qiE "investments/(PlatformManager|page)" && echo "TS ERROR IN NEW FILES" || echo "OK"</automated>
    <human-check>Load /investments: add a platform with a kind, rename it inline, delete a platform with no holdings (confirm dialog), and confirm the reassign flow copy appears when deleting a platform that has holdings.</human-check>
  </verify>
  <done>PlatformManager renders on /investments; add/edit/delete + reassign-on-422 all wired through the /api proxy; tsc clean for the new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /platforms write routes | Untrusted form input (name, kind, reassign_to) crosses into the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-AC | Elevation of Privilege | POST/PUT/DELETE /platforms | high | mitigate | Every write route carries `dependencies=[Depends(require_api_key)]` (V4); GET /platforms stays open, consistent with existing GET reads |
| T-05-02-INP | Tampering | PlatformCreate/Update | medium | mitigate | Pydantic v2 schemas validate name/kind at the API boundary (V5); reassign_to is an int query param (type-coerced), used only in a parameterized UPDATE |
| T-05-02-REP | Repudiation | apply_*_platform | medium | mitigate | Every platform mutation writes one AuditLog row (before/after) via writes.py — no write path skips the audit helper (D-16) |
</threat_model>

<verification>
- `pytest backend/tests/test_write_tools.py -x` green (platform add/reassign cases pass).
- POST /platforms returns 401 without key, 201 with key.
- DELETE /platforms/{id} with holdings → 422 + affected_count; with `?reassign_to=` → reassigns then deletes.
- `npx tsc --noEmit` clean for investments/PlatformManager.tsx and investments/page.tsx.
- Human: platform CRUD + reassign flow works on /investments.
</verification>

<success_criteria>
Platform CRUD works end-to-end through the UI with audited writes and the reassign-then-delete flow; `/investments` is now a `"use client"` page hosting the platform manager, ready for the holdings slice to add grouped holding cards.
</success_criteria>

<output>
Create `.planning/phases/05-investment-subsystem/05-02-SUMMARY.md` when done
</output>
