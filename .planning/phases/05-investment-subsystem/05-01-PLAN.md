---
phase: 05-investment-subsystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/models.py
  - alembic/versions/004_investment_platforms.py
  - backend/tests/test_portfolio.py
  - backend/tests/test_prices.py
  - backend/tests/test_scheduler.py
autonomous: false
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07]
user_setup: []

must_haves:
  truths:
    - "`alembic upgrade head` on the current `9c1a4f7d2b8e` DB creates `platforms` and `portfolio_value_history` and adds `holdings.platform_id` without touching existing data"
    - "`alembic downgrade -1` cleanly reverses the migration"
    - "Three new pytest files exist and collect (import cleanly) so downstream slices have a red-test target"
    - "yfinance and apscheduler are installable and importable in the backend image"
  artifacts:
    - alembic/versions/004_investment_platforms.py
    - backend/models.py (Platform, PortfolioValueHistory, holdings.platform_id)
    - backend/tests/test_portfolio.py
    - backend/tests/test_prices.py
    - backend/tests/test_scheduler.py
    - backend/requirements.txt (yfinance, apscheduler pins)
  key_links:
    - "migration down_revision == '9c1a4f7d2b8e' (chains onto 003_app_settings.py current head)"
    - "Platform ORM __tablename__ == migration table name == 'platforms'; PortfolioValueHistory maps 'portfolio_value_history'"
    - "holdings.platform_id ForeignKey('platforms.id') nullable=True (existing/unassigned rows valid)"
---

<objective>
Land the shared schema substrate and Wave-0 test scaffolds that every downstream Phase-5 slice depends on: the fourth Alembic migration (D-17) creating `platforms` + `portfolio_value_history` + a nullable `holdings.platform_id` FK, the matching SQLAlchemy models, the new backend dependencies (`yfinance`, `apscheduler`), and the three MISSING pytest files (`test_portfolio.py`, `test_prices.py`, `test_scheduler.py`) per 05-VALIDATION.md so later plans have red-test targets.

Purpose: Correctness-by-construction requires the migration to land before any slice that writes the new tables (wave ordering). This plan is deliberately dependency-free so it runs first, alone, in Wave 1.
Output: One migration file, extended `backend/models.py`, updated `requirements.txt`, three new test files with import-clean skeletons + the recompute/adapter/scheduler test stubs marked so downstream plans fill them in.
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-investment-subsystem/05-CONTEXT.md
@.planning/phases/05-investment-subsystem/05-RESEARCH.md
@.planning/phases/05-investment-subsystem/05-PATTERNS.md
@.planning/phases/05-investment-subsystem/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify new backend packages before install (T-05-SC)</name>
  <action>BLOCKING human gate before the first install of yfinance/apscheduler (Task 2). Do not add the pins or install until the human verifies both packages on pypi.org per the steps below and types "approved". This gate is never auto-approvable (workflow.auto_advance ignored) per the package-legitimacy protocol.</action>
  <what-built>Nothing yet — this gate precedes the first `pip install` of `yfinance` and `apscheduler`. 05-RESEARCH.md § Package Legitimacy Audit marks both `[ASSUMED]` (choice) though legitimacy is verified: yfinance 1.5.1 (github.com/ranaroussi/yfinance, first release 2019), apscheduler 3.11.3 (github.com/agronholm/apscheduler, first release 2009). `httpx>=0.27.0` is already pinned in requirements.txt — no new HTTP dep.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/yfinance/ — confirm it is the ranaroussi/yfinance project, latest 1.5.x, many releases over years (not a days-old typosquat).
    2. Open https://pypi.org/project/APScheduler/ — confirm it is agronholm/apscheduler, 3.11.x, long release history.
    3. Confirm neither name is a look-alike of the intended package (no `yfinancee`, `apschedulr`, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding the pins, or name a substitute package.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add yfinance + apscheduler pins to requirements.txt</name>
  <read_first>
    - backend/requirements.txt (the file being modified — `httpx>=0.27.0` already present at L13; do NOT re-add httpx)
    - .planning/phases/05-investment-subsystem/05-RESEARCH.md § Standard Stack (verified versions: yfinance 1.5.1, apscheduler 3.11.3) and § Runtime State Inventory (Docker image needs rebuild after this change)
  </read_first>
  <action>Add two lines to backend/requirements.txt: `yfinance>=1.5.1` and `apscheduler>=3.11.3`. Do NOT add `pycoingecko` — 05-RESEARCH.md § Alternatives Considered recommends a raw httpx call to CoinGecko `/simple/price` instead (httpx already pinned). Keep existing pins untouched.</action>
  <verify>
    <automated>python -m pip install -r backend/requirements.txt --dry-run 2>&1 | grep -qiE "yfinance|apscheduler" && echo OK</automated>
  </verify>
  <done>requirements.txt lists yfinance>=1.5.1 and apscheduler>=3.11.3; httpx appears exactly once; dry-run resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Platform + PortfolioValueHistory models and holdings.platform_id</name>
  <read_first>
    - backend/models.py (the file being modified — `Account` L43-53 template for Platform; `PriceCache` L176-195 money/currency/timestamp column conventions; `Holding` L126 and `Transaction.account_id` FK L69-71 for the nullable FK)
    - .planning/phases/05-investment-subsystem/05-PATTERNS.md § "backend/models.py" (exact analog line refs: Account for Platform, PriceCache/Transaction for PortfolioValueHistory, Transaction.account_id for holdings.platform_id)
    - .planning/phases/05-investment-subsystem/05-RESEARCH.md § Code Examples (migration skeleton lists exact columns)
  </read_first>
  <behavior>
    - Test: `Platform` maps `__tablename__ == "platforms"` with columns id (pk), name (String(128), unique), kind (String(32), nullable).
    - Test: `PortfolioValueHistory` maps `__tablename__ == "portfolio_value_history"` with snapshot_date (Date), ticker (String(32)), quantity (Numeric(28,8)), market_value (Numeric(18,2)), cost_basis (Numeric(18,2)), currency (String(8) default IDR).
    - Test: `Holding` now exposes a `platform_id` mapped column (Integer, ForeignKey platforms.id, nullable).
  </behavior>
  <action>In backend/models.py add class `Platform(Base)` mirroring `Account` (id pk; `name` `mapped_column(String(128), unique=True, index=True, nullable=False)`; `kind` `mapped_column(String(32), nullable=True)`). Add class `PortfolioValueHistory(Base)` (`__tablename__="portfolio_value_history"`) with columns per the D-13 shape using PriceCache's Numeric/String/server_default conventions: `snapshot_date` Date not-null, `ticker` String(32) not-null indexed, `quantity` Numeric(28,8) not-null, `market_value` Numeric(18,2) not-null, `cost_basis` Numeric(18,2) not-null, `currency` String(8) server_default "IDR" not-null. Add `platform_id: Mapped[int | None] = mapped_column(ForeignKey("platforms.id"), nullable=True, index=True)` to the existing `Holding` class. Do NOT alter existing Holding/PortfolioEvent/PriceCache columns (reused as-is, D-17). Import `ForeignKey`/`Date`/`Numeric`/`String` as needed (follow existing imports).</action>
  <verify>
    <automated>python -c "from backend.models import Platform, PortfolioValueHistory, Holding; assert Platform.__tablename__=='platforms'; assert PortfolioValueHistory.__tablename__=='portfolio_value_history'; assert 'platform_id' in Holding.__table__.columns; print('OK')"</automated>
  </verify>
  <done>Three model changes importable; tablenames and platform_id column present; existing columns unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Alembic migration 004 — platforms, portfolio_value_history, holdings.platform_id (D-17)</name>
  <read_first>
    - alembic/versions/003_app_settings.py (current head; revision `9c1a4f7d2b8e` — the new migration's down_revision)
    - alembic/versions/002_new_tables.py (create_table + create_index conventions in this repo)
    - .planning/phases/05-investment-subsystem/05-RESEARCH.md § Code Examples "Migration skeleton (D-17)" (exact upgrade/downgrade body, unique index on (snapshot_date, ticker))
    - .planning/phases/05-investment-subsystem/05-PATTERNS.md § "alembic/versions/004_*.py"
  </read_first>
  <action>Create alembic/versions/004_investment_platforms.py with `down_revision = "9c1a4f7d2b8e"` and a fresh `revision` id. `upgrade()`: `op.create_table("platforms", ...)` (id pk, name String(128) not-null unique, kind String(32) nullable); `op.add_column("holdings", sa.Column("platform_id", sa.Integer, sa.ForeignKey("platforms.id"), nullable=True))`; `op.create_table("portfolio_value_history", ...)` with the six D-13 columns and `currency` `server_default="IDR"`; `op.create_index("ix_portfolio_value_history_date_ticker", "portfolio_value_history", ["snapshot_date","ticker"], unique=True)` to enforce one-row-per-holding-per-day (D-13). `downgrade()`: drop index, drop portfolio_value_history, drop holdings.platform_id, drop platforms (reverse order). Match the create_table style of 002/003. entrypoint.sh already runs `alembic upgrade head` — no wiring needed.</action>
  <verify>
    <automated>python -c "import importlib.util,glob; p=glob.glob('alembic/versions/004_investment_platforms.py')[0]; s=importlib.util.spec_from_file_location('m',p); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.down_revision=='9c1a4f7d2b8e'; assert hasattr(m,'upgrade') and hasattr(m,'downgrade'); print('OK')"</automated>
    <human-check>On a copy of the dev DB: `alembic upgrade head` then confirm `platforms`, `portfolio_value_history`, `holdings.platform_id` exist and existing tables are intact; then `alembic downgrade -1` reverses cleanly (D-17 manual smoke per 05-VALIDATION.md).</human-check>
  </verify>
  <done>Migration file chains onto 9c1a4f7d2b8e; upgrade/downgrade defined; module imports without error.</done>
</task>

<task type="auto">
  <name>Task 5: Wave-0 test scaffolds — test_portfolio.py, test_prices.py, test_scheduler.py</name>
  <read_first>
    - backend/tests/conftest.py (the `db_available` skip-fixture pattern new files must reuse — read it to copy the fixture/import shape)
    - backend/tests/test_write_tools.py (existing structure/imports to mirror in the new files)
    - .planning/phases/05-investment-subsystem/05-VALIDATION.md § Per-Task Verification Map + § Wave 0 Requirements (exact test IDs each file must declare)
  </read_first>
  <action>Create three pytest files that import cleanly and collect. `backend/tests/test_portfolio.py`: declare `test_recompute_holding_from_events`, `test_manual_price_override`, `test_staleness_ttl`, `test_avg_cost_realized_pnl`, each with a body `pytest.fail("not yet implemented — Plan 03/04 fills this")` (a real failing target, NOT skip, so downstream RED→GREEN is visible; import `pytest`). `backend/tests/test_prices.py`: declare `test_fetch_crypto_price` and `test_fetch_idx_price_fallback` the same way (filled by Plan 04). `backend/tests/test_scheduler.py`: declare `test_snapshot_job_partial_failure_tolerant` (filled by Plan 06). Reuse conftest's `db_available` fixture import for the DB-touching stubs so they skip gracefully when Postgres is down. Do NOT implement production modules here — these are red targets only. Do NOT write the literal token "not yet implemented" into any negatively-greped acceptance file; it lives only in test bodies which are never negative-greped.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_portfolio.py backend/tests/test_prices.py backend/tests/test_scheduler.py --collect-only -q 2>&1 | grep -qE "test_recompute_holding_from_events|test_fetch_crypto_price|test_snapshot_job_partial_failure_tolerant" && echo OK</automated>
  </verify>
  <done>Three files collect the seven named tests; each currently fails (red target) rather than passing vacuously; no production module imported that doesn't exist yet (stubs reference names lazily inside test bodies).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip → backend image | Third-party package install (yfinance, apscheduler) crosses into the runtime |
| Alembic migration → production DB | Schema change runs against a DB holding real (currently zero investment) rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | pip installs (yfinance/apscheduler) | high | mitigate | Task 1 blocking-human checkpoint verifies both packages on pypi.org before any install; both are years-old, high-release-count, real-repo packages per 05-RESEARCH.md audit |
| T-05-01-MIG | Tampering | alembic 004 migration | medium | mitigate | `platform_id` FK is nullable so existing (zero) holding rows survive; downgrade defined and human-smoke-tested (D-17); migration only ADDs objects, never drops/renames existing tables |
</threat_model>

<verification>
- `python -m pip install -r backend/requirements.txt --dry-run` resolves with yfinance + apscheduler.
- `python -c "from backend.models import Platform, PortfolioValueHistory, Holding"` succeeds; platform_id column present.
- Migration 004 imports; down_revision is 9c1a4f7d2b8e.
- `pytest backend/tests/test_portfolio.py backend/tests/test_prices.py backend/tests/test_scheduler.py --collect-only -q` collects the seven named tests.
- Human smoke: `alembic upgrade head` / `alembic downgrade -1` round-trip on a dev DB copy (D-17).
</verification>

<success_criteria>
Migration + models + deps + three red-target test files exist and import/collect cleanly; downstream slices (02–06) can build on `platforms`, `portfolio_value_history`, `holdings.platform_id`, and fill the red tests.
</success_criteria>

<output>
Create `.planning/phases/05-investment-subsystem/05-01-SUMMARY.md` when done
</output>
