---
phase: 18-ui-entry-points-for-balance-adjustment-liquid-investment-tra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/app/cashflow/AccountManager.tsx
  - ui/app/cashflow/AdjustBalanceModal.tsx
  - ui/e2e/balance-adjust.spec.ts
autonomous: true
requirements: [ACCT-02]

must_haves:
  truths:
    - "User sees an 'Adjust balance' text action on each liquid account row in AccountManager, between Edit and Delete (D-01)"
    - "Opening it shows a modal with a single 'Target balance' input and a live signed delta preview computed from that row's current_balance (D-02)"
    - "Preview reads 'Adjustment: +Rp {delta}' (green) when delta > 0, 'Adjustment: −Rp {|delta|}' (terracotta, U+2212) when delta < 0, and 'No change — target equals current balance.' (muted, submit disabled) when delta == 0 (D-07)"
    - "Submitting POSTs exactly {target_balance} to /api/accounts/{id}/adjust-balance and calls onChanged() (parent refetch of GET /cashflow/summary) on 2xx (D-01)"
    - "A 422 renders 'Couldn't save adjustment: {detail}. Nothing was changed.' and leaves the modal open"
  artifacts:
    - ui/app/cashflow/AdjustBalanceModal.tsx
    - ui/app/cashflow/AccountManager.tsx
    - ui/e2e/balance-adjust.spec.ts
  key_links:
    - "AccountManager passes each row's current_balance (already flowing in via summary.accounts from cashflow/page.tsx) into AdjustBalanceModal for the presentation-only delta preview"
    - "onChanged (wired to cashflow/page.tsx refreshAll) re-fetches GET /cashflow/summary after a 2xx — no optimistic local mutation (Pattern 5)"
---

<objective>
Add the ACCT-02 balance-adjustment entry point: a per-row "Adjust balance" action in
`AccountManager.tsx` that opens a small overlay modal with a single target-balance input and a
live computed-delta preview, submitting `POST /api/accounts/{id}/adjust-balance {target_balance}`
and refetching the cashflow summary on success — turning `balance-adjust.spec.ts` GREEN.

Purpose: ACCT-02 — the user can set a liquid account's balance; the backend stores the delta as a
visible "Adjustment" record (balances stay derived, never a stored field).
Output: new `AdjustBalanceModal.tsx` + an "Adjust balance" row action in `AccountManager.tsx`;
`balance-adjust.spec.ts` passes.

Implements: D-01 (per-account Adjust-balance action in AccountManager + overlay modal), D-02 (delta
preview reads current_balance from summary.accounts; presentation-only, authoritative delta computed
server-side), D-07 (form-level money preview + single atomic submit, no agent-proposal confirm step).
UI-only: the endpoint shipped and was verified in Phase 13; no backend endpoint, schema, or read is
created or changed.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-CONTEXT.md
@.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-RESEARCH.md
@.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-PATTERNS.md
@.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-UI-SPEC.md
@.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-VALIDATION.md

GRAPHIFY MANDATE: graphify-out/graph.json exists. Run `graphify query "AccountManager row action Edit
Delete extractDetail current_balance"` and `graphify query "HoldingModal overlay modal shell card
button row"` to orient BEFORE reading raw source; then open only the ranges you need.

18-UI-SPEC.md Surface 1 (Copywriting Contract + Color + Spacing tables) is the visual/copy source of
truth. 18-PATTERNS.md "NEW balance-adjust dialog (ACCT-02)" section has the exact analog line ranges.
CLAUDE.md rules that bind here: inline-style styles.ts tokens only (no Tailwind); UI calls the Next.js
proxy (/api/...) which injects MONAI_API_KEY server-side — never reference the key or the raw backend
URL from a client component; onChanged() parent-refetch, never optimistic mutation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the RED route-mocked e2e spec for balance adjustment (ACCT-02)</name>
  <files>ui/e2e/balance-adjust.spec.ts</files>
  <read_first>
    - ui/e2e/cashflow-crud.spec.ts (existing route-mock + summary-fixture convention in the same page context; copy the `page.route("**/api/cashflow/summary*", ...)` fixture shape so an account row carries `current_balance`)
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-VALIDATION.md (per-task verification map row 18-01: mock the write endpoint, assert payload + success refetch)
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-RESEARCH.md ("Success response shapes": adjust-balance returns 201 {transaction_id, amount}; 422 returns a plain-string {detail})
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-UI-SPEC.md (Surface 1 copy strings the spec asserts)
  </read_first>
  <action>
    Create a new spec `ui/e2e/balance-adjust.spec.ts` with a `test.describe("balance adjustment (ACCT-02)")`
    block. Route-mock GET /api/cashflow/summary so at least one account row in `summary.accounts` has a
    known `current_balance` (e.g. 1_000_000). Route-mock POST /api/accounts/*/adjust-balance twice: a
    success case (201 body `{ transaction_id: 1, amount: "500000" }`) and a 422 case (body
    `{ detail: "some backend message" }`). Test 1 (success): open the account row's "Adjust balance"
    action, type a target balance, assert the preview line text updates to the signed "Adjustment: +Rp …"
    / "−Rp …" / "No change — target equals current balance." copy per current_balance vs target, click
    "Save adjustment", and assert (a) the captured request body deep-equals exactly `{ target_balance: <number> }`
    (a JS number via parseFloat, not a string, and no delta field) and (b) GET /api/cashflow/summary is
    re-fetched after the 2xx. Test 2 (422): submit against the 422 mock and assert the error text
    "Couldn't save adjustment: some backend message. Nothing was changed." renders and the modal stays open.
    The spec is expected RED now (the UI entry point does not exist yet) and turns GREEN in Task 2. Do not
    build any app code in this task.
  </action>
  <acceptance_criteria>
    - File `ui/e2e/balance-adjust.spec.ts` exists, compiles, and runs under Playwright.
    - Contains two tests (success + 422) in a `describe("balance adjustment (ACCT-02)")` block.
    - Asserts the POST body deep-equals `{ target_balance: <number> }` on `/api/accounts/*/adjust-balance` (no `delta` field; value is a number).
    - Asserts the preview copy strings and the exact error copy "Couldn't save adjustment: {detail}. Nothing was changed."
    - `cd ui && npx playwright test e2e/balance-adjust.spec.ts` fails only because the "Adjust balance" entry point is not yet built (RED baseline), not due to a spec syntax error.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npx playwright test e2e/balance-adjust.spec.ts</automated>
  </verify>
  <done>RED spec authored with success + 422 mocks; payload/preview/error assertions in place; run shows RED because the target UI is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AdjustBalanceModal + the AccountManager "Adjust balance" row action (D-01, D-02, D-07)</name>
  <files>ui/app/cashflow/AdjustBalanceModal.tsx, ui/app/cashflow/AccountManager.tsx</files>
  <read_first>
    - ui/app/cashflow/AccountManager.tsx (the `Account` type at L21 + `Props` at L23; the Edit/Delete row-action spans; the `extractDetail(r: Response)` helper at L296 — reuse verbatim, do not write a second copy)
    - ui/app/investments/HoldingModal.tsx (overlay shell L102-121 and Cancel/Submit button row L224-247 — copy verbatim for the dialog shell)
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-PATTERNS.md ("NEW balance-adjust dialog (ACCT-02)" section: delta logic, submit body, fmtPlain redefinition)
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-UI-SPEC.md (Surface 1 table: copy, `color: tokens.color.muted3` trigger, green/terracotta preview, `padding: 32` card override)
    - .planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-RESEARCH.md (Pitfall 1: current_balance is an all-time unfiltered SUM — use it as-is for the preview; the authoritative delta is computed server-side)
  </read_first>
  <behavior>
    - Given a row with current_balance = 1_000_000 and a typed target of 1_500_000, the preview reads "Adjustment: +Rp 500,000" in green.
    - Given target 800_000 against current 1_000_000, preview reads "Adjustment: −Rp 200,000" in terracotta (U+2212, not hyphen).
    - Given target equal to current_balance, preview reads "No change — target equals current balance." (muted) and the submit button is disabled.
    - On submit the request body is exactly { target_balance: parseFloat(target) } (accepts zero/negative — no min/gt=0); on 2xx, onChanged() is called; on 422 the standard error copy renders and the modal stays open.
  </behavior>
  <action>
    Widen `AccountManager.tsx`'s `Account` type to include `current_balance: number` (the parent already
    passes the richer `summary.accounts` rows that carry it — no cashflow/page.tsx change needed). Insert an
    "Adjust balance" `<span role="button">` between the existing Edit and Delete actions, styled 12px /
    `color: tokens.color.muted3` / `cursor: pointer` (non-destructive, same visual class as Edit, per
    UI-SPEC), that opens the new modal for that row. Create `ui/app/cashflow/AdjustBalanceModal.tsx` copying
    HoldingModal's overlay backdrop + `{ ...card, maxWidth: 480, width: "100%", padding: 32, margin: 0 }`
    card and Cancel/Submit button row; heading "Adjust balance — {account name}". Body is a single "Target
    balance" number input with `step="any"` and NO `min`/`gt=0`. Compute `delta = parseFloat(target || "0") −
    currentBalance` and render the D-07 preview line per the three cases above using a locally-redefined
    `fmtPlain` (Intl.NumberFormat("en-US") grouping — no shared util exists; redefine locally per repo
    convention), with the green token for delta > 0, terracotta for delta < 0, muted for delta === 0. Disable
    the submit ("Save adjustment", "Saving…" while in-flight) when delta === 0. Submit `{ target_balance:
    parseFloat(target) }` to `/api/accounts/{id}/adjust-balance` via the /api proxy; reuse
    `extractDetail(r)` verbatim; on failure set error "Couldn't save adjustment: {detail}. Nothing was
    changed."; on 2xx call `onChanged()` and close. Do NOT add a ConfirmDialog second step (D-07 rejects it)
    and do NOT route through the agent /query proposal flow. Never reference MONAI_API_KEY or the raw backend
    URL — call only `/api/...`.
  </action>
  <acceptance_criteria>
    - `cd ui && npx playwright test e2e/balance-adjust.spec.ts` passes both tests (GREEN).
    - Source: AccountManager's `Account` type includes `current_balance`; an "Adjust balance" span sits between Edit and Delete with `tokens.color.muted3`.
    - The POST body is exactly `{ target_balance: <number> }` (parseFloat, no delta field) to `/api/accounts/{id}/adjust-balance`.
    - Preview renders the three D-07 strings; submit is disabled when delta === 0.
    - Error path renders "Couldn't save adjustment: {detail}. Nothing was changed." and keeps the modal open; success calls onChanged().
    - No `ConfirmDialog` wrapper and no `NEXT_PUBLIC_`/raw-backend-URL reference introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd ui && npx playwright test e2e/balance-adjust.spec.ts</automated>
  </verify>
  <done>Balance-adjust action + modal ship with a live signed delta preview and a single atomic submit; spec GREEN; delta preview presentation-only, authoritative delta computed server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js proxy | Untrusted form input (target_balance) crosses here; the proxy injects MONAI_API_KEY server-side |
| Next.js proxy → FastAPI | `require_api_key`-gated write; FastAPI recomputes the authoritative delta |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01-C | Tampering | Client-side delta preview in AdjustBalanceModal | low | accept | Preview is presentation-only; `apply_add_balance_adjustment` computes the authoritative delta from a fresh unfiltered SUM at write time. The body sends `target_balance` (not a delta), so a tampered preview cannot change what is stored. |
| T-18-01-I | Information Disclosure | API key exposure | low | mitigate | Submit only through the existing `/api/...` Next.js proxy (injects MONAI_API_KEY server-side). Never reference `MONAI_API_KEY`, a `NEXT_PUBLIC_` var, or the raw backend URL from the client component. |
| T-18-01-E | Elevation of Privilege | Write reachable off the web app | low | mitigate | adjust-balance stays a web-app-only, API-key-gated REST write; this plan adds no agent/MCP tool and does not touch `READ_TOOL_NAMES` (stays 15). |

No package installs occur in this plan (no npm/pip/cargo work) — T-18-SC (supply-chain) is not applicable. No high-severity threats identified.
</threat_model>

<verification>
- `cd ui && npx playwright test e2e/balance-adjust.spec.ts` — both tests GREEN (success payload + 422 error copy).
- `cd ui && npx playwright test` — full e2e suite green modulo the pre-existing out-of-scope failures logged in `.planning/phases/16-ui-extend-existing-components/deferred-items.md` (do not fix those here).
- Manual (human-UAT surface, not automated): against the docker-compose stack, an adjust-balance submit produces a visible "Adjustment" record and the derived balance reconciles.
</verification>

<success_criteria>
- "Adjust balance" appears on each liquid account row and opens the modal for that row.
- The delta preview matches `target − current_balance` with correct sign, color, and the delta === 0 disabled state.
- Submitting POSTs `{target_balance}` to `/api/accounts/{id}/adjust-balance` and refetches the summary on success; a 422 shows the standard error copy without closing the modal.
- `balance-adjust.spec.ts` passes; no backend endpoint/schema/read was added or changed.
</success_criteria>

<output>
Create `.planning/phases/18-ui-entry-points-for-balance-adjustment-liquid-investment-tra/18-01-SUMMARY.md` when done.
</output>
