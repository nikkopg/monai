---
phase: 06-mcp-server
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/test_mcp.py
autonomous: false
requirements: [MCP-01]
user_setup: []

must_haves:
  truths:
    - "fastmcp is an installable, importable dependency of the backend (import fastmcp succeeds)."
    - "backend/tests/test_mcp.py exists and defines the five MCP-01..MCP-04 test functions (skipped/xfail until Wave 1 wires the server)."
  artifacts:
    - backend/requirements.txt
    - backend/tests/test_mcp.py
  key_links:
    - "backend/requirements.txt pins fastmcp>=3.4,<4 so the image rebuild installs it before any MCP code imports it."
    - "test_mcp.py reuses the existing client / api_key / async_client fixtures from backend/tests/conftest.py (no new conftest)."
---

<objective>
Lay the Wave 0 foundation for the MCP server: add the `fastmcp` dependency (behind a human legitimacy checkpoint, because the automated seam flagged it SUS — a documented false positive) and create the `backend/tests/test_mcp.py` test scaffold with the five MCP-01..MCP-04 test stubs that Wave 1 will turn green.

Purpose: A test can only import the mounted MCP app once `fastmcp` is installed, so the dependency and the failing-test scaffold must exist before the wiring plan (06-02) runs. Per D-04/security, no new secret is introduced here.
Output: `backend/requirements.txt` with `fastmcp>=3.4,<4`; `backend/tests/test_mcp.py` with five stub tests (skipped until Wave 1).
</objective>

<execution_context>
@/home/user/monai/.claude/gsd-core/workflows/execute-plan.md
@/home/user/monai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-server/06-CONTEXT.md
@.planning/phases/06-mcp-server/06-RESEARCH.md
@.planning/phases/06-mcp-server/06-PATTERNS.md
@backend/tests/conftest.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm fastmcp package legitimacy before install</name>
  <action>Pause for a blocking human legitimacy check on the `fastmcp` PyPI package before Task 2 edits requirements.txt. The automated seam flagged `SUS` (false positive per RESEARCH); protocol requires one human confirmation before install. Never auto-approvable.</action>
  <what-built>Nothing installed yet. RESEARCH.md Package Legitimacy Audit ran `package-legitimacy check` on `fastmcp` and got `SUS` with reasons `["too-new", "unknown-downloads"]` — both assessed as false positives (the "too-new" date is the latest of 106 releases of an established project; "unknown-downloads" is a PyPI JSON-API limitation, not low usage). Protocol requires one human legitimacy confirmation before the install because a seam flagged SUS. This checkpoint is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/fastmcp/ and confirm the project page loads and shows an active, multi-release project (100+ releases).
    2. Confirm the PyPI "Homepage"/"Source" link resolves to https://github.com/PrefectHQ/fastmcp (authored by Jeremiah Lowin / Prefect).
    3. Confirm the latest version satisfies the `>=3.4,<4` pin (currently 3.4.x).
    4. Confirm you are comfortable adding it to backend/requirements.txt.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the requirements edit, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin fastmcp in backend/requirements.txt</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt (the file being modified — append target; current tail ends with `pytest>=8.0.0`)
    - .planning/phases/06-mcp-server/06-RESEARCH.md §"Standard Stack" / §"Installation" (source-of-truth pin: `fastmcp>=3.4,<4`, latest 3.4.4, requires-python >=3.10, monai runs 3.12)
  </read_first>
  <action>
    Append exactly one dependency line `fastmcp>=3.4,<4` to backend/requirements.txt (per RESEARCH §Installation). Do not change any other line. Do NOT add `starlette` (it is already present transitively via FastAPI, per RESEARCH Standard Stack). This dependency requires a Docker image rebuild before live/UAT (MEMORY: deploy requires rebuild) — that rebuild happens at the phase verification step, not here.
  </action>
  <verify>
    <automated>grep -q '^fastmcp>=3.4,<4$' backend/requirements.txt && echo PINNED</automated>
  </verify>
  <acceptance_criteria>
    - backend/requirements.txt contains a line matching exactly `fastmcp>=3.4,<4`.
    - No other line in requirements.txt is modified or removed.
    - `python -c "import fastmcp"` succeeds in an environment where requirements are installed (may require `pip install -r backend/requirements.txt` first in the dev venv).
  </acceptance_criteria>
  <done>fastmcp is pinned in requirements.txt and importable after install.</done>
</task>

<task type="auto">
  <name>Task 3: Create backend/tests/test_mcp.py with five MCP-01..MCP-04 stub tests</name>
  <files>backend/tests/test_mcp.py</files>
  <read_first>
    - backend/tests/conftest.py (fixtures to reuse verbatim: `client` TestClient, `api_key` monkeypatch = `_TEST_API_KEY`, `async_client` httpx ASGITransport — do NOT define new fixtures)
    - backend/tests/test_auth.py (the existing 401 / missing-key assertion pattern to mirror)
    - .planning/phases/06-mcp-server/06-RESEARCH.md §"Validation Architecture" (the Req→Test map: the five test names and what each asserts)
    - .planning/phases/06-mcp-server/06-PATTERNS.md §"backend/test_mcp.py (NEW ...)" (analog + exact tests-to-write list)
  </read_first>
  <action>
    Create backend/tests/test_mcp.py (canonical path — `testpaths = ["backend/tests"]` per pyproject.toml; VALIDATION.md's `backend/test_mcp.py` is imprecise, use `backend/tests/`). Define these five test functions, each with a real signature reusing the named fixtures but a body marked `pytest.mark.skip(reason="Wave 1 (06-02) wires the MCP server")` so the suite stays green in Wave 0: `test_mcp_endpoint_mounted(client, api_key)` (MCP-01 — `/mcp` handshake with key is not 404), `test_mcp_read_parity(client, api_key)` (MCP-02 — `tools/list` == the 15 `TOOLS` names AND a `tools/call` result equals a direct `backend.tools.TOOLS[name](...)` dict), `test_agent_read_tools_count(api_key)` (MCP-02/D-02 — `backend/query.py` builds a read-tool list of length 15), `test_mcp_no_write_tools(client, api_key)` (MCP-03 — no `propose_*` name appears in `tools/list`; calling one is unknown-tool), `test_mcp_requires_key(client)` (MCP-04 — `/mcp` request WITHOUT the `MONAI_API_KEY` header returns 401). Import the fixtures implicitly via pytest; import `backend.tools.TOOLS` for the parity/count assertions. Keep each stub body minimal (a skip marker plus a one-line docstring naming its requirement) — Wave 1 fills the assertions. Do not inline the `MONAI_API_KEY` literal value; read it from the `api_key` fixture return.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_mcp.py -q 2>&1 | grep -v '^#' | grep -Eq '5 skipped|5 passed|5 (skipped|deselected)' && echo STUBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests/test_mcp.py -q` collects exactly 5 tests and the run is green (5 skipped, no errors).
    - Each of the five named test functions is defined and references the correct fixtures (`grep -c 'def test_' backend/tests/test_mcp.py` returns 5).
    - No new fixtures are defined in test_mcp.py (fixtures come from conftest.py).
    - The full backend suite still passes: `python -m pytest backend/tests -q` is green.
  </acceptance_criteria>
  <done>test_mcp.py exists with five skipped MCP-01..MCP-04 stubs; collection is clean and the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency supply chain → backend image | A new PyPI package (`fastmcp`) enters the build; a malicious/typo-squatted package would run in-process with DB access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-SC | Tampering | `pip install fastmcp` (backend/requirements.txt) | high | mitigate | Blocking human-verify checkpoint (Task 1) confirms `fastmcp` on PyPI resolves to github.com/PrefectHQ/fastmcp before the requirements edit; never auto-approvable. Pin `>=3.4,<4` bounds the version range. |
| T-06-01-02 | Information Disclosure | test_mcp.py fixtures | low | accept | Tests use the `api_key` fixture's throwaway `_TEST_API_KEY`, not the real `MONAI_API_KEY`; no secret is hard-coded. |
</threat_model>

<verification>
- `grep -q '^fastmcp>=3.4,<4$' backend/requirements.txt` — dependency pinned.
- `python -m pytest backend/tests/test_mcp.py -q` — 5 stub tests, green (skipped).
- `python -m pytest backend/tests -q` — full suite green (no regressions from the new file).
- Human legitimacy checkpoint recorded as approved before the requirements edit.
</verification>

<success_criteria>
- fastmcp pinned in requirements.txt after human legitimacy confirmation.
- backend/tests/test_mcp.py exists with five skipped MCP-01..MCP-04 stubs and clean collection.
- Full backend suite green.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-server/06-01-SUMMARY.md` when done.
</output>
