---
quick_id: 260711-l41
slug: add-optional-per-holding-coingecko-id-fo
status: complete
date: 2026-07-11
---

# Quick 260711-l41 — Per-holding `coingecko_id` (Tier 1) — SUMMARY

**Outcome:** Crypto holdings can now carry an optional CoinGecko coin-id so price
refresh disambiguates tickers that map to multiple coins (e.g. TAO). A blank id
falls back to the fixed symbol map — existing holdings need no backfill.

## Tasks (6 atomic commits)

| Commit | Task |
|--------|------|
| `0c531eb` | Migration `005` + `Holding.coingecko_id` column |
| `856d0a0` | Task 2 RED — failing tests for `fetch_crypto_price(coin_id=...)` |
| `16ab6a2` | Task 2 GREEN — `fetch_crypto_price(ticker, coin_id=None)`; `refresh_all_prices` passes `h.coingecko_id` to the crypto adapter only |
| `5cf4bcf` | Task 3 RED — failing tests for write-tool threading |
| `c26f75d` | Task 3 GREEN — `coingecko_id` on `HoldingCreate/Update/Out`, threaded through `apply_add_holding`/`apply_edit_holding` (set-when-provided) + `/investments/summary` dict |
| `11ffbcd` | Task 4 — optional "CoinGecko id" input in `HoldingOverrideModal` (crypto only) + `HoldingRow` type |

Commits `c26f75d` and `11ffbcd` were finished by the orchestrator: the executor
completed the code but a harness Bash-safety-classifier outage blocked its last two
`git commit` calls. Code was already written/verified; orchestrator staged and
committed the same files, then merged.

## Verification (orchestrator, on rebuilt image)

- `docker compose up -d --build backend` — migration auto-applied at boot; head `7f3a1c9e4d20`.
- Migration reversible: `downgrade -1` drops the column (0), `upgrade head` re-adds (1).
- `pytest backend/tests -q` → **142 passed** (137 baseline + 5 new), zero failures.
- `cd ui && npx tsc --noEmit` → clean.
- **Live E2E:** `PUT /holdings/{TAO} {coingecko_id:"bittensor"}` → 200; `POST /prices/refresh`
  → TAO fetched a live CoinGecko price (3,839,960 IDR, `source=coingecko`, not stale).
  BBRI (no id, idx_stock) still fetches 2,790 via yfinance — fallback path intact.

## Scope boundaries (as planned)
- No search/picker UI (Tier 2), no auto symbol→id resolution.
- Buy events (`apply_add_portfolio_event`) do NOT thread `coingecko_id` — set via the
  direct add/edit-holding path (HoldingOverrideModal). `HoldingModal` (Log-event) untouched.
- No backfill; existing holdings use the symbol-map fallback.

## Reminder reinforced
The `backend` service has no source volume mount — container `pytest`/`alembic` run the
baked image. Always `docker compose up -d --build backend` before trusting them.
